-- เพิ่มคอลัมน์สำหรับ "หนังสือรับรองการหักภาษี ณ ที่จ่าย" (item ถัดจาก 119 ในเซสชันนี้)
--
-- wht_cert_no    — เลขที่เอกสารที่ล็อกไว้ครั้งแรกที่กดบันทึก (idempotent — เรียกซ้ำได้เลขเดิมเสมอ)
--                  แยกชุดตามทิศทาง: WHT-{ปี พ.ศ.}-{ลำดับ} (เราออกจริง) / DFT-{ปี พ.ศ.}-{ลำดับ}
--                  (ร่างให้คู่ค้าตรวจสอบ — ไม่ใช่เลขทางการ)
-- wht_income_type — สรุปประเภทเงินได้ตามมาตรา 40 ที่เลือกไว้ตอนออกเอกสาร (snapshot ข้อความเต็ม
--                  เช่น "5. อื่นๆ — ค่าบริการ (มาตรา 40(8))") เพื่อให้พิมพ์ซ้ำภายหลังยังตรงกับ
--                  ที่เลือกไว้ครั้งแรก ไม่ต้องเลือกใหม่ทุกครั้ง
--
-- ก่อนรัน SQL นี้ ฟีเจอร์ยังใช้งานได้ปกติ (ทดสอบแล้วจริง) — api/wht-certificate/save.js เขียน
-- คอลัมน์นี้แบบ defensive (try/catch, log warning ไม่ throw) ถ้ายังไม่มีคอลัมน์ ผลคือพิมพ์เอกสารได้
-- ปกติแค่เลขที่จะไม่ถูกจดจำข้ามการเรียกครั้งถัดไป (ได้เลขใหม่ทุกครั้งแทนที่จะเป็นเลขเดิม)

ALTER TABLE ledger_transactions
  ADD COLUMN IF NOT EXISTS wht_cert_no text,
  ADD COLUMN IF NOT EXISTS wht_income_type text;
