# Smile Slip Pro — Project Brief for Claude

โปรเจกต์ของ Vespa / Siam Global Network Enterprise
ภาษาหลักในโค้ดและ comment: **ไทย**
อัปเดตล่าสุด: 2026-08-13 (ต่อจากข้อ 82 — ทำข้อ 83 เสร็จสมบูรณ์ + ต่อยอดเพิ่มพิมพ์ใบเสร็จผ่าน Bluetooth: (1) ปุ่มเปิดลิ้นชักเงินสดผ่านเครื่องพิมพ์ Bluetooth โดยตรง (2) ปุ่มพิมพ์ใบเสร็จตรงผ่าน Bluetooth (วาดเป็นภาพ+ส่ง ESC/POS raster กัน Thai codepage ต่างกันข้ามยี่ห้อ) — ทั้งคู่ Web Bluetooth API, Android เท่านั้น — iPhone ทำไม่ได้ตามข้อจำกัดของแพลตฟอร์ม — revision ล่าสุด `smileslip-dashboard-00321-ndl` — **deploy production แล้ว + verified ว่าเรนเดอร์ถูกต้องไม่มี hydration error แต่ยังไม่เคยทดสอบกับฮาร์ดแวร์ Bluetooth จริงเลยทั้งสองปุ่ม รอผู้ใช้ทดสอบเองบนมือถือ Android + เครื่องพิมพ์ MP210 (หรือรุ่นอื่น) แล้วรายงานกลับ** — **⚠️ ระหว่าง deploy รอบนี้เกิด production พังจริงชั่วขณะ (ทุก endpoint ที่แตะ Supabase เป็น 500 หมด) จากสคริปต์ deploy เองที่ไม่ตัดเครื่องหมายคำพูดออกจากค่าใน `.env` ก่อนส่งเข้า production — แก้/rollback ทันทีที่พบ ไม่กระทบผู้ใช้จริง (ตรวจพบเองระหว่างเทส ไม่ใช่ผู้ใช้แจ้ง) ดูบทเรียนเต็มในข้อควรระวังข้อ 44** — **กะเงินสดค้างที่เคยพบไว้จากข้อ 82 ปิดเรียบร้อยแล้ว (ผู้ใช้ยืนยันเป็นแค่ข้อมูลทดสอบ ไม่มีปัญหาตามมา)** — **QR Bill Payment ที่เจอ error "เลขที่อ้างอิงไม่ถูกต้อง" ยืนยันแล้วว่า payload ถูกต้อง 100% ตามสเปกทางการ ธปท. ปัญหาอยู่ที่ข้อมูล Reference 1 ฝั่งธนาคาร ไม่ใช่บั๊กโค้ด — รอผู้ใช้ทวนค่าที่ถูกต้องกับธนาคารแล้วกรอกใหม่ในหน้าตั้งค่า POS** — เหลือเดิม: PDPA text ใน `#วิธีใช้งาน`/`terms.js`/`privacy.js` (deferred ตั้งใจ รอใกล้เปิดตัวจริง), White-Label แบบ LINE OA เต็มรูปแบบ (ยังไม่เริ่ม), QR สั่งอาหารที่โต๊ะ (วางแผนแล้ว ยังไม่เริ่ม), `enforce:true` เต็มรูปแบบของ 4 endpoint จากข้อ 63 (ต้องรื้อ init()/loadShopBody() ก่อน), `export.js`'s `vat`/`vat30` sheet ยังไม่รวมออเดอร์จัดส่ง (`pos_delivery_orders` ไม่มี VAT dual-write ของตัวเอง — ช่องว่างใหญ่กว่าที่ยังไม่ได้แก้ คนละจุดจาก sales/credit/topsellers/pl ที่แก้แล้วในข้อ 82), ตั้งค่าสาขา (ข้อ 75) รอรัน SQL เพิ่มคอลัมน์ `shop_branches.phone`/`shop_bank_accounts.branch_id` ก่อนถึงจะบันทึกได้จริง, **521 กลุ่มผู้ติดต่อซ้ำเดิมยังไม่ได้ลบ รอผู้ใช้ใช้เครื่องมือใหม่ (ปุ่ม "เลือกเฉพาะรายการซ้ำส่วนเกิน") ลบเอง หรือขอให้ Claude ลบให้**, ข้อมูล ledger เก่าก่อน Phase 3 Tier 6 deploy จะไม่โผล่ในหน้าเว็บ "บัญชี" อีกต่อไป — ตั้งใจไม่ backfill (ยังดูได้จาก Google Sheet ตรงๆ), ข้อเสนอแพ็คเสริมสาขา (ข้อ 65) ยังรอผู้ใช้ตอบ, ระบบลงชื่อเข้า-ออกงานพนักงาน (attendance) — ยังเป็นแค่ช่องกรอกมือ ยังไม่มีระบบสแกน/GPS จริง + ต้องรองรับรูปแบบเวลาทำงานที่ต่างกันตามแต่ละธุรกิจ (ตัวอย่างผู้ใช้เอง: ทำงาน 8:00-19:00 ทุกวัน ไม่มี OT แต่ให้วันหยุดเพิ่มแทน) — ยังไม่ได้ออกแบบ/เริ่มทำเลย, **งานกลยุทธ์/roadmap ภาพรวมที่ยังไม่ได้เริ่มทำ** (ผู้ใช้ระบุว่านอกจาก known gap ที่ backlog ไว้แล้ว ยังมีงานเชิงกลยุทธ์อีกที่ยังไม่ได้แตะเลย — รายละเอียดยังไม่ได้ระบุ ต้องถามผู้ใช้ตอนกลับมาทำต่อว่าหมายถึงอะไรบ้าง), Google Maps migration (ผู้ใช้แจ้งว่าปักหมุด Leaflet/OSM คลาดเคลื่อนจาก Google Maps จริงเล็กน้อย ขอให้เปลี่ยนเป็น Google Maps — ยังไม่ได้เริ่ม ถูกขัดจังหวะด้วยบั๊ก QR + payroll ก่อน ต้อง follow-up กับผู้ใช้))

---

## ภาพรวม

Smile Slip Pro คือ B2B SaaS **ครบวงจร**สำหรับร้านค้าและ SME ไทย (บัญชี + POS + สต็อคสินค้า + จัดส่ง ในที่เดียว จ่ายเงินที่เดียวจบ ไม่ต้องซื้อระบบแยกหลายเจ้า) — ให้พนักงานส่งรูปสลิปโอนเงินหรือบิลรายจ่ายเข้ากลุ่ม LINE บอทจะอ่าน OCR ด้วย Gemini AI แล้วบันทึกลง Google Drive + Google Sheets ของลูกค้าอัตโนมัติ มีระบบเครดิต (สแกน 1 ครั้ง = 1 เครดิต) และ Dashboard เว็บสำหรับจัดการร้านค้า

**หลักการ PDPA (แก้ไข 2026-07-30 — เปลี่ยนทิศทางจากเดิม):** เดิมโปรเจกต์นี้วางตำแหน่งเป็น "บอทอ่านสลิปเฉยๆ ไม่เก็บข้อมูลการเงินของคุณเลย เก็บแต่ใน Google ของคุณเอง" แต่ตอนนี้ product เปลี่ยนทิศทางเป็น **SaaS ครบวงจร (บัญชี 100% + POS + สต็อค)** ซึ่งจำเป็นต้องมีข้อมูลธุรกรรมจริงเต็มรูปแบบใน Supabase ถึงจะให้บริการบัญชี/รายงานภาษี/กระทบยอดได้จริง (เหมือนระบบ POS ที่ย้ายไป Supabase แล้วในเซสชันนี้ ก็เก็บข้อมูลจริงอยู่แล้ว — ยอดขายจริง, ชื่อลูกค้าจริง) — **หลักการใหม่:**
- ✅ **เก็บข้อมูลธุรกรรมจริงใน Supabase ได้** เพื่อให้บริการบัญชี/POS แก่ร้านค้าที่สมัครเป็นลูกค้าเรา (แบบเดียวกับซอฟต์แวร์บัญชีทั่วไป)
- ❌ **ห้ามขาย/แชร์ข้อมูลดิบที่ระบุตัวตนได้ต่อบุคคลที่สาม** โดยเฉพาะ `sender_name` (ชื่อคนโอนเงินให้ร้านค้า — เป็นบุคคลภายนอกที่ไม่เคยสมัคร/ยินยอมอะไรกับเราเลย) — ถ้าต้องการทำ analytics/ขายข้อมูลเชิงแนวโน้ม ให้ใช้ระบบที่เข้ารหัสแล้วเท่านั้น (`slip_analytics`/`sender_profiles` — SHA256 hash + amount bucket, ทำไว้แล้วสำหรับ Enterprise Marketing Intelligence) **ห้ามดึงจาก `ledger_transactions`/ตาราง POS ตรงๆ มาขายต่อเด็ดขาด**
- **ยังไม่ได้แก้:** หน้า `pages/terms.js`/`pages/privacy.js` ที่ลูกค้าเห็นจริง ยังเป็นเนื้อหาเก่า (ตำแหน่ง "ไม่เก็บข้อมูลการเงิน") — ผู้ใช้ตัดสินใจให้รอแก้ตอนใกล้เปิดตัวจริง (ยังไม่มีลูกค้าจริงตอนนี้ ผลกระทบต่ำ) **ต้องแก้ก่อนเปิดตัว/หาลูกค้าจริงเพิ่ม**

---

## เหตุการณ์และบั๊กที่แก้แล้ว (2026-07-15)

**บริบท:** วินโดว์เครื่อง dev ล่มก่อนหน้านี้ ย้ายโปรเจกต์ไปไดร์ใหม่ + ลงเครื่องใหม่ทั้งหมด (Node.js, Google Cloud SDK ไม่มีเลยตอนแรก) ระหว่างตรวจสอบพบว่าไฟล์หลายไฟล์ (`smileslip-dashboard/pages/pos.js`, `smileslip-dashboard/lib/*`, ไฟล์ API หลายสิบไฟล์) **ไม่เคย commit ขึ้น git มาก่อนเลย** ทำให้เกือบเสียงานถาวรตอนไฟล์เสียหาย (ดูข้อถัดไป)

1. **`pos.js` (4,212 บรรทัด, ไฟล์ใหญ่สุดในโปรเจกต์) เสียหายระดับ disk** — พบ NUL byte ยาว 4,096 ไบต์ (เท่าขนาด disk block) แทรกอยู่ 4 จุด (บรรทัด ~812, 1991, 2825, 3708) ทับซ้อนโค้ดจริงจนไฟล์ compile ไม่ผ่าน `/pos` ใช้งานไม่ได้เลย เพราะไฟล์นี้ไม่เคย commit จึงไม่มี backup ใน git — กู้คืนสำเร็จโดยดึง source จาก Cloud Build ของ deploy ที่สำเร็จล่าสุด (bucket `run-sources-smileslip-accounting-pro-asia-southeast1`) มาเทียบ byte-diff กับไฟล์เสีย พบว่าเหมือนกันทุกไบต์ยกเว้น 4 จุดที่เสีย → กู้คืนแล้ว commit เข้า git ทันที (commit `bd5ac1f`)
2. **บั๊กนำเข้าผู้ติดต่อจาก VCF (มือถือ) แสดงชื่อเป็นรหัสดิบ** — เช่น `=E0=B8=AA=E0=B8=B8...` แทนที่จะเป็นชื่อไทย สาเหตุ: `parseVCFText` ใน `pos.js` ไม่รองรับ vCard ที่เข้ารหัส `ENCODING=QUOTED-PRINTABLE` (พบมากในไฟล์ export จาก Android/Samsung) แก้โดยเพิ่ม decoder แปลง `=XX` เป็นไบต์แล้ว decode UTF-8 จริง รวมถึงรองรับกรณีค่ายาวถูกตัดข้ามบรรทัดแบบ soft-break ด้วย (commit `055e27e`)
3. **เพิ่มสินค้าใน POS ไม่ได้ (500 error)** — log จริงจาก production: `[pos/products] Sheets append error: The service is currently unavailable` (Google Sheets API 503 ชั่วคราว) แต่ `lib/google-pos.js` ไม่มี retry เลยสักฟังก์ชัน (ต่างจากบอทที่มี retry ให้ Gemini) แก้โดยเพิ่ม retry 3 ครั้งพร้อม backoff บน 429/500/502/503/504 ให้ `readSheet`/`appendSheet`/`updateSheetRow` (commit `9602058`)
4. **พบไฟล์เสียหายจากเหตุการณ์เดียวกันอีกไฟล์: `pages/api/pos/sales.js`** — ตอน deploy ขึ้น production ครั้งแรกหลังแก้ 3 ข้อบน Cloud Build fail ด้วย error `stream did not contain valid UTF-8` ตรวจสอบพบว่าไฟล์ขนาดเท่าเดิมทุกไบต์แต่มีข้อมูลผิดเพี้ยนกระจายอยู่ ~3,000 จุด (คนละแบบกับ NUL-byte padding ของ pos.js) — กู้คืนจาก Cloud Build source เดียวกัน (2026-07-08) แล้วรัน `npm run build` local เต็มโปรเจกต์เพื่อยืนยันว่าไม่มีไฟล์อื่นเสียหายอีก (สำเร็จทุก route) ก่อน deploy จริง (commit `ae8eb45` → revision `smileslip-dashboard-00234-j9l`, pin traffic 100% แล้ว)
   - **บทเรียน:** ไฟล์ที่เสียหายจากเหตุการณ์เครื่องล่มอาจมีมากกว่า 1 ไฟล์ และ syntax check แบบ sucrase เพียงอย่างเดียวไม่พอ (pos.js ผ่าน sucrase แต่จริง ๆ ก็เคยพังเพราะ NUL byte) — ทางที่ชัวร์สุดคือรัน `npm run build` เต็มโปรเจกต์ก่อน deploy ทุกครั้งหลังกู้ไฟล์ที่สงสัยว่าเสียหาย
5. **เพิ่มฟีเจอร์ QR รับเงินแบบ Biller ID (Thai QR Payment / Bill Payment)** — เดิม POS รองรับแค่พร้อมเพย์ส่วนบุคคล (Tag 29: เบอร์โทร/เลขนิติบุคคล 13 หลัก ผ่านไลบรารี `promptpay-qr`) แต่บางร้าน/บริษัทรับเงินผ่าน **Biller ID ที่ธนาคารออกให้ต่างหาก** (Tag 30, มาตรฐาน ITMX เดียวกันทุกธนาคาร ไม่จำกัดแค่ SCB/KBank) ซึ่งเป็นคนละระบบกับพร้อมเพย์โดยสิ้นเชิง (เงินไม่ผ่านทะเบียนพร้อมเพย์ แต่เข้าบัญชีที่ผูกกับ Biller ID นั้นตรง ๆ) เพิ่ม `lib/thai-qr-billpayment.js` (self-written ตามสเปก EMVCo/ธปท. เพราะไม่มี npm package รองรับ Tag 30) + แก้ `api/pos/promptpay-qr.js` ให้เช็ค `pos_configs.scb_biller_id` ก่อน ถ้ามีค่าใช้ Bill Payment แทนพร้อมเพย์ทันที + เพิ่มช่องกรอก "Biller ID จากธนาคาร" แยกในหน้าตั้งค่า POS ของ `pos.js` (self-service ต่อร้าน ไม่ hardcode) — commit `2cf344e`
   - **ข้อควรระวัง:** เลขที่โชว์บนหน้าจอ "QR รับเงิน" ของแอปธนาคาร (เช่น "เลขอ้างอิง" ใน K SHOP ของ KBank) **อาจไม่ใช่ Biller ID จริง** ต้องเข้าไปดูใน "แก้ไขข้อมูลร้านค้า"/merchant settings ของแอปธนาคารเพื่อหา Biller ID ตัวจริงมากรอก (พบเคสจริง: K SHOP โชว์ "เลขอ้างอิง" เป็นคนละเลขกับ Biller ID จริงในหน้า merchant settings เลย — คนละฟิลด์กันโดยสิ้นเชิง)
7. **นำเข้าผู้ติดต่อ VCF/CSV จำนวนมาก (2,000+ รายชื่อ) นำเข้าแล้วไม่ขึ้นเลยสักคน แต่ระบบขึ้นว่าสำเร็จ** — สาเหตุ: `runImport()` เดิมยิง `POST /api/pos/contacts` **ทีละคน** (2,183 คำขอติดกัน) แต่ละคำขอ trigger OAuth token refresh + อ่าน metadata ชีต + append ทีละแถวใหม่หมด ทำให้โดน rate limit ของ Google พังกลางทางแน่นอน และที่ร้ายกว่าคือโค้ดเดิม**ไม่เช็ค response เลย** ถือว่าสำเร็จเสมอแม้ request จริงจะ error ก็ตาม แก้โดย: เพิ่ม `appendRows()` ใน `lib/google-pos.js` ให้ append หลายแถวในคำขอเดียว, เพิ่ม branch bulk import ใน `api/pos/contacts.js` (`body.contacts` เป็น array) ยิงเป็น chunk ละ 500 แถว, แก้ `runImport()` ให้ส่งเป็น chunk ละ 300 จากฝั่งเว็บและ**เช็ค response จริงทุกก้อน** รายงานจำนวนสำเร็จ/ล้มเหลวตามจริง — ไม่ใช่ hardcode ว่าสำเร็จเสมอ (commit `36d21d3`)
   - ถือโอกาสแก้ `makeContactId()` เพิ่มด้วย เดิมใช้ `Date.now()` อย่างเดียวซึ่งชนกันได้ถ้าสร้างหลาย ID ใน loop เดียวกัน (synchronous) แบบตอน bulk import — เพิ่มส่วนสุ่มต่อท้ายกันชน
8. **เพิ่มสินค้าใน POS ไม่ได้ (ขึ้นว่าสำเร็จแต่ไม่โผล่ในรายการ) — สาเหตุคนละอันกับข้อ 3 เดิม** — ตรวจสอบพบว่า Google Sheets append ด้วย range แบบเปิดกว้าง `A:Z` ทำให้แถวใหม่เพี้ยนตำแหน่งไปทางขวาเรื่อยๆ ทุกครั้งที่ append (คอลัมน์ P → Y → ...) เพราะแท็บ "สินค้า" ของร้านที่เชื่อมต่อ POS **ก่อน** 2026-07-06 (ตอนขยาย schema เป็น 18 คอลัมน์) จะมี header แค่ 10 คอลัมน์ ไม่ตรงกับความกว้างข้อมูลจริง ยิ่ง append ยิ่งเพี้ยนสะสม (self-reinforcing) จนสินค้าที่เพิ่มไปหาย เพราะแอปอ่านคอลัมน์ A/B (รหัส/ชื่อ) ไม่เจอ — แก้โดยเปลี่ยน range append ให้ระบุขอบเขตคอลัมน์ตรงตามความกว้างแถวจริง (`colLetter()` helper) ทั้งใน `lib/google-pos.js` และ `lib/google-delivery.js` (มี pattern เดียวกัน) (commit `9b2854d`) — **ยืนยันด้วยการทดสอบยิง API จริงก่อน/หลังแก้ พบว่าต้องซ่อมข้อมูลที่เพี้ยนอยู่เดิมก่อนด้วย (เพราะข้อมูลเก่าที่เพี้ยนไปแล้ว "ยึด" ตำแหน่งไว้ ทำให้แก้แค่โค้ดอย่างเดียวไม่พอ ต้อง clear+เขียนข้อมูลที่ถูกต้องกลับเข้าคอลัมน์ A-R ใหม่ด้วย)**
   - ซ่อมข้อมูลร้าน "ดี แก๊ส" (shop_id `f8009a36-9331-40c8-9664-ab255ebbd35c`) แล้ว 3 รายการ (น้ำแก๊ส 15 กก., ถังแก๊ส 15 กก., น้ำแก๊ส 7 กก.) กลับมาอยู่คอลัมน์ A-R ถูกต้อง + อัปเดต header เป็น 18 คอลัมน์เต็มด้วย (ของเดิมมีแค่ 10) + แก้ช่องวันที่ที่เคยเป็นเลข serial ดิบ (`244538.6336`) ให้เป็นวันที่อ่านได้ปกติ — ยืนยันแล้วว่า appear ถูกต้องทั้งใน Sheets ดิบและผ่าน `GET /api/pos/products` ของแอปเอง
   - **ตัดสินใจแล้ว (2026-07-16):** ยังไม่มีลูกค้าจริงใช้งาน มีแค่ร้านทดสอบเดียวกันนี้ จึงไม่ต้องสแกนร้านอื่นเพิ่ม แต่เพิ่มการป้องกันล่วงหน้าไว้แทน (ดูข้อถัดไป)
9. **Preventive fix: `ensureTabExists()` auto-patch header ที่สั้นกว่ามาตรฐาน** — กันปัญหาแบบข้อ 8 เกิดซ้ำกับร้านในอนาคตที่เชื่อมต่อ POS ไว้ก่อน schema จะขยายคอลัมน์อีก (หรือ schema เปลี่ยนอีกในอนาคต) ทุกครั้งที่เปิดใช้แท็บ (`สินค้า`/`ยอดขาย`/`ผู้ติดต่อ`/ฯลฯ) จะเช็ค header แถวแรกเทียบกับ headers ที่ควรมี ถ้าสั้นกว่าจะเขียน header เต็มกลับเข้าไปทันที (แก้แค่แถว header เท่านั้น ไม่แตะข้อมูล) เหมือน pattern `getOrCreateYearSheet()` ของบอท (commit `779405a`)
10. **เพิ่มปุ่มแก้ไข/ลบในแท็บ "ออเดอร์จัดส่ง"** — เดิมมีแค่ปุ่มเปลี่ยนสถานะ ไม่มีทางแก้รายละเอียด (เช่น เปลี่ยนพนักงานส่ง) หรือลบออเดอร์เลย (ไม่ใช่บั๊ก แค่ยังไม่เคยสร้าง) เหตุผลใช้งานจริง: บางครั้งต้องเปลี่ยนผู้ส่ง หรือลูกค้ามารับเองแทนการจัดส่งเลยต้องลบทิ้ง — แก้ `api/pos/delivery.js` ขยาย PATCH ให้แก้ได้ทุกฟิลด์ (ชื่อ/เบอร์/ที่อยู่/วิธีชำระ/พนักงานส่ง/หมายเหตุ) + เพิ่ม DELETE (blank แถวทิ้ง แบบเดียวกับ products/contacts) และเพิ่ม modal แก้ไข + ปุ่มลบใน `pos.js` (commit `da96f29`)
11. **เพิ่ม `#สมัครพนักงานขนส่ง` / `#สมัครผู้จัดการสาขา`** — มิเรอร์ `#สมัครแอดมิน` เดิมทุกประการ (บอทดักจับ LINE userId อัตโนมัติจาก `event.source.userId` ตอนพิมพ์คำสั่งในกลุ่ม ไม่ต้องขอ user id เอง) ต่างแค่ผูกกับ **สาขา** ผ่าน `findShopBySource()` ที่รู้อยู่แล้วว่ากลุ่มไลน์ไหนคือสาขาไหน — พนักงานพิมพ์ในกลุ่มสาขาไหนก็ขึ้นสาขานั้นอัตโนมัติ **"แอดมิน" เดิมปล่อยไว้เหมือนเดิมทั้งร้าน ไม่ผูกสาขา** (ตัดสินใจแล้วไม่ retrofit ของเดิม) เพิ่ม role ใหม่แยกต่างหาก 2 ตัวแทน — เก็บคำขอ pending ในตาราง `branch_role_requests` ใหม่ อนุมัติผ่านแดชบอร์ด POS → ตั้งค่า → คำขอสมัคร (mirror UI สไตล์เดียวกับอนุมัติแอดมิน) อนุมัติ `delivery_staff` แล้ว sync เข้าแท็บ "พนักงาน" ทันที (เพิ่มคอลัมน์ "สาขา" ต่อท้าย H — ปลอดภัยเพราะมี auto-patch header จากข้อ 9 รองรับอยู่แล้ว) ส่วน `branch_manager` แค่ mark approved (ยังไม่มีระบบสิทธิ์แยกตามสาขาจริงจัง เป็น known gap) (commit `892d66f`)
    - **ต้องทำด้วยมือ:** ต้องรัน SQL สร้างตาราง `branch_role_requests` ก่อนใช้งานได้จริง (ดู SQL ด้านล่าง)
    - **พบปัญหาแทรก: CI/CD ของบอทพังอยู่ตั้งแต่ก่อนหน้านี้ (ไม่เกี่ยวกับโค้ด feature นี้)** — auto-deploy trigger `deploy-bot-on-push` fail ด้วย error ดึง build image `gcr.io/google-cloud-builders/gcloud` ไม่ได้ (`Permission "artifactregistry.repositories.downloadArtifacts" denied`) เป็นปัญหา IAM/Artifact Registry migration ของโปรเจกต์ ไม่ใช่บั๊กโค้ด — revision ล่าสุดของ `smileslip-service` ค้างอยู่ที่ 2026-07-03 ทั้งที่ push โค้ดใหม่ไปหลายรอบแล้ว แก้ชั่วคราวโดย deploy บอทด้วยมือผ่าน `gcloud run deploy` ตรง ๆ (เหมือน dashboard) จนกว่าจะมีคนไปแก้ IAM permission ของ Cloud Build service account ให้ดึง image จาก Artifact Registry ได้จริง
12. **หน้าขาย POS: แก้ราคาต่อรายการได้, ส่วนลดเลือกบาท/เปอร์เซ็นต์, ราคาประจำตัวลูกค้า** — เพิ่มช่องแก้ราคาต่อรายการในตะกร้าตอน checkout (เดิม fix ตามราคาสินค้าอย่างเดียว), เพิ่ม toggle ส่วนลด ฿/% (เดิมมีแค่ลดเป็นบาทตายตัว), และเพิ่มตัวเลือกลูกค้าใน checkout (ขยายจาก picker ลูกค้าเชื่อเดิมที่เคยโชว์เฉพาะตอนจ่ายแบบ "เชื่อ" ให้โชว์เสมอ) — เลือกลูกค้าเดิมแล้วระบบจะดึง **ราคาล่าสุดที่เคยขายให้ลูกค้าคนนี้ต่อสินค้า** จากประวัติการขาย มาใส่ให้อัตโนมัติ (ผ่าน `GET /api/pos/sales?customerId=`) (commit `1e68aa0`)
    - **เจอบั๊กจริง 2 จุดระหว่างทำ:** (1) `creditCustomer?.id` เป็น `undefined` เสมอ เพราะ contact ใช้ field `contact_id` ไม่ใช่ `id` — ทำให้ก่อนหน้านี้ขายเชื่อไม่เคยบันทึก customerId ลง Sheets จริงเลย (2) `GET /api/pos/sales` อ่านแค่ `A:L` ทั้งที่ `รหัสลูกค้า`/`ชื่อลูกค้า` อยู่คอลัมน์ M/N (index 12/13) — ไม่เคยถูกอ่านมาก่อนเลยตั้งแต่สร้างฟีเจอร์นี้ ทั้งสองจุดแก้แล้วในคอมมิตเดียวกัน
13. **แท็บ "ผู้ติดต่อ" เพี้ยนคอลัมน์หนักกว่าที่คิด — เจอ 8,365 แถวเพี้ยนสะสม** — ตรวจสอบพบว่าบั๊ก column-drift ข้อ 8 (append ด้วย `A:Z` เปิดกว้าง) เกิดกับแท็บ "ผู้ติดต่อ" มานานแล้วจากการนำเข้า VCF หลายรอบก่อนหน้า สะสมเป็น 8,365 แถวที่ข้อมูลจริงเลื่อนไปเริ่มที่คอลัมน์ S (บางแถวเก่าสุดยังมีชื่อเป็นรหัส quoted-printable ดิบ/`#ERROR!` จากก่อนแก้บั๊ก VCF ด้วย) — **ที่สำคัญ: แก้ range เป็น `A:W` แล้วยังไม่พอ** เพราะข้อมูลเก่าที่เพี้ยนยังคง "ยึด" ตำแหน่งให้ Google Sheets สับสนต่อไป (ทดสอบ append ใหม่หลังแก้ range ก็ยังเพี้ยนไปคอลัมน์เดิม) ต้อง **ล้างข้อมูลเก่าทั้งหมดออกก่อน** (ตามที่ผู้ใช้ตัดสินใจ เพราะข้อมูลเก่าจัดระเบียบยาก ให้ลบแล้วนำเข้าใหม่แทนที่จะกู้คืน) ล้างด้วย `values:clear` บนช่วงกว้าง (A1:BZ10000) แล้วเขียน header กลับเข้าไปใหม่ — ทดสอบ append หลังล้างแล้วยืนยันว่าลงคอลัมน์ A-W ถูกต้อง 100%
    - **บทเรียนสำคัญเพิ่มเติม:** แก้ code (range fix) เพียงอย่างเดียวไม่พอสำหรับชีตที่มีข้อมูลเพี้ยนสะสมอยู่แล้วจำนวนมาก ต้องล้าง/ซ่อมข้อมูลเก่าด้วยเสมอ ถึงจะกลับมาทำงานถูกต้อง
14. **เบอร์โทร/เลขภาษี หายเลข 0 หน้า + วันที่กลายเป็นเลข serial ดิบ** — พบระหว่างทดสอบซ่อมข้อมูลข้อ 13: Google Sheets `valueInputOption=USER_ENTERED` ตีความค่าที่หน้าตาเป็นตัวเลข/วันที่แล้วแปลงเองอัตโนมัติ ทำให้เบอร์โทรที่ขึ้นต้นด้วย 0 โดนตัด 0 ทิ้ง (`0812345678` → `812345678`) และบางแถววันที่กลายเป็นเลข serial (`244546.6845`) แทนข้อความอ่านได้ — แก้โดยเพิ่ม `asText()` helper ใน `api/pos/contacts.js` (เติม apostrophe (`'`) นำหน้าค่า บังคับให้ Sheets เก็บเป็นข้อความเสมอ) ใช้กับ phone/tax_id/contact_person_phone/created_at/updated_at ทั้ง 3 จุดที่เขียนข้อมูล (bulk import, เพิ่มทีละคน, PATCH)
    - **หมายเหตุ:** ปัญหานี้อาจมีอยู่ในไฟล์อื่นที่เขียน Sheets ด้วย pattern เดียวกัน (เช่น เบอร์โทรพนักงาน/ลูกค้าในแท็บอื่น) ยังไม่ได้ไล่แก้ให้ครบทุกไฟล์ ถ้าเจอปัญหาแบบเดียวกันที่อื่นให้ใช้วิธีแก้เดียวกันนี้
15. **`api/pos/receives.js` (บันทึกรับสินค้า) ล้างคอลัมน์ K-R ของสินค้าทิ้งทุกครั้งที่รับเข้า** — พบตอนอธิบายวิธีทำงานของฟีเจอร์นี้ให้ผู้ใช้ฟัง: โค้ดอ่าน/เขียนแค่ `สินค้า!A:J` (10 คอลัมน์) และ pad แค่ 10 ก่อนเรียก `updateSheetRow` ทั้งที่ schema จริงมี 18 คอลัมน์ (A-R) — ทุกครั้งที่รับสินค้าเข้าตัวไหน จะเขียนทับล้างประเภท/รหัสสินค้าอื่น/บาร์โค้ด/รายละเอียด/VAT/สถานะ ของสินค้าตัวนั้นทิ้งหมด (ไฟล์อื่นที่แตะชีตเดียวกัน เช่น products.js, loans.js ทำถูกต้องด้วยการ pad ครบ 18 อยู่แล้ว) แก้โดยเปลี่ยนเป็นอ่าน `A:R` และ pad เป็น 18 (commit `62c90c8`) — ตรวจสอบแล้วว่าร้าน "ดี แก๊ส" ยังไม่มีใครใช้ฟีเจอร์นี้ตั้งแต่ซ่อมข้อมูลสินค้ารอบก่อน จึงยังไม่มีข้อมูลเสียหายจากบั๊กนี้จริง
16. **ค้นหาผู้ติดต่อด้วยเบอร์โทรต้องพิมพ์ขีด (-) ให้ตรงเป๊ะถึงจะเจอ + รายชื่อแสดงหมดไม่แบ่งหน้า** — แก้ให้เทียบเบอร์โทรเฉพาะตัวเลข (ตัดขีด/วรรคออกทั้งฝั่งค้นหาและฝั่งข้อมูลก่อนเทียบ) พิมพ์ `0956789715` เจอเหมือนพิมพ์ `095-678-9715` โดยไม่กระทบการกรองชื่อ/อีเมลแบบเดิม (ยังกรองสดตามตัวอักษรที่พิมพ์เหมือนเดิมตามที่ผู้ใช้ยืนยันว่าดีอยู่แล้ว) + เพิ่ม pagination 20 รายชื่อ/หน้าในแท็บผู้ติดต่อ (รีเซ็ตหน้า 1 ทุกครั้งที่เปลี่ยนตัวกรอง/คำค้นหา) — แก้จุดค้นหาเบอร์โทรแบบเดียวกันในตัวเลือกลูกค้าอีก 3 จุดด้วย (checkout, ยืมสินค้า, จัดส่ง) (commit `b324337`)
17. **"บันทึกรับสินค้า" เชื่อมกับผู้จำหน่ายจริง (contact_id) + รองรับ VAT ต่อรายการ + จำราคาซื้อล่าสุดต่อผู้จำหน่าย** — เดิมกรอกชื่อผู้จำหน่ายเป็น freetext ไม่เชื่อมกับผู้ติดต่อ ไม่มี VAT เลย (เก็บแค่ยอดรวมเส้นเดียว) และไม่มีทางรู้ว่าผู้จำหน่ายแต่ละเจ้าเคยขายสินค้าตัวไหนราคาเท่าไหร่ (ผู้ใช้ต้องการข้อมูลนี้เพื่อวางแผน data product เปรียบเทียบราคาข้ามร้านในอนาคต — ตัดสินใจแล้วทำแค่ **ส่วนที่ 1** ก่อน คือเก็บข้อมูลระดับร้านเดียวเท่านั้น ส่วนการรวมข้อมูลข้ามร้าน (ส่วนที่ 2) ชะลอไว้รอตรวจสอบข้อกฎหมาย/PDPA ก่อน ยังไม่ทำ) —
    - เพิ่มตัวเลือกผู้จำหน่ายแบบค้นหาจากผู้ติดต่อจริง (ใช้ pattern ค้นหาเบอร์โทรแบบเดียวกับข้อ 16) แทน `<input list>` เดิม, ส่ง `supplierId` (contact_id) คู่กับชื่อไปเก็บด้วย
    - ขยาย `RECEIVE_HEADERS` จาก 6 → 9 คอลัมน์ (A-I): เพิ่ม G=รหัสผู้จำหน่าย, H=ยอดก่อน VAT, I=ยอด VAT รวม (คอลัมน์ E ยังเป็นยอดรวมสุทธิเหมือนเดิมเพื่อ backward compat)
    - แต่ละรายการสินค้าในใบรับสินค้ามี toggle "มี VAT 7%" ต่อชิ้น (`item.hasVat`) คำนวณ VAT เฉพาะรายการที่ติ๊ก แล้วรวมเป็นยอดก่อน VAT/VAT รวม/ยอดสุทธิ ให้ดูเหมือนใบกำกับภาษีจริง
    - เพิ่ม `GET /api/pos/receives?supplierId=` filter → หน้าเว็บเรียกดูประวัติราคาที่ผู้จำหน่ายรายนี้เคยขายให้ต่อ SKU มาเติมราคาอัตโนมัติเวลาเลือกผู้จำหน่ายเดิม (mirror ฟีเจอร์ราคาประจำตัวลูกค้าจากข้อ 12) พร้อมอัปเดตราคาย้อนหลังให้รายการที่เพิ่มไว้ในตะกร้าแล้วถ้าเจอราคาที่เคยซื้อ
    - Verified: sucrase parse ผ่าน + `npm run build` เต็มโปรเจกต์ผ่านทุก route (commit `d5a0411`)
18. **หน้าขาย POS: เลือกลูกค้าตอนเปิดบิลแล้วต้องเลือกซ้ำตอนชำระเงิน/จัดส่ง + ยอดขายบันทึกเป็น "cash sale" ทั้งที่เลือกลูกค้าไว้แล้ว** — ตรวจสอบพบว่า `newBillCust` (ลูกค้าที่เลือกตอนเปิดบิล) ใช้แค่ตั้งชื่อแท็บบิลใน UI เท่านั้น ไม่เคยถูกส่งต่อเข้า `creditCustomer` (ตัวที่จริงบันทึกลง Sheets ตอน checkout) หรือ `delivCust` (ตอนกดจัดส่ง) เลย ทำให้ต้องเลือกลูกค้าซ้ำทุกครั้ง และถ้าลืมเลือกซ้ำตอน checkout ยอดขายจะถูกบันทึกเป็น "cash sale" เสมอทั้งที่เลือกลูกค้าไว้แล้วตอนเปิดบิล — แก้โดยเพิ่ม `openCheckout()`/`openDelivery()` helper ที่ดึงลูกค้าจากบิลปัจจุบัน (`openBills.find(...).customer_id` → lookup ใน `contacts`) มาเติมให้อัตโนมัติก่อนเปิด modal ถ้ายังไม่มีให้ถามตามปกติ + เพิ่ม reset `creditCustomer`/`customerPrices` ใน `createBill()`/`switchBill()` กันลูกค้าเก่าเปื้อนข้ามบิล + แก้บั๊กค้นหาเบอร์โทรต้องพิมพ์ขีดในตัวเลือกลูกค้าตอนเปิดบิลใหม่ (จุดเดียวที่ตกหล่นจากข้อ 16) (commit `a033ca6`)
    - **บทเรียนสำคัญจากรอบนี้:** เจอ deploy จากกระบวนการอื่นที่ไม่ใช่เซสชันนี้แทรกเข้ามาเอง (revision `00245`,`00246` ไม่ทราบที่มา) ทำให้ traffic ค้างอยู่ revision เก่าซ้ำแล้วซ้ำอีกจนดูเหมือนโค้ดที่แก้ไปแล้วหลายรอบ (คอนแทคเพจ filter/pagination, VAT รับสินค้า) ไม่ขึ้นจริง ทั้งที่ commit ถูกต้องมาตั้งนานแล้ว — **สงสัยว่ามีอีก session/หน้าต่าง Claude Code เปิดทำงานพร้อมกันบนโปรเจกต์เดียวกัน** ต้องระวังเรื่องนี้เป็นพิเศษ, และ**gcloud run deploy ข้อความ "Done...serving 100 percent of traffic" ท้าย log เชื่อไม่ได้เต็มร้อย** (เจอซ้ำสองครั้งที่มันแสดงชื่อ revision เดิมจากรอบก่อนหน้าทั้งที่รันจริงและสร้าง revision ใหม่จริง) ต้องเช็ค `gcloud run revisions list --sort-by=~metadata.creationTimestamp` เทียบเวลาสร้างจริงเสมอ ไม่ใช่เชื่อ text output
    - **บทเรียนเพิ่มเติม:** ฟีเจอร์ "ปักหมุดที่อยู่บนแผนที่" (`MapPickerModal` ใน `pos.js`) มีอยู่แล้วสมบูรณ์ตั้งแต่ commit แรกสุดของวัน (`bd5ac1f`) ใช้ Leaflet + OpenStreetMap ฟรี ไม่ต้องมี API key เลย (มีปุ่ม "ใช้ตำแหน่งปัจจุบัน" GPS ด้วย) แต่ไม่เคยถูก deploy ขึ้น production จนกว่าจะ deploy รอบนี้ — เกือบไปเปิดใช้ Google Maps API แบบเสียเงินซ้ำซ้อนโดยไม่จำเป็น (สร้าง API key แล้วลบทิ้งทันทีที่รู้ตัว) **บทเรียน: ต้องเช็คโค้ดที่มีอยู่จริงในไฟล์ก่อนเสนอ solution ใหม่เสมอ แม้จะดูเหมือนฟีเจอร์ไม่มีจากมุมมองผู้ใช้ก็ตาม เพราะอาจจะมีอยู่แล้วแค่ยังไม่ได้ deploy**
    - **ตรวจสอบความปลอดภัยเพิ่มเติม (2026-07-16):** ผู้ใช้ถามว่าโดนแฮ็กไหมหลังเจอ deploy revision แปลกปลอม — ตรวจ Cloud Audit Log พบว่าทุก deploy (รวมที่แปลกปลอม) มาจาก principal เดียวกัน (`smileslip.official@gmail.com`) เดียวกับที่ session นี้ auth ไว้ ไม่มี user/service account/API key แปลกปลอมใน IAM ไม่มี Scheduled Task หรือ process ค้างในเครื่อง สรุปว่าไม่พบสัญญาณถูกแฮ็ก น่าจะเป็นคำสั่ง deploy ของเซสชันนี้เองที่ค้างคิว build นานผิดปกติ
19. **ทดสอบระบบเต็มรูปแบบ (สร้างออเดอร์จัดส่งจริง + เพิ่มสินค้าจริง) เจอบั๊กเบอร์โทรหาย 0 หน้าอีก 2 จุด** — ผู้ใช้ขอให้ทดสอบทั้งระบบขาย→จัดส่ง→พนักงาน และเพิ่มสินค้า สร้างออเดอร์จัดส่งจริงไปหาพนักงาน "Testing" ที่เชื่อมไว้แล้ว ยืนยันว่า LINE push ส่งสำเร็จ (log ไม่มี error, API คืน 200) และข้อมูลลงคอลัมน์ถูกต้องครบทุกคอลัมน์ **แต่พบว่าเบอร์โทรลูกค้าในออเดอร์จัดส่งเหลือ "812345678" (หาย 0 หน้า)** เพราะ `api/pos/delivery.js` เขียนคอลัมน์เบอร์โทรแบบดิบไม่ผ่าน `asText()` เหมือนบั๊กที่เคยแก้ใน `contacts.js` (ข้อ 14) — แก้ทั้ง POST และ PATCH แล้ว + เจอจุดเดียวกันซ้ำใน `api/pos/staff.js` (เบอร์โทรพนักงาน) และ `api/pos/staff-requests.js` (วันที่ sync พนักงานเป็นเลข serial ดิบ) แก้ครบทั้ง 3 ไฟล์ (commit `b187505`) — **ยังไม่ได้ซ่อมข้อมูลเก่าที่เพี้ยนไปแล้วของพนักงาน "Testing" (เบอร์ `629299552` ควรเป็น `0629299552`, วันที่เพิ่มเป็นเลข serial `244546.796`) เพราะเป็นข้อมูลทดสอบในร้านทดสอบ ผลกระทบต่ำ ผู้ใช้แก้ผ่านหน้าเว็บเองได้เลยตอนนี้ที่บั๊กแก้แล้ว**
    - สร้างข้อมูลทดสอบไว้ในร้าน "D Gas": ผู้ติดต่อ "ทดสอบระบบ (Claude test)", สินค้า "สินค้าทดสอบ Claude" (SKU `PNRMUNV`), ออเดอร์จัดส่ง `DEL20260716170516` — ทั้งหมด label ชัดเจนว่าเป็นข้อมูลทดสอบ รอผู้ใช้ยืนยันว่าจะลบทิ้งหรือเก็บไว้ดูเอง
    - **ซ่อมข้อมูลเก่าที่เพี้ยนของพนักงาน "Testing" แล้ว (2026-07-18):** ตรวจสอบซ้ำพบว่าเบอร์โทร (`0629299552`) จริงๆ ถูกต้องอยู่แล้ว (คงเป็นเพราะมีคน save ทับผ่านหน้าเว็บหลัง `asText()` fix deploy ไปแล้ว) เหลือแค่คอลัมน์ "วันที่เพิ่ม" ที่ยังเป็นเลข serial ดิบ `244546.796` — **เจอสาเหตุที่แท้จริงเพิ่มเติม ไม่ใช่แค่ "ไม่ผ่าน asText() เฉยๆ":** decode เลข serial นี้ด้วย epoch มาตรฐานของ Sheets (30 ธ.ค. 1899) แล้วได้ปี ค.ศ. ~3112 ซึ่งผิดปกติชัดเจน เทียบกับเวลาโดยจริงที่รู้อยู่แล้วจาก Supabase (`branch_role_requests.approved_at` = 2026-07-16T12:06:12 UTC ไม่โดนบั๊กนี้) พบว่าค่าที่ควรจะเป็น (serial ~46,220) ต่างจากค่าจริงในชีต (244,546.796) อยู่พอดี ~198,326 วัน ≈ 543.08 ปี ตรงกับส่วนต่าง พ.ศ./ค.ศ. เป๊ะ — สรุปว่า Google Sheets ตีความปี "2569" (พ.ศ.) ในสตริงวันที่ดิบว่าเป็นปี ค.ศ. ตรงๆ โดยไม่ลบ 543 ก่อน ทำให้ auto-parse วันที่เพี้ยนไปข้างหน้า ~543 ปี (นี่เป็นกลไกคนละแบบกับบั๊ก "เบอร์โทรหาย 0 หน้า"/"วันที่เป็นเลข serial" ที่เจอมาก่อนหน้านี้ — อันนี้คือ auto-parse ปี พ.ศ. ผิดโดยเฉพาะ) — แก้โดยเขียนค่าที่ถูกต้อง `'16/7/2569 19:06:12` (นำหน้าด้วย `'` บังคับ text กัน Sheets parse ซ้ำ) กลับเข้าคอลัมน์ G ตรงๆ ผ่าน Sheets API เดียว ยืนยันผลลัพธ์แล้วว่าแสดงถูกต้องและไม่เพี้ยนอีก
      - **ข้อควรระวังเพิ่มจากบั๊กนี้:** ทุกจุดที่เขียนสตริงวันที่แบบไทย (มีปี พ.ศ.) ลง Google Sheets โดยไม่ผ่าน `asText()` เสี่ยงเพี้ยน ~543 ปีแบบนี้ได้เสมอ ไม่ใช่แค่เสี่ยงกลายเป็นเลข serial ธรรมดา — ต้องผ่าน `asText()` (หรือเทียบเท่า) ทุกจุดที่เขียนวันที่เป็นสตริงดิบ
20. **หน้าจัดส่ง: "ที่อยู่อื่น (พิมพ์เอง)" มีแค่ช่องวางลิงก์ Google Maps ไม่มีปุ่มปักหมุดเหมือนหน้าผู้ติดต่อ** — เพิ่มปุ่ม "🗺️ เปิดแผนที่วางหมุด" ใช้ `MapPickerModal` (Leaflet) ตัวเดียวกัน — **เจอบั๊กระหว่างทำ:** เผลอใส่ `<button>` ซ้อนใน `<button>` เดิม (HTML ไม่อนุญาต browser auto-restructure DOM ทำให้กดปุ่มไม่ติดเงียบๆ ไม่มี error ใน console เลย) แก้โดยเปลี่ยน wrapper ด้านนอกจาก `<button>` เป็น `<div role="button" tabIndex={0}>` แทน — ทดสอบคลิกจริงยืนยันแผนที่เปิดและโหลด tile ถูกต้องแล้ว (commit `64a3f7f`)
21. **หาสาเหตุ LINE push แจ้งเตือนพนักงานส่งไม่เข้าจนเจอ — ต้อง "เพิ่มเพื่อน" บัญชี Smile Slip ก่อน ไม่งั้น push เงียบหายแม้ API ตอบสำเร็จ** — ยิงทดสอบตรง 3+ ครั้ง (ข้อความธรรมดา + Flex Message จริง) LINE ตอบสำเร็จทุกครั้งพร้อม message ID จริงตลอด, token ที่ deploy จริงตรงกับที่ทดสอบเป๊ะ, โควต้าข้อความยังเหลือเยอะ (37/300) — แต่ผู้ใช้ (ยืนยันว่าเป็นเครื่อง/บัญชีทดสอบของตัวเอง เป็นคนพิมพ์สมัครและอนุมัติเอง) ไม่เห็นข้อความเลยสักอันจนกว่าจะกด **"เพิ่มเพื่อน"** บัญชีไลน์ Smile Slip เอง — **สรุปสาเหตุที่แท้จริง:** การพิมพ์คำสั่ง (`#สมัครพนักงานขนส่ง`/`#สมัครผู้จัดการสาขา`/`#สมัครแอดมิน`) ในกลุ่มไลน์ทำให้บอทรู้จัก LINE user ID เท่านั้น **ไม่ได้แปลว่าเป็นเพื่อนกับ OA** ซึ่งจำเป็นสำหรับรับ push ส่วนตัว — LINE Push API จะตอบสำเร็จ (200 + message ID) แม้ผู้รับยังไม่ได้เพิ่มเพื่อน แต่ข้อความจะไม่ถูกส่งจริงจนกว่าจะเพิ่มเพื่อนก่อน (ไม่มี error ฝั่งไหนเลยให้เห็นเลยสักจุด — เป็นบั๊ก UX เงียบที่อันตรายมาก เพราะพนักงานที่อนุมัติแล้วจะไม่รู้ตัวเลยว่าไม่เคยได้รับงานส่งอะไรมาก่อนจนกว่าจะมีคนถามว่าทำไมไม่มา) — **แก้แล้ว:** เพิ่มข้อความเตือน + ลิงก์เพิ่มเพื่อน (`https://lin.ee/wdnoEN5`) ต่อท้ายคำตอบกลับของทั้ง 3 คำสั่ง (`#สมัครพนักงานขนส่ง`, `#สมัครผู้จัดการสาขา`, `#สมัครแอดมิน`) ใน `smileslip-pro/index.js` (commit `adb1b37`, deploy บอทด้วยมือเพราะ CI/CD ยังพังอยู่ตามข้อ 11)
    - **บทเรียนสำคัญ:** LINE Push API "สำเร็จ" (HTTP 200 + message ID) **ไม่ได้แปลว่าข้อความถึงผู้รับจริง** ถ้าผู้รับยังไม่ได้เพิ่มเพื่อนกับ OA — ต้องอาศัยการยืนยันจากฝั่งผู้รับจริงเท่านั้นถึงจะรู้ ห้ามเชื่อแค่ response ของ API ว่า "ส่งสำเร็จ = ได้รับแล้ว" เด็ดขาดสำหรับฟีเจอร์ที่พึ่งพา personal push (เช่น แจ้งเตือนงานส่ง)
22. **ระบบพนักงานส่งของ + การกระทบยอดฝั่งแอดมิน (Phase A + B) — ผู้ใช้ขอ 4 เฟส A→B→C→D ทำทีละเฟส deploy แยกกัน** — เฟส A: เพิ่มหน้า "🚚 งานจัดส่ง" ในหน้าพนักงาน `pos-staff.js` (เข้าด้วย PIN เดิม ไม่ต้อง login LINE) ให้พนักงานส่งดูรายละเอียดออเดอร์ + เลือกวิธีรับเงินจริง (เงินสด/โอน/ค้างจ่าย) + ถ้าโอนแสดง QR พร้อมเพย์ของร้าน + แนบรูปสลิป (กล้องมือถือ) + กรอกจำนวนถังเปล่าที่ลูกค้าคืนมา (เฉพาะสินค้าประเภทหมุนเวียน) แล้วกดยืนยันจัดส่งสำเร็จครั้งเดียว — backend (`api/pos/delivery.js` PATCH `confirm_delivery:true`) อัปเดตสถานะ+วิธีชำระจริง+สลิป+เวลา พร้อมคำนวณสต็อคสินค้าหมุนเวียนแบบ "แลกถัง" อัตโนมัติ: เพิ่ม "เปล่ารอรีฟิล" ของสินค้า และลด "ถังอยู่กับลูกค้า" ของผู้ติดต่อ ตามจำนวนที่คืนมาจริง (floor 0) — **ทดสอบยืนยันด้วยข้อมูลจริงแล้วว่าคำนวณถูกต้อง 100%** (ส่ง 1 คืน 1 → ลูกค้าเหลือ 0, สต็อคเปล่าเพิ่ม 1)
    - เฟส B: หน้า "🚚 ออเดอร์" ของแอดมินแสดงเวลา/ผู้ยืนยันจัดส่ง + ลิงก์สลิป + ปุ่ม "💰 ยืนยันรับเงินเข้าร้าน" และ "📦 ยืนยันรับของคืนเข้าคลัง" แยกขั้นตอนจากที่พนักงานยืนยันมา (สองชั้นกันเงิน/ของหาย) + แสดงว่าออเดอร์สร้างโดยแอดมิน/เจ้าของร้านคนไหน (`created_by` + `resolveCreatedBy()` เทียบ owner_line_id/shop_admins)
    - **บั๊กที่เจอระหว่างทำ:** เฟส A เขียนสถานะ `'ส่งสำเร็จ'` ซึ่งเป็นคนละ string กับ `'ส่งแล้ว'` ที่หน้าแอดมินใช้อยู่เดิม (ปุ่มเปลี่ยนสถานะเองของแอดมิน) ทำให้ order ที่พนักงานยืนยันจัดส่งจะไม่ถูกจับคู่กับ logic เดิมเลยถ้าไม่แก้ — เจอจากการ grep หา string ทั้งโปรเจกต์ก่อนเริ่มเฟส B แก้ให้ตรงกันเป็น `'ส่งแล้ว'` **บทเรียน: ต้อง grep หา status string ที่มีอยู่แล้วในโปรเจกต์ก่อนเพิ่ม status ใหม่เสมอ กันสร้าง synonym ซ้ำที่ logic อื่นไม่รู้จัก**
    - **พบระบบแยกที่ไม่เกี่ยวข้อง:** ระหว่าง grep เจอว่ามี `/delivery` (pages/delivery.js + lib/google-delivery.js) เป็นโมดูลจัดส่งแยกต่างหากจากระบบ POS โดยสิ้นเชิง (คนละ Sheet, คนละ Supabase table `delivery_configs`, คนละ status enum) — เป็นฟีเจอร์คู่ขนานที่มีมาก่อนแล้วตั้งแต่ 2026-07-03 ไม่ใช่ของที่ต้องรวมหรือย้ายมาแก้ในงานนี้ ผู้ใช้ทดสอบ/ใช้งานทั้งหมดผ่านระบบ POS-integrated delivery (`pos.js` + `api/pos/delivery.js`) เท่านั้น
    - ขยาย `ORDER_HEADERS` จาก 14 → 20 คอลัมน์ (A-T) รวม 2 รอบ: เฟส A เพิ่ม ลิงก์สลิป/ยืนยันจัดส่งเมื่อ/ยืนยันโดย/รับเงินเข้าร้านแล้ว, เฟส B เพิ่ม รับของคืนเข้าคลังแล้ว/สร้างโดย
    - `pos-staff.js` และ `pos-location.js` ไม่เคย commit เข้า git มาก่อนเลย (ความเสี่ยงเดียวกับ pos.js เดิม) — commit เข้าไปแล้วระหว่างทำเฟสนี้
    - **เฟส C เสร็จแล้ว:** เพิ่มปุ่มกรอง "🧾 มียอดค้าง/ถังค้าง" ในหน้าผู้ติดต่อ กรองเฉพาะ `debt>0` หรือ `cylinders>0` เรียงยอดค้าง(บาท)มากไปน้อยก่อนตามด้วยจำนวนถัง พร้อมสรุปยอดรวมด้านบน — **ไม่ต้องแก้ backend เลย** เพราะ field `debt`/`cylinders` และปุ่ม "รับชำระ"/แก้ไขค่าเองมีอยู่แล้วในระบบ (แค่ไม่มีทางกรองหาคนที่ค้างอยู่ใน 2000+ รายชื่อมาก่อน) — ทดสอบยืนยันด้วยข้อมูลจริงแล้ว (กรองเจอ "เวสธค" ถัง 3 ใบ ตรงกับออเดอร์จัดส่งจริงที่มีอยู่)
    - **พบข้อสังเกตสำคัญระหว่างทำ:** รายงาน "💳 เงินเชื่อ" เดิมใน tab รายงาน อ่านจากชีต "ยอดขาย" (ขายหน้าร้าน) เท่านั้น **ไม่รวมยอดค้างจากออเดอร์จัดส่ง** (ค้างจ่าย) ซึ่งอัปเดต `contact.debt` ไปคนละทางกัน — เป็น 2 กลไกคนละระบบที่ไม่คุยกัน ตัวกรองใหม่นี้ (อ่านจาก `contact.debt` ตรงๆ) เลยครอบคลุมกว่ารายงานเดิมสำหรับคำถาม "ใครติดเงินอยู่บ้าง" — ยังไม่ได้รวมสองระบบเข้าด้วยกัน เป็น known gap ที่อาจต้องแก้ในอนาคตถ้าพบว่าตัวเลขไม่ตรงกัน
    - **เฟส D ส่วนที่ 1 เสร็จแล้ว (2026-07-17/18):** เพิ่มปุ่ม "🖨️ พิมพ์ใบเสร็จ" + "🧾 ใบกำกับภาษี" หลังชำระเงินสำเร็จในหน้าขาย POS — ตัดสินใจใช้ `window.print()` (print dialog ของ OS เอง) เป็นทางหลักแทน Web Bluetooth ตรงๆ เพราะร้านมีทั้งพนักงานใช้ iPhone และ Android ปนกัน (ถามผู้ใช้ยืนยันแล้ว) และ Web Bluetooth ใช้ได้แค่ Android/Chrome เท่านั้น iPhone/Safari ปิดกั้นสิ้นเชิง — `window.print()` ทำงานผ่าน AirPrint (iPhone) และ print service ของ Android ได้เหมือนกันทั้งคู่ ไม่ต้องเดา BLE UUID ของเครื่องพิมพ์ที่ไม่รู้ยี่ห้อ/รุ่น
      - เพิ่มตัวเลือกขนาดกระดาษ 58mm/80mm ในหน้าตั้งค่า POS (`pos_configs.receipt_paper_size` — **ต้องรัน SQL ด้วยมือก่อน** ดู "ต้องทำด้วยมือ" ด้านล่าง) — เขียนโค้ด API ให้ทนทาน (แยก query/update คอลัมน์นี้ต่างหาก) กันพังถ้ายังไม่ได้รัน SQL
      - ใบกำกับภาษี: เก็บลง tab ใหม่ "ใบกำกับภาษี" (คนละระบบกับใบกำกับภาษีที่ Smile Slip Pro ออกให้ร้านตอนซื้อแพ็กเกจ) เลขที่รันต่อปีไม่ให้ขาดหาย รูปแบบ `INV-{ปี พ.ศ.}-{ลำดับ 5 หลัก}`, คำนวณ VAT อัตโนมัติจาก `vat_type` ของสินค้าแต่ละชิ้น, ฟอร์มข้อมูลผู้ซื้อดึงค่าเริ่มต้นจากผู้ติดต่อที่เลือกไว้ตอน checkout อัตโนมัติ
      - **บั๊กที่เจอระหว่างทดสอบจริง:** เลขที่ใบกำกับภาษีออกมาเป็น `INV-พ.ศ. 2569-00001` (มีคำว่า "พ.ศ." กับจุด/วรรคปนอยู่) เพราะใช้ `toLocaleDateString('th-TH',{year:'numeric'})` ซึ่งคืนสตริงเต็มไม่ใช่แค่ตัวเลข — แก้เป็นคำนวณปี พ.ศ. เอง (ปี ค.ศ.+543) ได้ `INV-2569-00001` ถูกต้อง (commit `d443806`)
      - ทดสอบยิง API จริงยืนยันแล้ว: ชื่อร้าน/ที่อยู่/เลขภาษีร้าน/ข้อมูลผู้ซื้อ/แยกยอดก่อน-หลัง VAT/สะกดยอดเป็นตัวอักษรไทย (บาทถ้วน) ถูกต้องครบ
      - `pos-config.js` เป็นอีกไฟล์ที่ไม่เคย commit เข้า git มาก่อนเลย (ความเสี่ยงเดียวกับ pos.js เดิม) — commit เข้าไปแล้วระหว่างทำเฟสนี้
    - **ยังไม่ทำ (เฟส D ส่วนที่ 2):** พิมพ์ตรงผ่าน Web Bluetooth (ESC/POS) แบบไม่ต้องผ่าน print dialog สำหรับ Android โดยเฉพาะ — จงใจไม่ทำเพราะ BLE service/characteristic UUID ต่างกันไปตามยี่ห้อเครื่องพิมพ์ ไม่มีมาตรฐานเดียวที่ใช้ได้กับทุกรุ่น เสี่ยงเดาผิดถ้าไม่มีเครื่องจริงให้ทดสอบ — ถ้าจะทำต่อต้องรู้ยี่ห้อ/รุ่นเครื่องพิมพ์ที่ร้านจะใช้จริงก่อน
23. **ขายหน้าร้าน (POS) ไม่หักสต็อค "เต็ม" ของสินค้าหมุนเวียนเลย + ไม่มีทางบอกว่าลูกค้าคืนถังเก่าหรือยืมไป + LINE ที่ส่งหาพนักงานขนส่งไม่มีลิงก์เปิดหน้ายืนยันจัดส่ง + หน้าออเดอร์แสดงทุกออเดอร์ปนกันหาไม่เจอ** — ผู้ใช้แจ้ง 3 จุดพร้อมกัน แก้ครบทั้ง 3 ทีเดียว:
    - **บั๊กสต็อคสินค้าหมุนเวียน:** `api/pos/sales.js` POST เดิมตอนขายสินค้าประเภท "หมุนเวียน" เพิ่มแค่ `at_customer` (กับลูกค้า) แต่ไม่เคยหัก `stock` (เต็ม) เลยตั้งแต่สร้างฟีเจอร์มา (ต่างจากตอนรับสินค้า/รีฟิลที่หักถูกต้อง) ทำให้ตัวเลข "เต็ม" ค้างเกินจริงเรื่อยๆ ทุกครั้งที่ขาย — แก้โดยหัก `stock` ทุกครั้งที่ขายสินค้าหมุนเวียนเหมือนสินค้าปกติ
    - **เพิ่มช่องเลือกคืนถังเปล่า/ยืมถังไป:** ในหน้าชำระเงิน (checkout modal) เพิ่มส่วน "🔄 สินค้าหมุนเวียน — ถังเก่าที่ลูกค้าเอามาคืน" แสดงเฉพาะเมื่อตะกร้ามีสินค้าประเภทหมุนเวียน ใส่จำนวนถังเปล่าที่คืนมาต่อ SKU ได้ (เว้นว่าง = ยืมไปยังไม่คืน) ส่ง `returned_qty` แนบไปกับ item ตอน POST `/api/pos/sales` แล้ว backend คำนวณ: `at_customer` สุทธิ = `+qty -returned_qty`, `empty_waiting` (เปล่ารอรีฟิล) `+returned_qty` — ตรรกะเดียวกับฝั่งจัดส่ง (`delivery.js` `confirm_delivery`) ที่มีอยู่แล้ว, และถ้า checkout ผูกกับลูกค้าไว้ (`creditCustomer`) จะอัปเดต `contact.cylinders` (ถังอยู่กับลูกค้า) ด้วยผลสุทธิเดียวกัน (เดิมขายหน้าร้านไม่เคยอัปเดตค่านี้ต่อลูกค้าเลย มีแต่ฝั่งจัดส่งที่ทำ) — **ทดสอบยิงจริงยืนยันแล้ว 2 เคส:** (1) ซื้อ 1 คืน 1 ไม่ผูกลูกค้า → เต็ม -1, กับลูกค้าสุทธิ 0, เปล่า +1 ถูกต้อง (2) ซื้อ 1 ไม่คืน ผูกลูกค้า "ทดสอบระบบ (Claude test)" → เต็ม -1, กับลูกค้า +1, ถังลูกค้า 0→1 ถูกต้องครบ
    - **ปุ่มเปิดหน้ายืนยันจัดส่งใน LINE:** `buildDeliveryFlex()` ใน `api/pos/delivery.js` เดิมมีแค่ปุ่มเปิด Google Maps/บันทึกพิกัด ไม่มีทางกดจากข้อความ LINE ไปหน้า `pos-staff.js` เพื่อยืนยันจัดส่งได้เลย (ต้องเปิดแอปเองแล้วหา PIN + งานเอง) — เพิ่มปุ่มหลัก "✅ เปิดหน้ายืนยันจัดส่ง" ลิงก์ไป `${FRONTEND_URL}/pos-staff?shopId=...&order_no=...` เป็นปุ่มแรกในปุ่มทั้งหมด + แก้ `pos-staff.js` ให้อ่าน `order_no` จาก query string แล้วหลัง login ด้วย PIN สำเร็จจะพาไปหน้ายืนยันจัดส่งออเดอร์นั้นตรงๆ ทันที (เดิม PIN สำเร็จจะไปหน้าเมนูเสมอ) — **ทดสอบยืนยันแล้วด้วย deep link จริง:** เข้า `/pos-staff?shopId=...&order_no=DEL20260718052552` ใส่ PIN แล้วเด้งตรงไปหน้ายืนยันจัดส่งออเดอร์นั้นถูกต้อง (เห็นรายละเอียดลูกค้า/สินค้า/ตัวเลือกวิธีชำระ/ช่องคืนถังครบ)
    - **ตัวกรองหน้าออเดอร์จัดส่ง:** เพิ่มปุ่มกรองสถานะ (ทั้งหมด/🕒 ค้างจัดส่ง/✅ จัดส่งแล้ว) + ปุ่มกรองวันที่ (ทุกวันที่/📅 ค้างวันนี้/📅 ค้างเดือนนี้ — parse จากสตริงวันที่ไทย "D/M/พ.ศ." ผ่าน `parseThaiOrderDate()` ใหม่) ทั้งสองกลุ่มกรองพร้อมกันได้ (AND) หัวข้อแสดงจำนวนที่กรองได้/ทั้งหมด เช่น "(2/5)" — **ทดสอบยืนยันแล้วด้วยข้อมูลจริง:** กรอง "ค้างจัดส่ง" เจอ 2/5 ถูกต้อง, กรอง "จัดส่งแล้ว" เจอ 2/5 ถูกต้อง (ไม่รวมออเดอร์เก่าที่ยังเป็นสถานะ `ส่งสำเร็จ` ที่ค้างจากก่อนแก้บั๊ก status string ในข้อ 22 — ถูกต้องตามเจตนา เพราะ logic อื่นในระบบก็ไม่รู้จัก status นี้เหมือนกัน), กรอง "ค้างวันนี้" เจอเฉพาะ 2 ออเดอร์ที่สร้างวันนี้จริง
    - **ข้อมูลทดสอบเพิ่มจากรอบนี้:** สร้างบิลขาย 2 ใบในร้าน "D Gas" (`BILL20260718142607`, `BILL20260718142819`) ซื้อ "น้ำแก๊ส 15 กก." ทดสอบ cycle คืนถัง/ยืมถัง — กระทบสต็อคจริง (เต็ม 35→33, เปล่า 6→7) และถังลูกค้า "ทดสอบระบบ (Claude test)" (0→1) รอผู้ใช้ยืนยันว่าจะเก็บไว้ดูหรือล้างข้อมูลทดสอบทิ้ง
24. **ที่อยู่จัดส่งที่พิมพ์เองไม่ถูกบันทึกกลับเข้าข้อมูลลูกค้า (ต้องพิมพ์ซ้ำทุกครั้ง) + ราคาประจำตัวลูกค้าดันไปใช้ตอนขายหน้าร้านด้วย ทั้งที่ควรใช้เฉพาะตอนเปิดออเดอร์จัดส่ง** — ผู้ใช้แจ้ง 2 จุด แก้พร้อมกัน:
    - **บั๊กที่อยู่ไม่บันทึก:** `handleDelivery()` ใน `pos.js` ส่งที่อยู่ที่พิมพ์เอง (`delivAddrIdx===2`) ไปเก็บใน order เท่านั้น ไม่เคยเขียนกลับเข้า `address_1`/`address_2` ของ contact เลย ทำให้ลูกค้าคนเดิมต้องพิมพ์ที่อยู่ใหม่ทุกครั้งที่สั่งจัดส่ง (ต่างจากแก้ไขผ่านหน้าผู้ติดต่อโดยตรงที่บันทึกได้ปกติ) — แก้โดยหลังสร้างออเดอร์สำเร็จ ถ้าใช้ที่อยู่พิมพ์เองและลูกค้ายังไม่มี `address_1` (หรือมีแล้วแต่ `address_2` ว่าง) จะ PATCH `/api/pos/contacts` บันทึกที่อยู่ + maps link เข้าช่องที่ว่างอัตโนมัติ (ไม่ทับที่อยู่เดิมที่มีอยู่แล้วทั้งสองช่อง) — **ทดสอบยืนยันแล้วด้วยลูกค้าจริงที่ไม่เคยมีที่อยู่บันทึกไว้ ("สุคมสัน"):** สร้างออเดอร์ `DEL20260718151534` พิมพ์ที่อยู่ใหม่ → ยืนยันว่า `address_1` ของ contact ถูกเติมอัตโนมัติแล้ว ครั้งหน้าจะขึ้นเป็นตัวเลือก "ที่อยู่ 1" ให้กดใช้ได้เลยไม่ต้องพิมพ์ซ้ำ
    - **สลับตำแหน่งราคาประจำตัวลูกค้า:** เดิม `fetchCustomerPrices()` (ดึงราคาล่าสุดที่เคยขายให้ลูกค้าคนนี้มาเติมในตะกร้า) ถูกเรียกจากฝั่ง **ขายหน้าร้าน** (checkout modal ตอนเลือก/พรีฟิลลูกค้า) เท่านั้น ฝั่ง **จัดส่ง** (`delivCust`) ไม่เคยเรียกเลย — สลับให้ถูกทาง: ลบการเรียก `fetchCustomerPrices` ออกจาก `openCheckout()` และตัวเลือกลูกค้าใน checkout modal ทั้งหมด (ขายหน้าร้านใช้ราคากลางเสมอ แก้เองได้แต่ไม่ auto-fill), ย้ายไปเรียกที่ `openDelivery()` (พรีฟิลลูกค้าจากบิล) และตัวเลือกลูกค้าในหน้าจัดส่งขั้นตอน 1 แทน พร้อมย้าย badge "💰 ใช้ราคาประจำตัวแล้ว" ไปแสดงในหน้าจัดส่งด้วย — **ทดสอบยืนยันแล้วทั้งสองทาง:** ขายหน้าร้านแก้ราคาเป็น 555 แล้วเลือกลูกค้าที่เคยซื้อราคา 480 → ราคายังคง 555 ไม่ถูกเขียนทับ (ถูกต้อง), เปิดหน้าจัดส่งเลือกลูกค้าคนเดิม → ราคาเปลี่ยนจาก 555 กลับเป็น 480 (ราคาประจำตัว) อัตโนมัติพร้อม toast "ใช้ราคาประจำตัวของลูกค้าคนนี้แล้ว" (ถูกต้องตามที่ต้องการ)
    - **Deploy จริงยืนยันแล้ว (2026-07-18):** deploy ทั้ง 2 รอบของวันนี้ (ข้อ 23 + ข้อนี้) เจอ `gcloud run deploy` โชว์ข้อความ "serving 100 percent" พร้อมชื่อ revision เดิม (`00256-2fg`) ซ้ำสองรอบทั้งที่ deploy โค้ดใหม่จริง — ตรงกับบั๊กที่เคยเจอมาก่อน (ข้อ 18) ต้องเช็ค `gcloud run revisions list --sort-by=~metadata.creationTimestamp` เทียบเวลาสร้างจริงทุกครั้งถึงเจอ revision จริง (`00259-r7f`) แล้ว `gcloud run services update-traffic` เองด้วยมือ — ระหว่างนี้ยังเจอ revision ปริศนาเพิ่มอีก 2 ตัว (`00257-w5m`, `00258-8td`) ที่ traffic 0% ตลอด ไม่ทราบที่มา แต่ตรงกับ pattern เดิมที่เคยสอบสวนแล้วสรุปว่าไม่ใช่การถูกแฮ็ก (ข้อ 18) — verified บน production จริงหลัง pin traffic ว่าฟีเจอร์ใหม่ทั้งหมดขึ้นแล้ว (ตัวกรองออเดอร์ + ที่อยู่ที่บันทึกไว้)
25. **ระบบสินค้าหมุนเวียนรอบใหญ่ (กลุ่ม A-E จากที่ผู้ใช้ขอสรุปก่อนเริ่มทำ + ข้อเสนอเพิ่มเติมของ Claude ที่ผู้ใช้อนุมัติทั้งหมด) — ทำทีเดียวหลายกลุ่ม ทดสอบยิงจริงทุกจุดก่อน commit:**
    - **กลุ่ม A (UX สินค้าหมุนเวียนเป็นสากล ไม่ผูกคำว่า "ถัง"):** กลับด้าน default ของช่องคืนของ/ยืมของทั้งใน `pos.js` (checkout) และ `pos-staff.js` (ยืนยันจัดส่ง) — เดิมเว้นว่าง = ยืมไปไม่คืน ใหม่**เว้นว่าง/ไม่กดปุ่ม = แลกเปลี่ยนเต็มจำนวนอัตโนมัติ** (ปกติที่สุด) ต้องกดปุ่ม "ยืม" ต่อรายการก่อนถึงจะกรอกจำนวนที่ยืม (ไม่คืนของเก่า) ได้ — คำนวณฝั่ง frontend เป็น `returned_qty = qty - borrowed` แล้วส่งเข้า backend เหมือนเดิมทุกจุด (ไม่ต้องแก้ backend เลย) + เปลี่ยนคำ UI จาก "ถังเก่า" เป็นใช้ `unit` ที่ตั้งไว้ต่อสินค้าแทน (ร้านแก๊สขึ้น "ถังเก่า" ร้านน้ำขึ้น "ขวดเก่า" อัตโนมัติ ไม่ hardcode)
    - **กลุ่ม B (แก้บั๊กรับสินค้าไม่รองรับสินค้าหมุนเวียน):** `api/pos/receives.js` เดิมเพิ่ม `stock` (เต็ม) เสมอไม่ว่าประเภทไหน ไม่เคยหักจาก `empty_waiting` (เปล่ารอรีฟิล) เลยแม้จะเป็นสินค้าหมุนเวียน — แก้ให้เช็คประเภทก่อน ถ้าเป็น "หมุนเวียน" หัก `empty_waiting` เท่ากับจำนวนที่รับเข้าเสมอ (สมมติฐาน: รับเข้า = ได้ของที่รีฟิล/บรรจุกลับมาแล้ว) + เพิ่มตัวกรองหมวดหมู่ + ปุ่ม "📋 เลือกจากรายการ" (browse แบบเห็นทั้งลิสต์ พร้อม badge "✅ เพิ่มแล้ว" กันเพิ่มซ้ำ) แทนการพิมพ์ค้นหาอย่างเดียวในหน้ารับสินค้า — **ยังไม่ได้ต่อยอดเข้า Excel export** (`api/pos/export.js` เป็นโค้ดคนละชุด reimplement รายงานเองแยกจาก `reports.js` — known gap ถ้าต้องการ export ฟีเจอร์ใหม่ในอนาคต ต้องทำเพิ่มต่างหาก)
    - **กลุ่ม C (มองเห็นสถานะสินค้าหมุนเวียนในหน้ารายงาน):** เพิ่ม report type ใหม่ `cyclical` ใน `api/pos/reports.js` — สรุปสต็อคต่อสินค้า (เต็ม/กับลูกค้า/เปล่ารอรีฟิล) + รายชื่อลูกค้าที่ถือสินค้าหมุนเวียนอยู่ (อ่านจาก `contact.cylinders`) เพิ่มแท็บ "🔄 สินค้าหมุนเวียน" ในหน้ารายงาน — เดิมหน้ารายงานไม่มีโค้ดอ้างอิงถึง cylinders/at_customer/empty_waiting เลยสักบรรทัด (ยืนยันจากการอ่านโค้ดจริงก่อนแก้)
    - **กลุ่ม D (ระบบส่งพนักงานไปเก็บเงินเชื่อ/สินค้ายืม ใหม่ทั้งหมด):** สร้างโมดูลใหม่คู่ขนานกับระบบจัดส่งที่มีอยู่ — tab ใหม่ "งานเก็บเงิน" ใน Google Sheets (`COLLECTION_HEADERS` 21 คอลัมน์ A-U ใน `lib/google-pos.js`) + API ใหม่ `pages/api/pos/collections.js` (GET/POST/PATCH/DELETE, LINE push พร้อมปุ่มเปิดหน้ายืนยันแบบเดียวกับระบบจัดส่ง) + ปุ่ม "📤 ส่งพนักงานไปเก็บ" ในหน้าผู้ติดต่อ (ตัวกรอง "มียอดค้าง/ถังค้าง") และหน้ารายงาน (แท็บสินค้าหมุนเวียน) เปิด modal เลือกประเภทงาน (เงินเชื่อ/สินค้ายืม/ทั้งคู่ — auto-guess จาก debt/cylinders ของลูกค้า, ถ้าร้านมีสินค้าหมุนเวียนแบบเดียวจะเดาจำนวนที่ต้องเก็บให้อัตโนมัติจากยอดรวม) + เลือกพนักงาน → สร้างงาน + LINE push
      - `pos-staff.js` เพิ่ม step ใหม่ `collections`/`collect-confirm` — พนักงานเปิดลิงก์จาก LINE (ข้าม PIN ไปหน้ายืนยันงานนั้นตรงเหมือนระบบจัดส่ง) เห็นยอด/รายการที่ต้องเก็บ (ค่าเริ่มต้น = เก็บได้ครบตามคาด แก้ได้ถ้าเก็บได้บางส่วน) แนบสลิปได้ถ้าโอน กด "✅ เก็บได้" หรือ "❌ เก็บไม่ได้" (พร้อมเหตุผล) — ฝั่งสำเร็จจะลดยอดหนี้/ถังของลูกค้าจริงตามจำนวนที่เก็บได้จริง (ไม่ใช่ตามที่คาดไว้) + เพิ่ม `empty_waiting` ของสินค้าตามจำนวนที่เก็บคืนมา
      - เพิ่ม tab ใหม่ในแอดมิน "🧾 เก็บเงิน/ของ" — ดูงานทั้งหมด กรองสถานะ (รอดำเนินการ/เสร็จแล้ว) + ปุ่มยืนยันรับเงิน/รับของเข้าร้านอีกชั้น (สองชั้นกันเงิน/ของหาย แบบเดียวกับ Phase B ของระบบจัดส่ง) + ยกเลิกงานที่ยังไม่เริ่ม
      - **ทดสอบยิงจริงครบทุก path แล้ว:** (1) ส่งงานเก็บถัง 11 ใบจากลูกค้า "สุคมสัน" → พนักงานตอบเก็บได้บางส่วน (8/11) → ยืนยันถูกต้อง: ถังลูกค้า 11→3, เปล่ารอรีฟิลสินค้า 27→35, ยืนยันรับของเข้าคลังฝั่งแอดมินสำเร็จ (2) ส่งงานเก็บเงินเชื่อ ฿500 (ทดสอบด้วยการตั้งหนี้ปลอมชั่วคราวแล้วลบทิ้งหลังทดสอบ) → พนักงานตอบ "เก็บไม่ได้" พร้อมเหตุผล → ยืนยันว่ายอดหนี้ไม่เปลี่ยนแปลง (500 คงเดิม) ถูกต้องตามที่ควรเป็น
    - **ข้อเสนอเพิ่มเติมที่ผู้ใช้อนุมัติแต่ยังไม่ได้ทำ (รอทำต่อ):** เพดาน/แจ้งเตือนสต็อคเปล่าล้น, ประวัติยืม/แลกเปลี่ยนแบบ audit log รายรายการ, จำกัดจำนวนยืมสูงสุดต่อลูกค้า (เหมือน credit limit), ใบเสร็จ/ใบกำกับภาษีแสดงแยกรายการ "แลกเปลี่ยน" vs "ยืม", รายงานต้นทุนบรรจุ/รีฟิลแยกจากต้นทุนซื้อใหม่
    - **กลุ่ม E (รับสินค้าอัตโนมัติผ่านรูปถ่ายใน LINE) เสร็จแล้ว — ทำแยกทีหลัง A-D เพราะแตะโค้ดบอท LINE (`smileslip-pro/index.js`) คนละไฟล์:**
      - พิมพ์ `#รับสินค้า` (หรือ `#รับของ`) ในกลุ่ม LINE → บอทเช็คว่าร้านเปิด POS ไว้หรือยัง (`pos_configs.pos_sheet_id`) แล้ว "ปลดล็อค" ผู้พิมพ์ (userId) ไว้ 10 นาทีผ่าน `setAwaitingReceive()`/`consumeAwaitingReceive()` — ใช้ pattern เดียวกับ duplicate-guard เดิม (Redis SET/GET/DEL หลักพร้อม fallback in-memory Map ถ้าไม่มี Redis) — ต่างจาก duplicate-guard ตรงที่ใช้ธรรมดา (ไม่ใช่ NX) เพราะต้อง "อ่านค่ากลับมา" (shopId+branchName) ตอน consume ไม่ใช่แค่เช็คว่ามีอยู่ไหม
      - รูปถัดไปจาก userId เดียวกันภายใน 10 นาที จะถูกดักที่ต้น image handler (ก่อนเช็คเครดิต — ฟีเจอร์นี้ไม่ตัดเครดิตสแกนสลิป เพราะเป็นฟีเจอร์ POS คนละระบบ) แล้วแยกไปคนละ flow ทั้งหมด ไม่ผ่าน flow สลิปโอนเงินปกติเลย
      - เพิ่ม `extractReceiveDataWithGemini()` — schema คนละแบบกับสลิป (`supplier`, `invoice_no`, `invoice_date`, `items:[{name,qty,unitPrice}]`) reuse `withRetry()`/pattern JSON-extraction เดิมทุกอย่าง แค่เปลี่ยน prompt
      - เขียนผลลง tab ใหม่ **"รับสินค้ารอยืนยัน"** ใน POS Sheet ของร้านเอง (`ensurePendingReceiveSheet()`/`appendPendingReceive()` ฝั่งบอท, `PENDING_RECEIVE_HEADERS`/`rowToPendingReceive` ฝั่ง dashboard ใน `lib/google-pos.js`) — **คนละ tab กับ "รับสินค้า" ที่ยืนยันแล้ว** เจตนาเพื่อไม่ให้ OCR ที่อาจอ่านผิด/จับคู่สินค้าไม่ได้ไปตัดสต็อคจริงตรงๆ โดยไม่มีคนตรวจก่อน (ตรงตามที่ผู้ใช้ขอ: "ให้แอดมินมากดรับสินค้าอีกทีหนึ่ง...รอยืนยันหรือแก้ไข")
      - รูปใบส่งของอัปโหลดขึ้น Google Drive โฟลเดอร์ย่อย "รับสินค้า" (แยกจากโฟลเดอร์รายรับ/รายจ่ายของสลิปปกติ) ผูกสาขาอัตโนมัติจาก `findShopBySource()` เหมือนสลิปทั่วไป
      - Dashboard: เพิ่ม API `pages/api/pos/receives-pending.js` (GET รายการรอตรวจ, DELETE ปฏิเสธ/ลบออกจากคิว — **ไม่แตะสต็อคเอง**) + sub-view ใหม่ "🔔 รอยืนยันจาก LINE (N)" ในแท็บ "📥 รับสินค้า" — กด "✅ ตรวจสอบ/ยืนยัน" จะจับคู่ชื่อสินค้าที่ OCR อ่านมากับสินค้าจริงในระบบอัตโนมัติ (เทียบชื่อ/คำค้นแบบ substring) พรีฟิลลงฟอร์มรับสินค้าปกติให้แก้ไขก่อนกดยืนยันจริง (ตัดสต็อคจริงผ่าน `/api/pos/receives` เดิมที่มีอยู่แล้ว ไม่ได้เขียน logic ตัดสต็อคซ้ำ) รายการที่จับคู่ไม่ได้จะไม่ถูกใส่ในฟอร์ม แต่ขึ้นเตือนในหมายเหตุ + toast ให้ไปเพิ่มสินค้าใหม่ก่อน — ยืนยันสำเร็จแล้วลบออกจากคิวรอให้อัตโนมัติ
      - **ทดสอบยิงจริงครบทุก path แล้ว** (จำลองแถวที่บอทจะสร้างโดยเขียนตรงผ่าน Sheets API แทนการส่งรูปจริงเข้า LINE เพราะไม่มีรูปใบส่งของจริงให้ทดสอบ แต่ทดสอบ pipeline ฝั่ง dashboard ทั้งหมดด้วยข้อมูลจริง): (1) จำลอง 2 รายการ (1 จับคู่ได้กับ "น้ำแก๊ส 15 กก.", 1 จับคู่ไม่ได้ "อุปกรณ์ทดสอบ XYZ") → กด "ตรวจสอบ/ยืนยัน" → ฟอร์มพรีฟิลถูกต้องครบ (ชื่อผู้จำหน่าย/จำนวน/ราคา) + หมายเหตุเตือนรายการที่จับคู่ไม่ได้ถูกต้อง → ยืนยันจริง → สต็อค 37→47 + ต้นทุนเฉลี่ยคำนวณใหม่ถูกต้อง + แถวรอยืนยันหายไปจากคิวอัตโนมัติ (2) ทดสอบปฏิเสธผ่านเรียก API ตรง (เบราว์เซอร์อัตโนมัติกด native `confirm()` dialog ไม่ได้ เป็นข้อจำกัดของเครื่องมือทดสอบ ไม่ใช่บั๊กแอป) → ยืนยันแถวถูกลบออกจากคิวถูกต้องโดยไม่กระทบสต็อคเลย — ลบข้อมูลทดสอบ (revert สต็อค/ต้นทุนกลับที่เดิม) หลังทดสอบเสร็จแล้ว
      - **Known gap:** ยังไม่รองรับกรณีสินค้าในใบส่งของเป็นสินค้าใหม่ที่ไม่เคยมีในระบบเลย (ต้องไปเพิ่มสินค้าใหม่เองก่อนแล้วค่อยกลับมากดยืนยันใหม่ ไม่มี "+ เพิ่มสินค้าใหม่จากตรงนี้เลย" แบบ inline)
26. **แก้บั๊ก Peak Time Heatmap (เวลาเพี้ยน 7 ชม.) + ระบบ PIN พนักงานรายบุคคล + ล้างสถิติสลิปเก่า — งานชุดใหญ่ที่ผู้ใช้ขอสรุปก่อนเริ่มทำอีกรอบ (VAT/รายจ่ายยังไม่เสร็จ อยู่ระหว่างทำต่อ):**
    - **แก้บั๊ก Heatmap:** `recordAnalytics()` ใน `smileslip-pro/index.js` เดิมใช้ `new Date().getHours()`/`getDay()`/`toISOString()` ตรงๆ ซึ่งเป็นเวลา UTC ของเซิร์ฟเวอร์ Cloud Run ไม่ใช่เวลาไทย ทำให้ `hour_of_day`/`day_of_week`/`slip_date` ใน `slip_analytics` เพี้ยนไป 7 ชั่วโมงมาตลอด (สลิปตอนดึกอาจเพี้ยนวันที่ไปด้วย) — เจอจากการไล่โค้ดตอบคำถามผู้ใช้ที่สังเกตว่า Heatmap ไม่ตรงกับที่คีย์จริง — แก้โดยใช้ `getThaiDateTime().raw`/`isoDate` แทน (helper เดียวกับที่จุดอื่นในไฟล์ใช้อยู่แล้ว) + ลบข้อมูลเก่าที่เพี้ยนทิ้งทั้งหมดใน `slip_analytics`/`sender_profiles` ของร้านทดสอบตามที่ผู้ใช้ขอ (เพราะจะเทสระบบใหม่ทั้งหมด)
    - **ระบบ PIN พนักงานรายบุคคล (เดิมทั้งร้านใช้ PIN เดียวร่วมกัน ยกเลิกไปแล้วตามที่ผู้ใช้สั่ง):** เพิ่มคอลัมน์ `PIN` ท้าย `STAFF_HEADERS` (I) — พนักงานตั้ง PIN เองผ่านลิงก์ที่ระบบส่งให้ทาง LINE อัตโนมัติ (`sendPinSetupLink()`) ทันทีที่ (ก) แอดมิน/ผู้จัดการสาขากดอนุมัติคำขอ `#สมัครพนักงานขนส่ง` ใน `api/pos/staff-requests.js` หรือ (ข) แอดมินเพิ่มพนักงานเองพร้อม LINE ID ใน `api/pos/staff.js` — ลิงก์พาไปหน้าใหม่ `pos-staff.js` step `setpin` (deep-link ด้วย `staff_id`+`setpin=1` ข้ามหน้ากรอก PIN ไปเลย เหมือน pattern เดียวกับลิงก์ยืนยันงานจัดส่ง/เก็บเงิน — การมีลิงก์คือการยืนยันตัวตน)
      - `api/pos/verify-pin.js` เขียนใหม่ทั้งหมด — จากเช็ค PIN เดียวของร้าน (`pos_configs.staff_pin`) กลายเป็นค้นหา PIN ที่ตรงกันในชีต "พนักงาน" ทั้งหมดแล้วคืนตัวตนพนักงานคนนั้น (`staff_id`/`name`/`role`) กลับไป — `pos-staff.js` ใช้ชื่อที่ได้มา auto-fill "👋 สวัสดีคุณ {name}" แทนการให้พิมพ์ชื่อเองแบบเดิม (เชื่อถือได้กว่าเพราะผูกกับ PIN จริง) + เพิ่ม rate-limit กัน brute-force PIN 4 หลัก (in-memory, 15 ครั้ง/15 นาทีต่อร้าน)
      - แอดมิน (หน้าตั้งค่า → พนักงาน) เห็นสถานะ "🔐 ตั้ง PIN แล้ว" / "⚠️ ยังไม่ได้ตั้ง PIN" ต่อคน + ปุ่ม "📨 ส่งลิงก์ตั้ง PIN" (ส่งซ้ำได้ทุกเมื่อ ไม่ล้าง PIN เดิม) และปุ่ม "🚫 ปิดใช้งาน PIN" (ล้าง PIN ทันที ไม่ส่งลิงก์ใหม่ให้ — ใช้ตอนพนักงานออกจากงานกันแอบเข้าระบบด้วย PIN เก่า) — ลบ UI ตั้งค่า Staff PIN แบบเก่าทั้งหมดออกจากหน้าตั้งค่าแล้ว (`api/pos/pos-config.js` เลิกอ่าน/เขียน `staff_pin` แล้วเช่นกัน แต่ไม่ได้ลบคอลัมน์ในตาราง Supabase ทิ้ง แค่เลิกใช้)
      - **ทดสอบยิงจริงครบทุก path แล้ว:** ตั้ง PIN ผ่าน deep link → login ด้วย PIN ใหม่ auto-identify ถูกต้อง ("Testing ") → แอดมินกดปิดใช้งาน PIN → PIN เดิมและ PIN ร้านเก่า (`1234`) ใช้ล็อกอินไม่ได้ทั้งคู่ (401 ทั้งคู่) ยืนยันครบ
    - **แก้เพิ่มระหว่างทาง:** หน้ายืมสินค้า (`ยืมสินค้า`) เดิมค้นหาสินค้าได้ทุกประเภทรวมถึงสินค้าหมุนเวียนด้วย ทั้งที่ backend หัก/คืนสต็อคจริงเฉพาะประเภท "นับสต็อค" เท่านั้น (ยืมสินค้าหมุนเวียนผ่านหน้านี้จะไม่มีผลอะไรกับสต็อคเลยเงียบๆ) — กรองสินค้าประเภทหมุนเวียนออกจากตัวเลือกในหน้ายืมสินค้าแล้ว (มีระบบแลกเปลี่ยน/เก็บคืนของตัวเองแยกต่างหากอยู่แล้ว)
    - **ต่อจากนี้ทำครบแล้วในข้อ 27:** ระบบ VAT ครบวงจร + หน้ารายจ่าย + เชื่อมรายงาน/analytics (ดูข้อ 27)
27. **ระบบ VAT ครบวงจร (คำนวณ VAT ขาย/ซื้อ, toggle ร้านจด VAT, ใบกำกับภาษี PDF ทางการ, รับสินค้า 3-state VAT, รายงาน VAT) + หน้า "รายจ่าย" ใหม่ + เชื่อมรายงาน/กราฟวิเคราะห์ — ทำต่อจากข้อ 26 ตามที่ผู้ใช้อนุมัติล่วงหน้าไว้ทั้งหมด ("ทำตามนั้นทั้งหมด"):**
    - **toggle "ร้านนี้จดทะเบียน VAT"** เพิ่ม `pos_configs.vat_registered` (✅ รัน SQL แล้ว 2026-07-19 — ผู้ใช้รันเองใน Supabase, verified อ่าน/เขียนได้จริง) — เปิดแล้วใบเสร็จขายหน้าร้านปกติ (ไม่ใช่แค่ใบกำกับภาษีทางการ) จะแยกแสดงยอดก่อน VAT/VAT 7% ให้อัตโนมัติ
    - **`computeVatBreakdown()`** ใน `lib/google-pos.js` — helper กลางคำนวณ VAT ต่อบิลจาก `vat_type` ของสินค้าแต่ละชิ้น (reuse เดิมจาก `tax-invoice.js` มาให้ `sales.js` ใช้ด้วย) — `SALE_HEADERS` ขยาย 16→18 คอลัมน์ (เพิ่ม Q=ยอดก่อน VAT, R=ยอด VAT ต่อบิล) — **แก้ range อ่านชีต "ยอดขาย" จาก `A:P` เป็น `A:R` ทุกจุดใน `api/pos/reports.js`** (เดิมอ่านไม่ถึงคอลัมน์ใหม่ ทำให้ vat_subtotal/vat_amount เป็น 0 เสมอ)
    - **ใบกำกับภาษี/ใบเสร็จรับเงินที่ร้านออกให้ลูกค้า เปลี่ยนจาก HTML สไตล์สลิปความร้อนเป็น PDF ทางการ A4** — `lib/pos-tax-invoice-pdf.js` (ใหม่) มีรูปแบบเดียวกับ `lib/invoice-pdf.js` (ที่แอดมิน Smile Slip Pro ใช้ออกใบกำกับภาษีตอนร้านซื้อแพ็กเกจ) แต่พารามิเตอร์ข้อมูลร้าน/ผู้ซื้อ/รายการสินค้าแบบไดนามิกแทนค่าตายตัว (ต้นฉบับรองรับแค่ 1 รายการเพราะเป็นค่าแพ็กเกจ, อันนี้รองรับหลายรายการ + auto-pagination ถ้าล้นหน้า) — `api/pos/tax-invoice-pdf.js` (ใหม่) GET stream PDF ตรง, `pos.js` เปลี่ยน `issueTaxInvoice()` จากพิมพ์ HTML เป็น `window.open()` PDF endpoint
    - **รับสินค้า: 3-state VAT ต่อรายการ** แทน checkbox `hasVat` เดิม (สมมติราคาก่อน VAT เสมอ) — `api/pos/receives.js` เปลี่ยนเป็น `vatType` ('รวม VAT แล้ว'/'ไม่รวม VAT'/'ไม่มี VAT' แบบเดียวกับ vat_type สินค้า) `splitVat()` แยกฐานราคาก่อน VAT ให้ต้นทุนถ่วงน้ำหนัก (weighted avg cost) คำนวณจากฐานก่อน VAT เสมอไม่ว่าเลือกแบบไหน — `pos.js` เปลี่ยน UI จาก checkbox เป็นปุ่มเลือก 3 สถานะสไตล์เดียวกับฟอร์มสินค้า
    - **รายงาน VAT ใหม่** (`type=vat` ใน `api/pos/reports.js`) — ภาษีขายแยกตามสาขา (จากคอลัมน์สาขาของยอดขาย) vs ภาษีซื้อ (รวมจากใบรับสินค้า + รายจ่าย ยังไม่แยกสาขาเพราะทั้งคู่ไม่ได้ผูกสาขา) + ยอด VAT สุทธิที่ต้องนำส่ง/ขอคืนได้ — เพิ่มแท็บ "🧾 ภาษี VAT" ในหน้ารายงาน POS
    - **หน้า "รายจ่าย" ใหม่ทั้งหมด** (ไม่เกี่ยวกับสต็อคสินค้า เช่น ค่าเช่า/ค่าน้ำไฟ/ค่าแรง) — `EXPENSE_HEADERS`/`rowToExpense`/`makeExpenseNo` ใน `lib/google-pos.js`, API ใหม่ `api/pos/expenses.js` (GET/POST/DELETE) + `api/pos/upload-photo.js` (อัปโหลดรูปบิล/สลิปทั่วไปไม่มี OCR คนละ endpoint กับ `process-slip.js`) + แท็บใหม่ "🧾 รายจ่าย" ใน `pos.js` (ฟอร์ม 3-state VAT + แนบรูป + ประวัติ + ลบ)
    - **เชื่อมรายจ่ายเข้ารายงาน P&L + หน้ากราฟวิเคราะห์ของ Dashboard** — เพิ่ม `type=expenses` ใน reports.js, แก้ `type=pl` ให้หัก `total_expenses` ออกจากกำไรขั้นต้นเป็น `net_profit`/`net_margin` จริง — **`api/pos/expenses.js` เขียนข้อมูลสองที่แบบเดียวกับ `sales.js`** (`writeExpenseToMainSheets()`): POS Sheets tab "รายจ่าย" (รายละเอียด) + Sheets บัญชีหลัก (tab ปี, type="รายจ่าย") ให้ขึ้นในหน้ากราฟวิเคราะห์/Ledger/`#กำไรขาดทุน` ของบอท LINE ทันทีโดยไม่ต้องสร้าง UI ใหม่ซ้ำซ้อน
    - **เจอบั๊กจริงระหว่างทดสอบยิง API จริง 2 จุด แก้แล้วทั้งคู่:**
      1. **`asText()` หาย** — `expenses.js` เขียน `created_at` แบบสตริงไทย (มีปี พ.ศ.) ลง Sheets โดยไม่ผ่าน `asText()` — ทดสอบยิงจริงบน tab "รายจ่าย" ที่เพิ่งสร้างใหม่ (ไม่มีข้อมูล/format มาก่อนเลย) พบว่าเพี้ยนเป็นเลข serial ทันที (`244549.5869` แทน `19/7/2569 14:08:48`) ยืนยันชัดเจนว่าบั๊ก "~543 ปี" ที่เจอมาก่อนหน้านี้ (ข้อ 19) เป็นความเสี่ยงจริงกับ**ทุก tab ใหม่**ที่ไม่ผ่าน `asText()` ไม่ใช่แค่เคสเก่าที่เจอแล้ว — แก้โดยเพิ่ม `asText()` ใน `expenses.js` (**พบว่าไฟล์อื่นในระบบ POS ส่วนใหญ่ก็ไม่ได้ผ่าน asText() เหมือนกัน แต่ยังไม่ได้ไล่แก้ — flag เป็นงานแยกต่างหากแล้ว ดู spawn_task/known gap ด้านล่าง**)
      2. **Column off-by-one ใน `writeExpenseToMainSheets()`** — ใส่ `''` แค่ 4 ตัวแทนคอลัมน์ K-O (5 คอลัมน์: เลขอ้างอิง/เลขภาษี/ชื่อผู้เสียภาษี/ยอดภาษี/ที่อยู่ผู้เสียภาษี) ทำให้ label/payment_method/recordedBy เลื่อนไปทางซ้าย 1 คอลัมน์ทั้งหมด (label ไปตกที่ ที่อยู่ผู้เสียภาษี, payment_method ไปตกที่ หมวดหมู่, recordedBy ไปตกที่ วิธีรับ-จ่าย) — เจอจากการทดสอบยิง POST จริงแล้วเช็คแถวที่เขียนจริงในชีตบัญชีหลัก (ไม่ใช่แค่เชื่อ response ของ API) แก้โดยเพิ่ม `''` เป็น 5 ตัว — ทดสอบซ้ำแล้วคอลัมน์ตรงทั้งหมด
      - **ทั้งสองบั๊กทดสอบกับข้อมูลจริงในชีตบัญชีหลักของร้าน "D Gas"** (คนละชีตกับ POS Sheet — เป็นชีตที่มีประวัติจริง 68 แถวจากการใช้งานบอทจริง) หลังพบบั๊กแล้วลบ/ล้างแถวทดสอบที่เขียนผิดออกทันที (`updateSheetRow` เคลียร์เป็นค่าว่างทั้งแถว) ยืนยันด้วย `GET /api/shop/analytics` ว่าตัวเลขกลับมาตรงกับก่อนทดสอบเป๊ะ (totalExpense 19558, totalCount 68) ก่อนจะถือว่าทดสอบผ่าน
    - **ยังไม่ได้ทำ (out of scope รอบนี้):** export Excel (`api/pos/export.js`) ยังไม่รองรับรายจ่าย/VAT ใหม่ (คนละไฟล์ reimplement แยกจาก reports.js — known gap เดิมจากก่อนหน้านี้), ยังไม่มี "+ เพิ่มสินค้าใหม่แบบ inline" ระหว่างยืนยันรับสินค้าจาก LINE
28. **Group F: backlog สินค้าหมุนเวียนที่ผู้ใช้อนุมัติไว้ก่อนหน้า (เพดานเปล่ารอรีฟิล, audit log ยืม/แลกเปลี่ยน, วงเงินยืมสูงสุดต่อลูกค้า, แยกรายการแลกเปลี่ยน/ยืมบนใบเสร็จ, ต้นทุนรีฟิลแยกจากซื้อใหม่):**
    - **เพดานเปล่ารอรีฟิล** เพิ่ม `PRODUCT_HEADERS` คอลัมน์ S `เพดานเปล่ารอรีฟิล` (`empty_ceiling`, ตั้งได้เฉพาะสินค้าประเภทหมุนเวียนในฟอร์มแก้ไขสินค้า) — เตือน (ไม่บล็อค) ตอนขายหน้าร้านถ้า `เปล่ารอรีฟิล > เพดาน` หลังการขาย + แสดง badge ⚠️ ในตาราง "สต็อคต่อสินค้า" ของแท็บรายงาน "🔄 สินค้าหมุนเวียน" พร้อมแบนเนอร์สรุปจำนวนสินค้าที่เกินเพดาน
    - **Audit log แยกรายการ (`บันทึกแลกเปลี่ยน` tab ใหม่)** — `CYCLICAL_LOG_HEADERS`/`logCyclicalTransaction()` ใน `lib/google-pos.js` (fail-safe, เรียกแล้วไม่ throw กันพัง transaction หลัก) บันทึกทุกครั้งที่มีการยืม/แลกเปลี่ยน/คืนสินค้าหมุนเวียนกับลูกค้า — เชื่อมเข้า 3 จุดที่ลูกค้าเจอจริง: `api/pos/sales.js` (ขายหน้าร้าน — แยก "แลกเปลี่ยน"/"ยืม" เป็นคนละแถวถ้ามีทั้งคู่ในบิลเดียว), `api/pos/delivery.js` (POST สร้างออเดอร์ = "ยืม" ยอดรวม เพราะ `cylinders_delivered` เป็นยอดรวมไม่แยก SKU อยู่แล้ว, PATCH `confirm_delivery` = "คืน" แยกราย SKU จริง), `api/pos/collections.js` (ยืนยันเก็บสำเร็จ = "คืน" แยกราย SKU) — **ตั้งใจไม่ผูกกับ "รับสินค้า"/"รีฟิล" ที่มีการบันทึกละเอียดอยู่แล้วในตัวเอง** (audit log นี้เจาะจงเหตุการณ์ที่ "ลูกค้า" เกี่ยวข้องเท่านั้น ตามที่ผู้ใช้ขอ "ประวัติยืม/แลกเปลี่ยน")
    - **วงเงินยืมสูงสุดต่อลูกค้า (เหมือน credit limit)** เพิ่ม `CONTACT_HEADERS` คอลัมน์ X `วงเงินยืมสูงสุด (ชิ้น)` (`cylinder_limit`, ตั้งได้ในฟอร์มผู้ติดต่อเฉพาะประเภทลูกค้า) — เช็คแบบ **soft warning ไม่บล็อคการขาย/จัดส่ง** (สอดคล้องกับระบบเครดิตเงินที่มีอยู่แล้วซึ่งก็ไม่บล็อคเช่นกัน) ใน `sales.js`/`delivery.js` — คืน `warnings: []` ใน response แล้ว `pos.js` โชว์เป็น toast แทน alert (ไม่ขัดจังหวะงานขาย)
    - **แยกรายการแลกเปลี่ยน/ยืมบนใบเสร็จ** — `itemsWithReturns` ใน `pos.js` (handleCheckout) ใส่ `returned_qty` ให้สินค้าหมุนเวียนทุกชิ้นเสมอ (แม้เป็น 0 ตอนยืมเต็มจำนวน — เดิมใส่เฉพาะตอนมีการคืนเท่านั้น ทำให้เคส "ยืมเต็มจำนวน" แยกไม่ออกจากสินค้าไม่หมุนเวียนบนใบเสร็จ) → `buildReceiptHtml()` แสดงบรรทัดย่อย "↔️ แลกเปลี่ยน X" และ/หรือ "📦 ยืม Y (ยังไม่คืน)" ต่อรายการอัตโนมัติ
    - **รายงานต้นทุนรีฟิลแยกจากต้นทุนซื้อใหม่** — ขยาย `type=cyclical` ใน `api/pos/reports.js` ให้อ่าน tab "รับสินค้า" ด้วย แยกยอดต้นทุน (ก่อน VAT) ของรายการที่เป็น SKU ประเภทหมุนเวียน (`refill_cost`) ออกจากรายการสินค้าประเภทอื่น (`new_purchase_cost`) ในช่วงวันที่ที่เลือก — แสดง 2 การ์ดในแท็บรายงาน "🔄 สินค้าหมุนเวียน"
    - **ทดสอบยิงจริงครบทุกกลไกกับข้อมูลจริงของร้าน "D Gas"** (ตั้งเพดาน/วงเงินชั่วคราวบนสินค้า/ลูกค้าจริงที่มีอยู่แล้ว → ขายเกินทั้งสองเงื่อนไขพร้อมกัน → ยืนยัน warnings ทั้งคู่ขึ้นถูกต้อง, audit log บันทึกถูกต้อง (วันที่ไม่เพี้ยนเพราะผ่าน `asText()`), stock/at_customer/cylinders เปลี่ยนค่าตรงตามคาด (37→36, 9→10, 1→2) → revert ค่าทั้งหมดกลับที่เดิมหลังทดสอบผ่าน)
    - **Deploy production แล้ว (2026-07-19)** — ผู้ใช้ยืนยันให้ deploy งานทั้งชุด (ข้อ 27-28: VAT/รายจ่าย/backlog สินค้าหมุนเวียน) ขึ้น `smileslip-dashboard` ทันทีหลังเสร็จ — deploy ผ่าน `gcloud run deploy` (PowerShell, parse `.env` เอง) → **ข้อความ "Done...serving 100 percent" โชว์ชื่อ revision เดิม (`00260-556`) อีกครั้งเหมือนที่เคยเจอมาก่อน (ข้อ 18/24) ยืนยันซ้ำว่าเชื่อไม่ได้จริง** — เช็คจริงด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` เจอ revision ใหม่จริง (`smileslip-dashboard-00261-56p`) แล้ว `gcloud run services update-traffic ... --to-revisions=smileslip-dashboard-00261-56p=100` เอง — verified บน production จริงแล้วว่า `GET /api/pos/expenses`, `type=vat` ใน reports.js, และ `empty_ceiling` ในสินค้าใช้งานได้ถูกต้อง
29. **แก้บั๊กสาขาไม่ถูกส่งเข้าบัญชีหลัก + เชื่อมรับสินค้าเข้าบัญชีหลัก + รับรายจ่ายอัตโนมัติผ่าน LINE + export VAT/รายจ่าย — ผู้ใช้ตอบคำถามที่ค้างไว้จากข้อ 27 (POS รายจ่าย ควรรวมกับบัญชีหลักหรือแยก, บอทจะแยกรายจ่าย/รับสินค้ายังไง) แล้วขอ "ทำทุกอันเลย":**
    - **เจอบั๊กจริงระหว่างตอบคำถามผู้ใช้ (ไม่ใช่แค่เพิ่มของใหม่):** หน้า POS มีตัวเลือกสาขาอยู่แล้ว (`selectedBranch` state ใน `pos.js`) แต่ **ขายหน้าร้าน (`handleCheckout`) และรายจ่าย (`submitExpense`) ไม่เคยส่งค่านี้ไปกับ POST เลย** (มีแค่ "ยืมสินค้า" จุดเดียวที่ส่งถูก) ทำให้รายงาน VAT แยกสาขาที่ทำไว้ในข้อ 27 ใช้งานไม่ได้จริงมาตลอด — แก้ `pos.js` ให้ส่ง `branch: selectedBranch?.branch_name` ทั้ง 3 จุด (ขาย/รายจ่าย/รับสินค้า) และแก้ `writeToMainSheets()`/`writeExpenseToMainSheets()` ให้ใช้ `branch` จาก request ก่อนเสมอ (ไม่งั้นทุกสาขาจะถูกนับรวมเป็นสาขาเดียวกันหมดในบัญชีหลักเพราะโค้ดเดิมใช้ `branchName` default ของร้านตรงๆ)
    - **เชื่อม `api/pos/receives.js` (รับสินค้าเข้าสต็อค) เข้าบัญชีหลัก** — เดิมไม่เคยเขียนเข้าบัญชีหลักเลยตั้งแต่สร้างฟีเจอร์ ทำให้ต้นทุนซื้อสินค้าไม่โผล่ในกราฟวิเคราะห์/Ledger/`#กำไรขาดทุน` ของบอทเลย ทั้งที่เป็นรายจ่ายจริง — เพิ่ม `writeReceiveToMainSheets()` เขียนเป็น type=รายจ่าย หมวดหมู่ `"ซื้อสินค้าเข้าสต็อค (POS)"` (แยกจากรายจ่ายทั่วไปให้ดูออกว่าเป็นต้นทุนขายไม่ใช่ค่าใช้จ่ายดำเนินงาน)
    - **รับรายจ่ายอัตโนมัติผ่านรูปถ่ายใน LINE (`#รายจ่าย`)** — มิเรอร์ `#รับสินค้า` ทุกประการแต่แยก awaiting-state/cache คนละชุด (`awaitexp:` vs `awaitrecv:`) กันสับสนว่ารูปถัดไปคือรับสินค้าหรือรายจ่าย — ตัดสินใจ**ใช้คำสั่งคนละคำชัดเจนแทนให้ Gemini เดาจากรูป** (ผู้ใช้ถามว่าจะแยกยังไง แนะนำแบบนี้เพราะเดาผิดจะเงียบไม่มี error ให้เห็น อันตรายกว่า) — `extractExpenseDataWithGemini()` อ่าน label/vendor/amount/vatType(3-state)/invoice_no/date จากบิล เขียนลง tab ใหม่ `"รายจ่ายรอยืนยัน"` (คนละ tab กับ `"รายจ่าย"` ที่ยืนยันแล้ว) — ฝั่ง Dashboard เพิ่ม `api/pos/expenses-pending.js` + แท็บ "🔔 รอยืนยันจาก LINE" ในหน้ารายจ่ายของ `pos.js` กด "ตรวจสอบ/ยืนยัน" พรีฟิลข้อมูลลงฟอร์มปกติให้แก้ก่อนบันทึกจริง (ไม่ได้เขียน logic บันทึกซ้ำ)
    - **Export Excel เพิ่ม type=expenses/vat** ใน `api/pos/export.js` — พร้อมแก้ range อ่านชีต "ยอดขาย" จาก `A:P` เป็น `A:R` ทุกจุดในไฟล์นี้ด้วย (เดิมพลาดจุดนี้จากตอนทำข้อ 27 เพราะแก้แค่ `reports.js` ไม่ได้ไล่แก้ `export.js` ที่มี logic คล้ายกันแยกไฟล์)
    - **เหตุการณ์ระหว่างทดสอบ: เจอข้อมูลทดสอบเก่าตกค้างในบัญชีหลักจริงของร้าน "D Gas"** — ระหว่างทดสอบ `receives.js`→บัญชีหลัก พบว่ามีแถวทดสอบ 3 แถว (`BILL20260719144503` จากทดสอบ Group F, `BILL20260719142702`/`BILL20260719142740` ชื่อ `TEST-asText-*`) หลงเหลืออยู่ในชีตบัญชีหลักจริงมาตั้งแต่รอบทดสอบก่อนหน้า เพราะตอนนั้นลบแค่แถวใน POS Sheet ไม่รู้ว่า `sales.js` เขียนเข้าบัญชีหลักด้วย (dual-write มีอยู่ก่อนแล้วตั้งแต่ session ก่อนหน้า) — ลบออกจากบัญชีหลักจริงทันทีที่เจอ ยืนยันด้วย `GET /api/shop/analytics` ว่ากลับมาตรง 68 รายการ/19,558 บาทเหมือนเดิม — **บทเรียน: เวลาทดสอบฟีเจอร์ที่มี dual-write (POS Sheet + บัญชีหลัก) ต้องเช็ค/ลบข้อมูลทดสอบจากทั้งสองที่เสมอ ไม่ใช่แค่ที่เดียว** (แถวเก่า `BILL20260718142819` จากข้อ 23 ที่เคย flag ไว้ว่ารอผู้ใช้ตัดสินใจ ไม่ได้แตะเพราะยังไม่ใช่ของเราตัดสินใจ)
    - **ยังไม่ได้ทำ:** อัปเดตหน้า "ช่วยเหลือ" ในเว็บ Dashboard (sidebar) และเมนู `#วิธีใช้งาน` แบบละเอียดของบอทให้พูดถึง `#รายจ่าย` ด้วย (แก้แค่บรรทัดเดียวใน `#ช่วยเหลือ` แล้ว — ยังไม่ได้ไล่แก้จุดอื่นตามข้อควรระวัง #33)
    - **Deploy production แล้วทั้ง 2 โค้ด (2026-07-19)** — ผู้ใช้ยืนยันให้ deploy ทั้ง dashboard และบอท LINE — `smileslip-dashboard` revision จริง `smileslip-dashboard-00262-gw6` (ข้อความ deploy โชว์ revision เดิมผิดอีกครั้งตามเคย), `smileslip-service` (บอท) revision จริง `smileslip-service-00142-9ph` — ทั้งคู่ traffic pin 100% แล้ว + verified บน production จริงว่า `/api/pos/expenses-pending`, `/api/pos/export?types=expenses`, และบอทบูตสำเร็จ (Redis connected, startup probe ผ่าน) ใช้งานได้ปกติ
30. **Verified Hyper-Local Market Price Index + Procurement Fraud Detection (v1 retail-only) — ผู้ใช้ส่งสเปกมาจากไฟล์ `ระบบงานเก้บข้อมุลราคาสินค้า แนวทางวิเคราะห์.txt` ขอให้วิเคราะห์ความเป็นไปได้ก่อน แล้วให้ "ทำเลย" หลังคุยรายละเอียดกัน — ยังไม่ได้รัน SQL/ทดสอบจบ end-to-end จริง (รอ SQL):**
    - **สเปกเดิม:** ดักจับราคาจากใบรับสินค้า → ดัชนีราคากลางระดับอำเภอนิรนาม 100% (เก็บสถิติใน Supabase เท่านั้น ข้อมูลจริง/รูปอยู่ใน Sheets/Drive ลูกค้า) + จับทุจริตพนักงานจัดซื้อ (เทียบราคาจริงกับราคากลาง) + แยกชั้นราคาส่ง/ปลีกพร้อม KYB approval สำหรับร้านค้าส่ง — ผู้ใช้เสนอ 3 แนวทางขายข้อมูล (subscription feature / ขายบทวิเคราะห์ B2B / ใช้เป็นท่อโฆษณา) **ห้ามขาย raw data เด็ดขาด**
    - **ตัดสินใจ scope v1 (ตามคำแนะนำที่ให้ไปและผู้ใช้ตอบรับ):** ทำแค่ **ราคาปลีก + จับทุจริตจัดซื้อ** ก่อน — **ตัด KYB/แยกราคาส่ง/wholesale gating ออกทั้งหมด ยกไป v2** (ยังไม่มี `business_type`/`is_wholesale_verified` ในโค้ด/schema ตามที่ตัดสินใจ)
    - **เจอช่องว่างจริงระหว่างสเปกกับโค้ด 3 จุด (แก้แล้วทั้งหมด):**
      1. **ไม่มีฟิลด์รูปภาพใน `api/pos/receives.js` เลย** — ทั้งฟอร์มเว็บและตอนยืนยันจากคิว LINE (`รับสินค้ารอยืนยัน` มี `image_url` จริงแต่ไม่เคย thread ต่อเข้าฟอร์ม) แก้โดยเพิ่มคอลัมน์ J `ลิงก์รูปภาพหลักฐาน` ใน `RECEIVE_HEADERS`, เพิ่มปุ่มแนบรูปในฟอร์มรับสินค้าเว็บเอง (reuse `/api/pos/upload-photo`), และ `loadPendingIntoForm()` ดึง `pending.image_url` มาใส่ `receivePhotoUrl` อัตโนมัติเมื่อกด "ตรวจสอบ/ยืนยัน" จากคิว LINE
      2. **`shop_profiles.address` เก็บรวมเป็นสตริงเดียว** (`register.js` เดิม concat `ต.X อ.Y จ.Z` ทิ้ง ไม่แยกคอลัมน์) ทั้งที่ฟอร์มสมัครมีแยก province/district จริงตั้งแต่ frontend — ตรวจ Supabase จริงพบว่ามีแค่ 4 ร้านทั้งระบบ ทุกแถวแมตช์ regex `อ\.(.+)\s+จ\.(.+)` ครบ 100% เขียน `scripts/backfill-district-province.js` (one-off, ยังไม่ได้รัน) + แก้ `register.js` ให้เขียน `district`/`province` แยกคอลัมน์ให้ร้านใหม่ทันที (แยก update ต่างหากจาก insert หลัก แบบ defensive pattern เดียวกับ `receipt_paper_size`/`vat_registered` กัน insert หลักพังถ้ายังไม่รัน SQL) — **known gap:** ถ้าร้านแก้ `address` เองทีหลังผ่านหน้า Settings (`update-profile.js` ยังเป็น free-text address เดียว ไม่มีช่อง district/province แยก) จะไม่ sync กับ district/province อัตโนมัติ ยังไม่ได้ทำหน้า Settings ให้แก้แยกได้
      3. **canonical_name ต้องคำนวณตอนยืนยันจริง ไม่ใช่ตอน OCR** — เพราะแอดมินอาจแก้ชื่อ/เพิ่มรายการเองก่อนกดยืนยัน (มี gate คนกลางตั้งใจกันข้อมูล OCR ผิดอยู่แล้ว) — ทำ `canonicalizeItems()` เรียก Gemini **แบบ text-only ไม่ใช่รูป** (ถูกกว่า OCR มาก) **1 call ต่อทั้งใบรับสินค้า ไม่ใช่ต่อ SKU** ที่ `api/pos/receives.js` ตอนยืนยันจริง พร้อมเช็คแคช `canonical_name_cache` (global ไม่ผูก shop_id เพราะต้องรวมชื่อข้ามร้าน) ก่อนเสมอ — ผู้ใช้ถามว่าจะเพิ่มค่าใช้จ่ายไหม ตอบไปว่าเพิ่มจริงแต่น้อยมาก + แนะนำแคชแล้วผู้ใช้รับทราบ
    - **ตัดสินใจสำคัญที่ deviate จากสเปกตรงตัว (ต้องบอกผู้ใช้ไว้ชัดเจนแล้ว):** สเปกบอกห้ามเก็บอะไรที่ระบุตัวตนร้านค้าในตารางกลางเด็ดขาด แต่กฎ "ต้องมี ≥5 ร้านต่ออำเภอ" ทำไม่ได้เลยถ้าไม่รู้เลยว่าแต่ละแถวมาจากร้านไหน (นับ distinct ไม่ได้) — แก้โดยเพิ่ม **`shop_hash`** (sha256 ทางเดียวของ `shopId + MARKET_HASH_SALT`) ใน `anonymous_market_prices` ใช้แค่ `COUNT(DISTINCT shop_hash)` เท่านั้น ห้าม join กลับ `shop_profiles` และห้ามคืนค่าออกไปใน API ใดๆ — เป็น pattern เดียวกับ `sender_hash` ที่มีอยู่แล้วจริงใน `slip_analytics`/`sender_profiles` (ไม่ใช่ของใหม่ที่คิดเอง) — เพิ่ม `MARKET_HASH_SALT` (random 32-byte hex) ใน `smileslip-dashboard/.env` แล้ว (ยังไม่ deploy)
    - **⚠️ รอทนาย/กฎหมายก่อนเปิดใช้จริงในวงกว้าง (บอกผู้ใช้ไว้แล้วในแชท ยังไม่มีคำตอบ):** การรวบรวม+กระจายสัญญาณราคาข้ามคู่แข่งในพื้นที่เดียวกัน แม้จะนิรนามและมีเกณฑ์ 5 ร้าน บางเขตอำนาจถือเป็นประเด็น anti-trust/price-signaling ได้ — Claude ให้ความเห็นทางเทคนิคได้เท่านั้น ไม่ใช่ผู้ตัดสินความถูกต้องทางกฎหมาย **ห้ามเปิดฟีเจอร์นี้ให้ลูกค้าใช้งานจริงในวงกว้างจนกว่าจะมีทนาย/ที่ปรึกษากฎหมายยืนยัน**
    - **สถาปัตยกรรมที่ทำแล้ว:** `lib/market-price.js` (ใหม่ — `canonicalizeItems`, `hashShopId`, `insertAnonymousMarketPrices`, `checkProcurementFraud`, `getShopDistrictProvince` — ทุกฟังก์ชัน fail-safe ไม่ throw ออกนอกไฟล์) เรียกจาก `api/pos/receives.js` หลังบันทึกรับสินค้าสำเร็จเท่านั้น (ไม่กระทบ core flow ถ้า error) — `api/pos/procurement-alerts.js` (ใหม่ GET/PATCH) + แท็บ "🚩 ราคาผิดปกติ" ในหน้ารายงาน POS (`pos.js`) ให้แอดมินดู/เปลี่ยนสถานะ red flag
    - **ทดสอบรอบแรก (ก่อนรัน SQL):** ยิง `POST /api/pos/receives` จริงกับ "D Gas" ยืนยันว่า core receiving ยังทำงานปกติ (`ok:true`) ไม่พังแม้ตารางกลางยังไม่มีอยู่จริง (fail-safe ทำงานถูกต้อง)
    - **ผู้ใช้รัน SQL แล้ว (2026-07-20)** — verified ทั้ง 4 ตาราง/คอลัมน์มีจริงผ่าน REST query แล้วรัน `node scripts/backfill-district-province.js` สำเร็จครบ 4/4 ร้าน (ทุกร้านมีแค่ อ.หางดง จ.เชียงใหม่ ตอนนี้)
    - **เจอบั๊กจริงตอนทดสอบ end-to-end รอบสอง (หลังรัน SQL) — แก้แล้ว:** `canonicalizeItems()` เงียบๆ คืนค่าเปล่าทุกครั้ง ไม่มี error เลย ตรวจสอบพบว่า **gemini-3.5-flash ใช้ "thinking token" ภายในก่อนตอบเสมอ** (เจอจริงกินไป 488-1363 token แม้ request เล็กมาก) ทำให้ `maxOutputTokens: 512` เดิมน้อยเกินไป `finishReason` เป็น `MAX_TOKENS` ตัด JSON กลางคัน (parse ไม่เจอ `[...]` ปิดครบเลยคืน `{}` เงียบๆ) — แก้โดยเพิ่มเป็น `4096` + รวมทุก `part` ของ response ก่อน parse (เจอว่า Gemini บางครั้งแบ่งเอาต์พุตเป็นหลาย part โค้ดเดิมอ่านแค่ part แรก) + เพิ่ม log เตือนชัดเจนถ้ายัง parse ไม่ได้อีก — **บทเรียนสำคัญ:** โมเดล thinking-enabled ต้องกัน `maxOutputTokens` ไว้เผื่อ thinking token เสมอ ห้ามตั้งค่าต่ำแบบ text classification ธรรมดา (เช่น `detectCategory()` ในบอทที่ตั้งไว้แค่ 32 — จุดนั้นอาจมีปัญหาเดียวกันซ่อนอยู่แบบไม่รู้ตัว เพราะ fallback ปิดบังไว้ ยังไม่ได้ไปตรวจ)
    - **ทดสอบ end-to-end ครบหลังแก้บั๊กแล้ว กับข้อมูลจริงของร้าน "D Gas":** รับสินค้า #1 (ราคาปกติ ฿200 + รูปแนบ) → `canonical_name_cache`/`anonymous_market_prices` บันทึกถูกต้อง (`shop_hash` ไม่ใช่ `shop_id` ตรงๆ ยืนยันด้วยตาว่าไม่ใช่ UUID เดิม) → รับสินค้า #2 (ราคา ฿500 แพงผิดปกติ) → ขึ้น warning "แพงกว่าราคากลางจังหวัด (฿200) อยู่ 150%" ถูกต้อง + บันทึก `procurement_alerts` ครบทุก field → ทดสอบหน้า UI "🚩 ราคาผิดปกติ" กดเปลี่ยนสถานะเป็น "จบเรื่องแล้ว" สำเร็จจริง — ลบข้อมูลทดสอบออกจากทั้ง Sheets (POS+บัญชีหลัก) และ Supabase (3 ตาราง) ครบทุกที่แล้ว (ระหว่างเช็คเจอ 2 แถวที่คิดว่าเป็นข้อมูลทดสอบตกค้างแต่ตรวจสอบแล้วเป็นข้อมูลจริงของผู้ใช้ที่เพิ่งใช้ #รับสินค้า จริงระหว่างที่ทดสอบพอดี — ไม่ได้แตะ)
    - **Deploy production แล้ว (2026-07-20)** — deploy `smileslip-dashboard` 2 รอบ (รอบแรกตอน MARKET_HASH_SALT พร้อม, รอบสองหลังแก้บั๊ก Gemini) revision จริงล่าสุด `smileslip-dashboard-00264-d2b` (ข้อความ deploy โชว์ revision เดิมผิดซ้ำอีกตามเคยทั้ง 2 รอบ) traffic pin 100% แล้ว
    - **ยังไม่ได้ทำ (v2/นอกสโคปรอบนี้):** KYB/wholesale gating ทั้งหมด, หน้า approve เอกสารยืนยันธุรกิจ, แยกเก็บเอกสาร KYB คนละที่จาก Google Drive ลูกค้า (ต้องเป็นพื้นที่บริษัทเอง), หน้า Settings ให้แก้ district/province แยกจาก address, โมเดลขายข้อมูล B2B/โฆษณา (แนวทางที่ 2-3 ที่ผู้ใช้เสนอ — ยังเป็นแค่แนวคิด ไม่ได้เริ่มสร้าง)
31. **ตามข้อ 30: (1) ไล่แก้บั๊ก Gemini thinking-token truncation ที่จุดอื่นในโปรเจกต์ (2) เพิ่มสวิตช์ซ่อนฟีเจอร์ดัชนีราคากลางจากลูกค้าจนกว่าจะปรึกษาทนาย — ผู้ใช้ยืนยันเองว่าให้เก็บข้อมูลเบื้องหลังต่อเนื่องปกติ ห้ามหยุดเก็บ แค่ซ่อนผลลัพธ์:**
    - **ไล่ตรวจจุด `detectCategory()` ในบอทตามที่ flag ไว้ในข้อ 30 — เจอบั๊กจริงตามคาด:** ทดสอบยิง Gemini ตรงด้วย prompt/หมวดหมู่จริงของ `detectCategory()` พบ `finishReason: "MAX_TOKENS"` ตัดคำตอบกลางคันจริง (`maxOutputTokens: 32` เดิมน้อยเกินไปมาก) — แก้ `smileslip-pro/index.js`: เพิ่มเป็น `1024`, เพิ่ม timeout 5000→8000ms (กัน request ที่ใช้ thinking token เยอะ timeout ก่อนตอบ), รวมทุก `part` ของ response ก่อน parse (เหมือน `market-price.js`), เพิ่ม `finishReason` ใน log เพื่อ debug ได้ง่ายขึ้นถ้าเกิดอีก — verified `node -c index.js` ผ่าน
    - **grep หา `maxOutputTokens` ทั้งโปรเจกต์เจออีกจุด:** `pages/api/pos/process-slip.js` (`ocrWithGemini()` — OCR สลิปที่อัปโหลดผ่านหน้าเว็บ POS โดยตรง คนละจุดกับ OCR หลักของบอทใน LINE) ตั้งไว้แค่ `256` เดิม แก้เป็น `1024` + รวมทุก part + เพิ่ม error log ตอน parse ไม่ได้ (เดิม catch เงียบไม่ log อะไรเลย) — verified `npx next build` ผ่านทุก route
    - **ตรวจแล้วว่าปลอดภัย:** ฟังก์ชัน OCR หลักของบอท (`extractDataWithGemini`/`extractDataHybrid` ที่อ่านสลิปจริงทุกใบใน LINE) **ไม่ได้ตั้ง `maxOutputTokens` เลย** (ใช้ default ของ API) จึงไม่เคยเสี่ยงบั๊กนี้ — ฟีเจอร์คีย์หลักของระบบปลอดภัยมาตลอด บั๊กนี้กระทบแค่ 3 จุดรอง (canonicalization ของดัชนีราคา, หมวดหมู่อัตโนมัติ, OCR สลิปจากหน้าเว็บ POS)
    - **บทเรียนสรุป (ใช้กฎเดียวกับที่บันทึกไว้ในข้อ 30):** ทุกจุดที่เรียก `gemini-3.5-flash` ต้องตั้ง `maxOutputTokens` อย่างน้อย ~1000 ขึ้นไปเสมอไม่ว่างานจะดูเล็กแค่ไหน (แม้จะขอแค่คำเดียวกลับมา) เพราะโมเดลนี้กิน thinking token ภายในก่อนตอบเสมอ และต้องรวมทุก `part` ของ response ก่อน parse เสมอ (ไม่ใช่อ่านแค่ `parts[0]`)
    - **เพิ่มสวิตช์ `MARKET_PRICE_FEATURE_LIVE` (2026-07-20)** — ผู้ใช้ต้องการปรึกษาทนายเรื่อง anti-trust/price-signaling ก่อนเปิดฟีเจอร์ดัชนีราคาให้ลูกค้าเห็น แต่ต้องการให้ **เก็บข้อมูลเบื้องหลังต่อเนื่องตามปกติระหว่างรอ** (ไม่ใช่หยุดเก็บแล้วมาเริ่มนับใหม่ตอนเปิดใช้งานจริง) เพื่อให้มีข้อมูลสะสมพร้อมทันทีที่ทนายอนุมัติ — แยกสวิตช์เป็นไฟล์ใหม่ `lib/market-price-flag.js` (`export const MARKET_PRICE_FEATURE_LIVE = false`) ต่างหากจาก `lib/market-price.js` เพราะไฟล์หลังใช้ service role key + `crypto` ฝั่งเซิร์ฟเวอร์เท่านั้น ห้ามถูก import เข้า client bundle — ไฟล์ flag นี้ import ได้ทั้งฝั่ง server (`api/pos/receives.js`) และฝั่ง client (`pages/pos.js`) อย่างปลอดภัย
      - `api/pos/receives.js`: `checkProcurementFraud()` (บันทึก `procurement_alerts`) และ `insertAnonymousMarketPrices()` (บันทึก `anonymous_market_prices`) **ยังเรียกทำงานไม่มีเงื่อนไขเหมือนเดิมทุกประการ** ไม่ขึ้นกับ flag เลย — จุดเดียวที่ gate คือตอนส่ง response กลับ: `warnings: MARKET_PRICE_FEATURE_LIVE ? warnings : []` ทำให้ frontend ไม่มีทางเห็น toast แจ้งเตือนราคาผิดปกติได้เลยตราบใดที่ flag ยังเป็น false (แต่ Supabase ยังมีข้อมูลครบสมบูรณ์เบื้องหลัง)
      - `pages/pos.js`: import flag แล้วซ่อนปุ่มแท็บ "🚩 ราคาผิดปกติ" ทั้งปุ่มด้วย conditional spread (`...(MARKET_PRICE_FEATURE_LIVE ? [{key:'fraud',...}] : [])`) — เพราะไม่มีปุ่มให้กด `reportType` จึงไม่มีทางกลายเป็น `'fraud'` ได้เลย (ไม่มี default/auto-navigate ไปแท็บนี้ที่ไหนในไฟล์) หน้ารายงานจึงมองไม่เห็นฟีเจอร์นี้เลยทั้งหน้า แม้ backend ยังทำงานเก็บข้อมูลอยู่
      - **วิธีเปิดใช้งานจริงในอนาคต (ทนายอนุมัติแล้ว):** แก้ค่าเดียวใน `lib/market-price-flag.js` จาก `false` เป็น `true` แล้ว deploy dashboard รอบเดียว — ข้อมูลที่สะสมไว้ทั้งหมดระหว่างรอ (ตั้งแต่ 2026-07-20) จะโผล่ทันทีโดยไม่ต้องรอสะสมใหม่
    - **ยังไม่ได้ deploy จุดใดในข้อนี้เลย** (`detectCategory()` fix ต้อง deploy บอท `smileslip-service`, `process-slip.js` fix + `MARKET_PRICE_FEATURE_LIVE` gating ต้อง deploy dashboard `smileslip-dashboard` — คนละ service ต้อง deploy แยกกันทั้งคู่)
    - **Deploy production แล้ว (2026-07-20)** — dashboard revision จริง `smileslip-dashboard-00266-2w8`, บอท revision จริง `smileslip-service-00144-crw` (ข้อความ deploy โชว์ revision เดิมผิดอีกตามเคยทั้งคู่ ต้องเช็คด้วย `gcloud run revisions list` แล้ว pin traffic เองเหมือนทุกครั้ง) — verified บอท boot สำเร็จ (Redis connected)
32. **ฟีดแบ็ก 11 ข้อจากไฟล์ผู้ใช้ส่งมา (`เพิ่มเติม 20072569-1.txt`) — ทำครบ 9/11 ข้อในรอบนี้ (2 ข้อสุดท้ายเป็นงานใหญ่แยกทำต่อ):**
    - **ข้อ 6 (คำถาม ไม่ใช่บั๊ก) "สำนักงานใหญ่คือที่ไหน":** ตรวจสอบจริงพบว่า `สำนักงานใหญ่` คือค่า default ของ `shop_profiles.branch_name` ที่ตั้งให้อัตโนมัติตอนสมัครสมาชิกถ้าไม่ได้พิมพ์ชื่อสาขาเอง (ยืนยันกับร้าน D Gas จริง: `branch_name='สำนักงานใหญ่'` ทั้งที่มีสาขาจริงอยู่ใน `shop_branches` คือ "มะลิ 1"/"ดีแก๊ส หางดง") — ยอดขายที่ไม่ได้เลือกสาขาตอนขายจะถูกจัดเข้ากลุ่มนี้แทน ไม่ใช่บั๊ก แนะนำผู้ใช้ไปแก้ชื่อใน Settings ให้ตรงกับสาขาจริงแทน
    - **ข้อ 10 (ส่วนแรก, คำถาม ไม่ใช่บั๊ก):** ยืนยันว่า state การรอรูปหลัง `#รับสินค้า`/`#รายจ่าย` ผูกกับ LINE userId ของคนพิมพ์เท่านั้น คนอื่นในกลุ่มส่งรูปอะไรก็ไม่ปนกัน
    - **ข้อ 10 (ส่วนสอง, บั๊กจริง) — ส่งรูปเป็นอัลบั้มหลายใบ อ่านได้แค่ใบแรก:** `consumeAwaitingReceive`/`consumeAwaitingExpense` เดิม "อ่านแล้วลบทันที" (consume-once) พอรูปแรกจับได้ก็ลบ state ทิ้ง รูปที่ 2 เป็นต้นไปในอัลบั้มเดียวกันจึงหลุดไปเข้า flow สแกนสลิปปกติแทน — แก้เป็น "อ่านอย่างเดียวไม่ลบ" (peek) ปล่อยให้หมดอายุเองตาม TTL 10 นาทีแทน รองรับทุกรูปในอัลบั้มได้ครบ (`smileslip-pro/index.js`)
    - **ข้อ 1 (POS หน้าขาย):** เพิ่มปุ่ม "🔍 ทั้งหมด (N)" โผล่เมื่อมีบิลเปิดมากกว่า 3 บิล เปิดแผงด้านข้าง (slide-in) ค้นหาชื่อบิล/ชื่อลูกค้าได้ กดผลลัพธ์สลับบิลทันที — ทดสอบจริงผ่านเบราว์เซอร์แล้ว (เปิด 4 บิล ค้นหากรองถูกต้อง กดสลับบิลสำเร็จ)
    - **ข้อ 2 (ใบกำกับภาษี):** เพิ่มช่องค้นหาผู้ติดต่อในฟอร์มออกใบกำกับภาษี (ค้นชื่อ/เบอร์โทร) เลือกแล้วเติมชื่อ/เบอร์/เลขภาษี/ที่อยู่/สาขาอัตโนมัติ + เพิ่มคอลัมน์ `เบอร์โทรผู้ซื้อ` ใหม่ในชีต "ใบกำกับภาษี" (14 คอลัมน์ A-N, เดิม 13) — `lib/pos-tax-invoice-pdf.js` เดิมรองรับ `buyer.phone` ไว้แล้วในโค้ด PDF (คอมเมนต์บอกไว้) แค่ไม่เคยถูก wire เข้าจริง ตอนนี้ต่อครบแล้ว
    - **ข้อ 3 (เก็บเงิน/ของ):** เพิ่มตัวกรองวันที่ (ทุกวันที่/วันนี้/สัปดาห์นี้/เดือนนี้) + เปลี่ยนค่าเริ่มต้นของตัวกรองสถานะจาก "ทั้งหมด" เป็น "รอดำเนินการ" (ซ่อนงานที่เสร็จแล้วเป็นค่าเริ่มต้น ตามที่ผู้ใช้เลือก — ยังดูประวัติย้อนหลังได้ผ่านตัวกรอง "เสร็จแล้ว")
    - **ข้อ 4:** เปลี่ยนคำ "มียอดค้าง/ถังค้าง" เป็น "ยอดค้างชำระ/ค้างสินค้า" ในหน้าผู้ติดต่อ (ตัดคำว่า "ถัง" ที่ไม่สากลออก)
    - **ข้อ 5 (ประวัติรับสินค้า):** จัดกลุ่มเป็นรายเดือน (dropdown เลือกเดือน พร้อมจำนวนรายการต่อเดือน) + แบ่งหน้า 20 รายการ/หน้า
    - **ข้อ 6 (ส่วนหลัง, รายจ่าย):** เพิ่มปุ่มเลือกหมวดหมู่ที่พบบ่อย (ค่าเช่าร้าน/ค่าน้ำ/ค่าไฟ/ค่าน้ำมัน/เงินเดือน/ค่าโทรศัพท์/ค่าซ่อมบำรุง/ค่าวัสดุสิ้นเปลือง/ค่าการตลาด) แทนพิมพ์เองทุกครั้ง (ยังพิมพ์เองได้) + เพิ่ม `category_breakdown` ใน `api/pos/reports.js` (`type=expenses`) พร้อมกราฟแท่งแสดงยอดต่อหมวดหมู่ในหน้ารายงาน
    - **ข้อ 7 (ยืนยันจัดส่งของพนักงาน):** เพิ่มส่วนลดรวมทั้งบิล (บาท/เปอร์เซ็นต์ — พนักงานแก้ราคาต่อชิ้นไม่ได้ตามที่ผู้ใช้ยืนยันว่าโอเค เพราะแอดมินตรวจสอบได้ทีหลังถ้าผิดพลาด) ยอดที่ QR/บันทึกจริงตรงกับยอดหลังหักส่วนลดเสมอ + เพิ่มหน้าสรุป+ปุ่ม "🖨️ พิมพ์ใบเสร็จ" (window.print ที่เขียนขึ้นใหม่เฉพาะหน้านี้ ไม่ได้ใช้ร่วมกับ `buildReceiptHtml()` ของ `pos.js` เพราะอยู่คนละไฟล์) หลังยืนยันจัดส่งสำเร็จ ก่อนกลับไปหน้ารายการ
    - **ข้อ 8 ส่วนแรก (แดชบอร์ด บัญชี):** เพิ่มการ์ดสรุปรายรับรวม/รายจ่ายรวม/กำไร-ขาดทุนสุทธิ ในหน้า "บัญชี → บัญชี (ledger)" ของ `dashboard.js` ตามตัวกรองที่เลือกอยู่ (เดิมมีแต่ตารางรายการ ไม่มีผลรวมเลย)
    - **ข้อ 8 ส่วนที่เหลือ — อัปเกรดหน้า "บัญชี → ภาษี" เสร็จแล้ว (ผู้ใช้ยืนยันว่าหมายถึงหน้านี้ ไม่ใช่ POS→รายงาน→VAT ที่ทำไว้แล้วในข้อ 27 — คนละแหล่งข้อมูลกัน หน้านี้อ่านจากชีตบัญชีหลักของบอท ไม่ใช่ POS Sheets):**
      - **`api/sheets/tax-report.js` เขียนใหม่ทั้งหมด (เดิมมีแค่สรุปตามคู่ค้าอย่างเดียว):** คำนวณภาษีขาย (Output VAT = SUM(tax_amount) ของแถว `type='รายรับ'`) และภาษีซื้อ (Input VAT = SUM ของแถว `type='รายจ่าย'`) แยกกันจริง + ภาษีสุทธิ (บวก=ต้องนำส่ง, ลบ=ขอคืนได้) — ใช้ข้อมูลชุดเดียวกับที่มีอยู่แล้ว (column C=ประเภท, N=ยอดภาษี) แค่ตีความใหม่ตามทิศทางธุรกรรมแทนที่จะกองรวมทุกอย่างเป็น "ภาษีรวม" เฉยๆ แบบเดิม
      - เพิ่ม query param `branch` — ไม่ระบุ = รวมทุกสาขา (พร้อมตาราง `branchBreakdown` แยกให้ดูทุกสาขาเทียบกัน), ระบุ = กรองเฉพาะสาขานั้น — คำนวณ `branchBreakdown` จากข้อมูล**ก่อน**กรองสาขาเสมอ (ให้เห็นภาพรวมทุกสาขาได้แม้ตอนนั้นกำลังดูสาขาใดสาขาหนึ่งอยู่ก็ตาม)
      - เพิ่ม export **Excel** (`format=xlsx`, 4 sheet: สรุป/แยกตามสาขา/สรุปตามคู่ค้า/รายการทั้งหมด — reuse `xlsx` library pattern เดียวกับ `api/export/excel.js` ที่มีอยู่แล้ว) และ **PDF** (`format=pdf`, ไฟล์ใหม่ `lib/vat-report-pdf.js` — reuse pdfkit + Sarabun font pattern เดียวกับ `lib/invoice-pdf.js`/`lib/pos-tax-invoice-pdf.js`) เป็นรายงานสรุประดับบริหาร 1 หน้า (การ์ดภาษีขาย/ซื้อ/สุทธิ + ตารางแยกสาขา + ตารางสรุปคู่ค้า) พอสำหรับนักบัญชียื่น ภ.พ.30/กระทบยอดเบื้องต้นได้ทันที — รายการละเอียดทุกแถวให้ใช้ Excel/CSV แทน (ข้อมูลเยอะเกินจะพิมพ์ PDF ให้อ่านง่าย) — CSV เดิมยังใช้ได้เหมือนเดิม เพิ่มแค่แถวสรุปภาษีขาย/ซื้อ/สุทธิต่อท้าย
      - `dashboard.js` (บัญชี → ภาษี): เพิ่ม dropdown เลือกสาขา (reuse `branches` state เดียวกับแท็บ ledger), เปลี่ยนการ์ดสรุปจาก "ภาษีรวมทั้งหมด" อันเดียวเป็น 3 การ์ด (ภาษีขาย/ภาษีซื้อ/สุทธิ — สีเขียว/แดง/น้ำเงินหรือส้มถ้าเป็นยอดขอคืน), เพิ่มตารางแยกตามสาขา (โชว์เฉพาะตอนมีมากกว่า 1 สาขาในข้อมูล), เพิ่มปุ่ม Export Excel + PDF ข้าง CSV เดิม
      - **เจอบั๊กจริงระหว่างทดสอบ PDF (แก้แล้ว):** ชื่อคู่ค้ายาวๆ ใน `lib/vat-report-pdf.js` ตัดคำขึ้นบรรทัดใหม่ (wrap) แทนที่จะตัดจบด้วย "…" ตามที่ตั้งใจ ทำให้ทับซ้อนกับแถวถัดไปในตาราง — สาเหตุ: pdfkit ต้องใส่ `height` คู่กับ `ellipsis: true` ถึงจะบังคับตัดจบจริง (ใส่แค่ `ellipsis: true` อย่างเดียวไม่พอ ยังปล่อยให้ wrap ได้อยู่) แก้โดยเพิ่ม `height: 14` ให้ทั้งคอลัมน์ชื่อคู่ค้าและเลขผู้เสียภาษี — ทดสอบซ้ำแล้วตัดจบถูกต้อง ไม่ทับซ้อนอีก
      - **ทดสอบจริงครบทุกจุดกับข้อมูลจริงของร้าน "D Gas" (ผ่าน curl + browser):** JSON (ภาษีซื้อ ฿894.83 จาก 5 รายการ, ภาษีขาย ฿0, สุทธิขอคืนได้ ฿894.83, branchBreakdown ถูกต้อง), กรองสาขา "มะลิ 1" คืน 0 ถูกต้อง (ข้อมูลทั้งหมดอยู่สาขา "ดีแก๊ส หางดง"), Excel export ได้ไฟล์ `.xlsx` ใช้งานได้จริง (25KB, 4 sheets), PDF export ได้ไฟล์ถูกต้องสวยงามอ่านง่าย (ตรวจสอบด้วยสายตาจริงผ่าน Read tool) — ฝั่งหน้าเว็บทดสอบผ่านเบราว์เซอร์จริงด้วย: เลือกปี/สาขา กด "ดูรายงาน" แสดงผลตรงกับ API เป๊ะ ปุ่ม export หายไปถูกต้องเมื่อไม่มีข้อมูล (totalRows=0)
    - **ข้อ 9 (multi-admin) — ทำเสร็จแล้ว:** แทนที่ `ADMIN_PASSWORD` เดี่ยวด้วยระบบบัญชีแอดมินจริง (ผู้ใช้เลือก "ทำ multi-admin เต็มรูปแบบตั้งแต่ตอนนี้เลย" + "เก็บ ADMIN_PASSWORD ไว้เป็น fallback ฉุกเฉิน")
      - ตาราง `company_admins` ใหม่ (2 บทบาท: `owner` จัดการแอดมินคนอื่นได้ + ใช้งานทุกอย่าง, `staff` ใช้งานทุกอย่างเหมือนกันยกเว้นจัดการแอดมินคนอื่น) — SQL ยังไม่ได้รัน (ดูหัวข้อ SQL ด้านล่าง)
      - `lib/admin-auth.js` (ใหม่) — `hashPassword`/`verifyPassword` ก็อปแบบเดียวกับ `register.js`/`email-login.js` เป๊ะ (pbkdf2 1000 รอบ sha512) ให้ pattern เดียวกันทั้งโปรเจกต์, `signAdminToken`/`signLegacyAdminToken`/`verifyAdminRequest`/`countActiveOwners`
      - **จุดสำคัญที่สุดที่ตัดสินใจ: token ยังขึ้นต้นด้วย `smileslip-admin:` เหมือนเดิมทุกตัวอักษร** (แค่เปลี่ยนข้อมูลหลัง prefix เป็น adminId แทนแค่ timestamp) เพื่อไม่ต้องแก้ไฟล์ที่มี `verifyAdmin()` copy กระจายอยู่ ~12 ไฟล์ API admin เดิมเลยสักไฟล์ (เช็คแค่ prefix เหมือนเดิม) — ไฟล์ใหม่ที่ต้องรู้ตัวตนจริง (`api/admin/admins.js`) ใช้ `verifyAdminRequest()` แยกต่างหาก
      - `api/admin/login.js` เขียนใหม่ — ส่ง `{email,password}` = login บัญชีส่วนตัว, ส่ง `{password}` อย่างเดียว = รหัสฉุกเฉินเดิม (ยังใช้ได้ตลอดตามที่ผู้ใช้ยืนยัน) คืน `needsBootstrap:true` ถ้า login ด้วยรหัสฉุกเฉินสำเร็จแต่ยังไม่มีแอดมินในตารางเลยสักคน
      - `api/admin/admins.js` (ใหม่) GET/POST/PATCH — bootstrap เจ้าของระบบคนแรกต้องพิสูจน์ด้วยรหัสฉุกเฉินเดิมก่อนเสมอ (`bootstrapPassword` ต้องตรงกับ `ADMIN_PASSWORD`), หลังจากนั้นเพิ่ม/แก้ไขแอดมินต้องเป็น `owner` เท่านั้น (ยกเว้นแก้ข้อมูล/รหัสผ่านของตัวเอง), **กันไม่ให้ระงับ/ลดสิทธิ์ `owner` คนสุดท้ายของระบบ** (เช็คด้วย `countActiveOwners()` ก่อนทุกครั้ง กันบริษัทล็อกตัวเองออกจากระบบทั้งหมด)
      - `pages/admin.js` — เพิ่มช่องอีเมล optional ในฟอร์ม login (เว้นว่าง = รหัสฉุกเฉิน), หน้า "ตั้งบัญชีเจ้าของระบบคนแรก" อัตโนมัติเมื่อ `needsBootstrap`, badge แสดงตัวตน+บทบาทในแถบด้านบน (หรือ "⚠️ เข้าด้วยรหัสฉุกเฉิน" ถ้าไม่มีตัวตน), แท็บใหม่ "แอดมิน" (โผล่เฉพาะตอนมีตัวตนจริง ไม่ใช่รหัสฉุกเฉิน) — ทุกคนเปลี่ยนรหัสผ่านตัวเองได้, เฉพาะ `owner` เห็น/จัดการรายชื่อแอดมินทั้งหมด (เพิ่มใหม่/สลับสิทธิ์/ระงับ-เปิดใช้งาน)
      - **เจอบั๊กจริงระหว่างทดสอบ (สำคัญ ควรจำไว้ใช้ที่อื่นด้วย):** `supabase.from(table).select('*', { count: 'exact', head: true })` **คืน `error:null, count:null, status:204` เสมอแม้ตารางนั้นไม่มีอยู่จริงเลยก็ตาม** (พิสูจน์แล้วด้วยการยิงตรงกับชื่อตารางมั่วๆ ก็ได้ผลเหมือนกัน) — ทำให้เช็ค "ตารางมีอยู่จริงไหม/มีกี่แถว" ด้วย `head:true` **ใช้ไม่ได้เลย** ต้องเปลี่ยนเป็น `.select('id', { count: 'exact' })` (ไม่ใส่ `head:true`) ถึงจะได้ `error` (`PGRST205`) กลับมาจริงเวลาตารางไม่มีอยู่ — เจอจากการทดสอบ `needsBootstrap` ตอนตารางยังไม่ถูกสร้าง (ได้ `true` ผิดๆ ทั้งที่ error-check ดูถูกต้องแล้วในโค้ด) แก้ทั้ง 2 จุดใน `login.js`/`admins.js` แล้ว — **ถ้าจะเช็คว่าตาราง Supabase มีอยู่จริงไหมในอนาคต ห้ามใช้ `head:true` เด็ดขาด**
      - **ทดสอบก่อนรัน SQL:** ยิง `curl` ตรงยืนยันว่า legacy login ยังทำงานปกติ 100% แม้ตาราง `company_admins` ยังไม่มีอยู่จริง (`needsBootstrap:false` ถูกต้อง, ไม่ crash)
      - **ผู้ใช้รัน SQL แล้ว (2026-07-20) — ทดสอบ end-to-end ครบทุก path ด้วยบัญชีทดสอบผ่าน curl แล้วลบทิ้งหลังทดสอบ:** (1) legacy login หลังรัน SQL คืน `needsBootstrap:true` ถูกต้อง (2) bootstrap owner คนแรกสำเร็จ ต้องพิสูจน์ด้วย `bootstrapPassword` ตรงเป๊ะ (3) login ด้วยอีเมล/รหัสผ่านที่เพิ่ง bootstrap สำเร็จ (4) bootstrap ซ้ำรอบสองถูกปฏิเสธถูกต้อง (กลายเป็น path "เพิ่มแอดมินปกติ" เพราะมีคนแล้ว ปฏิเสธเพราะไม่มี owner token) (5) owner เพิ่ม staff คนใหม่สำเร็จ (6) staff login สำเร็จ (7) staff เรียก `GET /api/admin/admins` ถูกปฏิเสธถูกต้อง (เฉพาะ owner) (8) staff เปลี่ยนรหัสผ่านตัวเองด้วย current_password ผิดถูกปฏิเสธ ใส่ถูกสำเร็จ (9) owner ระงับตัวเองถูกปฏิเสธ (10) owner ลดสิทธิ์ตัวเอง (owner คนเดียวที่เหลือ) ถูกปฏิเสธ (11) owner ระงับ staff สำเร็จ → staff login ไม่ได้ (12) owner เปิดใช้งาน staff กลับ → staff login ได้ปกติ — **ครบทุก guard rail ตามที่ออกแบบไว้ ทำงานถูกต้อง 100%**
    - **Deploy production แล้ว (2026-07-20)** — dashboard revision จริง `smileslip-dashboard-00267-zkh` (ข้อความ deploy โชว์ revision เดิมผิดอีกตามเคย) traffic pin 100% แล้ว — verified บน production จริงว่า `/api/admin/login` (legacy + `needsBootstrap`) และ `/api/sheets/tax-report` (แยกภาษีขาย/ซื้อ, ข้อ 8 ส่วนที่เหลือ) ทำงานถูกต้องทั้งคู่
5.5. **[spawn_task ที่แจ้งไว้ 2026-07-19]** พบว่าไฟล์ POS API หลายไฟล์ (`collections.js`, `loans.js`, `delivery.js`, `contacts.js`, `products.js`, `staff-requests.js`, `tax-invoice.js`, `staff.js`, `sales.js`, `receives.js`) เขียนสตริงวันที่ไทยลง Sheets โดยไม่ผ่าน `asText()` เหมือนกับที่เพิ่งเจอบั๊กจริงใน `expenses.js` — ยังไม่ได้ไล่แก้ทั้งหมด (สาเหตุที่ยังดูเหมือนใช้งานได้ปกติในบางไฟล์ อาจเป็นเพราะ column format ของ tab เก่าถูกซ่อมมาแล้วจากเหตุการณ์ก่อนหน้า ไม่ใช่เพราะโค้ดปลอดภัยจริง) — รอทำต่อเป็นงานแยก
6. **แก้ 5 ข้อจาก punch list ตรวจสอบโค้ดเต็มโปรเจกต์ (commit `e01c927`):**
   - **Stripe webhook idempotency** — เพิ่ม insert `event.id` ลงตาราง `stripe_processed_events` ก่อนประมวลผลทุกครั้ง ถ้า insert ชนซ้ำ (unique violation, code `23505`) แปลว่าเคยประมวลผลไปแล้ว ข้ามได้เลย ถ้า error เป็นแบบอื่น (เช่น ตารางยังไม่ถูกสร้าง) จะ fail-open ทำงานต่อไปเหมือนเดิมกันไม่ให้ checkout พังทั้งระบบ — **สร้างตาราง `stripe_processed_events` แล้ว (verified 2026-07-16 ผ่าน REST query) กันซ้ำได้จริงแล้ว ไม่ fail-open อีกต่อไป**
   - **`api/admin/pos-stats.js` และ `delivery-stats.js`** เช็ค token ผิดรูปแบบมาตั้งแต่สร้าง (เทียบ decoded token กับ `ADMIN_PASSWORD` ตรง ๆ แทนที่จะเช็ค prefix `smileslip-admin:` แบบไฟล์ admin อื่น) ทำให้ admin login ปกติเรียกใช้ไม่ได้ 401 ตลอด — แก้ให้ตรงกับ pattern เดียวกับไฟล์ admin อื่นแล้ว
   - **`lib/google-delivery.js`** เดิมรู้อยู่แล้วว่าบัญชี Google ภาษาไทยได้ tab ชื่อ "แผ่น1" ไม่ใช่ "Sheet1" แต่ API ทุกไฟล์ hardcode "Sheet1" ตรง ๆ — แก้ที่ต้นตอโดยบังคับ rename tab แรกเป็น "Sheet1" เสมอตอนสร้าง spreadsheet ใหม่ (ไม่ต้องแก้ทุก API route)
   - **`api/shop/branches.js`** เพิ่มการเช็คจำนวนสาขาปัจจุบันเทียบ `MAX_BRANCHES` ตาม tier ก่อน insert (เดิมกันแค่ฝั่ง client เท่านั้น ยิง API ตรงเพิ่มเกิน limit ได้) — ต้องตรงกับ `MAX_BRANCHES` ใน `smileslip-pro/index.js` เสมอถ้าจะแก้ค่า limit ในอนาคต
   - ลบ `StampSVG()` dead code ใน `pages/invoice/request.js` ออกแล้ว (ยืนยันซ้ำว่าไม่มีที่เรียกใช้ที่ไหนก่อนลบ)
   - **ยังไม่ทำ:** ข้อ KBank/SCB auto-confirm webhook (`api/banking/kbank-notify.js`, `scb-notify.js`) ยังเป็น scaffolding 0% เหมือนเดิม — ต้องมี API credential จริงจากธนาคารก่อนถึงจะเขียนต่อได้ (รอผู้ใช้ติดต่อธนาคารขอ credential)

33. **30-Day Free Trial Lock Mechanism + Package Tier & Feature Gating Matrix — ผู้ใช้ส่งสเปกมาขอทำ 4 บั๊ก + ปรับแพ็กเกจ + gating matrix "ให้เสร็จแล้วดีพลอย" ก่อนเริ่มฟีเจอร์ที่เหลือจากไฟล์ก่อนหน้า:**
    - **แก้บั๊ก 4 ข้อที่ผู้ใช้แจ้งมา (ทุกข้อเจอ root cause จริงจากการอ่านโค้ด ไม่ใช่แค่เดา):**
      1. **ขายเชื่อไม่อัปเดตยอดค้างชำระของผู้ติดต่อ** — `api/pos/sales.js` ไม่เคยเขียน `custExisting[13]` (debt) เลยทั้งตอนขายเชื่อ (POST) และตอนรับชำระ/ยกเลิกบิล (PATCH) — แก้ทั้งสองจุด: POST บวกยอดเชื่อเข้า debt, PATCH ลบยอดกลับลงตอนบิลถูกยกเลิก/แก้ไข
      2. **งานเก็บเงิน/ของ ขึ้นว่า "เสร็จแล้ว" ก่อนแอดมินยืนยันรับจริง** — หน้ารายงานเดิมเช็คแค่ `status !== 'รอดำเนินการ'` ไม่ได้แยกชั้น "พนักงานรายงานว่าเก็บได้" ออกจากชั้น "แอดมินยืนยันรับเงิน/ของเข้าร้านแล้ว" จริง — เพิ่ม `isFullyDone` เช็คทั้งสองชั้นถูกต้อง
      3. **สต็อคสินค้าหมุนเวียนไม่อัปเดตหลังยืนยันจัดส่ง** — `api/pos/delivery.js` `confirm_delivery` ไม่เคยอัปเดต `stock`/`at_customer` เลย (อัปเดตแค่ `empty_waiting` และเฉพาะตอนมีการคืนเท่านั้น) กับคำนวณถังลูกค้าผิด (ลบตรงๆ ไม่บวกยอดที่ยืมไปก่อน) — เขียนใหม่ให้ตรรกะตรงกับ `sales.js` (checkout หน้าร้าน) เป๊ะ
      4. **`pos-staff.js` ไม่โชว์ชื่อสาขาของพนักงาน** — `verify-pin.js` มี `branch_name` อยู่แล้วใน `rowToStaff()` แต่ไม่เคยส่งกลับในตัวตนพนักงาน — เพิ่ม field เดียว + โชว์ในหน้า header ฝั่งพนักงาน
    - **ลดขั้นตอนสมัครสมาชิก (item 11)** — ตัดฟอร์มกรอกบัญชีธนาคารออกจาก Step 3 ทั้งหมด (ฟอร์มสมัครสมาชิกเดิม step 2 → 3 ขั้นตอน เหลือ 3 ขั้น: ธุรกิจ/ที่อยู่/ตั้งรหัสผ่าน) — ตรวจสอบแล้วว่า `api/register.js` เขียนบัญชีธนาคารแบบ conditional (`if (bankName && accountNumber)`) อยู่แล้ว จึงตัดออกได้โดยไม่กระทบ backend เลย ร้านเพิ่มบัญชีธนาคารทีหลังได้ผ่านหน้า Settings ตามปกติ
    - **30-Day Free Trial Lock Mechanism:**
      - Starter ไม่ใช่ freemium ถาวรอีกต่อไป แต่เป็น "ทดลองใช้ฟรีเต็มรูปแบบ 30 วัน" (ไม่ต้องผูกบัตร ใช้ POS/บอทสแกนสลิปได้เต็มรูปแบบระหว่างทดลอง) — เพิ่มคอลัมน์ `trial_started_at`/`trial_ends_at`/`status` (default `'active'`)/`trial_day25_notified` ใน `shop_profiles` (SQL ยังไม่ได้รัน ดู "ต้องทำด้วยมือ" ด้านล่าง)
      - **ร้านเก่าก่อนหน้านี้ทั้งหมดได้รับการยกเว้นตลอดไปตามที่ผู้ใช้ตัดสินใจ** ("ยกเว้นร้านเก่า ไม่ต้องนับ trial ย้อนหลัง") — กลไกคือร้านเก่าจะไม่มี `trial_started_at`/`trial_ends_at` เลย (เป็น NULL ตลอดไป เพราะ `api/register.js` เป็นจุดเดียวที่ตั้งค่านี้ และรันแค่ตอนสมัครใหม่เท่านั้น) ทุก query/cron ที่เช็ค trial กรองด้วย `.not('trial_ends_at','is',null)` เสมอ จึงไม่มีทางแตะร้านเก่าโดยไม่ตั้งใจ
      - `lib/shop-access.js` (ใหม่) — `blockIfTrialExpired(req,res,shopId)` เรียกที่ต้นทุก endpoint POS ที่เขียนข้อมูล (`sales.js`,`delivery.js`,`collections.js`,`products.js`,`contacts.js`,`expenses.js`,`receives.js` ฯลฯ รวม 15 ไฟล์ — GET ยังอ่านได้ปกติเสมอไม่ถูกบล็อก) — **fail-safe เสมอ**: ถ้าคอลัมน์ `status` ยังไม่ถูกสร้างหรือ query error ใดๆ ถือว่า "เขียนได้" (ไม่ล็อกผิดคนโดยไม่ตั้งใจ ยอมรับความเสี่ยงฝั่งนี้มากกว่า)
      - `smileslip-pro/index.js` STEP 1.9 (ก่อน STEP 2 ตรวจเครดิต) เช็ค `shop.status === 'trial_expired'` แล้วตอบข้อความเตือนแทนสแกน OCR (ข้อมูลเดิมใน Sheets/Drive ไม่ถูกแตะเลย) — `shop` มาจาก `findShopBySource()` ที่ `select('*')` อยู่แล้ว จึงไม่ต้อง query เพิ่ม
      - **`api/cron/expire-trials.js` (ใหม่, dashboard)** — cron รายวันเช็คร้านที่ `trial_ends_at` หมดแล้วและยังไม่มี `stripe_subscription_id` จริง → ตั้ง `status='trial_expired'` — Stripe webhook (`webhooks/stripe.js`) รีเซ็ต `status='active'` ทันทีเมื่อสมัครสมาชิกจริงสำเร็จ (แยก `.update()` ต่างหากจากการอัปเดต tier หลัก กันพังกันคนละเรื่อง)
      - **Day-25 LINE nudge (`/cron/trial-day25-nudge`, ใหม่, บอท)** — แจ้งเตือนร้านที่เหลือเวลา ≤5 วันก่อนหมดทดลอง (วันที่ 25/30) พร้อมสรุปการใช้งานช่วงทดลอง (จำนวนรายการ/รายรับ-รายจ่ายสะสม อ่านจาก Sheets แบบ best-effort) + ปุ่มลิงก์อัปเกรด — กันส่งซ้ำด้วย `trial_day25_notified`
    - **Package Tier & Feature Gating Matrix (`lib/tier-features.js`, ใหม่)** — `hasFeature(tier, feature)`:
      - **3 ฟีเจอร์ล็อกเฉพาะ Shop Pro (`pro`) เท่านั้น** — สต็อกสินค้าหมุนเวียน (`cyclical_stock`), แอปพนักงานส่งของ (`delivery_staff_app`), ระบบเงินเชื่อ/ลูกหนี้ (`credit_ar`) — **Starter (trial ฟรี) และ Advance ขึ้นไปใช้ได้ปกติ มีแค่ Shop Pro (แพ็กเกจเสียเงินที่ถูกที่สุด) เท่านั้นที่ไม่มีสามข้อนี้** — เป็นกลไก upsell ที่ตั้งใจ (ตัดสินใจแล้วตามสเปกผู้ใช้ ไม่ใช่บั๊ก) เพื่อดันให้ร้านที่เคยใช้ครบตอน trial อัปเกรดเป็น Advance แทนที่จะหยุดแค่ Shop Pro
      - `vat_report` ต้อง business+ (ตรงกับที่ `tax-report.js` เช็คอยู่แล้วเดิม), `market_price_index` ต้อง enterprise (และยังต้องรอ `MARKET_PRICE_FEATURE_LIVE` ด้วย)
      - **ผู้ใช้ยืนยันให้ล็อกทันทีทุกร้าน Shop Pro ที่มีอยู่แล้ว** ("ตอนนี้ไม่มีบัญชีจริงแม้แต่อันเดียว ฉันทดลองหมดเลย") — ไม่ต้อง grandfather ร้านที่มีอยู่สำหรับ gating นี้ (ต่างจาก trial lock ที่ grandfather ร้านเก่า)
      - Enforce ฝั่ง backend ที่ทุก write endpoint ที่เกี่ยวข้อง (`sales.js` เชื่อ, `delivery.js` ค้างจ่าย, `collections.js` ทั้งหมด, `products.js`/`verify-pin.js` หมุนเวียน/แอปพนักงาน) — **`products.js` PATCH บล็อกเฉพาะตอน "เปลี่ยนประเภทเป็นหมุนเวียนใหม่" เท่านั้น** (เทียบ `existing[10] !== 'หมุนเวียน'` ก่อนบล็อก) ไม่บล็อกการแก้ไขฟิลด์อื่นของสินค้าหมุนเวียนที่มีอยู่แล้ว (กันไม่ให้ shop ที่เคยสร้างสินค้าหมุนเวียนไว้ตอน tier สูงกว่าแล้วโดน error ตอนแก้ราคา/ชื่อทีหลัง)
      - Enforce ฝั่ง UI ที่ `pos.js` — ซ่อนตัวเลือก "เชื่อ"/"ค้างจ่าย" ในหน้าชำระเงิน/สร้างออเดอร์จัดส่ง, ซ่อนตัวเลือกประเภทสินค้า "หมุนเวียน" ในฟอร์มเพิ่มสินค้า, ซ่อนทั้งแท็บ "เก็บเงิน/ของ" และปุ่ม "ส่งพนักงานไปเก็บ" ทั้งหมด สำหรับ Shop Pro
    - **ปรับ copy หน้า `pricing.js`** — เปลี่ยนข้อความ "50 สลิปแรกฟรี" เป็น "ทดลองใช้ฟรีเต็มรูปแบบ 30 วัน" ทั้ง hero banner และการ์ด Starter, เพิ่มคำเตือนชัดเจนใน Shop Pro ว่า "⚠️ ไม่รวม: สต็อกหมุนเวียน/แอปพนักงานส่งของ/ระบบเงินเชื่อ" และใน Advance ว่า "🔓 ปลดล็อก..." กันลูกค้าสับสน/ประหลาดใจตอนซื้อ
    - **Deploy production แล้ว (2026-07-20)** — bot revision จริง `smileslip-service-00145-zlx`, dashboard revision จริง `smileslip-dashboard-00268-slt` (ข้อความ deploy โชว์ revision เดิมผิดอีกตามเคยทั้งคู่ เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เองเหมือนทุกครั้ง) — verified: บอท boot สำเร็จ (Redis connected), หน้า `/pricing` โชว์ copy ทดลองใช้ 30 วัน + คำเตือน Shop Pro ใหม่ถูกต้องบน production จริง, endpoint `/cron/trial-day25-nudge` มีอยู่จริงและปฏิเสธ request ไม่มี secret ถูกต้อง (401)
    - **ผู้ใช้รัน SQL แล้ว + สร้าง Cloud Scheduler jobs ครบแล้ว (2026-07-21)** — verified คอลัมน์ trial มีจริงผ่าน REST query (ร้านเก่าทุกร้าน `trial_started_at`/`trial_ends_at` เป็น NULL ตามที่ตั้งใจ ไม่มีใครถูกแตะ), ทดสอบรัน `smileslip-expire-trials`/`smileslip-trial-day25-nudge` ด้วยมือสำเร็จทั้งคู่ (HTTP 200 / log จบไม่มี error) — ระบบ 30-Day Free Trial Lock **มีผลจริงครบวงจรแล้ว** ทั้งการล็อกอัตโนมัติเมื่อครบ 30 วันและ LINE nudge วันที่ 25 — ส่วน feature gating matrix (ล็อก Shop Pro 3 ฟีเจอร์) มีผลตั้งแต่ deploy แล้วเช่นกัน (ไม่ต้องรอ SQL เพราะใช้แค่ `subscription_tier` ที่มีอยู่แล้ว)

34. **7 ข้อจากไฟล์ฟีดแบ็ก `เพิ่มเติม 20072569-2.txt` (ข้อ 2,3,5,6,8,10,12 — ข้อ 4,7,9,11,13 ทำไปแล้วในข้อ 33) + วิเคราะห์ 2 ไอเดียฟีเจอร์ใหม่ (ยังไม่ทำ ผู้ใช้ขอแค่ความเห็น) — ทดสอบยิงจริงกับร้าน "D Gas" ทุกจุดที่แตะ backend ก่อน build เสมอ:**
    - **ข้อ 2 (บันทึกด้วยมือต้องเลือกสาขา):** `dashboard.js` modal "✍️ บันทึกรายการด้วยมือ" เพิ่ม dropdown เลือกสาขา (บังคับ, default = สาขาที่เปิดอยู่/สำนักงานใหญ่) — `api/sheets/add-transaction.js` ใช้ `branch` จาก request แทนชื่อร้านตรงๆ เสมอ (เดิม hardcode เป็นชื่อร้านหลักไม่ว่าเลือกสาขาไหนก็ตาม)
    - **ข้อ 3 (วันที่ย้อนหลัง):** เพิ่ม helper กลาง `resolveRecordDateTime(transactionDate)` ใน `lib/google-pos.js` — คืนทั้งรูปแบบไม่ pad (ใช้เขียนคอลัมน์ "วันที่-เวลา" ของ POS Sheet เอง ตรงกับ `toLocaleString('th-TH')` เดิมเป๊ะ) และรูปแบบ pad 2 หลัก (ใช้เขียน Sheets บัญชีหลัก ตรงกับ `toLocaleDateString(...,{day:'2-digit'})` เดิม) — **ต้องแยก 2 รูปแบบเพราะโค้ดเดิมของโปรเจกต์ใช้ไม่ตรงกันระหว่าง 2 จุดอยู่แล้ว** ถ้าใช้ format เดียวจะพัง date-filter แบบ `startsWith` ที่มีอยู่เดิมสำหรับวันที่/เดือนหลักเดียว (เจอจากการไล่โค้ดก่อนเขียน ไม่ใช่เดา) — ใช้ใน `receives.js`/`sales.js`/`expenses.js` POST ทั้ง 3 ไฟล์ (เขียนคอลัมน์วันที่ของ "รายการ" แยกจาก `recorded_at`/timestamp จริงที่ยังคงเป็นเวลาระบบเสมอ) + เพิ่ม date picker (default วันนี้, max วันนี้) ในฟอร์มขาย/รับสินค้า/รายจ่ายของ `pos.js` — **ทดสอบ backdate จริงกับสินค้า "น้ำแก๊ส 15 กก." (SKU PBSV5A4) → 15 มิ.ย. 2569 สำเร็จ ยืนยันว่า POS Sheet ("15/6/2569") และ Sheets บัญชีหลัก tab "2026" (ไม่ใช่ tab ปีปัจจุบันถ้า backdate ข้ามปี — ใช้ปีของ transactionDate เลือก tab เสมอ) ถูกต้องทั้งคู่ โดย recorded_at ยังเป็นวันจริงที่บันทึก** ลบข้อมูลทดสอบแล้ว
    - **ข้อ 5 (ส่งพนักงานเก็บเงิน/ของ ดึงยอดอัตโนมัติ):** ยอดเงินเชื่อ (`debt_amount`) จริงๆ auto-fill อยู่แล้วจากโค้ดเดิม (`cust.debt`) — ส่วนที่ขาดจริงคือจำนวนสินค้าหมุนเวียนแยกตาม SKU (เดิมเดาได้แค่ร้านที่มีสินค้าหมุนเวียนชนิดเดียว) เพิ่ม endpoint `GET /api/pos/collections?holdingsFor=<contact_id>` คำนวณจาก log "บันทึกแลกเปลี่ยน" (ยืม +qty, คืน -qty, แลกเปลี่ยน สุทธิ 0) — **เจอข้อจำกัดจริงระหว่างทดสอบ:** log ที่มาจากตอนสร้างออเดอร์จัดส่ง (`ยืม` ตอน POST) ไม่มี SKU (เก็บเป็น "สินค้าหมุนเวียน (รวม)" รวมยอดเดียว — known gap เดิมจากข้อ 28) ทำให้คำนวณแยก SKU ได้ไม่ครบทุกเคส — แก้โดยเทียบผลรวมที่คำนวณได้กับยอดรวมจริง (`cust.cylinders`) ก่อนเสมอ **ถ้าไม่ตรงจะไม่เดาส่งเดา** แต่โชว์ยอดรวมพร้อมคำเตือนให้แอดมินกรอกแยกเองแทน (ปลอดภัยกว่าโชว์เลขผิด) — ใช้ endpoint เดียวกันนี้ทำช่องขยายดูรายละเอียดในหน้ารายงาน "🔄 สินค้าหมุนเวียน" ด้วย (ข้อ 6 ส่วนแรก)
    - **ข้อ 6 (ประวัติหนี้ไม่โชว์รายการสินค้า):** `openDebtHistory()` เดิมดึงแค่ออเดอร์จัดส่งค้างจ่าย ไม่เคยรวมยอดขายเชื่อหน้าร้านเลย (บั๊กจริง: ลูกค้าที่ค้างจากขายเชื่อหน้าร้านล้วนๆ ไม่เห็นรายการเลยสักบิลทั้งที่ยอดรวมแสดงถูกต้อง) แก้ให้ดึงทั้ง `sales.js` (payment_method='เชื่อ') และ `delivery.js` (ค้างจ่าย) มารวมกัน + แสดงรายการสินค้าต่อบิล (`item.name×qty`) ที่ไม่เคยแสดงมาก่อนเลย + badge แหล่งที่มา (🏪 หน้าร้าน / 🚚 จัดส่ง)
    - **ข้อ 8 (พนักงานจัดส่งรับเงินบางส่วน) — เจอบั๊กจริงเพิ่มระหว่างทำ:** `api/pos/delivery.js` PATCH `confirm_delivery` **ไม่เคยเพิ่มยอดค้างชำระของลูกค้าเลยสักครั้งตอนพนักงานเลือก "ค้างจ่าย" ตอนยืนยันจัดส่ง** (ต่างจากตอนสร้างออเดอร์ที่เพิ่มถูกต้อง) เพราะ debt-add logic เดิมผูกอยู่กับ `credit_settled`/POST เท่านั้น — แก้พร้อมเพิ่มฟีเจอร์ที่ขอ: เพิ่มช่อง "จ่ายแล้วตอนนี้ (บาท)" ใน `pos-staff.js` ตอนเลือกค้างจ่าย ส่ง `partial_paid_amount` ไป backend คำนวณ `debtAdded = total - paidNow` (ไม่ต่ำกว่า 0) แล้วบวกเข้า `contact.debt` จริง + โชว์ยอดค้างในหน้าสำเร็จและใบเสร็จที่พิมพ์ — **ทดสอบยิงจริงกับลูกค้า "ศิริลักษณ์" (฿100 จ่าย 40 → ค้าง 60) ยืนยันถูกต้อง 100% แล้วลบ/คืนค่าทดสอบทั้งหมด**
    - **ข้อ 10 (ตั้งค่าสาขา: ที่อยู่/ชื่อออกใบกำกับภาษี) — เจอ 2 ช่องว่างจริงตรงกับที่ผู้ใช้อธิบาย:**
      1. `shop_branches` มี `brand_name` อยู่แล้ว (ไม่เคยบันทึกไว้ใน CLAUDE.md) แต่ไม่มี `address` เลย — เพิ่มคอลัมน์ `address` (SQL ด้านล่าง) + UI แก้ไขในหน้า "จัดการสาขา" ของ `dashboard.js` (เว้นว่าง = ใช้ที่อยู่ร้านหลัก ตรงกับ semantic เดิมของ `brand_name`) — **เขียน API แบบ fail-safe**: `api/shop/branches.js` POST/PATCH แยกเขียน `address` เป็น query ต่างหากจาก insert/update หลักเสมอ (กันพังการสร้าง/แก้สาขาถ้ายังไม่ได้รัน SQL — ทดสอบแล้วจริงว่าสร้างสาขาได้ปกติแม้คอลัมน์ยังไม่มี)
      2. **`shop_profiles.branch_name` (ชื่อสาขาสำนักงานใหญ่) ไม่เคยมีช่องแก้ไขเลยสักที่ในทั้งโปรเจกต์** — `api/shop/update-profile.js` ไม่รับ `branchName` มาตั้งแต่สร้างไฟล์ (ตรงกับที่ผู้ใช้บอกว่า "ให้เปลี่ยนสาขาสำนักงานใหญ่แล้วเปลี่ยนไม่ได้" — คือบั๊กจริง ไม่ใช่ UI confuse) แก้แล้ว + เพิ่มช่องกรอกในหน้าตั้งค่าโปรไฟล์ของ `dashboard.js` (label "ชื่อสาขาสำนักงานใหญ่" แยกชัดเจนจาก `shop_branches` ที่เป็นสาขาย่อยเพิ่มเติม) — **ทดสอบแก้+คืนค่าจริงกับร้าน "D Gas" สำเร็จ**
      - ต่อยอด: ใบกำกับภาษี (`tax-invoice.js`/`tax-invoice-pdf.js`) เพิ่มคอลัมน์ `seller_name`/`seller_address` (O-P) **แช่แข็งค่าไว้ ณ ตอนออกจริง** (ไม่คำนวณใหม่ตอนพิมพ์ซ้ำทีหลังถ้าตั้งค่าสาขาถูกแก้ไขในอนาคต) — `pos.js` resolve ตอนออกจริง: `selectedBranch.brand_name || shopInfo.shop_name`, `selectedBranch.address || shopInfo.address` (ว่าง = เหมือนบริษัทหลักตามที่ผู้ใช้ขอ toggle) — ใบเสร็จปกติ (`buildReceiptHtml`) ก็ใช้ resolve เดียวกันนี้ด้วย
    - **ข้อ 12 (มือถือแท็บแน่นเกินไป):** จัดกลุ่มแท็บ POS ใหม่ — แท็บหลัก (ขาย/ออเดอร์/ผู้ติดต่อ/สินค้า) อยู่บนแถบเสมอ, แท็บรอง (เก็บเงิน/ของ, รับสินค้า, รายจ่าย, รายงาน, ตั้งค่า) ย้ายเข้าปุ่ม "⋯ เพิ่มเติม" เปิด bottom-sheet เลือก — ปุ่ม "เพิ่มเติม" เปลี่ยนเป็นชื่อแท็บที่เลือกอยู่เองถ้ากำลังอยู่ในแท็บรอง — **build ผ่านแล้ว แต่ทดสอบผ่านเบราว์เซอร์จริงไม่สำเร็จ** (หน้าเว็บค้างที่ "กำลังโหลด..." ในเบราว์เซอร์ทดสอบแบบ sandboxed ของเครื่องมือ ไม่ใช่ปัญหาโค้ด — สงสัยว่า LIFF SDK หรือ auth flow ต้องพึ่ง network ภายนอกที่ sandbox บล็อกไว้) — โค้ดผ่าน code review + build check แล้ว แต่แนะนำให้ผู้ใช้ลองเปิดเองบนมือถือจริงเพื่อยืนยันภาพหน้าจอ
    - **วิเคราะห์ 2 ไอเดียฟีเจอร์ใหม่ (ผู้ใช้ขอแค่ความเห็น "ลองช่วยคิดให้หน่อย" — ยังไม่ได้ทำ ไม่ได้รับอนุมัติให้เริ่ม):**
      1. **QR สั่งอาหารที่โต๊ะ (ร้านอาหาร):** เป็นไปได้จริง รียูส infrastructure POS ที่มีอยู่ได้เยอะ (สินค้า/checkout/QR พร้อมเพย์) แต่ต้องสร้าง "ออเดอร์ประเภทที่ 3" คู่ขนานกับขายหน้าร้าน+จัดส่งที่มีอยู่แล้ว (หน้าสั่งสาธารณะไม่ต้อง login ผูกกับโต๊ะ, สถานะ pending→ยืนยันรับออเดอร์→ส่งแล้ว→ออกบิล→ปิด, ซิงก์เรียลไทม์ระหว่างมือถือลูกค้ากับหน้าจอพนักงาน) — ประเมินว่าใหญ่พอๆ กับตอนสร้างโมดูล Delivery ทั้งระบบ (ใช้เวลาหลายวัน) ไม่ใช่งานเล็ก แนะนำให้แยกเป็นโปรเจกต์เฉพาะ วางแผนเป็นเฟสย่อยเหมือน Delivery เดิม ไม่ควรรวบรัดทำในชุดฟีดแบ็กเดียวกับงานเล็กอื่น
      2. **เว็บลูกค้าสั่งจัดส่งเอง:** รียูสของเดิมได้เยอะกว่า (`api/pos/delivery`/`products` มีอยู่แล้ว) ไม่ต้องซิงก์เรียลไทม์แบบ QR โต๊ะ (จัดส่งเป็น async อยู่แล้ว) เล็กกว่าและทำได้เร็วกว่าชัดเจน — ควรให้ออเดอร์จากลูกค้าเข้าคิว "รอยืนยัน" ก่อนเสมอ (mirror pattern เดิมของ "รับสินค้ารอยืนยัน"/"รายจ่ายรอยืนยัน" จาก LINE) กันสแปม/พิมพ์ผิดจากคนนอกโดยไม่มีคนตรวจก่อน ต้องมี rate-limit เพราะเป็นหน้าเปิดสาธารณะไม่ต้อง login — **แนะนำ:** ถ้าจะทำ ให้ทำอันนี้ก่อน QR โต๊ะ เพราะเล็กกว่า/เสี่ยงน้อยกว่า/ใช้ของเดิมได้เยอะกว่า

35. **สรุปไฟล์ฟีดแบ็ก 2 ฉบับ (`21072569-1.txt` — package matrix audit ที่ทำไปแล้วจากข้อ 33, Excel report engine ใหม่, ฟีเจอร์ Enterprise 4 อัน) แล้วผู้ใช้ขอเรียงลำดับ "ง่ายก่อน ใหญ่ทีหลัง" — ทำ 2 ข้อแรกเสร็จแล้ว (ตารางเปรียบเทียบแพ็กเกจ + เว็บสั่งซื้อลูกค้า), กำลังทำ Excel engine/โอนย้ายสต็อก/สิทธิ์พนักงาน ต่อ, เก็บ White-Label กับ QR โต๊ะไว้คุยรายละเอียดก่อน:**
    - **ตารางเปรียบเทียบฟีเจอร์ทุกแพ็กเกจ ในหน้า `/pricing`** — เพิ่มตาราง scroll แนวนอนได้บนมือถือ ใต้การ์ดแพ็กเกจเดิม (การ์ดเดิมคงไว้ทั้งหมด ไม่ลบ) สรุป 20 แถวฟีเจอร์/ลิมิตจากเนื้อหาการ์ดที่มีอยู่แล้ว (ไม่ใช่ logic ใหม่) + ปุ่มสมัคร/อัปเกรดอันที่สองต่อท้ายตาราง (อันเดิมในการ์ดยังอยู่) — verified ผ่านเบราว์เซอร์จริงว่าตารางเรนเดอร์ถูกต้องครบทุกแถว
    - **เว็บสั่งซื้อ/สั่งจัดส่งสาธารณะสำหรับลูกค้า (`pages/order.js`, ไม่ต้อง login)** — ร้านแชร์ลิงก์ `/order?shopId=xxx` ให้ลูกค้าเอง (คัดลอกได้จากแท็บตั้งค่าใน POS) ลูกค้าเลือกสินค้า+กรอกข้อมูล+เลือกวิธีชำระ (เก็บปลายทาง/โอนแล้วแนบสลิป) → เข้าคิว **"ออเดอร์ลูกค้ารอยืนยัน" เสมอ ไม่กลายเป็นออเดอร์จัดส่งจริงทันที** (mirror pattern เดียวกับ "รับสินค้ารอยืนยัน"/"รายจ่ายรอยืนยัน" จาก LINE — กันสแปม/พิมพ์ผิดจากคนนอกที่ไม่มีคนตรวจก่อน) — แอดมินตรวจสอบ+เลือกพนักงานส่งในแท็บ "🚚 ออเดอร์" → กด "ยืนยัน" → สร้างเป็นออเดอร์จัดส่งจริงผ่าน `/api/pos/delivery` ปกติ (ไม่เขียน logic สร้างออเดอร์ซ้ำ)
      - **ตัดสินใจสำคัญ:** ไม่รับสินค้าประเภท "หมุนเวียน" จากช่องทางนี้ (ต้องคุยเรื่องแลก/ยืมของเก่ากับร้านโดยตรง ไม่เหมาะให้ลูกค้าตัดสินใจเองผ่านหน้าเว็บ)
      - **ความปลอดภัย:** คำนวณยอดรวมใหม่จากราคาสินค้าจริงในระบบเสมอ (ไม่เชื่อราคาที่ฝั่งลูกค้าส่งมา กันปลอมราคา) + rate-limit ต่อ IP ต่อร้าน (10 ครั้ง/10 นาที, in-memory เหมือน `verify-pin.js`) + เพิ่ม `GET /api/pos/public-shop-info` คืนแค่ชื่อร้าน+สถานะรับออเดอร์ ไม่คืนข้อมูลอ่อนไหว (เลขภาษี/เบอร์โทร/อีเมลเจ้าของ)
      - **เจอปัญหาสถาปัตยกรรมระหว่างออกแบบ (แก้แล้ว):** ตอนแรกจะรียูส `handleDelivery()`/`cart` ของหน้าขายเดิมเพื่อยืนยันคำสั่งซื้อลูกค้า แต่พบว่า `cart` เป็น state ที่ผูกกับบิลที่เปิดอยู่ในแท็บ "ขาย" — ถ้าเขียนทับจะทำให้บิลที่กำลังขายอยู่จริงหายได้ (side effect ที่ไม่ตั้งใจ) แก้โดยเขียนฟังก์ชันยืนยันแยกต่างหาก (`submitCustomerOrderConfirm`) ที่ POST ตรงไป `/api/pos/delivery` โดยไม่แตะ `cart`/`openBills` เลย
      - **ทดสอบยิงจริงครบ end-to-end กับร้าน "D Gas":** ส่งคำสั่งซื้อ (สินค้าทดสอบ 2 ชิ้น ฿198) → ปรากฏในคิวรอยืนยันถูกต้อง (ราคาคำนวณฝั่ง server ตรง ไม่เชื่อ client) → จำลองแอดมินยืนยันเลือกพนักงาน "Testing" → สร้างออเดอร์จัดส่งจริงสำเร็จ + ลบออกจากคิวรอถูกต้อง → ลบข้อมูลทดสอบแล้ว
      - **ยังไม่ได้ทดสอบผ่านเบราว์เซอร์จริง** (เครื่องมือทดสอบแบบ sandboxed ของ session นี้ทำให้ `useRouter().query` ไม่ populate จาก URL แม้ `window.location.search` จะถูกต้อง — เจอปัญหาเดียวกันกับตอนทดสอบข้อ 34/มือถือ สงสัยเป็นข้อจำกัดของเครื่องมือทดสอบเอง ไม่ใช่บั๊กโค้ด เพราะ backend ผ่านการทดสอบตรงทุกจุดแล้ว) — แนะนำให้ผู้ใช้ลองเปิดเองเพื่อยืนยันหน้าตา
    - **Excel report engine — 3 แม่แบบสำเร็จรูปใหม่ (Business+) ใน `api/pos/export.js`** — ผู้ใช้เลือกใช้ `xlsx` เดิม (ไม่เพิ่ม `exceljs` ตามสเปกตรงๆ กันปัญหา lock file แบบเดียวกับที่เคยห้าม recharts) และให้ **สร้างโครงสร้าง custom-per-shop ไว้เลย** แม้ยังไม่มีลูกค้าจริงต้องใช้:
      - **`vat30`** — ภาษีซื้อ-ขายระดับรายการ (ภ.พ.30): ภาษีขายจากใบกำกับภาษีที่ออกจริงเท่านั้น (มีเลขภาษีผู้ซื้อครบ ต่างจาก `vat` เดิมที่รวมทุกบิลขาย), ภาษีซื้อจากรับสินค้า (คืนเลขภาษีผู้จำหน่ายจากตารางผู้ติดต่อ) + รายจ่าย (**ไม่มีเลขภาษีคู่ค้าเก็บในระบบ — โชว์ "-" ตามจริง เป็น known gap ของ `EXPENSE_HEADERS` ไม่ใช่บั๊กใหม่**)
      - **`sales_by_branch`** — cross-tab สาขา × วิธีชำระ (เงินสด/โอน/เชื่อ) พร้อมแถวรวมทุกสาขา
      - **`cyclical_inventory`** — สต็อคสินค้าหมุนเวียนต่อชนิด (เต็ม/กับลูกค้า/เปล่ารอรีฟิล) + คำนวณมูลค่าสินทรัพย์รวม (ทุกสถานะ × ราคาทุน/หน่วย) + รายชื่อลูกค้าที่ถือสินค้าหมุนเวียนอยู่
      - **`custom`** — โหลดไฟล์ `lib/custom-templates/{shop_id}.js` แบบ dynamic (ผ่าน `fs.existsSync` + `require` runtime กัน build พังตอนยังไม่มีไฟล์) ถ้าไม่มีไฟล์สำหรับร้านนั้นจะขึ้น sheet แจ้งเฉยๆ ไม่ error — เอกสารรูปแบบไฟล์ที่ต้อง export อยู่ใน `lib/custom-templates/README.md`
      - เพิ่ม `hasFeature(tier, 'excel_report_templates')` (business+) ใน `lib/tier-features.js` — gate เฉพาะ 4 ประเภทใหม่ (**ไม่ย้อนไปล็อก 8 ประเภทเดิมที่ไม่เคยมี tier gating มาก่อน** เพื่อไม่กระทบผู้ใช้เดิม) + เพิ่ม checkbox ในหน้า export modal ของ `pos.js` (โผล่เฉพาะ business+)
      - **เจอบั๊กจริงระหว่างทดสอบ (ไม่เกี่ยวกับงานนี้โดยตรง แต่เจอจากการยิงจริง):** `api/pos/tax-invoice.js` ไม่เคยผ่าน `asText()` เลยตั้งแต่สร้างไฟล์ — ทดสอบ `vat30` แล้วพบใบกำกับภาษีจริงของร้าน "D Gas" (INV-2569-00002, ลูกค้า "ศิริลักษณ์" ฿5,000) มีวันที่ออกเพี้ยนเป็นเลข serial (`244549.658`) ตรงกับบั๊ก "~543 ปี" ที่เจอซ้ำหลายรอบในโปรเจกต์นี้ — แก้ทั้งโค้ด (`asText()`) และซ่อมข้อมูลเดิมที่เพี้ยนไปแล้วโดยตรง (คำนวณจากเลขบิลอ้างอิง `BILL20260719084716` ที่ฝังวันที่ไว้ในชื่อ) + **เจอข้อมูลทดสอบเก่าตกค้างเพิ่ม** (INV-2569-00001 "TEST-asText-contact" ฿1 — ค้างมาจากรอบทดสอบก่อนหน้า) ลบทิ้งพร้อมกัน
      - **ทดสอบยิงจริงครบทั้ง 4 ประเภทกับข้อมูลจริงของร้าน "D Gas"** ผ่าน `xlsx` parser ตรวจสอบเนื้อหาทุก sheet แล้ว ถูกต้องครบ (cross-tab สาขา×วิธีชำระ, มูลค่าสินทรัพย์คำนวณถูกต้องตามสูตร, custom แสดงข้อความ placeholder ถูกต้องเมื่อไม่มีไฟล์)
    - **สิทธิ์พนักงานละเอียด (granular staff permissions) — เสร็จแล้ว:** เดิมพนักงานที่มี PIN เข้า `pos-staff.js` ทำได้แค่ยืนยันบิล/จัดส่ง/เก็บเงินเท่านั้น ไม่มีทางเห็นข้อมูลระดับบริหารเลย — เพิ่ม 4 สิทธิ์แยกต่อคน (ตั้งได้จากฟอร์มเพิ่ม/แก้ไขพนักงานใน `pos.js`): **ดูยอดขายรวม**, **ดูกำไรขาดทุน**, **จัดการสต็อกสินค้า**, **export รายงาน VAT** — ค่าเริ่มต้นทุกอันเป็น `false` เสมอ (พนักงานเดิมทุกคนไม่ได้สิทธิ์อะไรเพิ่มจนกว่าเจ้าของร้านจะเปิดเอง)
      - `STAFF_HEADERS` ขยาย 9→13 คอลัมน์ (J-M) ใน `lib/google-pos.js` + เพิ่ม `getStaffPermissions(token, sheetId, staffId)` helper กลาง (fail-safe คืนสิทธิ์ปิดหมดถ้า error ใดๆ) ให้ API routes อื่นเรียกใช้ตรวจสอบสิทธิ์แบบรวมศูนย์จุดเดียว
      - **เจอบั๊กแฝงระหว่างทำที่ต้องแก้ก่อน (ยังไม่ทันเกิดจริงเพราะเพิ่งขยายคอลัมน์วันนี้ แต่จะพังทันทีที่มีคนตั้ง PIN หลังมีสิทธิ์ถูกตั้งไว้แล้ว):** `staff-setpin.js` (พนักงานตั้ง PIN เอง) เดิมอ่าน/เขียนแค่ `A:I` (9 คอลัมน์) แล้ว pad แถวเป็น 9 ก่อน `updateSheetRow` ทับทั้งแถว — ถ้าพนักงานคนนั้นมีสิทธิ์ถูกตั้งไว้แล้ว (คอลัมน์ J-M) การตั้ง PIN ใหม่จะ**เขียนทับล้างสิทธิ์ทั้งหมดทิ้งไปเงียบๆ** แก้เป็นอ่าน/pad เป็น `A:M`/13 คอลัมน์เหมือนไฟล์อื่น (`staff.js`, `verify-pin.js`) ก่อน commit — เจอจากการไล่ grep หาทุกจุดที่แตะชีต "พนักงาน" ไม่ใช่จากการทดสอบพังจริง
      - **Server-side enforcement แบบไม่กระทบเจ้าของร้านเลย** — API routes ที่เกี่ยวข้อง (`reports.js`, `products.js` PATCH, `export.js`) รับ `staffId` เป็น query/body param **ที่ไม่บังคับ**: ไม่ส่งมา (ทางที่ `pos.js` เรียกอยู่ปกติ) = ทำงานเหมือนเดิมทุกประการไม่ถูกกระทบ, ส่งมา (ทางที่ `pos-staff.js` เรียก) = เช็คสิทธิ์เฉพาะเจาะจงก่อนเสมอ ไม่ผ่านคืน 403 — `reports.js` type=`sales`/`topsellers` ต้อง `perm_view_revenue`, type=`pl` ต้อง `perm_view_pl`; `products.js` PATCH ต้อง `perm_manage_stock`; `export.js` types=`vat`/`vat30` ต้อง `perm_export_vat`
      - เพิ่ม step ใหม่ "📊 จัดการร้าน" ใน `pos-staff.js` (โผล่ในเมนูเฉพาะพนักงานที่มีสิทธิ์อย่างน้อย 1 อย่าง) — ปุ่มย่อยตามสิทธิ์ที่มี: ดูยอดขายรวม/กำไรขาดทุน (การ์ดสรุปจาก `reports.js`), จัดการสต็อก (รายการสินค้า+ช่องแก้ไขจำนวนสต็อกอินไลน์ ผูก `staffId`), export VAT (ดาวน์โหลด Excel ตรงผ่าน `window.open`) — ล้างสถานะสิทธิ์/ข้อมูลที่โหลดไว้ทุกครั้งที่ "ออกจากระบบ" กันข้อมูลของคนก่อนหน้าเปื้อนข้ามคนตอนใช้เครื่องเดียวกัน
      - **ทดสอบยิงจริงครบทุก endpoint กับพนักงาน "Testing" ของร้าน "D Gas" (เปิด/ปิดสิทธิ์สลับไปมาจริงผ่าน API แล้วเช็คผล):** ไม่มีสิทธิ์ → เรียก `reports.js?type=pl&staffId=...` ได้ 403 ถูกต้อง → เปิด `perm_view_pl` → เรียกสำเร็จ 200 พร้อมข้อมูลถูกต้อง (net_profit ตรงกับที่เจ้าของร้านเห็น) → `type=sales` ยังคง 403 อยู่ (ไม่รั่วข้ามสิทธิ์กัน) → เปิด `perm_manage_stock` → PATCH สต็อกสำเร็จ 200 → ปิดสิทธิ์ → PATCH เดิม 403 ถูกต้อง → เปิด `perm_export_vat` → export VAT ได้ไฟล์ `.xlsx` จริง → ปิดสิทธิ์ → 403 → ทางเจ้าของร้าน (ไม่ส่ง `staffId` เลย) ทำงานได้ปกติทุกจุดตลอดการทดสอบไม่ถูกกระทบ — ล้างสิทธิ์ทดสอบทั้งหมดกลับเป็น `false` ทุกอันหลังทดสอบเสร็จ (เจอ Google Sheets API rate limit ระหว่าง cleanup รอบแรกแล้ว retry สำเร็จรอบสอง ไม่มีข้อมูลทดสอบตกค้าง)

36. **White-Label (MVP — เอาชื่อ Smile Slip Pro ออกจากเอกสาร/ชื่อไฟล์ให้ Enterprise เท่านั้น) — ผู้ใช้ถาม White-Label แบบ BYO LINE OA เต็มรูปแบบ (ผูก Channel Access Token/Secret ของลูกค้าเอง) ก่อน อธิบายไปว่าเป็นไปได้จริงแต่ effort เยอะ (refactor `smileslip-pro/index.js` ทั้งไฟล์ + webhook routing ใหม่) ผู้ใช้เลยขอตัดเรื่อง LINE ออก เอาแค่ "ลบชื่อเราออกจากบิล/ใบเสร็จ/ใบกำกับภาษี/ชื่อไฟล์" แทน — **เรื่อง LINE OA ยังไม่ได้ทำ ยังเป็นแค่บทสนทนา/ไอเดียที่ค้างไว้ ไม่ใช่งานที่ทำเสร็จแล้ว**:**
    - **ตรวจสอบก่อนแก้พบว่าเอกสารหลักที่ลูกค้าร้านเห็นบ่อยสุดสะอาดอยู่แล้ว** (`buildReceiptHtml` ใบเสร็จปกติ, ใบเสร็จยืนยันจัดส่งใน `pos-staff.js`) ไม่เคยมีชื่อเราเลย — จุดที่มีชื่อเราจริงมีแค่ 4 ที่: footer ใบกำกับภาษีทางการ (`lib/pos-tax-invoice-pdf.js`), footer รายงาน VAT PDF (`lib/vat-report-pdf.js`), แถวหัวข้อในเนื้อหา Excel (`api/export/excel.js` — `รายงานสรุป Smile Slip Pro`), ชื่อไฟล์ดาวน์โหลด Excel ทั้ง 2 เส้นทาง (`api/pos/export.js`, `api/export/excel.js` — prefix `SmileSlip_`/`SmileSlip_POS_`)
    - **ตัดสินใจ:** ค่าเริ่มต้าน (ทุกแพ็กเกจที่ไม่ใช่ Enterprise) **ต้องมีชื่อเราปรากฏทุกจุด** (เดิมไม่มีในใบเสร็จ/ใบส่งของเลย — เพิ่มเข้าไปใหม่ เป็น growth loop แบบ "Powered by") — Enterprise (`hasFeature(tier,'white_label')`, ผูกกับ `TIER_LEVEL.enterprise`) ลบออกให้หมดทุกจุดรวมถึงเนื้อหาในไฟล์ Excel ด้วย (ไม่ใช่แค่ชื่อไฟล์)
    - **`lib/branding.js` (ใหม่)** — helper กลาง pure-JS ใช้ได้ทั้ง client (`pos.js`,`pos-staff.js`,`order.js`) และ server (`lib/*-pdf.js`, `api/pos/export.js`, `api/export/excel.js`): `withBrandFooter(existingFooter, isWhiteLabel)` ต่อบรรทัดแบรนด์ (`ออกโดย Smile Slip Pro · smileslippro.com` — ใส่โดเมนด้วยเพื่อ growth-loop effect) ท้าย footer เดิมถ้าไม่ใช่ White-Label, `sanitizeFilenamePart(str,maxLen=60)` กันอักขระผิดกฎ filename (`\/:*?"<>|`) + จำกัดความยาว + fallback "ร้านค้า" ถ้าว่างหลัง sanitize
    - **ชื่อไฟล์ Excel สำหรับ Enterprise** — ฝั่ง POS (`api/pos/export.js`) ใช้ `branchLabel` ที่มีอยู่แล้วในไฟล์ (`branch || branchName || shopName` — fallback ชื่อร้านหลักอัตโนมัติถ้าไม่ได้กรองสาขา) แทน `SmileSlip_POS_{shopName}`, ฝั่ง Dashboard (`api/export/excel.js`) ใช้ `shop.shop_name` ตรงๆ แทน `SmileSlip_{shopName}` — ทั้งคู่ผ่าน `sanitizeFilenamePart()` ก่อนเสมอ
    - **เพิ่ม `hasFeature(tier,'white_label')`** ใน `lib/tier-features.js` (enterprise เท่านั้น) — thread ผ่าน: `api/pos/tax-invoice-pdf.js`→`generatePosTaxInvoicePdf({isWhiteLabel})`, `api/sheets/tax-report.js`→`generateVatReportPdf({isWhiteLabel})`, `api/pos/verify-pin.js` คืน `isWhiteLabel` ระดับ response (ไม่ใช่ใน `staff` object เพราะเป็นคุณสมบัติของร้านไม่ใช่พนักงาน) ให้ `pos-staff.js` ใช้ต่อกับใบเสร็จจัดส่ง, `api/pos/public-shop-info.js` คืน `isWhiteLabel` (ไม่คืน tier ดิบๆ ให้หน้าสาธารณะ กันข้อมูลระดับแพ็กเกจรั่วไหลโดยไม่จำเป็น) ให้ `pages/order.js` ใช้
    - **ทดสอบยิงจริงกับร้าน "D Gas" (ตั้งใจสลับ tier ชั่วคราวระหว่างทดสอบแล้วคืนค่าเดิม):** พบว่า D Gas เป็น `enterprise` อยู่แล้วจริงๆ (ทดสอบรอบแรกเลยไม่มี contrast เพราะ flip ไป enterprise ที่เป็นอยู่แล้ว) แก้โดยสลับไป `business` ก่อน (ยืนยัน `isWhiteLabel:false`, ชื่อไฟล์มี prefix `SmileSlip_` ถูกต้องทั้ง 2 เส้นทาง) แล้วสลับกลับ `enterprise` (ยืนยัน `isWhiteLabel:true`, ชื่อไฟล์ไม่มี prefix, ฝั่ง POS ใช้ `สำนักงานใหญ่` เป็นชื่อไฟล์อัตโนมัติเพราะไม่ได้กรองสาขา) → parse เนื้อหา Excel ยืนยันแถวหัวข้อ `รายงานสรุป Smile Slip Pro` เทียบกับ `รายงานสรุป` ถูกต้องตามเงื่อนไข → generate PDF ใบกำกับภาษีทดสอบ 2 เวอร์ชัน (WL/non-WL) แล้วเปิดอ่านด้วยตาจริงผ่าน Read tool ยืนยัน footer ต่างกันถูกต้อง (`เลขที่ใบกำกับภาษี: TEST-0001 · ออกโดย Smile Slip Pro · smileslippro.com` vs `เลขที่ใบกำกับภาษี: TEST-0001` เฉยๆ) → unit-test `lib/branding.js` โดยตรงผ่าน ESM script ยืนยันทุก edge case (ว่าง/มีของเดิม/WL/sanitize อักขระผิดกฎ/สตริงว่าง/ตัดความยาว) ถูกต้องครบ → คืนค่า tier ร้านกลับเป็น `enterprise` เดิมยืนยันแล้ว → ลบไฟล์ทดสอบทั้งหมดออกจาก working directory ครบ (ไม่มีอะไรหลงเหลือ)
    - **ยังไม่ได้ทดสอบผ่านเบราว์เซอร์จริง**: `buildReceiptHtml`/`printDeliveryReceipt` (pos.js/pos-staff.js) และ footer ใน `pages/order.js` เป็นแค่ string-interpolation ตรงไปตรงมาโดยใช้ helper เดียวกับที่ unit-test แล้ว (ยังไม่ได้เปิดจริงผ่าน UI เพราะเป็นหน้าที่ผูกกับ query string ซึ่งเจอปัญหา sandbox เดิมจากข้อ 34/35) — ตรวจสอบด้วย code review + build check แล้วเท่านั้น
    - **ยังไม่ได้ทำ (ตามที่ผู้ใช้ขอตัดออกจากสโคปนี้):** White-Label แบบผูก LINE OA ของลูกค้าเอง (BYO Channel Access Token/Secret) — เก็บไว้เป็นไอเดียใหญ่รอคุยรายละเอียดต่อในอนาคต ถ้าจะทำจริงต้องรื้อ webhook routing (`destination` field matching) + refactor จุดยิง LINE API ทั้งไฟล์ `smileslip-pro/index.js` ตามที่อธิบายไว้ในบทสนทนา
    - **Deploy production แล้ว (2026-07-21)** — revision จริง `smileslip-dashboard-00270-p5n` (ข้อความ deploy โชว์ revision เดิม `00269-lpk` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — verified บน production จริงว่า `/api/pos/public-shop-info` คืน `isWhiteLabel:true` ถูกต้องสำหรับร้าน D Gas (enterprise) และ `/api/export/excel` ชื่อไฟล์ไม่มี prefix `SmileSlip_` แล้วจริง

37. **QR สั่งอาหารที่โต๊ะ — เริ่มวางแผนสถาปัตยกรรม/แบ่งเฟสแล้ว (ผู้ใช้ขอคุยรายละเอียดตามที่ตกลงไว้จากข้อ 34) ยังไม่ได้เริ่มเขียนโค้ดสักบรรทัด เป็นแค่การออกแบบ:**
    - **จุดต่างจากระบบที่มีอยู่:** ขายหน้าร้าน/จัดส่งจบทีเดียวหรือ async ไม่ต้องรีเฟรชสด แต่โต๊ะอาหารเป็น "บิลที่ยังไม่ปิด" ผูกกับโต๊ะ ลูกค้าสั่งได้หลายรอบระหว่างมื้อ (กับข้าว→ของหวาน) ทุกรอบต้องรวมเป็นบิลเดียวของโต๊ะนั้น + พนักงานต้องเห็นแบบเรียลไทม์ว่าโต๊ะไหนสั่งอะไรเพิ่ม — เป็น "ออเดอร์ประเภทที่ 3" ตามที่วิเคราะห์ไว้ในข้อ 34
    - **ความเสี่ยงทางเทคนิคที่เจอจริง (ไม่ใช่แค่ทฤษฎี):** ระบบทั้งหมดใช้ Google Sheets เป็น datastore ซึ่งมี rate limit จริง (เจอ "Quota exceeded" ระหว่างทดสอบสิทธิ์พนักงานในข้อ 35/ตอนทดสอบ White-Label ในข้อ 36 จากการยิงหลายรอบติดกันธรรมดาเท่านั้น) — ถ้าให้หน้าจอพนักงาน + มือถือลูกค้าทุกโต๊ะ poll เช็คอัปเดตถี่ๆ ผ่าน Sheets แบบเดียวกับโมดูลอื่น จะโดน rate limit เร็วมากในร้านที่มีหลายโต๊ะพร้อมกัน
    - **แนวทางที่เสนอ (ยังไม่ได้ทำ รอคุย/อนุมัติ):** ข้อมูล "ออเดอร์สดของโต๊ะที่ยังไม่ปิดบิล" (ชื่อเมนู/จำนวน/โต๊ะไหน) ไม่ติด PDPA constraint แบบข้อมูลสลิป (ไม่ใช่ข้อมูลการเงินส่วนบุคคล) จึงเสนอให้เก็บใน **Supabase** แทน Sheets (เร็ว ไม่ติด rate limit) แล้วค่อยเขียนลง Sheets "ยอดขาย" ตอนโต๊ะ**ปิดบิลจริง**เท่านั้น ผ่าน checkout/VAT/ใบเสร็จเดิมที่มีอยู่แล้วทั้งหมด (ไม่เขียน logic ซ้ำ) — ฝั่งอัปเดตแบบเรียลไทม์เสนอ short-polling (5-10 วิ) เป็น MVP ก่อน (ไม่เพิ่ม WebSocket/infra ใหม่ ตรงกับ philosophy หลีกเลี่ยง dependency ใหม่ของโปรเจกต์นี้)
    - **แผนแบ่งเฟส (ตาม pattern เดียวกับตอนสร้าง Delivery module):**
      - **A** — ตารางโต๊ะ + QR ต่อโต๊ะ (`/table-order?shopId&tableId`, tableId เป็น token สุ่มกันเดา) + หน้าสั่งอาหารสาธารณะ (reuse UI pattern จาก `order.js`) + session ผูกโต๊ะใน Supabase (หลายมือถือสแกน QR โต๊ะเดียวกัน = เข้า session เดียวกันอัตโนมัติโดยคีย์ด้วย tableId)
      - **B** — หน้าจอพนักงานดูออเดอร์สดทุกโต๊ะ (polling) — รับออเดอร์ใหม่/มาร์คเสิร์ฟแล้ว
      - **C** — ปุ่ม "เช็คบิล/ปิดโต๊ะ" ดึงรายการสะสมทั้งหมดเข้า cart ปกติ ผ่าน checkout flow เดิม 100%
      - **D** — เมนูหมดชั่วคราว (86), ปุ่มพิมพ์ QR ต่อโต๊ะ, ปุ่ม "เรียกพนักงาน"
    - **ประเมินซ้ำอีกครั้ง:** ยังคงเป็นงานระดับหลายวัน (เท่าตอนสร้าง Delivery module) แต่ตอนนี้มีแผนเป็นเฟสชัดเจนแล้ว ไม่ใช่ก้อนใหญ่คลุมเครือเหมือนตอนวิเคราะห์ครั้งแรก — **ตัดสินใจแล้ว: ยังไม่เริ่ม** เก็บไว้ใน backlog พร้อมโอนย้ายสต็อกข้ามสาขา + White-Label LINE OA เต็มรูปแบบ รอสัญญาณลูกค้าจริง (ร้านอาหาร) ก่อนค่อยเริ่ม เพราะระบบตอนนี้ยังไม่มีลูกค้าจริงกลุ่มร้านอาหารเลย

38. **สินค้า/ลิงก์สั่งซื้อแยกต่อสาขา + แก้บั๊กไม่มีลิงก์แผนที่หน้ายืนยันจัดส่ง** — ผู้ใช้ชี้ปัญหาว่าบางร้านมีหลายสาขาแต่ขายสินค้าคนละชนิดกัน (ไม่ใช่แค่เรื่องสต็อกแยกที่ deferred ไว้ในข้อ 78 — เป็นคนละปัญหา เล็กกว่ามาก แค่ต้อง "ติดป้าย" ว่าสินค้าไหนขายที่สาขาไหน):
    - เพิ่มคอลัมน์ T `สาขาที่ขาย` ใน `PRODUCT_HEADERS` (comma-separated ชื่อสาขา) — **ว่าง = ขายได้ทุกสาขาเสมอ** (ค่าเริ่มต้น กันสินค้าเดิมทั้งหมดหายไปจากทุกที่ทันทีตอน deploy)
    - กรองสินค้าตามสาขา 2 จุด: หน้าขาย POS (`displayProducts` กรองด้วย `selectedBranch`, **เฉพาะแท็บขาย ไม่กรองแท็บจัดการสินค้า** กันสาขาอื่นแก้ไขสินค้าไม่ได้) และเว็บสั่งซื้อสาธารณะ (`GET /api/pos/products?branch=`)
    - **ลิงก์ `/order` แยกต่อสาขา** — เพิ่ม query param `?branch=xxx` ใน `pages/order.js` — ล็อกสาขาไว้อัตโนมัติ (ไม่ต้องให้ลูกค้าเลือกเอง) + กรองสินค้าที่โชว์ให้ตรงสาขานั้นไปในตัว — **validate `branch` จาก URL กับสาขาจริงของร้านก่อนเสมอ** (ไม่ตรง = fallback เป็นโหมดเดิม ให้ลูกค้าเลือกเอง กันลิงก์พิมพ์ผิด/ปลอม) — เพิ่มการ์ดคัดลอกลิงก์แยกต่อสาขาในหน้าตั้งค่า POS (ลิงก์รวมเดิมยังอยู่)
    - **Server-side enforcement** — `api/pos/customer-orders.js` เช็คว่าสินค้าที่ลูกค้าสั่งมาต้องขายได้ที่สาขาที่ระบุจริง (หรือขายได้ทุกสาขา) ก่อนยอมรับเสมอ กันลูกค้ายิง request ตรงสั่งสินค้าที่สาขานั้นไม่ได้ขาย
    - **แก้บั๊กที่ผู้ใช้แจ้ง:** หน้ายืนยันจัดส่งของพนักงาน (`pos-staff.js`, step `deliver-confirm`) มีแค่ที่อยู่เป็นข้อความ ไม่มีลิงก์แผนที่ให้กด (ทั้งที่หน้ารายการก่อนหน้ามีปุ่ม "🗺️ แผนที่" อยู่แล้ว — data `maps_link` มีอยู่แล้ว แค่ไม่เคยเอามาโชว์ในหน้ารายละเอียด) — เพิ่มปุ่มเดียวกันในการ์ดรายละเอียดออเดอร์แล้ว
    - **ทดสอบยิงจริงครบกับข้อมูลจริงร้าน "D Gas"** (2 สาขา: มะลิ 1, ดีแก๊ส หางดง): ตั้งสินค้าจริง 1 ตัวให้ขายเฉพาะสาขา "มะลิ 1" → filter ตามสาขาถูกต้อง (มะลิ 1 เจอ, ดีแก๊ส หางดง ไม่เจอ) → สั่งซื้อผ่านสาขาผิดถูกปฏิเสธ 400 ถูกต้อง → สั่งผ่านสาขาถูกสำเร็จ 200 → ลบคำสั่งซื้อทดสอบ + คืนค่าสินค้ากลับเป็น "ขายได้ทุกสาขา" (`branches: []`) เรียบร้อย ยืนยัน state สุดท้ายตรงกับก่อนทดสอบ
    - **ยังไม่ได้ทดสอบ UI ผ่านเบราว์เซอร์จริง** (ปุ่มติ๊กเลือกสาขาในฟอร์มสินค้า, การ์ดคัดลอกลิงก์ต่อสาขา, ปุ่มแผนที่ใน pos-staff.js) — เป็น string-interpolation/JSX ตรงไปตรงมา ตรวจสอบด้วย code review + build check แล้วเท่านั้น เหมือนงานอื่นในเซสชันนี้ที่ติดข้อจำกัดเดิมของเครื่องมือทดสอบกับหน้าที่ผูก query string

39. **QA Stress Test เต็มรูปแบบ (ผู้ใช้ขอสวมบทบาท "User QA Agent" ทดสอบ ~20 รูปแบบ เน้นมุมมองพนักงานแต่ละบทบาทต้องคีย์ข้อมูลแบบไหน) — เจอบั๊กจริง 5 ข้อจากการยิง API ตรงกับร้าน "D Gas" (ไม่ใช่แค่ทฤษฎี) แก้ครบทุกข้อแล้ว เรียงตามความรุนแรงที่ผู้ใช้ขอ:**
    - **🔴 สต็อคเสกได้ฟรีจาก qty ติดลบ (Critical)** — ยิง `POST /api/pos/sales` ใส่ `qty:-3` แล้วสต็อคจริง**เพิ่มขึ้น**แทนที่จะลด (`stock - (-3) = stock + 3`) เพราะไม่มีจุดไหนเช็ค `qty > 0` เลย — ยืนยันจริงว่าสต็อคขยับจาก 16→19 จากบิลปลอมเดียว — เจอ pattern เดียวกันใน `delivery.js` (`confirm_delivery`) ด้วย — แก้โดยเพิ่ม validate `qty > 0`/`price >= 0` ทุกจุดที่รับ `items` array ใน `sales.js` และ `delivery.js` (ทั้ง POST สร้างออเดอร์ และ PATCH ยืนยันจัดส่ง) + เพิ่ม `receives.js` กันต้นทุนติดลบ (กระทบ weighted avg cost/P&L) + `collections.js` กัน `collected_amount`/item qty ติดลบไม่ให้ยอดค้าง/ถังลูกค้าเพิ่มย้อนกลับ (ตรรกะเดียวกัน `debt - (-x) = debt + x`)
    - **🔴 ราคาปลอม ไม่มีใครเช็ค (Critical)** — ยิงราคา `1` บาทสำหรับสินค้าจริง ฿2,500 ระบบรับบันทึกเป็นบิล ฿1 จริง ตัดสต็อคจริง — **ตัดสินใจสำคัญ:** ไม่ล็อคราคาให้ตรงกับฐานข้อมูลเสมอเหมือน `customer-orders.js` เพราะพนักงานตั้งใจแก้ราคาต่อรายการได้ (ฟีเจอร์มีอยู่แล้ว) — แก้แบบไม่กระทบฟีเจอร์นี้: เพิ่ม warning (ไม่บล็อค) ถ้าราคาที่ขายจริงต่ำกว่าต้นทุน (`product.cost`) ให้เจ้าของร้านเห็นสัญญาณ เหมือน pattern เดียวกับ "procurement fraud detection" ที่มีอยู่แล้ว
    - **🟠 สร้างออเดอร์จัดส่งไม่มีที่อยู่/แผนที่/เบอร์โทร/ราคา 0 บาท เงียบๆ (Major)** — ตรงกับที่ผู้ใช้สังเกตเจอเอง (พนักงานส่งของเปิดมาเจอไม่มีที่ทาง) — ยืนยันด้วย API ตรงว่า `delivery.js` validate แค่ `customer_name`/`items.length` เท่านั้น — แก้เพิ่ม warning array (ไม่บล็อค) ทั้งฝั่ง server (`delivery.js` คืน `warnings[]`) และฝั่ง client (`pos.js` `handleDelivery()` เพิ่ม `confirm()` ถามก่อนส่งงานถ้าขาดข้อมูลสำคัญ)
    - **🟠 ส่วนลดติดลบทำให้ยอดขายเพิ่มแทนที่จะลด (Major)** — พิมพ์ผิดเครื่องหมาย `-50` แทน `50` → total เพิ่มจาก 100→150 แทนที่จะลด ไม่มีจุดไหนเตือน/บล็อค — แก้ทั้ง client (`pos.js` onChange กันพิมพ์ค่าติดลบ) และ server (`sales.js` validate `discount >= 0`)
    - **🟠 ยอดขายที่บันทึกไปแล้วไม่มีทางยกเลิกได้เลยผ่าน API (Major)** — เจอระหว่างพยายาม cleanup ข้อมูลทดสอบเอง (`sales.js` มีแค่ PATCH สำหรับ "รับชำระเงินเชื่อ" ไม่มี DELETE/cancel เลย) — พนักงานคีย์บิลผิด (ราคา/จำนวน/สินค้าผิด) ต้องรบกวนแอดมินเข้า Sheets แก้เองเสมอ — เพิ่ม `DELETE /api/pos/sales { bill_no }` ใหม่: คืนสต็อค/ถังลูกค้า/ยอดค้างชำระ (ถ้าเป็นบิลเชื่อ) ย้อนกลับตรรกะเดียวกับตอนบันทึกขายทุกประการ แล้ว blank แถวทิ้ง + เพิ่มปุ่ม "ยกเลิก" ในตารางประวัติยอดขายของหน้ารายงาน — **known gap ที่บอกไว้ชัดเจน:** ถ้าบิลนั้น "ชำระแล้ว" (เขียนเข้าบัญชีหลักไปแล้ว) การยกเลิกจะไม่ลบแถวในบัญชีหลักอัตโนมัติ (หาแถวที่แน่ชัดยากเพราะบัญชีหลักไม่ได้เก็บ reference กลับมาที่ bill_no เสมอไป) ต้องลบเองถ้าจำเป็น
    - **พบแต่ยังไม่แก้ (deferred ตามที่ผู้ใช้ตัดสินใจ — เป็นงานสถาปัตยกรรมใหญ่แยกคุยทีหลัง):** ทุก endpoint ใน `pages/api/pos/*.js` ไม่มีการยืนยันตัวตนใดๆ นอกจากรู้ `shopId` (UUID) — ต่างจาก `customer-orders.js` ที่ตั้งใจป้องกัน — ถ้า `shopId` หลุด (เช่นจากลิงก์ `/order`/LINE push ที่ถูก forward) จะยิง API ตรงสร้าง/ลบ/แก้ข้อมูลได้หมดโดยไม่ต้อง login เลย
    - **ทดสอบยิงจริงครบทุกข้อกับร้าน "D Gas" ก่อน/หลังแก้ ยืนยันด้วยตัวเลขจริง** (สต็อค 16→19 ตอนยังไม่แก้ vs ปฏิเสธ 400 หลังแก้, below-cost warning ทดสอบด้วยการตั้งต้นทุนชั่วคราวแล้วคืนค่าเดิม, cancel bill ทดสอบคืนสต็อคถูกต้อง 15→16) ลบ/คืนค่าข้อมูลทดสอบทั้งหมดสะอาด 100% ไม่มีอะไรตกค้าง
    - **เหตุการณ์ระหว่างทำ:** ไฟดับกลางเซสชัน (ระหว่างเขียนสคริปต์ทดสอบ ก่อนรันจริง) — ตรวจสอบตรงกับข้อมูลจริงบน Sheets ยืนยันว่าไม่มีข้อมูลทดสอบตกค้างเลย (ไฟดับพอดีจังหวะที่ยังไม่ได้รันสคริปต์) ก่อนจะเริ่มทดสอบต่อหลังไฟมา
    - **Deploy production แล้ว (2026-07-22)** — revision จริง `smileslip-dashboard-00271-g49` — verified บน production จริงว่า negative qty ถูกปฏิเสธถูกต้อง

40. **QA Stress Test รอบ 2-3 (ผู้ใช้ขอให้เช็คซ้ำแบบละเอียดอีกครั้งหลัง deploy รอบแรก "อาจจะเจอข้อผิดพลาดเล็กๆน้อยๆที่มองข้าม") — เจอเพิ่มอีก 3 จุดจริง ไม่ใช่แค่ทฤษฎี แก้ครบและ deploy แล้ว:**
    - **`products.js` POST/PATCH ไม่กัน price/cost/stock ติดลบเลยตั้งแต่สร้างสินค้า** — ต่างจากข้อ 39 ที่แก้แค่จุด "ใช้" สินค้า (ขาย/จัดส่ง) จุดนี้คือ "ต้นทาง" ข้อมูลสินค้าเอง — สินค้าที่สร้างด้วยราคา/ทุน/สต็อคติดลบจะไหลไปกระทบทุกจุดที่อ่านข้อมูลนี้ต่อ (รายงาน, VAT, การ์ดสรุป) — แก้เพิ่ม validate ทั้ง POST (สร้างใหม่) และ PATCH (แก้ไข)
    - **`contacts.js` POST/PATCH ไม่กัน debt/cylinders ติดลบ** — จุดนี้ต่างจาก sales.js/delivery.js/collections.js ที่มี `Math.max(0,...)` ป้องกันไว้แล้วเวลาอัปเดตยอดผ่าน flow ปกติ — แต่แก้ไขตรงจากฟอร์มผู้ติดต่อ (เช่น แอดมินพิมพ์ยอดค้างชำระเอง) ไม่ผ่านกลไกนั้นเลย ยังกรอกติดลบได้ตรงๆ
    - **`tax-invoice.js` ไม่กันจำนวน/ราคาติดลบ** — เอกสารทางการ (ใบกำกับภาษี) ที่ออกให้ลูกค้าจริงต้องไม่มีตัวเลขติดลบเด็ดขาด (ผิดหลักบัญชี/กฎหมาย) — เพิ่ม validate แบบเดียวกับ sales.js/delivery.js
    - **`customer-orders.js` (เว็บสั่งซื้อสาธารณะ) ไม่มีเพดานบนของ qty** — เดิมมีแค่ `Math.max(1,...)` (กันติดลบ/0) แต่ไม่มีเพดานบน — เพิ่ม cap ที่ 10,000 ต่อรายการ กันหน้าเว็บสาธารณะ (ไม่ต้อง login) โดนกรอกเลขมั่ว/สแปมทำให้คิวรอตรวจสอบมียอดเพี้ยนเกินจริงจนแอดมินตรวจยาก
    - **เช็คเพิ่มตามที่ผู้ใช้ขอ (Input Validation dimension) — ทดสอบยิงจริงแล้วผ่านหมด ไม่พบปัญหา:** ทดสอบ emoji + `<script>` tag + ข้อความยาว 5,000 ตัวอักษรในชื่อผู้ติดต่อ/หมายเหตุ → round-trip ผ่าน Sheets ถูกต้อง 100% ไม่มีข้อมูลเพี้ยน (React escape JSX text อัตโนมัติอยู่แล้ว ไม่มีความเสี่ยง XSS ตอนแสดงผลในหน้าเว็บ) — ทดสอบ rate limit ของ `customer-orders.js` (10 ครั้ง/10 นาทีต่อ IP+ร้าน) ยิงจริง 12 ครั้งติดกัน ยืนยันว่าครั้งที่ 11-12 โดน 429 ถูกต้องตามที่ออกแบบไว้
    - **พบแต่ยังไม่แก้ (เข้าข่ายเดียวกับ auth model ที่ deferred ไว้ในข้อ 39):** ไม่มีจุดไหนเพิ่มเติมที่ร้ายแรงระดับ Critical/Major อีกแล้วในรอบนี้ — ที่เหลือคือ known gap เดิมที่บันทึกไว้แล้ว (auth model, ไม่ลบแถวบัญชีหลักอัตโนมัติตอนยกเลิกบิลที่ชำระแล้ว)
    - **ทดสอบยิงจริงครบทุกจุดกับร้าน "D Gas" ก่อน/หลังแก้ (เคสถูกปฏิเสธ + เคสปกติที่ต้องยังทำงานได้)** ลบ/คืนค่าข้อมูลทดสอบสะอาดหมดทุกรอบ ไม่มีอะไรตกค้าง
    - **Deploy production แล้วครบ 2 รอบเพิ่ม (2026-07-22)** — revision จริงรอบ 2 `smileslip-dashboard-00272-x6p` (products.js/contacts.js/customer-orders.js), รอบ 3 `smileslip-dashboard-00273-45d` (tax-invoice.js) — verified บน production จริงว่า tax-invoice.js ปฏิเสธ qty ติดลบถูกต้อง — ข้อความ deploy โชว์ revision เดิมผิดซ้ำๆ ตามเคยทุกรอบ ต้องเช็ค `gcloud run revisions list` แล้ว pin traffic เองทุกครั้งเหมือนเดิม

41. **ระบบเงินสด/ปิดกะ (เปิดกะ-ปิดกะ) ต่อพนักงานผ่าน PIN — เสร็จแล้ว ทดสอบยิงจริงครบกับร้าน "D Gas" + deploy production แล้ว:**
    - **สเปกที่ผู้ใช้ต้องการ (ตามที่อธิบาย):** เปิดกะ → คีย์ยอดเงินสดตั้งต้นในลิ้นชัก → ระหว่างกะระบบต้องรู้เงินสดเข้า (ขายเงินสด) และเงินสดออก (รายจ่ายที่จ่ายด้วยเงินสด) อัตโนมัติจากข้อมูลที่มีอยู่แล้ว → ปิดกะ → นับเงินสดจริงเทียบกับยอดที่ควรมีตามระบบ (เช็คส่วนต่าง/เงินขาด-เกิน) → บันทึกยอดที่เก็บออกไป (ฝากธนาคาร/เข้าตู้เซฟ) กับยอดที่ยกไปเป็นทุนตั้งต้นกะถัดไปแยกกัน — ผู้ใช้ตอบ 2 คำถามออกแบบผ่าน AskUserQuestion ก่อนเริ่มทำ: **(1) กะผูกกับพนักงานรายคนเสมอ** (ไม่ใช่ต่อสาขา/เครื่อง — พนักงานหลายคนหมุนเวียนใช้เครื่องคิดเงินเดียวกันได้ คนละกะกัน) **(2) ถ้านับเงินได้ไม่ตรง ยังปิดกะได้ แต่บังคับพิมพ์หมายเหตุอธิบายก่อน + ต้องแจ้งเจ้าของร้านทาง LINE อัตโนมัติ**
    - **คำถามเรื่องเปิดลิ้นชักอัตโนมัติ — ตอบแล้วว่าทำไม่ได้ในสโคปนี้:** ลิ้นชักเงินสดจริงเปิดผ่านสัญญาณ ESC/POS "kick-drawer" ส่งเป็น raw byte ตรงไปเครื่องพิมพ์ใบเสร็จที่เสียบสายอยู่ ไม่ใช่ผ่าน `window.print()` ที่ระบบใช้อยู่ตอนนี้ ต้องใช้ Web Serial API (Chrome/Edge เดสก์ท็อปเท่านั้น ไม่รองรับมือถือ/Safari) และต้องรู้ยี่ห้อ/รุ่นเครื่องจริงก่อนถึงจะเขียนได้ — เหตุผลเดียวกับที่เคยตัดสินใจไม่ทำ Web Bluetooth ESC/POS พิมพ์ใบเสร็จโดยตรงในข้อ 22 (ไม่มีมาตรฐานเดียวที่ใช้ได้ทุกรุ่น เสี่ยงเดาผิดถ้าไม่มีเครื่องจริงทดสอบ) — **ยังไม่ทำ รอถ้ามีเครื่องพิมพ์จริงให้ทดสอบ**
    - **สถาปัตยกรรม:** tab ใหม่ "กะเงินสด" ใน Google Sheets (`CASH_SHIFT_HEADERS`/`makeShiftNo`/`rowToCashShift` ใน `lib/google-pos.js`) + API ใหม่ `pages/api/pos/cash-shifts.js` (GET รายการ/พรีวิวยอดสดของกะที่เปิดอยู่, POST เปิดกะ, PATCH ปิดกะ) — `SALE_HEADERS`/`EXPENSE_HEADERS` ขยายเพิ่มคอลัมน์ `shift_no` ท้ายสุด (ว่าง = ไม่ได้เปิดกะไว้ ไม่กระทบร้านที่ไม่ใช้ฟีเจอร์นี้เลย)
      - **`computeExpectedCash()`** (ใน `cash-shifts.js`) คำนวณ "เงินสดที่ควรมี" ใหม่จากข้อมูลจริงเสมอทุกครั้ง (เงินสดตั้งต้น + SUM ยอดขายเงินสดที่ผูก `shift_no` นี้ − SUM รายจ่ายเงินสดที่ผูก `shift_no` นี้, กรอง `payment_method==='เงินสด'` และไม่รวมบิลที่ถูกยกเลิก) — ไม่เชื่อค่าที่ client ส่งมาเลย ทั้งตอนพรีวิวก่อนปิดกะและตอนปิดกะจริง (คำนวณซ้ำอีกครั้งตอน PATCH กันเคส race condition ที่มีรายการใหม่เข้ามาระหว่างเปิด modal กับกดปิดกะจริง)
      - **1 พนักงานเปิดกะซ้อนกันไม่ได้** (ต้องปิดกะเดิมก่อน) แต่หลายพนักงานเปิดกะพร้อมกันบนเครื่องเดียวกันได้ — แยกด้วย `shift_no` ที่ผูกกับทุกธุรกรรมเงินสดอย่างชัดเจน (ไม่ใช้วิธีเดาจากช่วงเวลา เพราะกะซ้อนเวลากันได้ถ้าคนละคน)
      - **ปิดกะยอดไม่ตรง (variance≠0) ไม่บล็อค แต่บังคับกรอกหมายเหตุก่อนเสมอ** + `pushLineMessage()` แจ้งเจ้าของร้าน (`shop_profiles.owner_line_id`) อัตโนมัติ (pattern เดียวกับที่ `delivery.js` ใช้อยู่แล้ว) — ยอดตรงพอดี (variance=0) ไม่ต้องมีหมายเหตุ ไม่แจ้ง LINE
      - **`verify-pin.js` เพิ่ม param `purpose`** — เดิม endpoint นี้ gate ทั้งหมดไว้เบื้องหลัง `hasFeature(tier,'delivery_staff_app')` (เพราะสร้างไว้สำหรับแอปพนักงานส่งของ/`pos-staff.js` โดยเฉพาะ) แต่เปิด/ปิดกะเงินสดควรใช้ได้ทุก tier เสมอ (เป็นฟีเจอร์จัดการเงินสดพื้นฐาน ไม่ใช่ฟีเจอร์แอปพนักงานส่งของ) — ส่ง `purpose:'cash_shift'` จาก `pos.js` จะข้ามการเช็ค tier gate นี้ไปเฉพาะกรณีนี้เท่านั้น (`pos-staff.js` เดิมไม่ส่ง `purpose` เลยจึงยังถูก gate เหมือนเดิมทุกประการ ไม่กระทบพฤติกรรมเดิม)
      - **`pos.js` UI:** ปุ่มสถานะกะที่ header (🔓 เปิดกะ / 🟢 ชื่อพนักงาน·ปิดกะ) → modal เปิดกะ 2 ขั้น (ยืนยัน PIN ผ่าน `verify-pin` → กรอกเงินสดตั้งต้น) → modal ปิดกะ (ดึงพรีวิวยอดสดจาก GET ก่อนเสมอ → กรอกยอดนับได้จริง เห็นส่วนต่างสด real-time ตอนพิมพ์ → หมายเหตุบังคับเฉพาะตอนยอดไม่ตรง → กรอกยอดเก็บออกไป เห็นยอดยกไปกะถัดไปสด real-time) — เก็บ `activeShift` ไว้ใน `localStorage` ต่อ shopId + เช็คกับ server ทุกครั้งที่โหลดหน้าว่ากะนั้นยังเปิดอยู่จริง (กันเคสปิดกะไปแล้วจากเครื่องอื่นแต่ localStorage เครื่องนี้ไม่ทันอัปเดต) — `handleCheckout()`/`submitExpense()` แนบ `shift_no` เข้า POST อัตโนมัติเฉพาะตอนจ่ายด้วยเงินสดและมีกะเปิดอยู่เท่านั้น
    - **เจอบั๊กจริง 2 จุดระหว่างทดสอบยิง API จริง (ไม่ใช่แค่ของฟีเจอร์นี้ — กระทบทุก POS route ที่เรียก `ensureTabExists()` ซึ่งคือแทบทุก route ในระบบ):**
      1. **`ensureTabExists()` (`lib/google-pos.js`) เดิมเสี่ยงพังข้อมูลจริงเงียบๆ ถ้าโดน quota exceeded** — เจอจริงระหว่างทดสอบยิงถี่ๆ: metadata fetch (`GET spreadsheets/{id}`) เดิมเป็น raw `fetch()` ไม่ผ่าน retry wrapper และไม่เช็ค `.ok` ก่อนอ่าน `meta.sheets` — พอโดน 429 (quota exceeded) จะได้ response error ที่ไม่มี `.sheets` เลย ทำให้ `exists` เพี้ยนเป็น falsy ทั้งที่ tab มีอยู่จริง (เช่น tab "ยอดขาย"/"รายจ่าย" ที่มีข้อมูลจริงอยู่แล้ว) โค้ดเดิมจะพยายาม `addSheet` สร้างใหม่ (ล้มเหลวเงียบๆ เพราะชื่อซ้ำ ไม่เช็ค response) แล้ว `appendSheet(headers)` แบบไม่มีเงื่อนไขต่อทันที ทำให้ **header ถูกเขียนซ้ำเป็นแถวขยะทับท้ายข้อมูลจริงที่มีอยู่แล้ว** — ยืนยันเกิดขึ้นจริงกับ tab "ยอดขาย"/"รายจ่าย"/"กะเงินสด" ระหว่างทดสอบฟีเจอร์นี้ (เจอ ghost header row 2 แถวใน "ยอดขาย", 1 แถวใน "รายจ่าย") ต้องแก้ข้อมูลที่เพี้ยนไปแล้วด้วยมือ (เคลียร์แถวขยะ + คืนสต็อคสินค้าที่กระทบ) — **แก้โดยเปลี่ยนทุก fetch ใน `ensureTabExists()` ให้ผ่าน `sheetsFetch()` (retry+throw เหมือนฟังก์ชันอื่น) และเช็ค `.ok` ก่อนเสมอ ถ้า metadata/addSheet/header-read/header-write fetch ใดๆ ล้มเหลว จะ throw error ทันที (fail loud) แทนที่จะเดาผิดแล้วพังข้อมูลเงียบๆ** — **นี่คือบั๊กที่กระทบทุก POS API route ในระบบ ไม่ใช่แค่ cash-shifts.js เพราะ `ensureTabExists()` ถูกเรียกที่ต้นเกือบทุก handler**
      2. **`cash-shifts.js` เขียน `opened_at`/`closed_at` เป็นสตริงไทยดิบไม่ผ่าน `asText()`** — เจอบั๊ก "~543 ปี" แบบเดียวกับที่เจอซ้ำหลายจุดในโปรเจกต์นี้ (ยืนยันจริง: `opened_at` อ่านกลับมาเป็น `"244552.9427"` แทน `"22/7/2569 22:37:33"`) — แก้โดยเพิ่ม `asText()` helper ในไฟล์ (pattern เดียวกับ `expenses.js`) ครอบทั้ง `now` ตอน POST (`opened_at`) และตอน PATCH (`closed_at`) — ทดสอบซ้ำแล้วยืนยันวันที่แสดงถูกต้องไม่เพี้ยนอีก
    - **ทดสอบยิงจริงครบทุก path กับร้าน "D Gas" (พนักงาน "Testing", PIN 1234) ผ่านทั้งหมด:** verify-pin ผิด/ถูก (purpose=cash_shift ข้าม tier gate ได้จริง), เปิดกะซ้อนสำหรับพนักงานเดียวกันถูกปฏิเสธ, พรีวิวยอดสดถูกต้องก่อน/หลังมีรายการขาย/รายจ่าย, ปิดกะยอดไม่ตรงไม่มีหมายเหตุถูกปฏิเสธ, ปิดกะยอดไม่ตรงมีหมายเหตุสำเร็จ + แจ้ง LINE เจ้าของร้านจริง (`notified_owner:true`), เปิดกะใหม่หลังปิดกะเดิมสำเร็จ, isolation ระหว่าง 2 กะถูกต้อง (ยอดขายกะแรกไม่ปนกะที่สอง), ปิดกะยอดตรงเป๊ะไม่ต้องมีหมายเหตุและไม่แจ้ง LINE — ลบ/คืนค่าข้อมูลทดสอบทั้งหมดสะอาด 100% (บิล/รายจ่าย/แถวกะ/สต็อคสินค้า) ยืนยันด้วยการอ่านชีตตรงว่าตรงกับก่อนทดสอบเป๊ะ
    - **Deploy production แล้ว (2026-07-23)** — revision จริง `smileslip-dashboard-00274-g98` (ข้อความ deploy โชว์ revision เดิม `00273-45d` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — verified บน production จริงว่า `/api/pos/verify-pin` (purpose=cash_shift) และ `/api/pos/cash-shifts` ทำงานถูกต้อง
    - **ยังไม่ได้ทำ (out of scope รอบนี้):** เปิดลิ้นชักเงินสดอัตโนมัติ (ต้องมีเครื่องพิมพ์จริงก่อน), หน้าประวัติกะแบบดูย้อนหลังทั้งหมด (ตอนนี้มีแค่ GET list ผ่าน API แต่ยังไม่มี UI แสดงประวัติกะใน `pos.js`), รายงานสรุปกะ (เช่น เทียบผลงานพนักงานแต่ละคนจากประวัติกะ)

42. **Sync บิล/โต๊ะที่เปิดค้างข้ามอุปกรณ์ — ผู้ใช้เจอปัญหาจริง: เข้าผ่านแท็บเล็ตกับคอมด้วย LINE บัญชีเดียวกัน แต่เห็นโต๊ะที่เปิดค้างไม่ตรงกัน:**
    - **สาเหตุที่แท้จริง:** `openBills` (บิล/โต๊ะที่เปิดค้างในหน้าขาย POS) เดิมเก็บอยู่ใน `localStorage` ของเบราว์เซอร์แต่ละเครื่องเท่านั้น (คีย์ `pos_bills_${shopId}`) ไม่เคยเก็บบนเซิร์ฟเวอร์เลย — ผูกกับตัวเครื่อง/เบราว์เซอร์ ไม่ได้ผูกกับบัญชี จึงไม่มีทาง sync ข้ามอุปกรณ์ได้เลยตั้งแต่แรก (ไม่ใช่บั๊ก แต่เป็นข้อจำกัดสถาปัตยกรรมเดิมที่ไม่เคยรองรับ multi-device)
    - **ผู้ใช้ยืนยัน scope ที่ต้องการชัดเจน (ทำให้ออกแบบง่ายลงมาก):** ใช้งานทีละเครื่อง (เปลี่ยนเครื่อง/เครื่องเสีย/อยู่นอกร้านแล้วคีย์ข้อมูล) **ไม่ใช่แก้พร้อมกันสองเครื่องพร้อมกัน** จึง**ไม่ต้องทำ conflict resolution ซับซ้อน** — แค่ last-write-wins (เครื่องไหน sync หลังสุดคือค่าที่ถูกต้อง) พอเพียงแล้ว
    - **สถาปัตยกรรม:** ตารางใหม่ `pos_open_bills` ใน Supabase (`shop_id` PK, `bills` jsonb, `table_names` jsonb — เก็บเป็น JSON blob ทั้งก้อนเหมือน localStorage เดิมทุกประการ ไม่ใช่ตารางเชิงสัมพันธ์ เพราะโครงสร้างบิลซับซ้อน/เปลี่ยนบ่อย) + API ใหม่ `pages/api/pos/open-bills.js` (`GET` อ่าน, `PUT` เขียนทับทั้งก้อน) — **ไม่แตะ Google Sheets เลย** เพราะเป็นแค่ "ร่าง" บิลที่ยังไม่ชำระเงินจริง (บิลที่ checkout สำเร็จแล้วยังคงบันทึกผ่าน `api/pos/sales.js` ตามปกติ)
    - **`pos.js`:** เพิ่ม `persistOpenBills(bills, names)` helper กลาง — เขียน `localStorage` ทันที (ให้ UI เร็ว/ใช้ต่อได้แม้เน็ตหลุดชั่วคราว) + debounce 1.2 วิ ก่อนยิง `PUT` ขึ้น server (กันยิงถี่เกินตอนแก้ตะกร้ารัวๆ เช่น พิมพ์จำนวนสินค้า) — แทนที่ raw `localStorage.setItem` เดิมทั้ง 6 จุด (สร้างบิล/ปิดบิล/sync ตะกร้า/เช็คเอาท์สำเร็จ/ส่งจัดส่งสำเร็จ/ตั้งชื่อโต๊ะ) ด้วยฟังก์ชันเดียวนี้
    - **ตอนโหลดหน้า:** ลอง `GET` จาก server ก่อนเสมอ (ให้สลับเครื่องแล้วเห็นตรงกันทันที) — ถ้าเรียกไม่ได้ (ออฟไลน์/ตารางยังไม่ถูกสร้าง) fallback เป็น `localStorage` เดิม — **เคส migration สำคัญ:** ถ้า server ว่างเปล่าแต่เครื่องนี้มีบิลค้างใน `localStorage` อยู่แล้ว (เครื่องที่ใช้ก่อนฟีเจอร์นี้จะ deploy) ให้ถือว่าเป็นข้อมูลตั้งต้นแล้วดันขึ้น server ทันที ไม่ทำให้บิลที่เปิดค้างอยู่ก่อนหน้าหายไปเงียบๆ
    - **API fail-safe:** ถ้าตาราง `pos_open_bills` ยังไม่ถูกสร้าง (ยังไม่รัน SQL) ทั้ง `GET`/`PUT` คืน `{ok:true/false, tableMissing:true}` แทนการ throw — ฝั่งเว็บจะ fallback ไปใช้ `localStorage` เดิมต่อได้ไม่มีอะไรพัง (ทดสอบยืนยันแล้วจริงก่อนรัน SQL)
    - **ทดสอบยิงจริงกับร้าน "D Gas" ครบหลังรัน SQL:** จำลอง "เครื่อง A" เปิดโต๊ะ 1 บิล sync ขึ้น server → "เครื่อง B" (ยิงคนละ request แทนคนละอุปกรณ์) โหลดมาเห็นบิลของเครื่อง A ตรงทุกฟิลด์ (ชื่อบิล/รายการสินค้า/ชื่อโต๊ะที่ตั้งไว้) → "เครื่อง B" ปิดบิลนั้นแล้ว sync กลับ → "เครื่อง A" โหลดใหม่เห็นว่าบิลถูกปิดไปแล้วถูกต้อง (ยืนยัน last-write-wins ทำงานตามที่ออกแบบ) — จบการทดสอบด้วยสถานะว่างเปล่า (ไม่มีข้อมูลทดสอบตกค้าง)
    - **ผู้ใช้รัน SQL แล้ว (2026-07-23)** — ยืนยันตารางมีอยู่จริงและทดสอบ end-to-end ผ่านครบตามด้านบน

43. **เพิ่ม `#สมัครพนักงาน` + ขยายเมนูช่วยเหลือบอท + หน้ายืนยันบัญชี LINE ก่อน login — ผู้ใช้ทดสอบสาขาใหม่แล้วติดปัญหาสมัครพนักงานไม่ได้ + สลับบัญชี LINE ไม่ได้ตอน login:**
    - **`#สมัครพนักงาน` คำสั่งใหม่ (ชื่อง่ายกว่า ไม่ผูกกับงานส่งของ)** — ใช้ `role: 'delivery_staff'` เดียวกับ `#สมัครพนักงานขนส่ง` เป๊ะ (ไม่ต้องแก้ CHECK constraint ของตาราง `branch_role_requests`) แค่เปลี่ยน label ข้อความที่ตอบกลับเป็น "พนักงาน" แทน "พนักงานส่ง" — คำสั่งเดิม `#สมัครพนักงานขนส่ง` ยังใช้ได้เหมือนเดิมทั้งหมด (ไม่ตัดทิ้ง) — **พนักงานทุกตำแหน่งที่อนุมัติผ่านคำสั่งนี้ได้ PIN เข้าเว็บ POS เหมือนกันหมด ใช้เปิด-ปิดกะเงินสด/ยืนยันจัดส่ง/เก็บเงินได้ ไม่ได้จำกัดเฉพาะงานส่งของแต่อย่างใด** (ตำแหน่งที่แสดงในหน้าแก้ไขพนักงานเปลี่ยนได้ทีหลังจาก Dashboard)
    - **ขยาย `#ช่วยเหลือ`** เพิ่มส่วน "สมัครสิทธิ์ในกลุ่มนี้" แจกแจงทั้ง 3 คำสั่ง (`#สมัครแอดมิน`/`#สมัครพนักงาน`/`#สมัครผู้จัดการสาขา`) ที่หายไปจากเมนูนี้มาตลอด
    - **ขยาย `#วิธีใช้งาน`** เพิ่ม 3 หัวข้อใหม่ที่ขาดหายไป: `รับสินค้า` (flow `#รับสินค้า` ผ่านรูปถ่าย), `รายจ่าย` (flow `#รายจ่าย` ผ่านรูปถ่าย), `พนักงาน` (สมัครสิทธิ์ระดับสาขา แยกจากหัวข้อ `แอดมิน` เดิมที่เป็นสิทธิ์ระดับร้านทั้งหมด)
    - **อัปเดตข้อความในหน้า Dashboard → POS → ตั้งค่า → คำขอสมัคร** ให้บอกคำสั่งใหม่ `#สมัครพนักงาน` แทน `#สมัครพนักงานขนส่ง` (คงบอกว่าคำสั่งเดิมยังใช้ได้ด้วย)
    - **แก้บั๊ก UX หน้า login — สลับบัญชี LINE ไม่ได้:** เดิมถ้าเบราว์เซอร์มี LIFF session ค้างอยู่ (`liff.isLoggedIn()===true`) ระบบจะ auto-login ทันทีไม่ถามอะไรเลย ทำให้ผู้ใช้ที่ตั้งใจสลับบัญชี LINE (เช่น จะเปลี่ยนไปใช้บัญชีพนักงาน) เผลอเข้าบัญชีเดิมซ้ำทุกครั้งโดยไม่มีทางเลี่ยง — เพิ่มหน้า **"เข้าสู่ระบบด้วยบัญชีนี้ใช่ไหม?"** (โชว์รูป+ชื่อโปรไฟล์ LINE) ก่อนเข้าระบบทุกครั้งที่มี session ค้างอยู่ พร้อมปุ่ม "🔄 ไม่ใช่ ต้องการเปลี่ยนบัญชี LINE" (`liff.logout()` + reload) — **ไม่กระทบกรณีเปิดผ่านแอป LINE เอง (`liff.isInClient()`)** ยังคง auto-login เหมือนเดิม เพราะบัญชีผูกกับแอป LINE ที่เปิดอยู่ตรงๆ อยู่แล้ว ไม่มีความกำกวมให้ต้องถาม
    - **คำถามคู่กัน (ตอบแล้ว ไม่ใช่บั๊ก แต่เป็นสถาปัตยกรรมที่ต้องอธิบาย):** "บัญชีพนักงาน" ไม่มีทาง login เข้า `/pos` (หน้าขายเต็มรูปแบบ) ได้เลย — `/pos` ใช้บัญชีเดียวกับ Dashboard เท่านั้น (LINE/อีเมลของร้าน) ส่วน PIN พนักงานเป็นแค่การยืนยันตัวตน *ภายใน* หน้านั้น (เช่น ตอนเปิดกะเงินสด) ไม่ใช่การ login แยก — พนักงานที่อยากเข้าระบบโดยไม่ต้องรู้บัญชีร้านเลยต้องใช้ `/pos-staff?shopId=` แทน (PIN ล้วนๆ ไม่มี LINE/อีเมล) แต่จะได้หน้าจำกัดสิทธิ์ (จัดส่ง/เก็บเงิน/และถ้าเปิดสิทธิ์ไว้: ดูยอดขาย-กำไรขาดทุน-จัดการสต็อก-export VAT) ไม่ใช่หน้าขายเต็มรูปแบบ
    - **เจอซ้ำอีกครั้ง: บั๊ก CI/CD ของบอทที่เคยบันทึกไว้ในข้อ 11 ยังไม่ได้แก้จริง** — ลอง push แล้วปล่อยให้ Cloud Build auto-deploy บอทตามปกติ พบ build FAILURE ทันที ด้วย error เดิมเป๊ะ (`error pulling build step 0 "gcr.io/google-cloud-builders/gcloud": retry budget exhausted`) — ยืนยันว่าปัญหา IAM/Artifact Registry permission ของ Cloud Build service account **ยังไม่เคยถูกแก้จริงตั้งแต่ข้อ 11** แม้จะผ่านมาหลายสัปดาห์ — ต้อง deploy บอทด้วยมือผ่าน `gcloud run deploy` เสมอ อย่าไว้ใจ auto-deploy แม้แต่ครั้งเดียว (workaround เดิมยังจำเป็นอยู่ ไม่ใช่ทางเลือก)
    - **Deploy บอทแล้ว (2026-07-23)** — revision จริง `smileslip-service-00146-5vj` (deploy ด้วยมือ เพราะ auto-deploy fail ตามที่อธิบายข้างบน) — verified บอท boot สำเร็จ (Redis connected, startup probe ผ่าน) — **ส่วน dashboard (`login.js`/`pos.js` ข้อความ) push ขึ้น GitHub แล้วแต่ยังไม่ได้ deploy ขึ้น production** รอถามผู้ใช้ก่อน
44. **Login/สิทธิ์แคชเชียร์แบบจริงจังทั้งระบบ — ผู้ใช้ชี้ 3 ช่องโหว่จริงจากการอ่านโค้ดเอง (ถูกต้องทุกจุด): (1) login ไม่ถามยืนยันบัญชีก่อนสลับ LINE (แก้แล้วในข้อ 43) (2) `/pos` ไม่มี session/สิทธิ์ใดๆ เลยนอกจากรู้ shopId — ปุ่ม "← Dashboard" อยู่ถาวรไม่มีการกั้น + ตัวเลือก "บทบาท" ตอนสร้างออเดอร์เป็นแค่ label ไม่ผูกสิทธิ์จริง (3) ไลไอดีเดียวเป็นทั้งเจ้าของร้านหนึ่ง+แอดมินอีกร้านพร้อมกันไม่ได้ (owner-first short-circuit) — ทำ 3 เฟสตามแผนที่อนุมัติไว้ (`tingly-napping-moth.md`), ทดสอบยิงจริงทุกเฟสกับร้าน "D Gas":**
    - **Phase 1 — Multi-role identity:** `lib/identity.js` (`getRolesForLineId`) เป็นจุดเดียวที่รวบรวมทุกบทบาทจริงของไลไอดี (เจ้าของ + แอดมินทุกร้านที่อนุมัติแล้ว) แทนตรรกะเดิมที่เช็คเจ้าของก่อนแล้ว return ทันที — `check-user.js` คืน `{exists, roles:[]}` แทน role เดียว — `login.js` เพิ่มหน้าเลือกร้าน (picker) เมื่อพบ >1 บทบาท กรณีร้านเดียวเข้าตรงเหมือนเดิมทุกประการ — `auth/line.js`/`auth/callback/line.js` เลิกใช้ `.single()`/`.maybeSingle()` ที่จะพัง/เดาผิดถ้าเจอแถวซ้ำ เปลี่ยนมาใช้ helper กลาง — sanity check production ยืนยันไม่มี `owner_line_id` ซ้ำอยู่ก่อนแล้ว (2 ร้านทั้งระบบ) — **known limitation ที่ยังไม่รองรับ:** ไลไอดีเดียวเป็น**เจ้าของ 2 ร้านพร้อมกัน**ไม่ได้ (คนละเรื่องกับเจ้าของ+แอดมินที่รองรับแล้ว) เพราะ URL scheme ทั้งระบบผูก `?userId=owner_line_id` เป็นตัวระบุร้านตรงๆ ถ้าเป็นเจ้าของ 2 ร้าน ทั้งคู่จะได้ `userId` ค่าเดียวกันแยกไม่ออก — ต้อง redesign เป็น shopId-based routing ถ้าจะรองรับในอนาคต (ไม่ทำตอนนี้ตามขอบเขต)
    - **Phase 2 — หน้าตัดสินใจตอนสมัครซ้ำ + ปิดช่องโหว่ trial ซ้ำ:** `register.js` เช็ค `getRolesForLineId` ก่อนกรอกฟอร์มเสมอ ถ้าเจอบัญชีเดิมแสดงหน้าตัดสินใจ: **เข้าร้านเดิม** / **สมัครร้านใหม่แยกต่างหาก** (เฉพาะกรณีไม่ได้เป็นเจ้าของร้านอยู่แล้ว ตาม known limitation ข้างบน) / **ลบร้านเดิมถาวรแล้วสมัครใหม่** (ต้องพิมพ์ชื่อร้านยืนยันตรงเป๊ะ) — ผู้ใช้ยืนยันชัดเจนว่าต้องเป็น **hard delete จริง** เพื่อปิดช่องโหว่ลบแล้วสมัครใหม่ขอทดลองฟรี 30 วันซ้ำไม่จำกัดรอบ — เพิ่มตาราง `trial_used_line_ids` (แยกจาก `shop_profiles` โดยเจตนา คีย์ด้วย `line_user_id` ตรงๆ อยู่รอดข้ามการลบร้านเสมอ ห้ามลบตารางนี้เด็ดขาด) — `register.js` API เช็คตารางนี้ก่อนให้ trial ใหม่ทุกครั้ง ถ้าเคยใช้แล้วตั้ง `status:'trial_expired'` ทันที (ไม่ปล่อย trial fields เป็น NULL เพราะ NULL แปลว่ายกเว้นตลอดไปสำหรับร้านเก่า ตรงข้ามกับที่ต้องการ) — เพิ่ม `DELETE /api/shop/delete-shop` ยกเลิก Stripe subscription ทันทีถ้ามี (กันเก็บเงินต่อทั้งที่ร้านหายไปแล้ว) + ลบข้อมูลลูกทุกตารางที่ผูก shop_id ตรงๆ ก่อนเสมอ (ไม่พึ่ง `ON DELETE CASCADE` อย่างเดียว เพราะบางตารางสร้างผ่าน Supabase UI มาก่อนไม่รู้แน่ชัดว่ามี cascade จริงหรือไม่) — ทดสอบยิงจริงกับร้านทดสอบชั่วคราวครบทุก branch (ลบสำเร็จ, ปฏิเสธชื่อไม่ตรง, ปฏิเสธไม่ใช่เจ้าของ, สมัครใหม่หลังลบยังให้ trial ปกติเพราะตาราง `trial_used_line_ids` ยังไม่ถูกสร้าง — fail-safe ทำงานถูกต้อง)
    - **Phase 3 — ระบบ session/สิทธิ์แคชเชียร์ที่เซ็นชื่อจริงจัง (ทั้ง `/pos` โหมดแคชเชียร์ใหม่ และ `/pos-staff` เดิม):**
      - `lib/staff-session.js` — `issueStaffSession`/`verifyStaffSession` เซ็นด้วย HMAC-SHA256 (`STAFF_SESSION_SECRET` ใน env ใหม่ — **ยังไม่ได้ deploy ตัวแปรนี้ขึ้น production ต้องเพิ่มตอน deploy dashboard รอบหน้า**) payload `{shopId,staffId,exp}` อายุ ~14 ชม. fail-closed ถ้าไม่ตั้ง secret — ก่อนหน้านี้ทั้ง `/pos-staff` และ endpoint ต่างๆ ที่เช็ค `staffId` ใช้ค่าเปล่าๆ ที่ปลอมได้ตรงๆ ไม่มี session ที่พิสูจน์ได้เลยว่ามาจากการใส่ PIN ถูกจริง
      - `lib/pos-auth.js` — `requirePermission(req,res,token,sheetId,permKey)`: ไม่มี session (header `x-staff-session` หรือ query `?session=` สำหรับกรณี `window.open()` ที่แนบ header ไม่ได้ เช่นดาวน์โหลด Excel) = เจ้าของ/แอดมิน ทำงานเหมือนเดิมทุกประการ, มี session = ต้องมีสิทธิ์จริงเท่านั้น ดึงจาก Sheet สดทุกครั้ง (ถอนสิทธิ์มีผลทันที ไม่รอ token หมดอายุ — ทดสอบยืนยันแล้วจริง) — `blockAllStaffSessions()` สำหรับจุดที่ไม่มีสิทธิ์ไหนควรปลดล็อคให้พนักงานแตะได้เด็ดขาด (เช่น `pos-config.js` แก้ค่าธนาคาร/API key)
      - **ขยาย `STAFF_HEADERS`** จาก 13→21 คอลัมน์ (N-U เพิ่มท้ายสุด) เพิ่มสิทธิ์ใหม่ 8 ตัว: `perm_process_sales`, `perm_void_sales`, `perm_manage_customers`, `perm_manage_expenses`, `perm_manage_delivery`, `perm_manage_receiving`, `perm_issue_tax_invoice`, `perm_manage_staff` (ค่าเริ่มต้นว่าง=ไม่มีสิทธิ์เสมอ) — แก้ range `A:M`→`A:U` ทุกจุดที่อ่านชีต "พนักงาน" (`staff.js`, `staff-setpin.js`, `verify-pin.js`, `getStaffPermissions`) — **เจอบั๊กแฝงระหว่างทำ:** `staff-setpin.js` เดิม pad แค่ 13 คอลัมน์ก่อน `updateSheetRow` ทับทั้งแถว ถ้าพนักงานถูกตั้งสิทธิ์ไว้แล้ว (คอลัมน์ N-U) การตั้ง PIN ใหม่จะเขียนทับล้างสิทธิ์ทั้งหมดทิ้งเงียบๆ — แก้เป็น pad 21 คอลัมน์แล้ว (เจอจาก grep ทุกจุดที่แตะชีตนี้ ไม่ใช่จากทดสอบพังจริง)
      - `verify-pin.js` ออก session ทุกครั้งที่ PIN ถูก (ไม่ว่า purpose ไหน) คืน `sessionToken` ในคำตอบ — `staff.js`/`sales.js` DELETE/`contacts.js` PATCH-DELETE/`pos-config.js` PATCH ผูก `requirePermission`/`blockAllStaffSessions` แล้ว (Tier 1 ตามแผน) — **ถือโอกาสอัปเกรด `products.js` PATCH/`reports.js`/`export.js` จากการเช็ค `staffId` เปล่าๆ ใน body/query (ปลอมได้ตรงๆ) มาใช้ session ที่เซ็นชื่อแทน** เพราะเป็นจุดเดียวกับที่ต้อง migrate `pos-staff.js` อยู่แล้ว (Tier 4 เดิมตามแผน แต่ทำพร้อมกันเพราะแตะโค้ดจุดเดียวกัน)
      - **`pos-staff.js` migrate เข้า session ที่เซ็นชื่อ** — เพิ่ม `apiFetch()` wrapper แนบ `x-staff-session` ทุกคำขอ + persist session ใน `sessionStorage` (กู้คืนได้ถ้ารีเฟรชหน้า แก้จุดอ่อนเดิมที่ staffId เก็บใน React state อย่างเดียวหายทันทีที่รีเฟรช) — ปุ่ม export VAT (`window.open`) ส่ง `?session=` แทน `&staffId=`
      - **สิทธิ์แบบ hybrid ตามที่ผู้ใช้เลือก** — เพิ่ม `STAFF_PRESETS` (แคชเชียร์/ผู้จัดการสาขา/พนักงานส่งของ/กำหนดเอง) ในฟอร์มพนักงานของ `pos.js` กดแล้วติ๊กสิทธิ์แนะนำอัตโนมัติ ยังแก้ทีละอันต่อได้เหมือนเดิม
      - **ลิงก์แคชเชียร์ใหม่แยกต่างหาก** (`/pos?shopId=<id>&mode=cashier`, คัดลอกได้จากแท็บตั้งค่า POS) — บังคับใส่ PIN ก่อนเสมอ (`purpose:'pos_cashier'`) ไม่มีทางเห็น Dashboard/แท็บตั้งค่าได้เลยจากลิงก์นี้ (กันไว้ 2 ชั้น: ซ่อนปุ่มแท็บ + เช็ค `!cashierMode` ซ้ำตรง render block ของแท็บตั้งค่าเอง กันเผลอเข้าทางอ้อมผ่านลิงก์ลัด "เพิ่มพนักงาน" จากที่อื่นในหน้า) — ข้อมูลร้าน (ชื่อ/ที่อยู่/เลขภาษี/`subscription_tier` ใช้ gate ฟีเจอร์ในหน้า, **ไม่คืน** `owner_line_id`/อีเมล/บัญชีธนาคาร) ดึงจาก endpoint ใหม่ `GET /api/pos/cashier-shop-info` ที่ **ต้องมี session ถูกต้องเสมอ** ถึงจะเรียกได้ (คนละแบบกับ `public-shop-info.js` เดิมที่ไม่ต้อง auth เลยสำหรับหน้าสั่งซื้อสาธารณะ) — ลิงก์ของเจ้าของร้าน (`/pos?userId=...`) ไม่ถูกแตะเลย ทำงานเหมือนเดิมทุกประการ
      - **วิธีแนบ session ให้ทุก fetch() ในไฟล์ `pos.js` (7,930 บรรทัด) โดยไม่ต้องแก้ทีละจุด:** override `window.fetch` ชั่วคราวใน `useEffect` ที่ทำงานเฉพาะตอน `cashierMode` (เติม header `x-staff-session` ให้ทุก request ที่ขึ้นต้น `/api/` แล้ว cleanup กลับเป็น fetch เดิมตอน unmount/session เปลี่ยน) — **known gap ที่ตั้งใจไม่แก้รอบนี้:** ปุ่ม export/ใบกำกับภาษีที่ยิงผ่าน `window.open()` ตรงๆ ในฝั่ง `pos.js` เอง (ต่างจาก `pos-staff.js` ที่แก้แล้ว) ยังไม่แนบ `?session=` ทำให้ตอนนี้เรียกจากโหมดแคชเชียร์จะพฤติกรรมเหมือน "ไม่มี session" (เจ้าของ, ผ่านเสมอ) — ไม่ใช่ regression (ไม่เคยถูก gate มาก่อนเลย) แต่หมายความว่า Tier 1 gate ของ `export.js`/VAT ยังไม่มีผลกับปุ่มนี้ในโหมดแคชเชียร์จนกว่าจะแก้เพิ่ม
      - **Tier 2-4 ที่เหลือตามแผนเดิมยังไม่ได้ทำ (deferred ตามที่ระบุไว้ในแผนว่าเป็น phased rollout ไม่ใช่ครั้งเดียวจบ):** `sales.js` POST (`perm_process_sales`), `delivery.js`+`collections.js` (`perm_manage_delivery`), `expenses.js` (`perm_manage_expenses`), `receives.js` (`perm_manage_receiving`), `tax-invoice.js` (`perm_issue_tax_invoice`), `loans.js`/`procurement-alerts.js`/`staff-requests.js`/`customer-orders.js`/`expenses-pending.js`/`receives-pending.js` — endpoint เหล่านี้ยังไม่เช็ค session เลย (พฤติกรรมเหมือนเดิมทุกประการ ไม่ถดถอย แค่ยังไม่ได้เพิ่มการป้องกันเพิ่ม) — ปุ่ม "จัดการร้าน" ต่างๆ ใน `pos-staff.js` ที่เรียก endpoint เหล่านี้ (เช่น เก็บเงิน/จัดส่ง) จึงยังทำงานได้แม้ไม่มีสิทธิ์เฉพาะเจาะจง เพราะ endpoint ปลายทางยังไม่ enforce — ไม่ใช่บั๊กใหม่ (ระบบเดิมก็ไม่เคย enforce มาก่อน) แต่เป็นงานที่ควรทำต่อ
      - **ทดสอบยิงจริงครบทุก Tier 1 endpoint กับพนักงาน "Testing" ของร้าน "D Gas"** (สลับเปิด/ปิดสิทธิ์ทีละตัวผ่าน API จริง แล้วเช็คผล 403↔200 ทุกจุด, ยืนยันถอนสิทธิ์มีผลทันที, ยืนยันเจ้าของร้าน (ไม่มี session) ไม่ถูกกระทบทุกจุด): `staff.js` POST (perm_manage_staff), `sales.js` DELETE (perm_void_sales — เจอ Google Sheets quota exceeded ระหว่างทดสอบรอบแรกจากการยิงถี่ๆ ทำให้ void ค้างครึ่งทาง ต้อง retry แล้วตรวจสอบ+คืนค่าสต็อค/บิลด้วยมือ ก่อนสรุปว่าโค้ดถูกต้อง — ไม่ใช่บั๊กจากงานนี้), `contacts.js` PATCH/DELETE (perm_manage_customers), `pos-config.js` PATCH (บล็อกทุก session เสมอไม่ว่าสิทธิ์ใด), `products.js` PATCH (perm_manage_stock), `reports.js` type=sales/pl (perm_view_revenue/perm_view_pl, ยืนยัน type=inventory ที่ไม่ gate ยังทำงานได้ปกติแม้มี session), `export.js` types=vat ทั้งผ่าน header และผ่าน `?session=` query (จำลอง `window.open()`) — ลบ/คืนค่าข้อมูลทดสอบทั้งหมดสะอาด 100% ยืนยันด้วยการอ่านชีตตรงว่าสต็อค/บิล/สิทธิ์พนักงานกลับมาตรงกับก่อนทดสอบเป๊ะ
    - **Deploy production แล้ว (2026-07-23)** — revision จริง `smileslip-dashboard-00277-vqp` (ข้อความ deploy โชว์ revision เดิม `00276-bcb` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — เพิ่ม `STAFF_SESSION_SECRET` เข้า production env แล้วผ่านการ deploy รอบนี้ — verified บน production จริง: `/api/pos/verify-pin` (purpose=pos_cashier) ออก session สำเร็จ, `/api/pos/cashier-shop-info` ปฏิเสธไม่มี session (401) และผ่านเมื่อมี session ถูกต้อง, `/api/auth/check-user` คืน `roles` array แล้ว, `/api/pos/pos-config` PATCH บล็อก session พนักงานทุกกรณีถูกต้อง
45. **แก้ PIN ต้องไม่ซ้ำกันทั้งร้าน + ปิดช่องโหว่ enumeration ใน staff-setpin.js — ผู้ใช้ทดสอบตั้ง PIN เจอ error "มีคนอื่นใช้อยู่แล้ว" แล้วถามว่าทำไมไม่ผูกกับไลไอดีแทน (พนักงานส่งของก็รู้ไลไอดีอยู่แล้วจากการ push งานแจ้งเตือน) และสังเกตเองว่า error message เดิมเปิดให้ไล่เดาได้ว่า PIN ไหนถูกใช้แล้วบ้างในร้าน:**
    - **เปลี่ยนวิธีระบุตัวตนพนักงานทั้งระบบ:** เดิม `verify-pin.js` รับแค่ `{shopId, pin}` แล้วค้นหาข้ามพนักงานทั้งร้านว่า PIN นี้เป็นของใคร (บังคับให้ PIN ต้องไม่ซ้ำกันทั้งร้าน) — เปลี่ยนเป็นต้องระบุ "ใคร" ก่อนเสมอ (`staff_id` เลือกจากรายชื่อ หรือ `line_id` ที่ LIFF รู้อัตโนมัติ) แล้วค่อยเช็ค PIN เฉพาะแถวของคนนั้น — แก้ทั้งสองปัญหาพร้อมกัน: (1) 2 คนตั้ง PIN ซ้ำกันได้แล้วโดยไม่ชนกัน (2) ปิดช่องโหว่ enumeration ที่รากเลย (ไม่ใช่แค่จำกัดจำนวนครั้ง) เพราะไม่มีการเช็คข้ามคนให้ไล่เดาอีกต่อไป
    - เพิ่ม `GET /api/pos/staff-picker?shopId=` (ไม่ต้อง auth เพราะยังไม่รู้ว่าใครกำลังใช้เครื่อง — คืนแค่ชื่อ+สาขาของพนักงานที่ตั้ง PIN ไว้แล้วเท่านั้น ไม่คืนเบอร์โทร/ไลไอดี/สิทธิ์)
    - `verify-pin.js` rate limit เปลี่ยนคีย์จาก `shopId` เดี่ยวๆ เป็น `${shopId}:${staff_id||line_id}` (แม่นยำขึ้น — ลองผิดกับคนหนึ่งไม่ไปล็อกคนอื่นในร้านเดียวกัน)
    - `staff-setpin.js` ลบเช็ค PIN ซ้ำข้ามพนักงานออกทั้งหมด (ไม่จำเป็นอีกต่อไป)
    - `pos.js` (โหมดแคชเชียร์ + ปุ่มเปิดกะเงินสด) และ `pos-staff.js` เพิ่มหน้า "คุณคือใคร?" (เลือกชื่อจากรายการ) ก่อนหน้าใส่ PIN เสมอเมื่อไม่รู้ตัวตนอัตโนมัติ
    - **`pos-staff.js` เพิ่ม LIFF** (SDK/ID เดียวกับที่ Dashboard `/login` ใช้อยู่แล้ว) — เปิดจากในแอปไลน์เอง (เช่น กดลิงก์พุชแจ้งงานจัดส่ง/เก็บเงิน) จะรู้ไลไอดีอัตโนมัติทันที ข้ามหน้าเลือกชื่อไปเข้าหน้าใส่ PIN ตรง (ยังต้องใส่ PIN อยู่ดี แค่ไม่ต้องเลือกชื่อเอง) — เปิดนอกแอปไลน์ (เบราว์เซอร์ปกติ/ลิงก์ถูกแชร์ต่อ) fallback เป็นเลือกชื่อ+PIN เหมือนเดิมอัตโนมัติ — เพิ่ม timeout สำรอง 10 วิที่ทำงานอิสระจาก Script tag's onLoad/onError (เผื่อ CDN ของ LIFF ไม่ตอบสนองอะไรเลยแม้แต่ error กันหน้าค้าง "กำลังโหลด" ตลอดไป)
    - **ทดสอบยิงจริงกับร้าน "D Gas" ครบทุกเส้นทาง:** staff-picker คืนเฉพาะคนที่ตั้ง PIN แล้วจริง, verify-pin ปฏิเสธถ้าไม่มี staff_id/line_id เลย (400), ปฏิเสธ PIN ผิด (401), ยอมรับ PIN ถูกทั้ง 2 เส้นทาง (staff_id และ line_id), ตั้งใจทดสอบให้ 2 พนักงานใช้ PIN เดียวกัน (1234) แล้วยืนยันว่า login แยกกันถูกต้อง 100% ไม่ข้ามกันเลย (คืนค่า PIN ทดสอบกลับที่เดิมหลังทดสอบ) — **ทดสอบผ่านเบราว์เซอร์จริงสำเร็จสำหรับ `/pos-staff` ทั้ง flow** (เลือกชื่อ → ใส่ PIN → เข้าเมนู "สวัสดีคุณ Testing" → ออกจากระบบ → กลับมาหน้าเลือกชื่อ)
    - **`/pos?mode=cashier` และ `/pos?userId=...` ทดสอบผ่านเบราว์เซอร์จริงไม่ได้** — พบว่า `router.isReady` ของ Next.js ไม่กลายเป็น `true` เลยในเบราว์เซอร์ทดสอบของเครื่องมือนี้สำหรับหน้า `/pos` โดยเฉพาะ **เกิดเหมือนกันทั้งเส้นทางเดิมที่ไม่ได้แก้ (`?userId=`) และเส้นทางใหม่ (`?mode=cashier`)** — ยืนยันแล้วว่าไม่ใช่บั๊กจากงานนี้ (ทดสอบแยกทั้งสองเส้นทางเทียบกัน, ไม่มี error ใน console เลย, `window.location.search` ถูกต้องแต่ router ไม่ยอม hydrate query) เป็นข้อจำกัดของเครื่องมือทดสอบเองกับหน้า `/pos` ที่ bundle ใหญ่ที่สุดในระบบ (62kB) — ตรงกับ pattern เดียวกับที่เคยบันทึกไว้ในข้อ 34/35/38 — ตรวจสอบ logic ผ่าน code review + build ผ่าน + ทดสอบ pattern การ implement เดียวกันสำเร็จบน `/pos-staff` แล้ว แนะนำให้ผู้ใช้ทดสอบเองบนเบราว์เซอร์จริงก่อนเชื่อมั่นเต็มที่
    - **LIFF endpoint URL mismatch warning (ยังไม่ยืนยันผลกระทบจริง):** ทดสอบเจอ console warning `liff.init() was called with a current URL that is not related to the endpoint URL` เพราะ `NEXT_PUBLIC_LIFF_ID` ตัวเดียวกันถูกตั้ง endpoint URL ไว้ที่ `/login` ไม่ใช่ `/pos-staff` — ตาม LIFF SDK ปกติ `isInClient()`/`getProfile()` ควรยังทำงานถูกต้องแม้มี warning นี้ (เป็น pattern ปกติเวลาใช้ LIFF ID เดียวกันหลายหน้าในโดเมนเดียวกัน) แต่**ยังไม่เคยทดสอบจริงจากมือถือจริงที่เปิดผ่านแอปไลน์จริง** (จำลองในเบราว์เซอร์ทดสอบไม่ได้เพราะ `isInClient()` เป็น false เสมอนอกแอปไลน์) — แนะนำให้ผู้ใช้ทดสอบกดลิงก์พุชแจ้งงานจัดส่งจริงจากมือถือพนักงานเพื่อยืนยัน ถ้า LIFF auto-login ไม่ทำงานตามคาด ระบบจะ fallback ไปหน้าเลือกชื่อ+PIN แทนอัตโนมัติอยู่แล้ว (ไม่ใช่ทางตันถ้า LIFF ใช้ไม่ได้จริง)
    - **ตรวจสอบความสอดคล้องเพิ่มเติมตามที่ผู้ใช้ขอ ("ดูด้วยว่าเราต้องปรับอะไรอีกให้เข้ากับระบบ") — เจอบั๊กจริง 1 จุด แก้แล้ว:** `verify-pin.js` เดิม gate tier ด้วยเงื่อนไข `purpose !== 'cash_shift'` เท่านั้น ไม่ได้ยกเว้น `purpose === 'pos_cashier'` (ลิงก์แคชเชียร์ใหม่) ด้วย — ทำให้ร้าน Shop Pro (tier `pro`, ไม่มีฟีเจอร์ `delivery_staff_app`) จะโดน 403 "ต้องอัปเกรดแพ็กเกจ" ทันทีที่พยายามใช้ลิงก์แคชเชียร์ ทั้งที่ระบบสิทธิ์แคชเชียร์เป็นฟีเจอร์ความปลอดภัยพื้นฐาน (แยกบัญชีแคชเชียร์ออกจากเจ้าของร้าน) ไม่ใช่ฟีเจอร์ upsell ของแอปพนักงานส่งของ — ถามผู้ใช้ผ่าน AskUserQuestion ว่าจะเปิดให้ทุกแพ็กเกจใช้ได้หรือยังคงเกตไว้เหมือนเดิม **ผู้ใช้เลือก "เปิดให้ทุกแพ็กเกจใช้ได้ (แนะนำ)"** — แก้เป็น `const skipTierGate = purpose === 'cash_shift' || purpose === 'pos_cashier';` — ทดสอบยิงจริงด้วยการสลับร้าน "D Gas" ไปเป็น tier `pro` ชั่วคราว ยืนยันว่า `purpose=pos_cashier`/`cash_shift` ผ่าน gate ไปถึงขั้นเช็ค PIN ได้ปกติ (401 PIN ผิด แทนที่จะเป็น 403 featureLocked) ในขณะที่ไม่ส่ง `purpose` เลย (path ที่ `pos-staff.js` ใช้จริง) ยังคงโดน 403 ถูกต้องเหมือนเดิม (แอปพนักงานส่งของยังคงล็อกที่ Advance+ ตามเดิมตามการตัดสินใจในข้อ 33) แล้วคืนค่า tier ร้านกลับเป็น `enterprise` เดิม
    - **ตรวจสอบจุดอื่นที่เรียก `verify-pin.js` ครบทั้ง 3 จุด** (pos.js โหมดแคชเชียร์, pos.js เปิดกะเงินสด, pos-staff.js) ยืนยันว่าส่ง `purpose`/`staff_id`/`line_id` ถูกต้องตรงกับระบบใหม่ครบทุกจุดแล้ว ไม่พบจุดอื่นที่ตกหล่น
    - **Deploy production แล้ว (2026-07-24)** — revision จริง `smileslip-dashboard-00278-2q2` (ข้อความ deploy โชว์ revision เดิม `00277-vqp` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — verified บน production จริงว่า `/api/pos/staff-picker` และ `/api/pos/verify-pin` (ปฏิเสธถ้าไม่มี staff_id/line_id เลย) ทำงานถูกต้อง
    - **⚠️ สำคัญ ณ ตอน deploy — ร้าน "D Gas" ยังไม่มีพนักงานคนไหนตั้ง PIN ไว้เลย** (ตรวจสอบชีต "พนักงาน" จริงพบคอลัมน์ I ว่างทั้ง 2 คน — "Testing"/"ดีแก๊ส2024") ทำให้ผู้ใช้ที่เพิ่งกดลิงก์แคชเชียร์ทดสอบก่อนหน้านี้ (ตอนที่ยังเป็นโค้ดเก่า) เจอแค่หน้าใส่ PIN เฉยๆ ไม่ใช่บั๊ก — หลัง deploy รอบนี้แล้ว หน้าเลือกชื่อจะโชว์ข้อความ "ยังไม่มีพนักงานที่ตั้ง PIN ไว้" แทนจนกว่าจะมีคนตั้ง PIN อย่างน้อย 1 คนก่อน (ผ่านลิงก์ที่ส่งทาง LINE ตามปกติ หรือกดปุ่ม "📨 ส่งลิงก์ตั้ง PIN" ที่รายชื่อพนักงานในหน้าตั้งค่า)
    - **บั๊กจริงที่เจอทันทีหลัง deploy — ผู้ใช้ตั้ง PIN ให้พนักงานส่งของ "Testing" (บทบาท "พนักงานส่ง", มีแค่ `perm_manage_delivery`) จริงแล้วทดสอบเข้าลิงก์แคชเชียร์ พบว่าเข้าหน้าขายเต็มรูปแบบได้ปกติ ทั้งที่ไม่ควรมีสิทธิ์เลย:** ต้นเหตุคือระบบสิทธิ์เชิงลึก (Tier 1-4 ของแผน) เดิมกันแค่ "การกระทำ" บางอย่างที่ฝั่ง backend (เช่น ลบบิล, แก้ลูกค้า) แต่ไม่เคยกันที่ "ทางเข้า" เลยว่าใครควรเข้าหน้า `/pos?mode=cashier` เต็มรูปแบบได้บ้าง — พนักงานที่ไม่มีสิทธิ์อะไรเกี่ยวกับหน้าร้านเลย (เช่นพนักงานส่งของล้วนๆ) ก็ยังเห็น UI เต็มทุกแท็บได้ (แค่กด action บางอย่างไม่ผ่านเท่านั้น) — แก้โดยเพิ่ม `staffQualifiesForCashier()` ใน `lib/google-pos.js` (เช็คว่ามีสิทธิ์อย่างน้อย 1 ใน 11 สิทธิ์ที่เกี่ยวกับหน้าร้าน — **ตั้งใจไม่รวม `perm_manage_delivery`** เพราะพนักงานส่งของล้วนๆ ไม่ควรผ่าน แต่ผู้จัดการสาขาที่มีสิทธิ์อื่นควบคู่ไปด้วยจะยังผ่านได้ปกติแม้จะมี `perm_manage_delivery` ติดมาด้วย) — enforce จริงที่ `verify-pin.js` (ปฏิเสธด้วย 403 + `deliveryOnly:true` ถ้า `purpose==='pos_cashier'` แต่ไม่ผ่านเงื่อนไขนี้ — เช็คฝั่ง server กันพนักงานยิง `staff_id` ตรงข้ามหน้าเลือกชื่อ ไม่ใช่กันแค่ UI) + กรอง `staff-picker.js` ด้วย query param ใหม่ `mode=cashier` (ซ่อนพนักงานส่งของล้วนๆ ออกจากหน้าเลือกชื่อของ `/pos` ไปเลยกันสับสน — `/pos-staff` ไม่ส่ง `mode` นี้ จึงยังเห็นทุกคนตามปกติ ใครก็ใช้แอปพนักงานได้ไม่ผูกสิทธิ์เฉพาะ) — `pos.js` เพิ่มลิงก์ "🔗 ไปหน้าพนักงาน (pos-staff) แทน" ใต้ข้อความ error เมื่อโดนบล็อกด้วยเหตุนี้
    - **ทดสอบยิงจริงกับข้อมูลจริงของร้าน "D Gas" (ไม่ใช่ข้อมูลทดสอบที่สร้างเอง — ใช้พนักงาน "Testing" ที่ผู้ใช้ตั้ง PIN ไว้จริงตอนทดสอบ):** cashier-mode picker ไม่โชว์ "Testing" อีกต่อไป (แต่ pos-staff picker ยังโชว์ปกติ), verify-pin `purpose=pos_cashier` ของ "Testing" ถูกปฏิเสธ 403 ถูกต้อง, verify-pin แบบไม่มี purpose (ทางที่ `pos-staff.js` ใช้จริง) ของ "Testing" ยังผ่านได้ปกติไม่ถูกกระทบ — ทดสอบฝั่งบวกด้วยการตั้ง PIN ทดสอบชั่วคราวให้ "ดีแก๊ส2024" (บทบาทแคชเชียร์ มี `perm_process_sales`) ยืนยันว่าผ่าน `purpose=pos_cashier` ได้ปกติ แล้วล้าง PIN ทดสอบออกกลับเป็นค่าเดิม (ว่าง) ทันทีหลังทดสอบ
    - **Deploy production แล้ว (2026-07-24)** — revision จริง `smileslip-dashboard-00279-rnk` (ข้อความ deploy โชว์ revision เดิม `00278-2q2` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — verified บน production จริงกับข้อมูลพนักงาน "Testing" ตัวจริงของร้าน D Gas: cashier picker ไม่โชว์ชื่อแล้ว, verify-pin `purpose=pos_cashier` ปฏิเสธถูกต้อง, pos-staff picker ยังโชว์ชื่อตามปกติ

46. **ล้างข้อมูลประวัติธุรกรรมของร้าน "D Gas" กลับเป็นสถานะเหมือนเพิ่งสมัครใหม่ (ตามคำขอผู้ใช้ "เหมือนคนพึ่งใช้ใหม่เพื่อเทสระบบ") — ถามขอบเขตชัดเจนก่อนทำเพราะเป็นการลบถาวร ผู้ใช้เลือก "ล้างแค่ประวัติธุรกรรม เก็บสินค้า/ผู้ติดต่อ/พนักงานไว้":**
    - ล้างทุกแท็บประวัติธุรกรรมใน POS Sheet (ยอดขาย, รับสินค้า, รายจ่าย, ออเดอร์จัดส่ง, งานเก็บเงิน, ใบกำกับภาษี, ยืมสินค้า, บันทึกแลกเปลี่ยน, รับสินค้ารอยืนยัน, รายจ่ายรอยืนยัน, ออเดอร์ลูกค้ารอยืนยัน, กะเงินสด) ด้วย `values:clear` (เก็บ header แถวแรกไว้เสมอ)
    - ล้างชีตบัญชีหลักที่บอทเขียนตอนสแกนสลิป (แท็บปี 2026/2022) — คนละสเปรดชีตจาก POS Sheet โดยสิ้นเชิง (`shop_google_configs.google_sheet_id` ≠ `pos_configs.pos_sheet_id`)
    - ลบข้อมูล Supabase ที่เกี่ยวข้อง: `slip_analytics`/`sender_profiles` (Heatmap/RFM), `shop_usage_daily`, `procurement_alerts` (ทั้งหมด `eq('shop_id', shopId)`), รีเซ็ต `pos_open_bills` เป็น `{bills:[],table_names:[]}`
    - **เก็บสินค้า/ผู้ติดต่อ/พนักงานไว้ตามเดิม แต่รีเซ็ตยอดที่เกิดจากธุรกรรมกลับเป็น 0** — สต็อคสินค้าทั้ง 5 รายการ (เต็ม/กับลูกค้า/เปล่ารอรีฟิล) และยอดค้าง/ถังของผู้ติดต่อ (เจอ 1 รายชื่อจาก 2,122 รายชื่อที่มียอดจริง — "ศิริลักษณ์" ถัง 3 ใบ)
    - **เกิดอุบัติเหตุเล็กน้อยระหว่างทำแล้วกู้คืนได้ทัน:** ตอนแรกเขียนรีเซ็ตยอดผิดแถว (แถว 2 แทนแถวจริงของศิริลักษณ์ที่แถว 1686) แต่ตรวจสอบแล้วแถว 2 มียอดเป็น 0 อยู่แล้วตั้งแต่ต้น (ไม่ได้อยู่ใน non-zero list) จึงไม่มีข้อมูลเสียหายจริง แก้ไปแถวถูกต้องแล้ว
    - **ไม่ได้แตะ:** รูปสลิป/รูปใบเสร็จที่อัปโหลดไว้ใน Google Drive (ลบแค่ประวัติในชีต ไม่ลบไฟล์รูป — ผู้ใช้ไม่ได้ขอ)
    - ยืนยันผลลัพธ์ครบทุกจุดด้วยการอ่านค่ากลับหลังทำเสร็จ (ทุกแท็บ 0 แถว, สินค้า/ผู้ติดต่อ/พนักงานจำนวนเท่าเดิม, สต็อค/ยอดค้างเป็น 0 หมด)

47. **สร้างฟีเจอร์นำเข้าสินค้าเป็นชุดจาก CSV (เช่น export จาก FlowAccount) — ผู้ใช้ถามวิธีนำเข้าสินค้าจาก FlowAccount ก่อน (ระบบยังไม่มีฟีเจอร์นี้เลย มีแต่นำเข้าผู้ติดต่อ) แนะนำให้สร้างฟีเจอร์แทนพิมพ์มือ ผู้ใช้ยืนยัน "สินค้าที่จะทดลองใช้งานจริงมีเยอะมาก" แล้วส่งไฟล์ export จริงจาก FlowAccount มาให้ทดสอบด้วย:**
    - **Backend** (`api/pos/products.js`): เพิ่ม branch `POST { shopId, products: [...] }` (bulk, mirror pattern เดียวกับ `contacts.js`) ใช้ `appendRows()` เป็น chunk ละ 500 กัน rate limit ของ Google — ทุกรายการที่นำเข้าเป็นประเภท "นับสต็อค" เสมอ (ไม่รองรับตั้ง "หมุนเวียน" ผ่าน bulk เพราะมีผลข้างเคียงเรื่องแลกเปลี่ยน/ยืมที่ละเอียดอ่อนกว่า ผู้ใช้แก้เป็นรายตัวทีหลังได้) — ราคา/ทุน/สต็อคติดลบ clamp เป็น 0 แทนการปฏิเสธทั้งไฟล์ (ต่างจาก POST เดี่ยวที่ปฏิเสธด้วย 400 — เพราะไม่มีทางรายงาน error รายแถวกลับไปในโครงสร้าง response ง่ายๆ)
    - **เจอบั๊กจริง 2 จุดระหว่างทดสอบ ก่อน commit:**
      1. **`makeSKU()` ชนกันได้ถ้าเรียกใน loop เดียวกัน (synchronous)** — เดิม `'P' + Date.now().toString(36)...` ไม่มีส่วนสุ่ม เหมือนกับบั๊ก `makeContactId()` ที่เคยเจอและแก้แล้วมาก่อน (ดูข้อ 7) แต่ `makeSKU()` ไม่เคยถูกแก้ตาม — พิสูจน์จริงด้วยการยิง bulk import 2 สินค้าพร้อมกันแล้วได้ SKU ซ้ำกันเป๊ะ (`PYS75TO` ทั้งคู่) แก้โดยเพิ่มส่วนสุ่มต่อท้ายแบบเดียวกับ `makeContactId()`
      2. **`asText()` ไม่เคยถูกใช้ในไฟล์นี้เลยมาก่อน** — คอลัมน์ "วันที่อัปเดต" เขียนสตริงไทยดิบมาตลอด (บั๊ก "~543 ปี" แบบเดียวกับที่เจอซ้ำหลายจุดในโปรเจกต์นี้ แต่ไฟล์นี้ไม่เคยถูกไล่แก้มาก่อน) — แก้ทั้งจุด bulk import ใหม่และจุดเดิมทั้งหมด (POST เดี่ยว, PATCH ทั้ง 3 จุด: receive-back/refill/generic patch)
    - **Frontend** (`pos.js`): เพิ่มปุ่ม "📥 นำเข้า" ในแท็บสินค้า + modal คู่มือ/จับคู่คอลัมน์/พรีวิว/นำเข้า (มิเรอร์ UI ของผู้ติดต่อ) — แต่ parser คนละตัวจากผู้ติดต่อ (`parseProductCSVText` ใหม่ ไม่ใช้ `parseCSVText` เดิม) เพราะไฟล์ export จริงจาก FlowAccount ที่ผู้ใช้ส่งมาทดสอบมีแถวหัวเรื่อง+แถวว่างนำหน้าคอลัมน์จริง 2 แถวเสมอ (พิสูจน์จากไฟล์จริง) — ถ้าเดาว่าแถวแรกสุดคือ header จะจับคู่คอลัมน์ผิดหมด แก้ด้วย `findBestHeaderRowIndex()` (หาแถวที่มีเซลล์ไม่ว่างมากที่สุดในบรรดา 10 แถวแรก)
    - **`autoDetectProdMapping()`** จับคู่ทั้งชื่อคอลัมน์ไทยทั่วไปและอังกฤษแบบ FlowAccount จริง (Name/Unit/Category/ProductCode/BarCode/Description/UnitPrice(WithVat)/BuyPrice(WithVat)) — ราคา/ต้นทุนเลือกคอลัมน์ "รวม VAT" (`UnitPriceWithVat`/`BuyPriceWithVat`) ก่อนเสมอถ้ามี เพราะพร้อมใช้เป็นราคาขายจริงทันที + auto-set ตัวเลือกประเภท VAT ของทั้ง batch เป็น "รวม VAT แล้ว" ให้อัตโนมัติเมื่อจับคู่แบบนี้ (ยังแก้ในหน้าจอได้) — เพิ่มฟิลด์ mapping ใหม่ "รายละเอียด" (map ไปคอลัมน์ `รายละเอียดสินค้า` แยกจาก `หมายเหตุ`) แทนที่ "หมายเหตุ" เดิม เพราะไฟล์จริงมีแต่ "Description" ไม่มี "notes"
    - **กันซ้ำฝั่ง client** ด้วยรหัสสินค้า (ProductCode) ถ้าจับคู่ไว้ หรือชื่อ (case-insensitive) ถ้าไม่มี — เทียบกับสินค้าที่มีอยู่แล้ว + กันซ้ำในไฟล์เดียวกันเอง
    - **ทดสอบยิงจริงครบด้วยไฟล์ FlowAccount จริงที่ผู้ใช้ส่งมา** (`บริษัท สยาม โกลบอล เน็ทเวิร์ค เอ็นเตอร์ไพรส์ จำกัด_product.xlsx` — ตรงกับชื่อนิติบุคคลจริงของร้าน D Gas) แปลงเป็น CSV จำลองขั้นตอน Excel Save As แล้วรัน parser/mapping logic จริงผ่าน Node script (เพราะ `/pos` ทดสอบผ่านเบราว์เซอร์sandboxed ของเครื่องมือไม่ได้ — router query ไม่ hydrate ปัญหาเดิมที่เจอซ้ำหลายครั้ง ข้อ 34/35/38/45) ยืนยันว่า header row ถูกตรวจจับถูกต้อง (ข้ามแถวหัวเรื่อง+แถวว่าง), mapping ถูกต้องครบทุกคอลัมน์, ข้อความไทยไม่เพี้ยนเลย, ไม่มี SKU ซ้ำ — **นำเข้าจริงสำเร็จ 35 สินค้าเข้าร้าน D Gas จริง** (ผ่าน fetch POST ตรงด้วย Node เพราะ UI ทดสอบผ่านเบราว์เซอร์ไม่ได้ แต่เป็น backend เดียวกับที่ UI จะเรียกจริงทุกประการ — ตรวจสอบผลลัพธ์ในชีตจริงครบ: 40 SKU ไม่ซ้ำกันเลย, ข้อความไทยถูกต้อง 100%, ราคา/ต้นทุน/หมวดหมู่/รายละเอียดตรงกับไฟล์ต้นฉบับทุกแถวรวมถึงแถว edge-case "ส่วนลดท้ายบิล" ที่ราคา 0)
    - **Known gap ที่บอกไว้ชัดเจน:** ไฟล์ export ของ FlowAccount ไม่มีคอลัมน์จำนวนสต็อคคงเหลือเลย (เป็นแค่ price list ไม่ใช่ inventory report) สินค้าที่นำเข้าทั้งหมดจึงเริ่มที่สต็อค 0 เสมอ ผู้ใช้ต้องกรอกสต็อคเริ่มต้นเองผ่าน "รับสินค้า" หรือแก้ไขสินค้าทีละตัวหลังนำเข้า
    - **ยังไม่ได้ทดสอบผ่านเบราว์เซอร์จริง** (ปุ่ม/modal/การอัปโหลดไฟล์จริงผ่าน UI) เพราะ `/pos` ทดสอบผ่านเบราว์เซอร์ sandboxed ของเครื่องมือไม่ได้ (ปัญหาเดิม) — ตรวจสอบผ่าน code review + build check + ทดสอบ logic การ parse/mapping/import จริงทุกจุดผ่าน backend โดยตรงแล้ว แนะนำให้ผู้ใช้ลองอัปโหลดไฟล์เองผ่านเบราว์เซอร์จริงเพื่อยืนยันหน้าตา UI
    - **Deploy production แล้ว (2026-07-24)** — revision จริง `smileslip-dashboard-00280-gdv` (ข้อความ deploy โชว์ revision เดิม `00279-rnk` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — verified บน production จริงว่า `POST /api/pos/products` แบบ bulk (`products: [...]`) ทำงานถูกต้อง (ทดสอบยิงจริงแล้วลบแถวทดสอบทิ้ง ยืนยันจำนวนสินค้ากลับเป็น 40 เท่าเดิม — ระหว่างทดสอบรอบนี้ใช้ `curl -d` ผ่าน Bash tool อีกครั้งโดยไม่ทันคิด ทำให้ข้อความไทยเพี้ยนเป็น `?????` ซ้ำแบบเดียวกับที่เจอตอนทดสอบรอบแรก (ข้อ 47 เอง) — ยืนยันอีกครั้งว่าเป็นปัญหาการเข้ารหัส UTF-8 ของ `curl`/Bash บน Windows เท่านั้น ไม่ใช่บั๊กจริง เพราะการนำเข้าจริง 35 รายการก่อนหน้านี้ผ่าน Node fetch ตรงๆ ได้ผลลัพธ์ภาษาไทยถูกต้อง 100% — **บทเรียน: ห้ามใช้ `curl -d` ทดสอบ payload ที่มีภาษาไทยบนเครื่องนี้อีก ให้ใช้ Node `fetch()` เขียนเป็นสคริปต์เสมอ**)

48. **แก้ CI/CD ของบอทที่พังมานาน (ตั้งแต่ข้อ 11) จนสำเร็จจริงครั้งแรก — สาเหตุแท้จริงมี 2 ชั้นซ้อนกัน ไม่ใช่แค่ IAM อย่างที่สงสัยไว้เดิม:**
    - **ชั้นที่ 1 (บั๊กที่บังไว้มานาน):** `cloudbuild.yaml` อ้าง builder image `gcr.io/google-cloud-builders/gcloud` ซึ่ง**ไม่มีอยู่จริง** (พิมพ์ผิด/คนละที่กับที่ตั้งใจ — registry จริงของ Google คือ `gcr.io/cloud-builders/gcloud` ไม่มีคำว่า `google-` นำหน้า `cloud-builders`) ทำให้ error ที่เห็นมาตลอด (`artifactregistry.repositories.downloadArtifacts` denied) **หน้าตาเหมือนปัญหา IAM แต่จริงๆ คือ path ผิด** — แก้แค่บรรทัดเดียวในไฟล์นี้ (commit `5cb37b2`)
    - **ชั้นที่ 2 (ถูกบังไว้อยู่หลังชั้นที่ 1 มาตลอด ไม่เคยเห็นมาก่อนเพราะ build ตายตั้งแต่ pull image ไม่ได้):** พอ image path ถูกแล้ว เจอว่า service account ที่ Cloud Build ใช้รัน deploy จริง (`832247688217-compute@developer.gserviceaccount.com` — compute default SA ไม่ใช่ `@cloudbuild.gserviceaccount.com` ตัวเก่า) **ไม่มีสิทธิ์ deploy Cloud Run เลยสักอย่าง** ต้องเพิ่มทีละสิทธิ์ตามลำดับ error ที่โผล่มาเรื่อยๆ (ถามผู้ใช้ยืนยันก่อนแก้ IAM ทุกครั้งเพราะเป็นการเปลี่ยนสิทธิ์ระบบจริง):
      1. `roles/run.admin` (ระดับโปรเจกต์) — แก้ error `run.services.get` denied
      2. `roles/cloudbuild.builds.editor` (ระดับโปรเจกต์) — `--source=` deploy ต้องยิง Cloud Build ซ้อนอีกชั้นเพื่อ build image ก่อน deploy
      3. `roles/iam.serviceAccountUser` **bind เฉพาะบน resource ของ service account ตัวนี้เอง** (ไม่ใช่ระดับโปรเจกต์ — แคบกว่า ปลอดภัยกว่า) — แก้ error `iam.serviceaccounts.actAs` denied ตอนต้อง "ทำตัวเป็นตัวเอง" ระหว่าง deploy (auto-mode classifier บล็อกการ bind ระดับโปรเจกต์ของ role นี้ไว้เองเพราะเป็นสิทธิ์อ่อนไหว/ช่องทาง privilege escalation ที่พบบ่อย — ต้องขอยืนยันจากผู้ใช้แยกอีกรอบ แล้ว bind แบบ resource-level แทน)
    - **ทดสอบยืนยันจริงด้วยการ trigger build ด้วยมือ 3 รอบ** (`gcloud builds triggers run deploy-bot-on-push`) จนกว่าจะ SUCCESS จริง (ไม่ใช่แค่เชื่อ exit code) แล้วเจอปัญหาเดิมที่เคยบันทึกไว้ซ้ำอีกครั้ง: **build SUCCESS ไม่ได้แปลว่า traffic ไปที่ revision ใหม่อัตโนมัติ** (traffic ยังค้างที่ revision เก่า `00146-5vj` แม้ deploy revision ใหม่ `00147-jtg` สำเร็จแล้ว) เพราะ service นี้เคย pin traffic แบบ fix ตายตัวไว้ก่อนหน้า (ไม่ใช่ "always latest") — ต้อง `gcloud run services update-traffic --to-revisions=...=100` เองอีกเหมือนเดิมทุกครั้งหลัง deploy (ตรงกับข้อควรระวัง #35 เป๊ะ ใช้ได้กับทั้งบอทและ dashboard)
    - **สรุป:** CI/CD auto-deploy ของบอท (`deploy-bot-on-push` trigger) **ใช้งานได้จริงแล้วตอนนี้** (push ขึ้น `main` แล้ว Cloud Build จะ build+deploy ให้อัตโนมัติ) — **แต่ยังต้อง pin traffic เองด้วยมือทุกครั้งหลัง build สำเร็จ** เหมือนเดิม ไม่ได้แก้ปัญหานี้ไปด้วย (คนละสาเหตุกัน) — verified ด้วย log จริงว่า revision ใหม่ boot สำเร็จ (Redis connected) หลัง pin traffic แล้ว

49. **เริ่ม migration ข้อมูล Sheets→Supabase แบบ strangler-fig (แผนเต็มอนุมัติแล้ว, ดูเหตุผล/สถาปัตยกรรมเต็มในข้อความแชท) — Phase 0 + Tier A + Tier B + Tier C เสร็จสมบูรณ์ครบทุกตารางตามแผนแล้ว (13 ตาราง):**
    - **เหตุผล:** Sheets เป็นต้นเหตุบั๊กซ้ำๆ มานาน (column drift, ~543 ปี, เบอร์โทรหาย 0, ID ชนกัน, rate limit) + ต้องการปูทางให้ QR สั่งอาหารที่โต๊ะ (เขียนถี่/พร้อมกันหลายโต๊ะ) และระบบบัญชีเต็มรูปแบบ (ต้องการ ACID) ในอนาคต — **migration นี้เองไม่รวม 2 ฟีเจอร์นั้น** เป็นแค่ฐานราก
    - **สถาปัตยกรรม:** dual-write ทุกจุด — Sheets ยังเป็น primary (เขียนก่อนเสมอ, request fail ถ้า Sheets fail), Supabase เป็น secondary แบบ fire-and-forget (`dualWrite()` helper ใน `lib/supabase-pos.js` — ผิดพลาดแค่ log ไม่ throw) รูปสลิป/ใบเสร็จยังอยู่ Google Drive ของลูกค้าเหมือนเดิมเสมอ (ตาราง Supabase เก็บแค่ URL อ้างอิง)
    - **Phase 0 (โครงสร้างพื้นฐาน):** `lib/supabase-pos.js` (retry wrapper, `insertRow`/`insertRows`/`updateRow`/`softDeleteRow`/`dualWrite`, `tableExists()` — ห้ามใช้ `head:true` เด็ดขาดตามบั๊กที่เคยเจอ, `LEDGER_TYPE` constant), `scripts/verify-parity.js` (เทียบจำนวนแถว+ยอดรวมระหว่าง Sheets กับ Supabase), ALTER `ledger_transactions` (ตารางที่มีอยู่แล้วแต่ไม่เคยใช้ เพิ่มคอลัมน์ที่ Sheets มีแต่ตารางนี้ไม่มี)
    - **Tier A ครบ 4 ตาราง** (blast radius ต่ำสุด): `กะเงินสด` (`pos_cash_shifts`), `บันทึกแลกเปลี่ยน` (`pos_cyclical_log`), `รับสินค้ารอยืนยัน`/`รายจ่ายรอยืนยัน` (`pos_pending_receives`/`pos_pending_expenses` — เขียนจากฝั่งบอท), `ยืมสินค้า` (`pos_loans`)
    - **Tier B ครบ 5 ตาราง:** `รายจ่าย` (`pos_expenses` + เขียนคู่ `ledger_transactions`), `รับสินค้า` (`pos_receives` + เขียนคู่ `ledger_transactions`), `ใบกำกับภาษี` (`pos_tax_invoices`), `ออเดอร์ลูกค้ารอยืนยัน` (`pos_customer_orders`), `งานเก็บเงิน` (`pos_collections`)
    - **Tier C ครบ 4 ตาราง (traffic สูงสุด):** `ออเดอร์จัดส่ง` (`pos_delivery_orders`), `ผู้ติดต่อ` (`pos_contacts` — **ตามที่ผู้ใช้สั่งชัดเจน: สร้างแค่ตาราง+wiring สำหรับข้อมูลใหม่ ไม่ backfill ผู้ติดต่อเก่า 2,000+ แถวที่มีอยู่แล้ว** ประหยัด token/effort — ถ้าต้องการข้อมูลเก่าครบให้ import ใหม่แยกเป็นงานต่างหาก), `สินค้า` (`pos_products`), `ยอดขาย` (`pos_sales` + เขียนคู่ `ledger_transactions` type=income เฉพาะบิลที่ชำระแล้ว)
    - **ทุกตารางมี `UNIQUE(shop_id, <business-key>)`** เพราะตัว generate เลขที่เอกสารส่วนใหญ่ (`makeShiftNo`/`makeLoanNo`/`makeBillNo`/ฯลฯ) ไม่มีส่วนสุ่ม เสี่ยงชนกันถ้าเรียกในลูปเดียวกัน (insert Supabase แค่ fail เฉยๆ ไม่กระทบ Sheets) — ยกเว้น `makeContactId()`/`makeSKU()`/`makeCustomerOrderNo()` ที่มีส่วนสุ่มอยู่แล้ว
    - **พบบั๊กจริงระหว่างทดสอบ 3 จุด แก้ครบแล้ว:**
      1. **`ledger_transactions.type` มี CHECK constraint รับแค่ `'income'`/`'expense'` (อังกฤษ) เท่านั้น** — คนละแบบกับ Sheets column C ที่เป็นภาษาไทยเสมอ (พิสูจน์ด้วยการยิงทดสอบตรงๆ) เพิ่ม `LEDGER_TYPE` constant ใน `supabase-pos.js` ใช้ร่วมกันทุกไฟล์ที่ dual-write เข้าตารางนี้
      2. **`buyer_tax_id`/`buyer_phone` ใน `tax-invoice.js` ไม่เคยผ่าน `asText()`** (เลขภาษี/เบอร์โทรลูกค้าหาย 0 นำหน้าทุกใบที่ออกจริง) — แก้แล้วแยกจาก migration งานนี้ ทดสอบยืนยันแล้วจริง
      3. **`existing[N] = asText(...)` แบบ conditional เสี่ยงคืนค่าดิบไม่มี `'` นำหน้ากลับเข้า Supabase** ถ้าฟิลด์นั้นถูกแก้ในคำขอ PATCH นั้นๆ (พบใน `delivery.js`) — แก้โดยตัด `'` ทิ้งก่อนส่งเข้า Supabase เสมอ (`.replace(/^'/, '')`) หรือสร้าง payload จาก request ดิบโดยตรงแทนอ่านจาก `existing[]` (pattern ที่ใช้ใน `contacts.js`/`products.js`)
    - **พบบั๊กเชิงระบบที่กว้างกว่า migration นี้ระหว่างทดสอบ `collections.js` — แยกเป็น background task ต่างหาก (`task_8c0d885a`) ไม่ปนกับ migration:** `updateSheetRow()` เขียนด้วย `USER_ENTERED` เสมอ — pattern "อ่านทั้งแถว→แก้บางฟิลด์→เขียนทั้งแถวกลับ" ที่หลายไฟล์ PATCH handler ใช้ ทำให้ฟิลด์ที่หน้าตาเหมือนตัวเลข (เบอร์โทร/เลขภาษี) เสี่ยงหาย 0 นำหน้าทุกครั้งที่ PATCH แก้ฟิลด์อื่นโดยไม่แตะฟิลด์นั้นเลย (พิสูจน์จริงกับ `collections.js`) — ต้องไล่ตรวจ `delivery.js`/`sales.js`/`staff.js`/`loans.js`/`cash-shifts.js` เพิ่มเติม
    - **ทดสอบยิงจริงทุกตารางกับร้าน "D Gas"** ผ่าน Node `fetch()` เขียนเป็นสคริปต์เสมอ (ห้ามใช้ `curl -d` กับ payload ภาษาไทยบนเครื่องนี้ — ทำให้ข้อความเพี้ยนเป็น `?????` ตามที่เคยเจอมาแล้วหลายรอบ) ยืนยัน parity ตรงกัน 100% ทุกตาราง ครอบคลุมทุก action/branch (POST/PATCH ทุกเฟส/DELETE/bulk import) รวมถึงกรณีข้อมูลเก่าที่ไม่มีใน Supabase เลย (contacts) ไม่ throw แล้วเก็บกวาดข้อมูลทดสอบออกจากทั้ง Sheets และ Supabase สะอาดทุกรอบ (รวมถึง revert สต็อค/ยอดค้างชำระของลูกค้าจริงกลับเป๊ะ)
    - **ยังไม่ได้ทำ (ตามแผน — เฟสถัดไปหลัง burn-in):** Tier D (ledger หลักของบอทเอง `smileslip-pro/index.js` + เขียนคำสั่ง `#สรุป...` ใหม่ให้ query Supabase แทนอ่านทั้งชีต), Tier E (`reports.js`/`export.js` query Supabase ตรงแทนต่อ Sheets หลายแท็บ) — ทุก tier ที่ทำไปแล้ว (A+B+C) ยังเป็นแค่ dual-write เท่านั้น **ยังไม่ตัดการอ่านจาก Sheets มา Supabase เลยสักจุด** (read-cutover เป็นขั้นถัดไปหลัง burn-in ~2 สัปดาห์ตามแผน แล้วค่อย flip ให้ Supabase เป็น primary/Sheets กลายเป็นแค่ snapshot export) — ดูรายละเอียดเต็มในไฟล์แผน `tingly-napping-moth.md`
    - **Deploy production แล้ว (2026-07-25)** — revision จริง `smileslip-dashboard-00281-ghq` (ข้อความ deploy โชว์ revision เดิม `00280-gdv` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — deploy ผ่าน PowerShell (parse `.env` เองแบบเดียวกับ `deploy-web.sh` เพราะ Bash tool รันสคริปต์นี้บน Windows ไม่ได้) — **verified บน production จริงว่า dual-write ทำงานจริง**: ยิง `POST /api/pos/loans` ตรงกับ production URL แล้วเช็คว่าแถวเข้าทั้ง Sheets และ `pos_loans` บน Supabase จริง (ไม่ใช่แค่ทดสอบผ่าน dev server) แล้วลบข้อมูลทดสอบออกสะอาด — **เริ่มนับ burn-in period ของ migration ได้แล้วตั้งแต่ตอนนี้**

50. **Tier D (ส่วนเขียน) — dual-write ledger หลักของบอทเอง + merge บั๊ก leading-zero ที่แก้เสร็จจาก background task:**
    - **Tier D ส่วนเขียน:** `appendToGoogleSheet()` ใน `smileslip-pro/index.js` (ฟังก์ชันเดียวที่ใช้บันทึกทั้งสลิป OCR และคีย์เองด้วยมือ `รับ/จ่าย NNN`) เขียนคู่เข้า `ledger_transactions` แบบ fire-and-forget เพิ่ม `parseTransactionAt()` แปลงวันที่-เวลาไทย (อาจเป็น พ.ศ., รูปแบบไม่แน่นอนเพราะ OCR) เป็น timestamp จริง fallback เป็นเวลาปัจจุบันเสมอถ้า parse ไม่ได้ — ทดสอบยิงจริงผ่าน **signed LINE webhook เข้า bot ที่รันโลคัล** (คำนวณ HMAC ด้วย `LINE_CHANNEL_SECRET` จริง จำลอง "รับ 5 ทดสอบ..." จากบัญชีเจ้าของร้าน D Gas จริง) ยืนยัน parity ตรงทั้ง Sheets บัญชีหลักและ `ledger_transactions` (รวม `transaction_at` คำนวณถูกต้อง แปลง เวลา Bangkok→UTC ถูก) เก็บกวาดสะอาดแล้ว
    - **⚠️ Tier D ส่วนอ่าน (readSheetSummary/readAllBranchesSummary → query Supabase) และ Tier E ทั้งหมด (reports.js/export.js) ยังไม่ได้ทำ โดยเจตนา:** เหตุผลคือ migration นี้ทั้งหมด **ไม่เคย backfill ข้อมูลเก่าเลยสักตาราง** (ตัดสินใจไว้ตั้งแต่ Tier C ข้อ 2 ผู้ติดต่อ และใช้ต่อเนื่องกับทุกตารางหลังจากนั้น) — คำสั่ง `#สรุป...`/`#กำไรขาดทุน`/`#รายงาน` ของบอท และหน้ารายงานของ dashboard เป็น endpoint ที่ผู้ใช้จริงพึ่งพาดูยอดธุรกิจ ถ้าตัดไปอ่านจาก Supabase ตอนนี้ทันทีจะเห็นยอดว่างเปล่า/ไม่ครบสำหรับข้อมูลเก่าทุกร้าน (มีแค่ข้อมูลที่เกิดขึ้นหลัง deploy ของแต่ละ Tier เท่านั้น) เป็น regression ร้ายแรงที่กระทบผู้ใช้จริงทันที — **ต้องรอ burn-in สะสมข้อมูลระยะหนึ่งก่อน (ตามแผนเดิม ~2 สัปดาห์) หรือทำ backfill script ก่อนแยกต่างหาก ถึงจะตัดมาอ่าน Supabase ได้อย่างปลอดภัย** ไม่ใช่แค่ "เขียนโค้ดเสร็จแล้วสลับได้เลย" แบบ Tier A-C ที่เป็นแค่ dual-write (ฝั่งเขียน)
    - **บั๊กแยกที่แก้เสร็จจาก background task (`task_8c0d885a`) merge เข้ามาพร้อมกันรอบนี้:** ไล่แก้บั๊ก leading-zero ที่เจอตอนทดสอบ Tier B ข้อ 3 (collections.js) ทั่วทั้ง `pages/api/pos/*.js` ที่ใช้ pattern "อ่านทั้งแถว→แก้บางฟิลด์→เขียนทั้งแถวกลับ" — แก้ครบ `contacts.js`, `delivery.js`, `sales.js`, `loans.js`, `staff.js`, `staff-setpin.js`, `products.js`, `receives.js` (เปลี่ยนจาก wrap `asText()` แบบ conditional-only-when-touched เป็น wrap ครั้งเดียวไม่มีเงื่อนไขก่อน `updateSheetRow()` เสมอ) + เจอเพิ่มว่า `sales.js`/`receives.js` ไม่เคยผ่าน `asText()` สำหรับวันที่เลยทั้งไฟล์ (ต่างจาก `expenses.js` ที่แก้ไปแล้ว) แก้พร้อมกัน — ทดสอบยิงจริงกับร้าน D Gas ครบทุกไฟล์แล้วจากอีก session หนึ่ง (merge เข้า main อัตโนมัติแบบไม่มี conflict เพราะคนละไฟล์กับที่ session นี้แก้)
    - **Deploy production แล้วทั้งคู่ (2026-07-25)** — บอท revision จริง `smileslip-service-00163-qb8`, dashboard revision จริง `smileslip-dashboard-00282-vr4` (ข้อความ deploy โชว์ revision เดิมผิดอีกตามเคยทั้งคู่ เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง — รอบนี้เจอ Cloud Build ของบอทรันซ้อนกัน 2 build พร้อมกันจากการ push+merge ติดกันเร็ว แต่ทั้งคู่ SUCCESS ไม่มีปัญหา) — verified บอท boot สำเร็จ (Redis connected), dashboard endpoint ตอบ 200 ปกติ

51. **Tier D (ส่วนอ่าน) เสร็จสมบูรณ์ — คำสั่งสรุปยอด/cron ของบอทอ่านจาก Supabase แทน Sheets แล้ว — ผู้ใช้อนุมัติให้ข้าม burn-in period สำหรับร้าน D Gas โดยเฉพาะ:**
    - **บริบทการตัดสินใจ:** ข้อ 50 บันทึกไว้ว่าตั้งใจไม่ทำส่วนอ่านทันทีเพราะไม่เคย backfill ข้อมูลเก่า ถามผู้ใช้ก่อนว่าจะข้ามการรอ burn-in ไหม (ตัด reads ตอนนี้เลยจะทำให้สรุปยอดของ D Gas เห็นแค่ข้อมูลตั้งแต่วันที่ deploy Tier D เป็นต้นไป ไม่เห็นประวัติเก่าหลายเดือนใน Sheets) — ผู้ใช้ยืนยันชัดเจนว่า **"ตอนนี้ไม่มีใครใช้มีแค่ฉันที่ใช้คนเดียวเพราะเทสระบบ"** และเมื่อถามย้ำเจาะจงเรื่องประวัติเก่าของ D Gas เอง ผู้ใช้ตอบ **"โอเค ไม่เป็นไร ข้อมูลจริงยังอยู่ใน Sheetsครบ"** — จึงดำเนินการตัดมาอ่าน Supabase ทันทีโดยไม่ backfill (ข้อมูลจริงยังปลอดภัยครบใน Sheets เสมอไม่ว่ากรณีใด)
    - **เขียนใหม่ทั้งหมด:** `readSheetSummary(accessToken, sheetId, filterFn, branch, year)` และ `readAllBranchesSummary(...)` (เดิม: อ่านทั้ง Sheets range แล้ว filter/aggregate ด้วย JS ฝั่ง client) → `readSheetSummary(shopId, startISO, endISO, branchFilter)` / `readAllBranchesSummary(shopId, startISO, endISO)` (ใหม่: query ตรงเข้า `ledger_transactions` ด้วย `WHERE shop_id=... AND created_at >= startISO AND created_at < endISO` แล้ว aggregate ใน JS จากผลลัพธ์ที่ query กรองมาแล้ว — เร็วกว่าเดิมมาก โดยเฉพาะร้านที่มีประวัติสะสมนาน)
    - **เพิ่ม `bangkokMidnightUTC(year, month, day)`** — แปลง "วันที่ปฏิทินกรุงเทพ" เป็นขอบเขต UTC ตรงด้วย `Date.UTC(year, month-1, day, -7, 0, 0)` ล้วน (พึ่ง auto-normalize ของ `Date.UTC` เวลาใส่ค่าเดือน/วันเกินขอบ เช่น `day+1` เกินจำนวนวันในเดือนจะเลื่อนไปเดือนถัดไปอัตโนมัติ) — **จงใจไม่ใช้ local-timezone getter แบบ `getThaiDateTime()` เดิม** เพราะพบระหว่างทดสอบว่า `getThaiDateTime()` (ฟังก์ชันเดิม ไม่ได้แก้) เสี่ยง double-shift ถ้า container/เครื่องที่รันตั้ง timezone ไม่ใช่ UTC (พิสูจน์จริงบนเครื่อง dev ที่ระบบตั้ง timezone เป็น `Asia/Bangkok` เอง — `getThaiDateTime()` คืนวันที่เพี้ยนไปข้างหน้า 1 วัน) — Cloud Run container ตั้ง timezone เป็น UTC เสมอจึงไม่เจอบั๊กนี้บน production แต่เป็นความเสี่ยงแฝงที่มีอยู่แล้วก่อนหน้านี้ (ไม่ใช่บั๊กใหม่จากงานนี้ ไม่ได้แก้ `getThaiDateTime()` เอง เพราะใช้อยู่ในอีกหลายสิบจุดทั่วไฟล์ นอกสโคป — แค่เลี่ยงใช้ local getter ในฟังก์ชันใหม่ของงานนี้เท่านั้น)
    - **ปรับ call site ครบ 9 จุด:** `#สรุปวันนี้`/`#สรุปเดือนนี้`/`#กำไรขาดทุน`/`#สรุปอาทิตย์นี้`/`#สรุปปีนี้`/`#สรุปวันที่`/`#รายงาน`/`#สรุปทุกสาขา` (text-command dispatch), `/cron/daily-summary`, `/cron/weekly-summary` (คงตรรกะ `thisMonday`/`lastMonday`/`lastSunday` แบบ UTC ดิบของเดิมเป๊ะ ไม่ผ่าน `bangkokMidnightUTC()` เพราะ Date object เหล่านั้นไม่ได้แทนวันปฏิทินกรุงเทพ), `/cron/trial-day25-nudge` — ทุกจุดลบการแลก Google access token/เรียก `getOrCreateYearSheet()` ที่ไม่จำเป็นอีกต่อไปออก (ลบ query `shop_google_configs` ที่กลายเป็น dead code ในจุด trial-day25-nudge ด้วย)
    - **ทดสอบยิงจริงกับร้าน D Gas ครบ:** seed ธุรกรรมทดสอบผ่าน manual-entry จริง (`รับ 555 ทดสอบ tier d read` / `จ่าย 222 ทดสอบ tier d read`) → ยิง signed webhook เข้าบอทที่รันโลคัลทุกคำสั่งสรุป (สังเกตว่าทุกคำสั่งจบด้วย error "Invalid reply token" จาก LINE ซึ่งเป็นสัญญาณที่ถูกต้องว่าการคำนวณสรุปยอดสำเร็จหมดแล้ว มีแค่ reply กลับ LINE จริงที่ fail เพราะ token ปลอม) → เขียนสคริปต์แยกเทียบค่าที่คำนวณได้กับข้อมูลดิบใน Supabase ตรงๆ ยืนยันตรงกัน 100% (รายรับ 555, รายจ่าย 222, สุทธิ 333 ถูกต้องทั้งระดับวัน/เดือน/ปี) → trigger cron ทั้ง 3 ตัวตรงด้วย `TZ=UTC` (จำลอง timezone ของ container จริง กัน `getThaiDateTime()` double-shift ตามที่พบด้านบน) ยืนยันทำงานสำเร็จจริงไม่มี error (`daily-summary`/`weekly-summary` ส่ง LINE push จริงสำเร็จ, `trial-day25-nudge` ไม่มีร้านเข้าเงื่อนไขจึงข้ามถูกต้องเพราะ D Gas เป็น enterprise ไม่ได้อยู่ระหว่างทดลอง) — ลบข้อมูลทดสอบออกจากทั้ง `ledger_transactions` และ Sheets บัญชีหลัก (blank แถว) เรียบร้อยหลังทดสอบ
    - **สังเกตเจอระหว่างทดสอบ (ไม่ใช่บั๊กจากงานนี้ ไม่ได้แก้ในรอบนี้):** label ของ `#สรุปอาทิตย์นี้`/`cron/weekly-summary` ที่แสดง "จ {fmtDate(thisMonday)} – {fmtDate(lastSunday)}" ดูเหมือนจะสลับกัน (thisMonday มาก่อน lastSunday ทั้งที่ lastSunday คือวันก่อนหน้า thisMonday) เป็นโค้ดเดิมที่ไม่ได้แก้ในงานนี้ (ไม่ได้แตะบรรทัดนี้เลย) — flag ไว้เผื่อพบว่าข้อความ label สัปดาห์ที่แสดงจริงดูแปลกในอนาคต
    - **Deploy production แล้ว (2026-07-25)** — บอท revision จริง `smileslip-service-00165-gbg` (auto-deploy ผ่าน CI/CD ที่เพิ่งแก้ในข้อ 48 สำเร็จจริงเป็นครั้งแรก — เจอ Cloud Build วิ่งซ้อนกัน 2 build จากจังหวะ push ติดกันอีกครั้ง ทั้งคู่ SUCCESS) — traffic ยังค้างที่ revision เก่า (`00163-qb8`) หลัง build สำเร็จเหมือนทุกครั้งที่ผ่านมา ต้อง `gcloud run services update-traffic` เองอีกตามเคย — verified บอท boot สำเร็จ (Redis connected, startup probe ผ่าน) บน revision ใหม่จริง
    - **ยังไม่ได้ทำ (ตามแผนเดิม):** Tier E (`smileslip-dashboard/pages/api/pos/reports.js` และ `api/pos/export.js`/`api/export/excel.js`) — ยังไม่ได้เริ่มออกแบบเลย รอทำต่อเป็นงานถัดไป

52. **Tier E — `reports.js`/`export.js` อ่านจาก Supabase แทน Sheets สำหรับรายงานประเภท transaction log ล้วนๆ (migration ครบทุก Tier ตามแผนเดิมแล้ว: Phase 0 + Tier A-E):**
    - **ดีไซน์เดียวกับข้อ 51 (Tier D):** ย้ายเฉพาะรายงานที่เป็น transaction log ล้วนๆ ให้ query Supabase ตรง (`sales`/`credit`/`loans`/`topsellers`/`pl`/`expenses`/`vat`/`vat30`/`sales_by_branch`) — **ตั้งใจเก็บ `inventory` (แค็ตตาล็อกสินค้าเต็มรูปแบบ) และ `cyclical_inventory` (แค็ตตาล็อกสินค้า+รายชื่อผู้ติดต่อเต็มรูปแบบ) ไว้อ่านจาก Sheets ต่อไป** รวมถึงส่วนต้นทุน/หมวดหมู่สินค้าใน `topsellers`/`pl` และเลขภาษีคู่ค้าใน `vat30` — เพราะ `pos_products`/`pos_contacts` เป็นตาราง "current state" ที่ไม่เคย backfill ข้อมูลเก่าเลยตลอด migration นี้ (ตัดสินใจครั้งแรกที่ Tier C ข้อ 2 ผู้ติดต่อ) ร้านจริงที่มีสินค้า/ผู้ติดต่อสะสมมานาน (D Gas 40 สินค้า/2,000+ ผู้ติดต่อ) จะเห็นแค็ตตาล็อกหายไปเกือบหมดทันทีถ้าตัดมาอ่าน Supabase ตอนนี้ — ต่างจากรายงาน log ที่แค่เห็นประวัติสั้นลงซึ่งเป็นที่ยอมรับได้ (ผู้ใช้อนุมัติให้ข้าม burn-in ไว้แล้วตั้งแต่ข้อ 51 ครอบคลุมทั้ง Tier D และ E)
    - เพิ่ม adapter function (`saleFromRow`/`loanFromRow`/`expenseFromRow`/`receiveFromRow`/`orderFromRow`/`taxInvoiceFromRow`) แปลง Supabase row → shape เดียวกับ `rowToX()` เดิมทุกฟิลด์ ให้ logic คำนวณ/filter ที่มีอยู่แล้วในทั้งสองไฟล์ไม่ต้องแก้ไขเลย + `inRangeISO()` สำหรับ `pos_loans` (ไม่มีคอลัมน์ `transaction_at` แยกเหมือนตารางอื่น เพราะ loans.js ไม่รองรับ backdate ใช้ `created_at` ของ Supabase เองแทน) — duplicate ฟังก์ชันชุดนี้ไว้ทั้งใน `reports.js`/`export.js` ตามธรรมเนียมเดิมของโปรเจกต์ (เหมือน `parseThaiBEDate()` ที่ซ้ำกันอยู่แล้วหลายไฟล์) แทนที่จะ extract shared module ใหม่
    - **เจอบั๊กจริงที่ไม่เกี่ยวกับ migration แต่กระทบทุกรายงานมาตลอด ระหว่างทดสอบตัวกรองวันที่:** `parseThaiBEDate()` (มีอยู่ซ้ำกัน 3 ไฟล์: `reports.js`, `loans.js`, `export.js`) เดิม `str.split(',')` คาดหวังรูปแบบ `"D/M/BE, H:MM:SS"` (มี comma) แต่รูปแบบจริงที่ `resolveRecordDateTime().full`/`toLocaleString('th-TH')` คืนมาคือ `"D/M/BE H:MM:SS"` (คั่นด้วยวรรค **ไม่มี comma เลย**) ทำให้ `datePart` กลายเป็นสตริงทั้งก้อน (รวมเวลา) parse เป็น `Invalid Date` เงียบๆ แล้ว `inRange()` ปฏิบัติกับ Invalid Date เป็น "ผ่านตัวกรองเสมอ" (เทียบ `<`/`>` กับ NaN ได้ false ทั้งคู่) — สรุปคือ **ตัวกรอง `dateFrom`/`dateTo` ของทุกรายงาน (`reports.js`/`export.js`) และของหน้าจัดการยืมสินค้า (`loans.js`) ไม่เคยมีผลอะไรเลยมาตลอด** ไม่ว่าจะเลือกช่วงวันที่ไหนก็เห็นข้อมูลทั้งหมดเสมอ — แก้โดยเปลี่ยนเป็น `str.split(/[, ]/)[0]` (แยกด้วย comma หรือวรรคก็ได้) ทั้ง 3 จุดที่มี helper นี้ซ้ำกัน
    - **เจอบั๊กเพิ่มเติมระหว่างทดสอบ ไม่เกี่ยวกับ migration เช่นกัน (แยกเป็น background task ต่างหาก `task_5bd87623` ไม่ปนกับงานนี้):** `loans.js` PATCH (บันทึกคืนสินค้า) คืนสต็อคให้สินค้าประเภท "นับสต็อค" เสมอโดยไม่เช็คว่าตอนสร้างใบยืม (`POST`) ได้ตัดสต็อคออกจริงหรือไม่ (`deduct_stock` flag ไม่ถูกบันทึกไว้ที่ไหนเลยให้ตรวจสอบตอนคืน) — พิสูจน์จริงด้วยการสร้างใบยืม `deduct_stock:false` แล้วมาร์คคืน พบว่าสต็อคเพิ่มขึ้น 1 หน่วยทั้งที่ไม่เคยถูกตัดออกมาก่อนเลย (สต็อคเฟ้อ) — ระวังเวลาทดสอบ/ใช้งานฟีเจอร์นี้ในอนาคตจนกว่าจะมีคน fix
    - **ทดสอบยิงจริงกับร้าน D Gas ผ่าน dev server + `TZ=UTC`** (จำลอง container จริง): seed บิลขาย/รายจ่าย/ใบยืม/ใบกำกับภาษีจริงผ่าน API แต่ละประเภท → เรียก `reports.js` ยืนยันตัวเลขสรุปตรงกับข้อมูลที่บันทึกจริงทุกประเภท (sales/topsellers/pl/vat/expenses/loans/credit) รวมถึงยืนยัน `dateFrom`/`dateTo` filter ทำงานถูกต้องหลังแก้บั๊กข้างบน → ดาวน์โหลด+parse ไฟล์ Excel จาก `export.js` จริงด้วย `xlsx` library ยืนยันทุก sheet มีข้อมูลถูกต้องครบทุกประเภท (รวม `vat30`/`sales_by_branch`/`cyclical_inventory`) — ลบข้อมูลทดสอบออกจากทั้ง Sheets และ Supabase ครบทุกรอบ (หลีกเลี่ยงเรียก `loans.js` PATCH คืนของตอน cleanup เพื่อไม่กระตุ้นบั๊กที่เพิ่งเจอ — ลบ row ตรงแทน) รวมถึงคืนค่าสต็อคสินค้าที่กระทบกลับเป๊ะ
    - **Deploy production:** ยังไม่ได้ deploy — push ขึ้น GitHub แล้วเท่านั้น (dashboard ไม่มี auto-deploy จาก git push ตามที่บันทึกไว้ด้านล่าง) รอถามผู้ใช้ก่อน deploy จริง
    - **สรุปสถานะ migration ทั้งหมด ณ จุดนี้:** Phase 0 + Tier A + Tier B + Tier C + Tier D (เขียน+อ่าน) + Tier E เสร็จสมบูรณ์ครบตามแผนเดิมทุกข้อแล้ว — เหลือแค่ write-primary flip (สลับให้ Supabase เป็น primary/Sheets เป็นแค่ secondary/snapshot export) ตามที่ระบุไว้ใน `tingly-napping-moth.md` ขั้นตอนที่ 5-6 ซึ่งเป็นขั้นถัดไปที่ยังไม่ได้ทำ (รอ burn-in เพิ่มเติม/ตัดสินใจเพิ่มเติมจากผู้ใช้ก่อน)
    - **Deploy production แล้ว (2026-07-25)** — revision จริง `smileslip-dashboard-00283-wn2` (ข้อความ deploy โชว์ revision เดิม `00282-vr4` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — verified บน production จริงว่า `GET /api/pos/reports?type=sales` และ `GET /api/pos/export?types=sales,inventory` ทำงานถูกต้องกับร้าน D Gas จริง (คืนโครงสร้างถูกต้อง, ไฟล์ Excel ดาวน์โหลด+parse ได้จริง) — **migration ทั้งชุด (Phase 0 → Tier E) อยู่บน production ครบทุกส่วนแล้วตั้งแต่ตอนนี้**

53. **แก้บั๊ก loans.js สต็อคเฟ้อ (จากที่ flag ไว้ในข้อ 52) + รัน SQL trial_used_line_ids ปิดช่องโหว่ trial ซ้ำ — deploy production แล้ว (2026-07-29):**
    - **บั๊ก:** PATCH ของ `api/pos/loans.js` (บันทึกคืนสินค้า) บวกสต็อคกลับให้สินค้าประเภท "นับสต็อค" เสมอ โดยไม่เช็คว่าตอนสร้างใบยืม (POST) ได้ตัดสต็อคออกจริงหรือไม่ (`deduct_stock` เป็น optional flag ที่ไม่เคยถูกบันทึกไว้ที่ไหนเลย) — ใบยืมที่สร้างด้วย `deduct_stock:false` ทำให้สต็อคเฟ้อขึ้นเท่าจำนวนที่ยืมทุกครั้งที่มาร์คคืน (พิสูจน์จริงตอนทดสอบ Tier E: สต็อค 0→1 จากการมาร์คคืนเฉยๆ)
    - **แก้:** เพิ่มคอลัมน์ L `ตัดสต็อคตอนยืม` ('TRUE'/'FALSE') ใน `LOAN_HEADERS` บันทึกตอน POST + `rowToLoan()` คืน `stock_deducted` (**แถวเก่าที่คอลัมน์ว่าง = ถือว่าเคยตัด** คงพฤติกรรมเดิมของใบเก่าไม่เปลี่ยนย้อนหลัง — `ensureTabExists()` auto-widen header ให้เองตอนเปิดใช้ครั้งถัดไป) + PATCH คืนสต็อคเฉพาะเมื่อ `loan.stock_deducted` เป็นจริงเท่านั้น — มิเรอร์เข้า `pos_loans.stock_deducted` (boolean, default true เพื่อแถวเก่า — SQL: `scripts/fix-loans-stock-deducted.sql` ✅ ผู้ใช้รันแล้ว 2026-07-29)
    - **รัน SQL `trial_used_line_ids` แล้วด้วย (2026-07-29)** — ช่องโหว่ "ลบร้านแล้วสมัครใหม่ขอ trial ฟรีซ้ำไม่จำกัดรอบ" ปิดจริงแล้วตั้งแต่ตอนนี้ (โค้ดฝั่ง `register.js`/`delete-shop.js` มีอยู่แล้วตั้งแต่ข้อ 44 Phase 2 แค่รอตารางนี้) — verified ผ่าน REST query ว่าตารางมีจริง
    - **ทดสอบยิงจริงกับร้าน D Gas ครบทั้งสองเคสบน dev ก่อน commit:** `deduct_stock:false` → สต็อคไม่ขยับเลยทั้งตอนยืมและตอนคืน (บั๊กเดิมจะ +qty ผี), `deduct_stock:true` → สต็อค 5→3 ตอนยืม กลับมา 5 ตอนคืน สมมาตรถูกต้อง (หมายเหตุ: เคสตัดสต็อคตอนสต็อคไม่พอยังคง clamp ที่ 0 ด้วย `Math.max(0,...)` ตามพฤติกรรมเดิมของระบบ ไม่ได้แก้ในรอบนี้)
    - **Deploy production แล้ว (2026-07-29)** — revision จริง `smileslip-dashboard-00284-zhw` (ข้อความ deploy โชว์ revision เดิม `00283-wn2` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — **verified บน production จริง**: ยิงเคสบั๊กเดิม (ยืม 3 ชิ้นแบบไม่ตัดสต็อค → มาร์คคืน) กับ production URL ตรง สต็อคนิ่งที่ 0 ตลอด (บั๊กเดิมจะ +3) + Supabase mirror เขียน `stock_deducted:false` ถูกต้องจริงหลังรัน SQL — ลบข้อมูลทดสอบออกจากทั้ง Sheets และ Supabase สะอาดครบ

54. **Phase 2 — Write-Primary Flip: ตัด Google Sheets ออกจากโมดูล POS ทั้งหมด (Tier F → Tier 143 ตามแผนใน `tingly-napping-moth.md`) — ยังไม่ได้ deploy ส่วนที่เหลือ (Tier 140-143) ขึ้น production:**
    - **บริบท/เหตุผล:** ผู้ใช้ต้องการเตรียมระบบรองรับ QR สั่งอาหารที่โต๊ะ (เขียนพร้อมกันหลายโต๊ะ) + สเกลผู้ใช้ทั่วไปในอนาคต — Google Sheets API มี rate-limit ที่จ่ายเงินก็แก้ไม่ได้ ไม่เหมาะกับงานเขียนพร้อมกันเยอะๆ (โปรเจกต์นี้เจอปัญหาจาก Sheets มาซ้ำๆ ตลอดประวัติศาสตร์: column drift, บั๊ก "~543 ปี", ID ชนกัน, rate limit) — ตัดสินใจ **ไม่ backfill ข้อมูลเก่าเลยสักตาราง** (ร้านเดียวที่ใช้งานจริงคือ "D Gas" ยังอยู่ระหว่างทดสอบระบบ ผู้ใช้ยืนยันชัดเจนให้ "ปล่อยว่างทั้งคู่" ทั้งสินค้า 40 รายการและผู้ติดต่อ 2,000+ รายชื่อที่ไม่เคยถูกก็อปปี้เข้า Supabase — ข้อมูลจริงยังอยู่ครบใน Sheets เดิมเสมอ ไม่มีอะไรถูกลบ)
    - **จงใจไม่แตะ:** บอท LINE เอง (`smileslip-pro/index.js`) ยังคงเขียนเข้า Sheets บัญชีหลักของลูกค้าตามเดิมทุกประการ (เป็นจุดขาย PDPA "ข้อมูลการเงินอยู่ใน Google ของคุณเอง ไม่ใช่ของเรา") — ฟังก์ชัน `writeToMainSheets()`/`writeExpenseToMainSheets()`/`writeReceiveToMainSheets()` ใน `sales.js`/`expenses.js`/`receives.js`/`confirm-payment.js` (เขียนสำเนาเข้าบัญชีหลักของบอทให้ตรงกับที่มีอยู่แล้ว) ก็ยังคงเดิมทุกบรรทัด เปลี่ยนแค่ requirement ของ Google connection จากบังคับเป็น optional/best-effort เท่านั้น
    - **Tier F (พนักงาน/PIN/สิทธิ์):** สร้างตาราง `pos_staff` ใหม่ (`scripts/tierF-01-create-staff.sql`) — `staff.js`/`staff-picker.js`/`staff-requests.js`/`staff-setpin.js`/`verify-pin.js`/`getStaffPermissions()` ตัด Sheets ออกเต็มรูปแบบ **เจอปัญหาจริงระหว่างทดสอบ: ตาราง `pos_staff` ถูก deploy ขึ้น production ไปแล้วก่อนที่จะรัน SQL สร้างตาราง** ทำให้ทุก endpoint พนักงาน/PIN error 500 บน production จริงชั่วขณะหนึ่ง — แจ้งผู้ใช้รัน SQL ทันทีที่พบ (ผู้ใช้รันสำเร็จ แก้ปัญหาจบ)
    - **Products + Contacts:** `products.js`/`contacts.js` ตัด Sheets ออกเต็มรูปแบบ — ผลที่ตามมาโดยตรง (แจ้งผู้ใช้ล่วงหน้าก่อนทำ ได้รับยืนยัน "โอเค ตัดเลย"): สินค้า/ผู้ติดต่อเก่าที่ไม่เคย backfill จะหายจากหน้าขาย POS จริงทันทีหลัง deploy
    - **Sales & order flow:** `sales.js` (ทุก action รวม DELETE ยกเลิกบิล), `confirm-payment.js`, `pending-bills.js`, `customer-orders.js` (หน้าสั่งซื้อสาธารณะ) ตัด Sheets ออกเต็มรูปแบบ — ระหว่างทดสอบพบว่า D Gas เหลือสินค้า/ผู้ติดต่อ 0 รายการจริงใน Supabase (ตามที่คาดจากการไม่ backfill) ใช้ข้อมูลทดสอบชั่วคราวแทนแล้วลบทิ้งสะอาด
    - **Delivery/Collections/Loans/Expenses/Receives (Tier 140):** `delivery.js`/`collections.js`/`loans.js`/`expenses.js`/`expenses-pending.js`/`receives.js`/`receives-pending.js` ตัด Sheets ออกเต็มรูปแบบ — เปลี่ยน pattern "อ่านทั้งแถว→แก้→เขียนทั้งแถวกลับ" (จำเป็นสำหรับ Sheets) เป็น targeted partial update ต่อคอลัมน์ (Postgres ทำได้ตรงๆ) — `getCustomerCyclicalHoldings()` ใน collections.js query `pos_cyclical_log` (Supabase) แทน Sheets tab "บันทึกแลกเปลี่ยน" — `expenses-pending.js`/`receives-pending.js` เปลี่ยนจากอ่าน Sheets (ไม่เคยมี Supabase มาก่อนในไฟล์เหล่านี้) มาอ่าน `pos_pending_expenses`/`pos_pending_receives` ที่บอทเขียนแบบ dual-write ไว้อยู่แล้วตั้งแต่ Tier A — เพิ่ม adapter ใหม่ 7 ตัวใน `google-pos.js`: `loanFromRow`, `deliveryOrderFromRow`, `collectionFromRow`, `expenseFromRow`, `receiveFromRow`, `pendingReceiveFromRow`, `pendingExpenseFromRow`
    - **Cash-shifts + Tax-invoice (Tier 141):** `cash-shifts.js` — `computeExpectedCash()` query `pos_sales`/`pos_expenses` กรอง `shift_no` แทน Sheets — เพิ่ม `cashShiftFromRow()` adapter (แปลง `opened_at`/`closed_at` จาก timestamptz ของ Supabase กลับเป็นสตริงไทยให้ตรงกับที่หน้าเว็บเอาไปแสดงผลตรงๆ — ต่างจาก Sheets ที่เก็บสตริงไทยดิบอยู่แล้ว) — `tax-invoice.js`/`tax-invoice-pdf.js` ตัด Sheets ออกเต็มรูปแบบ (เลขที่ใบกำกับภาษีรันต่อปีใช้ count query แทนอ่านทั้งคอลัมน์)
    - **Reports/Export ตัดข้อยกเว้นที่เหลือ (Tier 142):** Tier E (2026-07-25) จงใจคงอ่านแค็ตตาล็อกสินค้า/ผู้ติดต่อจาก Sheets ไว้ก่อน เพราะกลัวข้อมูลเก่าหายกะทันหัน — ตอนนี้ products.js/contacts.js ตัด Sheets ออกไปแล้วเช่นกัน (Sheets ไม่ใช่ source of truth อีกต่อไป ข้อมูลจะค้าง/นิ่งตายตัวถ้ายังอ่านต่อ) จึงตัด `reports.js`/`export.js` มาอ่าน `pos_products`/`pos_contacts` (Supabase) ทั้งหมด: `type=inventory`, ต้นทุน/หมวดหมู่ใน `topsellers`/`pl`, รายชื่อสินค้าหมุนเวียน+ผู้ถือใน `cyclical`, เลขภาษีคู่ค้าใน `vat30`, sheet "คลังสินค้าหมุนเวียน" ใน export
    - **setup.js cleanup + final verification (Tier 143):** เลิกสร้าง Google Sheets spreadsheet (`pos_sheet_id`) ตอนเปิดใช้งาน POS ครั้งแรก — ยังคงสร้าง Drive folder (`pos_folder_id`) เหมือนเดิมเพราะยังใช้เก็บรูปสลิป/ใบเสร็จ/หลักฐานต่างๆ — แก้ `public-shop-info.js`/`cashier-shop-info.js` ที่ยังเช็ค `pos_sheet_id` เป็นสัญญาณ "เปิดใช้ POS แล้ว" (เจอจากการไล่ grep หาทุกจุดที่อ้างถึงคอลัมน์นี้ทั่วโค้ดเบส — ถ้าไม่แก้ ร้านใหม่ที่ activate POS หลัง deploy รอบนี้จะไม่มีทาง "accepting_orders"/"configured" เป็น true ได้เลยตลอดไป) เปลี่ยนไปเช็ค `pos_folder_id` แทน — ลบ `createPosSpreadsheet()` ที่กลายเป็น dead code ออกจาก `google-pos.js` (ยืนยันแล้วว่าไม่มีที่เรียกใช้ที่ไหนก่อนลบ)
    - **Known follow-up ที่ flag ไว้ ยังไม่ได้ทำ:** grep ยืนยันแล้วว่า `*_HEADERS`/`rowToX()` แบบ Sheets-array-based ทั้งหมดใน `google-pos.js` (PRODUCT_HEADERS, SALE_HEADERS, CONTACT_HEADERS, RECEIVE_HEADERS, STAFF_HEADERS, LOAN_HEADERS, EXPENSE_HEADERS, CASH_SHIFT_HEADERS, CYCLICAL_LOG_HEADERS, TAX_INVOICE_HEADERS, COLLECTION_HEADERS, CUSTOMER_ORDER_HEADERS, PENDING_RECEIVE_HEADERS, PENDING_EXPENSE_HEADERS + `rowToProduct`/`rowToSale`/`rowToContact`/ฯลฯ ที่จับคู่กัน) กลายเป็น dead code หมดแล้วเช่นกัน (ไม่มีที่เรียกใช้นอกไฟล์ตัวเองอีกต่อไป) — เป็นงาน cleanup ใหญ่กว่าขอบเขต "Tier 143: setup.js cleanup" ที่วางแผนไว้ จงใจไม่ทำตอนนี้ รอเป็นงานแยกต่างหาก
    - **ทดสอบยิงจริงกับร้าน D Gas ทุก Tier ก่อน commit เสมอ** (Node `fetch()` เขียนเป็นสคริปต์เสมอ ห้ามใช้ `curl -d` กับ payload ภาษาไทยบนเครื่องนี้, `TZ=UTC` จำลอง container จริง) — คำนวณสต็อค/ยอดค้างชำระ/ถังลูกค้า/ต้นทุนถ่วงน้ำหนัก/VAT/เงินสดที่ควรมีถูกต้องตรงกับที่คำนวณมือไว้ล่วงหน้าทุกจุด — ลบข้อมูลทดสอบสะอาดหมดทุกรอบ ทั้ง Supabase, `ledger_transactions`, และ Sheets บัญชีหลักจริงของร้าน D Gas (รวมถึงเจอ+เก็บกวาดข้อมูลทดสอบที่หลงเหลือจาก session แรกของ Tier F ก่อนหน้าด้วย)
    - **สถานะ deploy ณ จุดนี้ (2026-07-29/30):** Tier F + Products/Contacts + Sales/order flow **deploy ขึ้น production แล้ว** (revision `smileslip-dashboard-00284-zhw`, deploy ไปตั้งแต่ก่อน context ของ session นี้จะถูกสรุปย่อ) — **Tier 140-143 (delivery/collections/loans/expenses/receives, cash-shifts/tax-invoice, reports/export, setup.js cleanup) commit+push ขึ้น GitHub แล้วแต่ยังไม่ได้ deploy ขึ้น production** รอถามผู้ใช้ก่อน — production ตอนนี้อยู่ในสถานะผสม (บางไฟล์ตัด Sheets แล้ว บางไฟล์ยังรอ deploy) แต่ปลอดภัย เพราะไฟล์ที่ยังไม่ deploy (เช่น `delivery.js` เดิม) ยังพึ่ง `pos_configs.pos_sheet_id` ที่ยังมีอยู่จริงสำหรับร้านเก่าอย่าง D Gas (ตั้งมาตั้งแต่ 2026-07-03 ก่อน cutover)

**ข้อควรระวังใหม่ที่เพิ่มจากเหตุการณ์นี้:**
- **Git identity ของเครื่องนี้ตั้งแบบ repo-local เท่านั้น** (`user.name=Vespa`, `user.email=six.papigod@gmail.com`) ไม่ใช่ global — ถ้าย้ายเครื่อง/ไดร์อีกต้องตั้งใหม่
- **ผู้ใช้ต้องการให้ commit + push ขึ้น GitHub ทันทีทุกครั้งที่แก้ไฟล์** ไม่ต้องรอถามก่อน (แต่ยังต้องเช็คไม่ให้ commit secret และไม่ bundle ไฟล์เก่าที่ค้างอยู่โดยไม่ถามก่อน)
- **Push ขึ้น GitHub ≠ deploy ขึ้น production** — dashboard ไม่มี auto-deploy จาก git push (มีแค่บอท `smileslip-service` ที่ auto-deploy ผ่าน Cloud Build เมื่อ push main) ทุกครั้งที่แก้ไฟล์ฝั่ง dashboard ต้องเตือนผู้ใช้/ถามว่าจะ `gcloud run deploy` เข้า production เลยไหม
- **ไฟล์ `smileslip-dashboard/lib/*` และ `pages/pos*.js`, `pages/delivery.js` ฯลฯ จำนวนมากไม่เคยอยู่ใน git มาก่อน** — ถ้าเจอไฟล์ที่ยังไม่ track ระหว่างทำงาน ให้ถือว่าเป็นความเสี่ยงเดียวกับที่เกิดกับ `pos.js` และ commit ทันทีเมื่อมีโอกาส

55. **Phase 3 — ย้ายบัญชีหลักของบอทออกจาก Google Sheets เต็มรูปแบบ (Tier 0-6 ตามแผนใน `tingly-napping-moth.md`) — เสร็จสมบูรณ์ครบทุก Tier แล้ว, deploy production แล้วทั้งหมด:**
    - **บริบท:** `smileslip-pro/index.js` (บอท) เป็นคนละ codebase จาก dashboard ที่เพิ่งย้ายเสร็จในข้อ 54 — ก่อนหน้านี้บอทมีแค่ "Tier D" บางส่วน (อ่านสรุปยอดจาก Supabase แล้ว แต่**เขียน**ยังพึ่ง Sheets เป็นหลักอยู่) ผู้ใช้อนุมัติให้ทำให้ครบวงจรเหมือน dashboard เหตุผลเดียวกัน (rate limit ของ Sheets + product ที่เปลี่ยนทิศทางเป็น SaaS ครบวงจรตามที่บันทึกไว้ในหลักการ PDPA ด้านบน) — ตัดสินใจสำคัญ: **ไม่เก็บ Sheets ไว้เป็น fallback ตลอดไปเหมือนตอน dashboard** แต่ตัดออกเต็มรูปแบบ (Tier 6) เพราะบัญชีหลักของบอทมีแค่ 1 ตารางเดียว (`ledger_transactions`) ไม่ซับซ้อนเท่า POS module 14 ตาราง — Google Drive ยังคงอยู่เป็นฟีเจอร์เสริมสำรองรูปสลิป (ไม่บังคับ)
    - **บั๊กจริง 2 ข้อที่ Phase 3 นี้แก้ (ไม่ใช่แค่ performance):**
      1. ร้านที่ยังไม่เชื่อมต่อ Google เดิม**ไม่มีที่เก็บข้อมูลธุรกรรมถาวรที่ไหนเลย** (ทั้งสแกนสลิปและคีย์เอง) เพราะ insert เข้า `ledger_transactions` ซ่อนอยู่ใน branch ที่เชื่อม Google เท่านั้น — แต่ยังถูกตัดเครดิต + ตอบว่า "บันทึกสำเร็จ" ตามปกติทุกครั้ง (เจอตอนวางแผน วิเคราะห์โค้ดตรงๆ ก่อนเริ่มทำ)
      2. `ledger_transactions.slip_hash` มี `UNIQUE` constraint เดิมที่ไม่ผูกกับ `shop_id` เลย (`ledger_transactions_slip_hash_key`) — พิสูจน์จริงว่าบล็อกร้าน B ไม่ให้บันทึก fingerprint ที่ร้าน A เคยใช้ไปแล้ว ทั้งที่เป็นคนละร้านกันโดยสิ้นเชิง (เจอตอนเตรียม SQL Tier 0 ก่อนขอให้ผู้ใช้รัน ไม่ใช่แค่ตามแผนเฉยๆ)
    - **Tier 0 (SQL):** `scripts/phase3-tier0-ledger-dedup-constraint.sql` — DROP constraint เดิมที่ผิด (บั๊ก #2 ข้างบน) แล้วสร้างใหม่แบบ `UNIQUE(shop_id, slip_hash)` ที่ถูกต้อง — ผู้ใช้รันแล้ว
    - **Tier 1:** `findMatchedSupplier()` (จับคู่ชื่อผู้ส่งสลิปกับผู้จำหน่ายใน POS สำหรับ label เสริมตอนแสดงผล) เปลี่ยนจากอ่าน Sheets tab "ผู้ติดต่อ" ผ่าน Sheets API ตรงๆ มาเป็น query `pos_contacts` (Supabase) ตรงๆ — cosmetic เท่านั้น (ไม่กระทบ core flow) ใช้เป็นจุดเริ่มทดสอบ workflow signed-webhook ของ codebase นี้ก่อนแตะจุดเสี่ยงสูงกว่า
    - **Tier 2 (pure refactor):** ย้าย `getOrCreateDriveFolder`/`uploadToGoogleDrive`/`recreateShopGoogleAssets`/`getOrCreateYearSheet`/`appendToGoogleSheet`/`checkDuplicateInSheets`/`parseTransactionAt`/ฟังก์ชันคิว "รับสินค้ารอยืนยัน"/"รายจ่ายรอยืนยัน" ออกจาก `index.js` ไปโมดูลใหม่ `smileslip-pro/lib/ledger-google.js` (factory function รับ `axios`/`FormData`/`supabase`/`getThaiDateTime` เป็น deps) — พฤติกรรมเหมือนเดิม 100% แค่ย้ายที่อยู่โค้ด — **เจอบั๊กจริงระหว่างทำ:** สคริปต์ตัดข้อความอัตโนมัติที่ใช้แยกโค้ดออกมาตัดเอา `detectCategory()`/`INCOME_CATEGORIES`/`EXPENSE_CATEGORIES` ติดไปด้วยทั้งที่ไม่เกี่ยวกับ Sheets/Drive เลย จับได้ก่อน commit ด้วยการตรวจทาน diff เอง แก้แล้วคืนกลับเข้า `index.js` ที่เดิม
    - **Tier 3:** ตัด Sheets ออกจากคิว "รับสินค้ารอยืนยัน"/"รายจ่ายรอยืนยัน" (จาก `#รับสินค้า`/`#รายจ่าย` ผ่านรูปใน LINE) เต็มรูปแบบ — dashboard ฝั่งอ่าน (`receives-pending.js`/`expenses-pending.js`) อ่านจาก `pos_pending_receives`/`pos_pending_expenses` (Supabase) เพียงอย่างเดียวมาตั้งแต่ Phase 2 อยู่แล้ว เขียน Sheets คู่ขนานต่อไปมีแต่จะสร้างข้อมูลไม่มีใครอ่าน — เปลี่ยน insert Supabase จาก fire-and-forget เป็น required (throw ถ้าไม่สำเร็จ) เพราะกลายเป็นจุดเดียวที่บันทึกข้อมูลนี้แล้ว — **เจอ latent bug ระหว่างทำ:** เงื่อนไขเดิมเช็ค `pos_configs.pos_sheet_id` เป็นสัญญาณ "เปิดใช้ POS แล้ว" แต่คอลัมน์นี้เลิกถูกสร้างตั้งแต่ Phase 2 Tier 143 — ร้านใหม่ที่เปิด POS หลัง deploy นั้นจะเจอฟีเจอร์นี้ใช้งานไม่ได้เงียบๆ แก้เป็นเช็ค `pos_folder_id` แทน — ถือโอกาสเพิ่มส่วนสุ่มต่อท้าย `makePendingReceiveNo()`/`makePendingExpenseNo()` (เดิม `Date.now()` อย่างเดียวชนกันได้) ตามธรรมเนียม `makeContactId()`/`makeSKU()` ของ dashboard เพราะตอนนี้ insert เป็น required แล้ว
    - **Tier 4:** แทนที่ `checkDuplicateInSheets` (อ่านทั้งคอลัมน์ K ของ Sheets) ด้วย `checkDuplicateInSupabase()` ใหม่ (query `ledger_transactions` ตรงๆ ด้วย `shop_id`+`slip_hash`) — คง fail-open contract เดิมทุกประการ (return false เสมอถ้า query error ไม่บล็อกสลิปที่ถูกต้อง)
    - **Tier 5 (core, เสี่ยงสูงสุด):** แยก `appendToGoogleSheet` เดิมเป็น 2 ฟังก์ชันอิสระ — `persistLedgerTransaction()` (Supabase, **required**, throw ถ้าไม่สำเร็จ — จับ Postgres unique-violation 23505 แยกเป็น `err.isDuplicate=true` สำหรับ defense-in-depth กัน race condition ระหว่าง retry ซ้อนกันของสลิปเดียวกัน) กับ `writeToGoogleSheet()` (Sheets ล้วนๆ, **best-effort**) — เรียก persist ก่อนตัดเครดิตเสมอไม่ว่า Google จะเชื่อมต่อหรือไม่ (ทั้งเส้นทาง OCR สลิปและคีย์รายการเอง) แก้บั๊ก #1 ที่พบตอนวางแผน — ย้าย dedup check (Tier 4) + `detectCategory()` ให้ทำงานไม่ขึ้นกับ Google เชื่อมต่อเช่นกัน — ตัด gate เชื่อมต่อ Google ที่ไม่จำเป็นอีกต่อไปออกจากคำสั่งสรุปยอดกลุ่ม `#` และทั้ง 2 cron (daily/weekly summary) เพราะ `readSheetSummary` อ่านจาก Supabase อย่างเดียวมาตั้งแต่ Tier D อยู่แล้ว
    - **Tier 5.5 (cross-repo, dashboard):** `smileslip-dashboard/pages/api/sheets/update-transaction.js` (ปุ่ม "✏️ แก้ไขข้อมูล" ที่บอทแนบทุกครั้ง) เดิมหาแถวด้วยการสแกนคอลัมน์ K ของ Sheets และต้องเชื่อมต่อ Google ก่อนเสมอ — เปลี่ยนมาหาจาก `ledger_transactions` ด้วย `shop_id`+`slip_hash` ตรงๆ — เป็น hard prerequisite ก่อน Tier 6 (ไม่งั้นธุรกรรมใหม่หลัง Tier 6 จะไม่มีแถวใน Sheets ให้หา ลิงก์แก้ไขจะ 404 เงียบๆ) — ผลพลอยได้: แก้ไขรายการได้แม้ร้านไม่เคยเชื่อมต่อ Google เลย (เดิมทำไม่ได้เด็ดขาด) — **เจอบั๊กจริงระหว่างทดสอบ:** ใช้ pattern เดียวกับ `getThaiDateTime()` ของบอทตอนแรก (`new Date(x.getTime()+7*3600*1000)` แล้ว `toLocaleDateString('th-TH')` แบบไม่ระบุ `timeZone`) — พังจริงบนเครื่อง dev ที่ระบบตั้ง timezone เป็น `Asia/Bangkok` เอง (double-shift +7 ชม. ซ้ำสอง) แก้เป็นระบุ `timeZone:'Asia/Bangkok'` ตรงๆ ใน `Intl.DateTimeFormat` แทน (Cloud Run ใช้ UTC เสมอเลยไม่เคยเจอบั๊กนี้บน production แต่ dev เจอจริง) — **deploy ขึ้น production แล้ว** (revision `smileslip-dashboard-00286-q9b`) ก่อนเริ่ม Tier 6 ตามลำดับที่วางแผนไว้
    - **Tier 6 (จุดสิ้นสุด, ต้องขอ go-ahead แยกต่างหากตามแผน):** ตัด `writeToGoogleSheet` ออกจากทั้ง manual entry และ OCR path เต็มรูปแบบ (ทุกร้านรวม D Gas ที่ใช้งานจริง) — manual entry ตอบกลับด้วย Flex Message (ปุ่มแก้ไข) เสมอแล้ว ไม่ต้องแยกกรณีเชื่อม Google หรือไม่อีกต่อไป (แก้ inconsistency เดิมที่ OCR path ตอบ Flex เสมออยู่แล้วแต่ manual entry ไม่เคยทำแบบเดียวกัน) — `recreateShopGoogleAssets` ตัดครึ่งที่สร้าง spreadsheet ออก เหลือแค่สร้าง Drive folder ใหม่ตอน self-heal — ลบ dead code ครบ: `getOrCreateYearSheet`, `writeToGoogleSheet`, `checkDuplicateInSheets`, `ensurePendingReceiveSheet`/`appendPendingReceive`/`ensurePendingExpenseSheet`/`appendPendingExpense`/`PENDING_*_HEADERS` (ตายไปตั้งแต่ Tier 3) — อัปเดตข้อความ `#วิธีใช้งาน` (หัวข้อ "สลิป"/"กูเกิล") ให้สื่อสารถูกต้องว่า Google ไม่บังคับอีกต่อไป (เดิมบอกว่า "ต้องเชื่อมต่อ Google Drive ไว้ก่อนไม่งั้นจะไม่บันทึกลง Sheets" ซึ่งไม่จริงแล้ว) — **ยังไม่ได้แก้ข้อความ PDPA ที่ยังบอกว่า "ข้อมูลการเงินเก็บใน Google ของร้านเองไม่ได้เก็บที่ฐานข้อมูลกลาง" ในหัวข้อ "กูเกิล" เดิม** (ตัดสินใจไม่แก้ตอนนี้เพราะเป็นประเด็น PDPA-positioning เดียวกับที่ผู้ใช้ตัดสินใจเลื่อนแก้ `terms.js`/`privacy.js` ไว้ก่อน ไม่ใช่แค่ปัญหาข้อความ Sheets ธรรมดา — ลบข้อความส่วนนั้นออกไปเฉยๆ แทนที่จะเขียนคำอ้างใหม่)
      - **⚠️ เจอบั๊กจริงระหว่างลบ dead code (แก้ก่อน commit):** ลบ `makePendingReceiveNo`/`makePendingExpenseNo` ไปพร้อมกับบล็อกที่ลบทั้งที่ยังใช้งานจริงอยู่ (สร้าง `pending_no` ให้แถว Supabase) — `node -c` (syntax check) ผ่านปกติเพราะเป็นแค่ syntax ไม่ใช่ error แต่พอ `require()` โมดูลจริงแล้วเช็ค `Object.keys()` เจอ `ReferenceError` ทันที — **บทเรียนสำคัญ: `node -c` เพียงอย่างเดียวจับ reference error แบบนี้ไม่ได้ ต้อง require()/เรียกจริงเสมอเวลาลบ dead code เป็นชุดใหญ่**
    - **ทดสอบยิงจริงกับร้าน D Gas ทุก Tier ก่อน commit เสมอ** (signed LINE webhook ต่อ local dev instance + Node `fetch()` ตรงกับ Supabase, ห้ามใช้ `curl -d` กับ payload ภาษาไทยบนเครื่องนี้) — Tier 5/6 ทดสอบละเอียดเป็นพิเศษ: สร้างร้านทดสอบไม่เชื่อม Google ยืนยันบันทึกได้จริง+เครดิตตัดถูกต้อง, regression กับร้าน D Gas เชื่อม Google ยังทำงานเหมือนเดิม, ทดสอบ race-condition dedup ตรงๆ ครบ 5 เคส, **Tier 6 นับจำนวนแถว Sheets ก่อน/หลังยิง manual entry จริง ยืนยันว่าเท่าเดิมทุกประการ (ไม่มีการเขียน Sheets เกิดขึ้นเลย)** — ลบข้อมูลทดสอบสะอาดหมดทุกรอบทั้ง Supabase และ Sheets
    - **Deploy production แล้วทั้งบอทและ dashboard (2026-08-01/02):** บอท revision จริง `smileslip-service-00190-fdb` (Tier 6, traffic pin เองหลังยืนยันด้วย production webhook จริงว่า persist สำเร็จและไม่มีการเขียน Sheets), dashboard revision จริง `smileslip-dashboard-00286-q9b` (Tier 5.5) — ข้อความ deploy โชว์ revision เดิมผิดอีกตามเคยทั้งคู่ เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง — **สถานะ migration ทั้งโปรเจกต์ ณ จุดนี้: Phase 0-2 (dashboard/POS module) + Phase 3 (บอท) เสร็จสมบูรณ์ครบทุก Tier แล้วทั้งคู่ ไม่มี Sheets เหลือเป็น source of truth ที่ไหนในระบบอีกต่อไป (Sheets เดิมของลูกค้ายังอยู่ครบเป็น snapshot ประวัติศาสตร์ ไม่ถูกลบ)** — เหลือ known follow-up เดิมที่ flag ไว้จากข้อ 54 (ไล่ลบ `*_HEADERS`/`rowToX()` แบบ Sheets-array-based ที่ตายแล้วใน `google-pos.js` ของ dashboard) ยังไม่ได้ทำ

56. **ตามข้อ 54: (1) ลบ dead code `*_HEADERS`/`rowToX()` ใน `google-pos.js` (2) บังคับสิทธิ์พนักงาน Tier 2-4 ครบทุก endpoint ที่เหลือ — ยังไม่ได้ deploy dashboard รอบนี้ (แค่ commit+push):**
    - **ลบ dead code:** grep ทั้งโปรเจกต์ยืนยันก่อนลบทุกตัว (0 caller ภายนอก + 0 caller ภายในไฟล์ตัวเอง) — ลบ `readSheet`/`updateSheetRow`/`ensureTabExists` (generic Sheets I/O ที่ไม่มีใครเรียกอีกแล้วหลัง write-primary flip — `appendSheet`/`appendRows`/`getAccessToken`/`colLetter`/`sheetsFetch` ยังต้องเก็บไว้เพราะยังใช้จริงสำหรับ mirror-write เข้าบัญชีหลักบอท), `PRODUCT_HEADERS`/`SALE_HEADERS`/`LOAN_HEADERS`/`TAX_INVOICE_HEADERS`/`CONTACT_HEADERS`/`RECEIVE_HEADERS`/`STAFF_HEADERS`/`ORDER_HEADERS`(ของ `google-pos.js` เอง — คนละตัวกับ `google-delivery.js` ที่ยังใช้จริงสำหรับโมดูล `/delivery` แยกต่างหาก)/`EXPENSE_HEADERS`/`CASH_SHIFT_HEADERS`/`CYCLICAL_LOG_HEADERS`/`COLLECTION_HEADERS`/`PENDING_RECEIVE_HEADERS`/`PENDING_EXPENSE_HEADERS`/`CUSTOMER_ORDER_HEADERS` และ `rowToX()` ที่จับคู่กันทั้งหมด 15 ตัว — ลบไปรวม 571 บรรทัด (1063→492) — `makeCyclicalLogNo()` เปลี่ยนจาก exported เป็น internal-only (ยังใช้จริงใน `logCyclicalTransaction()` แต่ไม่มี caller ภายนอกไฟล์เลย) — verified `npx next build` ผ่านทุก route
    - **สิทธิ์พนักงาน Tier 2-4:** ต่อจาก Tier 1 (ข้อ 44) ที่ทำไว้แค่ `staff.js`/`sales.js DELETE`/`contacts.js`/`pos-config.js` — เพิ่ม `requirePermission()`/`blockAllStaffSessions()` ให้ครบทุก endpoint ที่ CLAUDE.md เดิม flag ไว้ว่า deferred:
      - `sales.js` POST → `perm_process_sales`
      - `delivery.js` POST/PATCH/DELETE, `collections.js` POST/PATCH/DELETE → `perm_manage_delivery` (label สิทธิ์ "จัดการจัดส่ง/เก็บเงิน" ครอบคลุมทั้งสองไฟล์นี้โดยออกแบบไว้แล้วตั้งแต่ต้น ไม่ใช่การตีความใหม่)
      - `expenses.js` POST/DELETE, `expenses-pending.js` DELETE → `perm_manage_expenses`
      - `receives.js` POST, `receives-pending.js` DELETE → `perm_manage_receiving`
      - `tax-invoice.js` POST, `tax-invoice-pdf.js` GET → `perm_issue_tax_invoice`
      - `loans.js` POST/PATCH → `perm_manage_stock` (ไม่มีสิทธิ์เฉพาะทางสำหรับ "ยืมสินค้า" ในสคีมาเดิม เลือกตัวที่ใกล้เคียงที่สุด เพราะยืม/คืนกระทบสต็อคเหมือนกัน — ไม่ได้อยู่ใน `pos-staff.js`/preset ไหนเลย เป็นฟีเจอร์เจ้าของร้านล้วนๆ)
      - `staff-requests.js` PATCH (อนุมัติคำขอ = สร้างพนักงานใหม่จริงเข้า `pos_staff` ทันที) → `perm_manage_staff` (สิทธิ์เดียวกับ `staff.js` POST — กัน session พนักงานคนไหนก็ตามอนุมัติคำขอสมัครให้ตัวเอง/พรรคพวกได้เอง)
      - `customer-orders.js` DELETE → `perm_manage_delivery` (POST ยังคงเปิดสาธารณะไม่ต้อง auth ตามเดิม เพราะเป็นทางเข้าจริงจากลูกค้าที่ไม่มี session เลย — จะไปเช็ค permission ไม่ได้)
      - `procurement-alerts.js` GET+PATCH → `blockAllStaffSessions()` เต็มรูปแบบ (ต่างจากไฟล์อื่นที่มี permKey เฉพาะทาง) — เหตุผล: เป็นเครื่องมือตรวจทุจริตจัดซื้อ**ของพนักงานเอง** ไม่มีสิทธิ์ไหนควรปลดล็อคให้ session พนักงานเห็น/แก้ได้เลยไม่ว่ากรณีใด (ฟีเจอร์นี้ไม่เคยถูกเรียกจาก `pos-staff.js` อยู่แล้วด้วย)
      - **`tax-invoice-pdf.js` เจอ known gap จากข้อ 44 จริง:** เรียกผ่าน `window.open()` ใน `pos.js` ซึ่งไม่ผ่าน `window.fetch` override ที่แนบ `x-staff-session` header ให้อัตโนมัติ (override แพตช์แค่ `fetch()` เท่านั้น `window.open()` เป็นคนละ browser API ไม่มีทางแนบ custom header ได้) — แก้โดยเพิ่ม `?session=` ต่อท้าย URL ตรงจุดเรียกใน `pos.js` (pattern เดียวกับที่ `pos-staff.js` ใช้อยู่แล้วสำหรับปุ่ม export VAT) — ตรวจสอบแล้วว่า `export.js` (เรียกผ่าน `fetch()` ธรรมดาใน `pos.js` ไม่ใช่ `window.open()`) ไม่มีช่องโหว่นี้อยู่แล้วเพราะ fetch() override ครอบคลุมถึง
    - **ทดสอบยิงจริงกับร้าน D Gas ผ่าน dev server ครบทั้ง 12 จุด:** พบว่าตาราง `pos_staff` ของร้านนี้ว่างเปล่า (ไม่มีพนักงานเหลืออยู่จากการทดสอบรอบก่อนๆ) จึงสร้างแถวพนักงานทดสอบชั่วคราวใหม่ (`_TierPermTest`) แทนแก้ไขแถวเดิม แล้ว**ลบทิ้งทั้งแถวเมื่อทดสอบเสร็จ** (ไม่ใช่แค่ restore ค่าเดิม เพราะไม่มีค่าเดิมให้ restore) — ยืนยันครบทุกจุด: ไม่มีสิทธิ์ → 403, มีสิทธิ์ที่ถูกต้อง → ผ่าน gate ไปเจอ validation ปกติของ endpoint นั้น (400 สำหรับ payload ไม่ครบ, 404 สำหรับ ID ปลอมที่ไม่มีจริง — พิสูจน์ว่า gate โปร่งใสไม่รบกวน logic เดิม), เจ้าของร้าน (ไม่มี session เลย) ไม่ถูกกระทบเลยทุกจุด (200 ปกติ) — เก็บกวาดแถวทดสอบสะอาด 100% หลังทดสอบ
    - **Deploy production แล้ว (2026-08-03)** — revision จริง `smileslip-dashboard-00287-96p` (ข้อความ deploy โชว์ revision เดิม `00286-q9b` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — verified บน production จริง: ยิง `POST /api/pos/sales` ด้วย `x-staff-session` ปลอม (garbage token) ได้ 401 "session พนักงานหมดอายุหรือไม่ถูกต้อง" ยืนยันว่า `requirePermission()` ทำงานจริงบน production แล้ว, เจ้าของร้าน (ไม่มี session เลย) เรียก GET เหมือนเดิมได้ปกติ 200 ไม่ถูกกระทบ

57. **ระบบ Owner-Session (Auth Overhaul) — เฟส A–C — ปิดช่องโหว่รากฐาน "ไม่มีใครพิสูจน์ตัวตนเจ้าของร้านเลยจริงๆ" ที่พบระหว่างทำข้อ 56 — ผู้ใช้ขอวางแผนละเอียดก่อน (Plan Mode) แล้วเลือกทำแค่ A+B+C รอบนี้ (เฟส D deferred):**
    - **ช่องโหว่ที่พบ:** ทุกหน้าเว็บ (`dashboard.js`, `pos.js` ฯลฯ) และทุก API endpoint (61 ไฟล์ใน `pages/api/{pos,shop,sheets,admin,invoice,export}/`) รู้ว่า "ใครคือเจ้าของร้าน" จากค่า `userId`/`shopId` ที่เป็น query string เปลือยๆ เท่านั้น — ไม่มีการเซ็นชื่อ ไม่มีวันหมดอายุ ไม่มีการตรวจสอบใดๆ เลย ถ้า `owner_line_id` หลุดออกไปทางไหนก็ตาม (URL แชร์กัน, screenshot, บอทฟอร์เวิร์ดผิดคน) ใครก็ยิง API สร้าง/แก้/ลบข้อมูลได้ทันทีในฐานะเจ้าของร้าน — endpoint ที่ไม่มี auth เลยแม้แต่นิดเดียวและเสี่ยงสูงสุด: `shop/delete-shop.js` (ลบร้านถาวร), `shop/admins.js` (privilege escalation path), `shop/bank-accounts.js` — แม้แต่ `lib/pos-auth.js`'s `requirePermission()` เดิม (ที่เสริมความแข็งแรงให้ระบบพนักงานในข้อ 44/56) ก็ตั้งอยู่บนสมมติฐาน "ไม่มี staff-session = เจ้าของร้าน ผ่านเสมอ" ซึ่งพิสูจน์ไม่ได้เลย
    - **Anti-pattern ที่หลีกเลี่ยง:** `lib/admin-auth.js` (แอดมินบริษัทเอง คนละระบบ) ใช้แค่ base64 encode ธรรมดา ไม่มีการเซ็นชื่อ timestamp ที่ฝังไว้ไม่เคยถูกอ่านมาเช็ค expiry จริง (token มีอายุตลอดชีพ) — ยึด `lib/staff-session.js` (HMAC-SHA256, fail-closed, timing-safe compare) เป็นต้นแบบเท่านั้น เพราะเป็น proven pattern ในโปรเจกต์นี้แล้ว
    - **เฟส A — `lib/owner-session.js` + `lib/owner-auth.js` (ใหม่ทั้งคู่):** payload `{shopId, ownerId, role:'owner'|'admin', exp}` เซ็น HMAC-SHA256 ด้วย `OWNER_SESSION_SECRET` (env ใหม่ — fail-closed ถ้าไม่ตั้ง) TTL 30 วัน (ยาวกว่า staff-session 14 ชม. มาก เพราะเจ้าของร้านไม่ได้ "เข้ากะ" แบบพนักงาน) — `requireOwnerAuth(req,res,shopId,{enforce,requireOwnerRole})`: ไม่มี token = ผ่านเสมอถ้า `enforce:false` (non-breaking ระหว่าง migration), บังคับจริงเฉพาะเมื่อ `enforce:true` — ออก token เพิ่มที่ 3 จุดพิสูจน์ตัวตนจริง: `check-user.js` (**ไม่ใช่ `auth/line.js` ตามแผนเดิม — ตรวจพบว่า `auth/line.js`'s POST handler เป็น dead code ไม่มีที่เรียกใช้เลยทั้งโปรเจกต์ แก้ที่จุดจริงที่ `login.js` เรียกหลัง `liff.getProfile()` แทน**), `email-login.js`, `auth/callback/line.js` (แนบผ่าน query string ตอน redirect เพราะเป็น HTTP redirect ล้วนๆ ไม่มี XHR response ให้แนบตรงๆ)
    - **เฟส B — `lib/client-owner-session.js` (ใหม่) + `login.js`/`dashboard.js`/`pos.js`:** เก็บ token ใน `localStorage['owner_session_'+shopId]` + override `window.fetch` แนบ header `x-owner-session` ให้ทุก request `/api/` อัตโนมัติ (reuse pattern เดียวกับที่ `pos.js` ใช้กับ cashier-mode `x-staff-session` อยู่แล้ว) — ไม่มี token ที่ตรงกับ shopId = redirect ไป `/login?next=...`
    - **เฟส C — บังคับจริง 18 endpoint OWNER-ONLY เสี่ยงสูงสุด แบ่ง 2 รอบ:**
      - รอบแรก (เสี่ยงสูงสุด): `shop/delete-shop.js` (`requireOwnerRole:true` — เฉพาะ owner ห้าม admin ลบร้านได้), `shop/admins.js` (`requireOwnerRole:true`), `shop/bank-accounts.js`, `update-profile.js`, `subscription.js`, `branches.js`
      - รอบสอง: `shop/analytics.js`, `heatmap.js`, `export/excel.js`, `export/analytics-pdf.js` (เรียกผ่าน `<a href>` — ไม่ผ่าน fetch override เลย ต้องแนบ `?ownerSession=` ต่อท้าย URL เองแทน pattern เดียวกับที่ `pos-staff.js` ใช้กับ VAT export), `pos/procurement-alerts.js` (มี `blockAllStaffSessions()` เดิมอยู่แล้ว เพิ่ม `requireOwnerAuth` คู่กันเพื่อกันคนที่ไม่มี session เลยแต่ปลอม shopId มาด้วย), `shop/testimonial.js` GET+POST (เดิม flag ไว้ว่า UNCLEAR ในแผน — ไล่ call site เจอว่า `useEffect` ที่เรียกมันประกาศต่อจาก fetch-override effect ทันทีในไฟล์เดียวกัน ทั้งคู่ dep บน `shopInfo?.id` เดียวกัน จึง safe เหมือน heatmap/analytics — ตัดสินใจ gate เองไม่ถามผู้ใช้เพราะข้อมูลที่ขาดตอนวางแผนตอนนี้ชัดแล้ว แถมมีความเสี่ยงจริงถ้าปล่อยไว้: ฟาร์ม 50 เครดิตฟรี + ปลอมรีวิวในนามร้านได้ด้วยแค่รู้ shopId)
    - **ค้นพบสำคัญ: "chicken-and-egg" timing bug** — endpoint ที่ถูกเรียก **ซิงโครนัสในคอลแบ็กเดียวกับที่ตั้ง `shopInfo`/`shopId` React state ครั้งแรก** (ก่อน React re-render แล้วรัน fetch-override `useEffect`) จะไม่มีทางได้ token แนบมาด้วยเลยในการเรียกครั้งแรกหลัง login/โหลดหน้า เพราะ override effect's dependency array ต้องรอ `shopId` ที่ render แล้วก่อน — **จงใจไม่ gate 4 ไฟล์นี้เพราะเหตุนี้:** `shop/data.js`/`shop/referral.js` (เรียกต่อกันซิงโครนัสใน `fetchData()` ของ dashboard.js), `pos/setup.js`/`pos/pos-config.js` (เรียกใน `loadShopBody()` ของ pos.js) — ต่างจาก endpoint ที่เรียกจาก useEffect แยกที่ dep บน `shopInfo?.id` (รันหลัง full render cycle แล้ว override ติดตั้งเสร็จแล้ว, เช่น tab-switch effect) หรือจาก user-triggered handler (ปุ่มกด/tab click) ซึ่งปลอดภัยเสมอ — ยืนยันด้วยการทดสอบจริงว่า 4 ไฟล์นี้ยังทำงานแบบเดิมทุกประการ (ไม่ enforce)
    - **ทดสอบยิงจริงกับร้าน D Gas ครบทุกไฟล์ทั้ง 2 รอบของเฟส C** (Node `.mjs` script เรียก `issueOwnerSession()` ตรงๆ แล้วยิง `fetch()` เทียบ 3 เคส: ไม่มี token→401, shopId ไม่ตรง→403, token ถูกต้อง→ผ่าน gate ไปเจอ logic ปกติ) + regression sweep ท้ายสุดยืนยันพร้อมกันว่า: เจ้าของร้านตัวจริง (มี token ถูกต้อง) ไม่ถูกกระทบทุกจุด, deep-link เดิมจากบอท LINE (`?userId=...` ไม่มี token) ยังใช้งานได้ (non-breaking เพราะเฟส A/B ไม่บังคับ), โหมดพนักงาน/แคชเชียร์ (`pos-staff.js`, staff-session) ไม่ถูกกระทบเลย (คนละ header คนละ token กัน)
    - **ข้อจำกัดเครื่องมือทดสอบ (ซ้ำกับที่เคยเจอมาหลายรอบในโปรเจกต์นี้ ไม่ใช่บั๊กจากงานนี้):** Browser-pane ของเซสชันนี้โหลด LIFF SDK จาก `static.line-scdn.net` ไม่ได้ (CDN ภายนอกถูกบล็อก) และ `router.isReady`/`router.query` ของ Next.js ไม่ hydrate เลยสำหรับหน้าที่ผูก query string (`/dashboard`, `/pos`) แม้ `window.location.search` จะถูกต้อง — ทดสอบผ่าน backend โดยตรงด้วย Node script แทนทั้งหมด แนะนำให้ผู้ใช้ทดสอบ login flow จริงผ่านเบราว์เซอร์จริงเพื่อยืนยันหน้าตา UI ก่อน deploy
    - **Deferred ตั้งใจตอนนั้น (เฟส D ทำต่อจริงในวันเดียวกัน ดูรายละเอียดด้านล่าง):**
      - แก้ deep-link ของบอท LINE (`smileslip-pro/index.js`, ~15+ จุดที่ใช้ pattern `${FRONTEND_URL}/<page>?userId=${shop.owner_line_id}`) ให้เป็น signed/short-lived token แทน `owner_line_id` plaintext — **ยังไม่ได้ทำ**
      - แก้ `pages/api/pos/staff-setpin.js` ให้เป็น signed one-time token แทนการพึ่ง "รู้ shopId+staff_id = ยืนยันตัวตน" อย่างเดียว — **ยังไม่ได้ทำ**
      - `shop/data.js`/`shop/referral.js`/`pos/setup.js`/`pos/pos-config.js` GET — ยังไม่ enforce เพราะปัญหา chicken-and-egg ข้างต้น ต้องแก้สถาปัตยกรรมการโหลดหน้าก่อนถึงจะ gate ได้อย่างปลอดภัย — **ยังไม่ได้ทำ**
    - **Deploy production แล้ว (เฟส A-C, 2026-08-04)** — ผู้ใช้ยืนยันให้ deploy ทันทีหลังงานเสร็จ — deploy ผ่าน PowerShell (parse `.env` เอง รวม `OWNER_SESSION_SECRET` ที่จำเป็นด้วย เพราะ `deploy-web.sh` ใช้ไม่ได้บน Windows) → revision จริง `smileslip-dashboard-00288-fvv` (ข้อความ deploy โชว์ revision เดิม `00287-96p` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง) — **verified บน production จริงครบทุกจุด** ผ่าน Node script ยิงตรงกับ `https://smileslip-dashboard-832247688217.asia-southeast1.run.app`: `shop/heatmap`, `analytics`, `testimonial` GET, `export/excel`, `pos/procurement-alerts` (ไม่มี token→401, shopId ไม่ตรง→403, token ถูกต้อง→200), `shop/admins` PATCH/DELETE (401/403/ผ่าน gate), **`shop/delete-shop` ยืนยันครบทั้ง 2 ชั้นป้องกัน** (ไม่มี token→401, shopId ไม่ตรง→403, token ถูกต้องแต่ `lineUserId` ปลอม→403 "คุณไม่ใช่เจ้าของร้านนี้" — ไม่มีร้านไหนถูกลบจริงระหว่างทดสอบ), `shop/admins` GET และ `shop/data` ยังคงเปิดตามเดิม (non-enforcing ตามที่ตั้งใจ)

57.1. **เฟส D — บังคับ owner-session จริงในจุดที่พนักงานเข้าไม่ได้ (ทำต่อทันทีในวันเดียวกันหลัง deploy A-C, ผู้ใช้ขอ "ทำให้เสร็จก่อน ค่อยเทสทีเดียวตอนท้าย"):**
    - **สิ่งที่แก้:** `lib/pos-auth.js`'s `requirePermission()`/`blockAllStaffSessions()` เดิมสมมติว่า "ไม่มี staff-session = เจ้าของร้าน ผ่านเสมอ" (พิสูจน์ไม่ได้เลย — ปลอม `shopId` เฉยๆ ก็ยิงเข้ามาได้) — เปลี่ยนเป็นเรียก `requireOwnerAuth(req, res, shopId, {enforce:true})` แทนตอนไม่มี staff-session เลย (โค้ดพาธที่ "มี" staff-session ไม่แตะเลย ยังเหมือนเดิม 100%) — `blockAllStaffSessions()` เพิ่ม parameter `shopId` ใหม่ (ต้องมีเพื่อเรียก `requireOwnerAuth`) อัปเดต 2 call site ที่มี (`pos-config.js` PATCH, `procurement-alerts.js`) ให้ส่งเข้าไปด้วย
    - **ขอบเขต:** ปิดช่องโหว่ 28 call site ใน 19 ไฟล์ (`contacts.js` PATCH/DELETE, `collections.js` POST/PATCH/DELETE, `expenses-pending.js` DELETE, `customer-orders.js` DELETE, `delivery.js` POST/PATCH/DELETE, `export.js` GET, `loans.js` POST/PATCH, `expenses.js` POST/DELETE, `pos-config.js` PATCH, `products.js` PATCH, `receives-pending.js` DELETE, `procurement-alerts.js` GET/PATCH, `sales.js` POST/DELETE, `reports.js` GET×2, `staff.js` POST/PATCH/DELETE, `tax-invoice-pdf.js` GET, `tax-invoice.js` POST, `receives.js` POST, `staff-requests.js` PATCH)
    - **การวิเคราะห์ความเสี่ยงก่อนแก้ (สำคัญที่สุด — เดิมกลัวว่าต้องแก้ dashboard.js/pos.js's fetch-override ก่อนด้วย ตามที่ plan บอกไว้ว่ากระทบ "ไฟล์ที่พนักงาน D Gas ใช้งานจริงทุกวัน เสี่ยงพังระบบมากที่สุด"):** ไล่เช็คทุก call site ทั้ง 28 จุดด้วย grep+read พบว่า**ทุกจุดอยู่หลัง method ที่ไม่ใช่ GET เสมอ** (POST/PATCH/DELETE) **หรือถ้าเป็น GET ก็ผูกกับปุ่มกด/tab-click เสมอ** (`export.js`, `reports.js`, `tax-invoice-pdf.js`, `procurement-alerts.js` GET — เรียกจาก `fetchReport()`/ปุ่ม export ใน `pos.js` ไม่ใช่ `loadShopBody()`) — **ไม่มีจุดไหนถูกยิงอัตโนมัติตอนโหลดหน้าครั้งแรกเลย** (ทุกอย่างใน `loadShopBody()`/`fetchData()`'s initial burst เป็น GET ล้วนๆ ไม่มี `requirePermission`/`blockAllStaffSessions` เกี่ยวข้องเลยสักจุด) — สรุปว่า fetch-override ที่มีอยู่แล้วเดิม (gated บน `[shopInfo?.id]`/`[cashierSession?.sessionToken]`) **เพียงพอแล้ว** เพราะ write action ทุกจุดเกิดจากการคลิกของผู้ใช้จริงเสมอ ซึ่งเกิดหลัง render หลายรอบผ่านไปแล้วตอนนั้น — **จึงไม่ต้องแก้ dashboard.js/pos.js เพิ่มเลยสักบรรทัดสำหรับเฟส D รอบนี้**
    - **ค้นพบเพิ่มเติมระหว่างวิเคราะห์ (พบแต่ไม่ได้แก้ เพราะไม่กระทบอะไรตอนนี้):** `pos.js`'s **cashier-mode** fetch-override (`x-staff-session`, บรรทัด ~958) มี chicken-and-egg bug แบบเดียวกับที่เจอใน `dashboard.js` ตอนเฟส C — gated บน `cashierSession?.sessionToken` (React state) แต่ `loadShopBody()` เรียกซิงโครนัสทันทีหลัง `setCashierSession(...)` ใน `verifyCashierPin()`/session-restore effect ก่อน effect จะมีโอกาสรัน — ทำให้ batch แรกสุดของ GET calls หลังแคชเชียร์ใส่ PIN ผ่าน (branches/setup/products/contacts/staff/ฯลฯ) ไม่มี `x-staff-session` แนบไปเลย — **ไม่กระทบอะไรตอนนี้เพราะ GET ทั้งหมดนั้นไม่ผ่าน `requirePermission`/`blockAllStaffSessions` อยู่แล้ว** (เหมือนที่วิเคราะห์ไว้ข้างบน) — เทียบกับ `pos-staff.js` ที่ใช้ pattern `sessionRef` (ref ไม่ใช่ state) มาตั้งแต่แรกอยู่แล้ว ไม่มีปัญหานี้เลย — **flag ไว้เป็นความเสี่ยงแฝง ถ้าในอนาคตมีคนเพิ่ม endpoint ใหม่ที่ต้องเช็คสิทธิ์เข้าไปใน `loadShopBody()`'s initial burst จะพังเงียบๆ แบบเดียวกับที่กลัวไว้ตอนแรก — ควรแก้เป็น ref-based pattern (เหมือน `pos-staff.js`) ก่อนถ้าจะขยาย initial-load burst ในอนาคต**
    - **ทดสอบยิงจริงกับร้าน D Gas ครบทั้ง 28 call site** (สร้างแถวพนักงานทดสอบชั่วคราว `_PhaseDTest*` เปิดสิทธิ์ทุกตัวเป็น `true` ก่อน ลบทิ้งหลังทดสอบ) — **Part 1 (สำคัญที่สุด — พิสูจน์ช่องโหว่ปิดจริง):** ไม่มี token เลย (ทั้ง staff-session และ owner-session) → **401 ทุกจุดครบ 28/28** (ก่อนหน้านี้จะผ่านเป็นเจ้าของร้านเงียบๆ) — **Part 2:** owner-token ที่ถูกต้อง (spot-check 5 ไฟล์ตัวแทน) → ผ่าน gate ปกติไม่ถูกกระทบ — **Part 3:** staff-session ที่มีสิทธิ์ถูกต้อง (spot-check 4 ไฟล์) → ผ่าน gate ปกติเหมือนเดิมทุกประการ (regression-safe กับงาน Tier 1-4 เดิม) — **Part 4:** staff-session ที่ `blockAllStaffSessions` files (`pos-config.js`/`procurement-alerts.js`) → ยัง 403 เสมอไม่ว่าจะมีสิทธิ์อะไรก็ตาม ถูกต้อง — ทุก payload ที่ใช้ตอนพิสูจน์ owner/staff-ผ่าน-gate จงใจอ้างอิง ID ปลอม/ไม่มีอยู่จริงเสมอ (กันไม่ให้เกิด side effect จริงแม้ auth จะผ่าน) ยืนยันด้วยการอ่านโค้ดจริงว่าไม่มีจุดไหนเขียนข้อมูลจริงก่อนจะ 400/404 — ลบแถวพนักงานทดสอบทิ้งสำเร็จหลังทดสอบ ไม่มีข้อมูลทดสอบตกค้าง
    - **Deploy production แล้ว (2026-08-04)** — revision จริง `smileslip-dashboard-00289-8pv` (ข้อความ deploy โชว์ revision เดิม `00288-fvv` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง) — **verified บน production จริงครบ 26/26 การทดสอบ** ผ่าน Node script ยิงตรงกับ production URL: ไม่มี token → 401 ทุกไฟล์ที่ทดสอบ (สุ่มครบทุก 19 ไฟล์อย่างน้อยไฟล์ละ 1 จุด), owner-token/staff-token-ที่มีสิทธิ์ → ผ่าน gate ปกติไม่ถูกกระทบ, staff-session บน `blockAllStaffSessions` file ยัง 403 เสมอ — สร้าง+ลบแถวพนักงานทดสอบ (`_PhaseDProdTest*`) สะอาดหลังทดสอบ

58. **3 บั๊ก/ฟีเจอร์ที่ผู้ใช้เจอทันทีตอนเริ่มใช้งานจริง+เทสระบบหลัง deploy เฟส D — นำเข้าสินค้า Excel ไม่ได้, ผู้ติดต่อเห็นไม่ครบ (2,642 คนจริง เห็นแค่ ~900-1,000), เกิดผู้ติดต่อซ้ำจากบั๊กนั้นเอง:**
    - **(1) รองรับนำเข้าสินค้าจากไฟล์ Excel (.xlsx/.xls) โดยตรง** — เดิม `pos.js`'s product-import modal (สร้างไว้ในข้อ 47) รับแค่ CSV (`accept=".csv"`, อ่านด้วย `readAsText()` เสมอ) มีข้อความในหน้าจอบอกให้ผู้ใช้ "เปิด Excel แล้ว Save As → CSV ก่อนอัปโหลด" — ผู้ใช้คาดหวังว่าจะอัปโหลดไฟล์ export จาก FlowAccount (.xlsx) ได้ตรงๆ — แก้โดยแยก parser เป็น 2 ชั้น: `buildProductImportFromRawRows()` ทำงานบน "แถวดิบ" (array of array) กลาง ไม่ผูกกับแหล่งไฟล์ — CSV แปลงข้อความเป็นแถวดิบผ่าน `parseCSVLine` เดิม, Excel ใช้ library `xlsx` (มีอยู่แล้วเป็น dependency ของโปรเจกต์ ใช้ฝั่ง server สำหรับ export อยู่แล้ว) dynamic import เฉพาะตอนเลือกไฟล์ Excel จริง (กัน bundle ของ `pos.js` ที่ใหญ่สุดในระบบบวมขึ้นสำหรับคนที่ไม่ใช้ฟีเจอร์นี้) แปลงผ่าน `sheet_to_json({header:1, raw:true})` เป็นแถวดิบแบบเดียวกัน — logic หา header row จริง/จับคู่คอลัมน์อัตโนมัติ/ตั้ง VAT-type default ไม่ต้องแก้เลยเพราะทำงานบนแถวดิบเดียวกันทั้งสองแหล่ง
    - **(2) แก้บั๊ก Supabase คืนสูงสุด 1,000 แถวเงียบๆ (root cause ของ "ผู้ติดต่อหาย")** — ตรวจสอบพบว่า `pos_contacts` มีจริง 2,121 แถว (ตอนแรกที่เจอ) แต่ `contacts.js` GET เดิม (ไม่มี `.range()`/`.limit()`) คืนกลับมาแค่ 1,000 แถว เพราะ Supabase/PostgREST มี default `db-max-rows` จำกัดไว้ที่ 1,000 เสมอไม่ว่าจะขอกี่แถวก็ตาม โดยไม่ error ให้เห็นเลย — แก้โดยวนอ่านทีละ 1,000 แถวด้วย `.range()` จนครบใน `contacts.js` GET — ทดสอบยิงจริงยืนยันคืนครบ 2,121 แถวแล้ว (ตอนนั้น) — เจอ pattern เดียวกันเป๊ะใน `products.js` GET (ยังไม่กระทบ D Gas เพราะสินค้าปัจจุบันน้อยกว่า 1,000 — ปัจจุบันจริงๆ คือ 0 สินค้า เพราะผู้ใช้ล้างข้อมูลไปเทสใหม่ — แต่เป็นบั๊กแฝงเดียวกันที่จะโดนซ้ำถ้านำเข้าแคตตาล็อกใหญ่ผ่าน Excel/CSV import ที่เพิ่งเพิ่มความสามารถให้ในข้อ (1)) แก้ไว้ล่วงหน้าด้วย
      - **ผลข้างเคียงที่ร้ายแรงกว่าตัวบั๊กเอง:** ระหว่างที่บั๊กยังไม่ถูกแก้ ผู้ใช้กดนำเข้า CSV ซ้ำหลายรอบ (คิดว่ายังไม่ครบ 2,000+ คนที่คาดหวัง) — client เทียบ dedup กับรายชื่อที่โหลดมาไม่ครบ (แค่ 1,000) จึงเข้าใจผิดว่าคนที่มีอยู่แล้วเกิน 1,000 คนเป็น "รายชื่อใหม่" แล้วส่งเข้าไปซ้ำ (server-side bulk-insert ไม่มี dedup check เองเลย พึ่งพา client ทั้งหมด) — ตรวจสอบ ณ ตอนที่ผู้ใช้แจ้งจริง (หลังลองซ้ำหลายรอบ) พบ **521 กลุ่มชื่อ+เบอร์ซ้ำ รวม 1,043 แถว จากทั้งหมด 2,642 แถว** (เหลือ unique จริง 2,120 คน) — ยังไม่ได้ลบ ให้เครื่องมือใหม่ในข้อ (3) จัดการแทน
    - **(3) สร้างเครื่องมือ "🧹 เช็ครายการซ้ำ/ไม่มีความเคลื่อนไหว" ในหน้าผู้ติดต่อ** — ผู้ใช้ขอปุ่มเช็ครายการซ้ำ + ฟิลเตอร์เลือกลูกค้าที่ไม่ได้มีความเคลื่อนไหวนาน 3/6/12 เดือน เพื่อเลือกลบเป็นชุด แทนที่จะให้ Claude ลบให้ครั้งเดียวจบ (ต้องการเครื่องมือถาวรไว้ดูแลรายชื่อต่อเนื่อง ไม่ใช่แค่ one-off cleanup)
      - `api/pos/contacts-cleanup.js` (ใหม่) — GET คืนแค่ `contact_id` + ข้อมูลที่คำนวณ (ไม่ใช่ contact เต็ม เพราะฝั่งเว็บมี state ผู้ติดต่อโหลดไว้แล้ว) ให้ฝั่งเว็บ merge เข้ากับ `contacts` เอง: `is_duplicate`/`duplicate_group_size` (จับคู่ชื่อ+เบอร์ normalize ตัดวรรค/ขีด/ตัวพิมพ์ นับทุกคนในกลุ่มรวมคนแรกด้วย ให้ผู้ใช้เลือกเองว่าจะเก็บ/ลบตัวไหน) และ `last_activity_at` (วันที่ล่าสุดที่มีธุรกรรมจริงผูก `customer_id` จาก 3 แหล่ง: `pos_sales`/`pos_delivery_orders`/`pos_collections` — **null = ไม่เคยมีธุรกรรมเลย ถือเป็น "ไม่มีความเคลื่อนไหว" มากที่สุดเสมอไม่ว่าจะเลือก threshold กี่เดือนก็ตาม** ตรงกับกรณีทั่วไปของผู้ติดต่อที่นำเข้ามาเป็นชุดใหญ่แต่ยังไม่เคยซื้อขายจริงกับร้าน)
      - `api/pos/contacts.js` — เพิ่ม `DELETE` แบบ bulk (`{shopId, contact_ids:[...]}`) แยกจาก `DELETE` เดิม (`contact_id` เดี่ยว) ยังคง soft-delete เหมือนเดิมทุกประการ (`deleted_at`, กู้คืนได้)
      - `pos.js` — modal ใหม่ในแท็บผู้ติดต่อ: ตัวกรองซ้ำ (toggle) + ตัวกรอง 3/6/12 เดือน (เลือกได้ทีละอัน) + "เลือกทั้งหมด (N)" + checkbox รายตัว + ปุ่ม "ลบที่เลือก" พร้อม confirm ก่อนลบเสมอ
    - **ทดสอบยิงจริงกับข้อมูลทดสอบชั่วคราวครบทุกจุด** (สร้าง 2 contact ชื่อ+เบอร์ซ้ำกัน + 1 contact ผูกกับ sale จริง แล้วลบทิ้งหมดหลังทดสอบ): ตรวจจับคู่ซ้ำถูกต้อง, `last_activity_at` คำนวณถูกต้องจากการ join จริงกับ `pos_sales`, bulk `DELETE` ต้องมี owner-session token ถึงจะผ่าน (401 ถ้าไม่มี — เจตนา ตรงกับ Phase D auth ที่บังคับไว้แล้วในข้อ 57.1), soft-delete ทำงานถูกต้องและหายจาก GET list จริง
    - **ยังไม่ได้ทดสอบ modal ผ่านเบราว์เซอร์จริง** (ข้อจำกัดเครื่องมือทดสอบเดิมกับหน้า `/pos` ที่ผูก query string — ซ้ำกับที่เจอมาหลายรอบในโปรเจกต์นี้) — ตรวจสอบผ่าน code review + build check + ทดสอบ backend โดยตรงแล้วเท่านั้น
    - **Deploy production แล้ว (2026-08-05)** — deploy 2 รอบ (รอบแรก: excel import + pagination fix — revision `smileslip-dashboard-00290-55w`; รอบสอง: cleanup tool — revision `smileslip-dashboard-00291-bws`, ข้อความ deploy โชว์ revision เดิมผิดทั้ง 2 รอบตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — **verified บน production จริง:** `contacts.js` GET คืนครบ 2,642 คน (ไม่ติด 1,000 อีกต่อไป), `contacts-cleanup.js` GET วิเคราะห์ครบ 2,642 คนคืนค่า `is_duplicate` ถูกต้อง (พบ 1,043 แถวที่ซ้ำ ตรงกับที่ตรวจนอก production ไว้ก่อนหน้า)
    - **ยังไม่ได้ทำ:** ลบ 521 กลุ่มผู้ติดต่อซ้ำที่มีอยู่จริงตอนนี้ — รอผู้ใช้กดใช้เครื่องมือใหม่เอง หรือขอให้ Claude ลบให้แทน

59. **ผู้ใช้ทดสอบเครื่องมือจากข้อ 58 ต่อทันที เจอ 2 บั๊กเพิ่ม — "เลือกทั้งหมด" ในเครื่องมือเช็ครายการซ้ำลบทั้งกลุ่มไม่เหลือใครเลย, นำเข้าสินค้า Excel ใส่ VAT ให้ทุกแถวเหมือนกันหมดทั้งที่บางรายการไม่มี VAT จริง:**
    - **(1) บั๊กเครื่องมือเช็ครายการซ้ำ — "เลือกทั้งหมด" รวมคนแรกของกลุ่มด้วย ไม่ใช่แค่ตัวซ้ำส่วนเกิน:** ผู้ใช้กด "เลือกทั้งหมด (1,043)" แล้วกด "ลบที่เลือก" — ลบไปจริง **981 แถว** (ตรวจสอบยืนยันด้วย `deleted_at` ตรงกันทุกแถวจาก timestamp เดียวกันเป๊ะ พิสูจน์ว่าเป็นคำขอ bulk DELETE ครั้งเดียว) — **กู้คืนกลับคืนหมดแล้ว** (soft-delete เดิม แค่ตั้ง `deleted_at=null` กลับ ยืนยันจำนวนผู้ติดต่อกลับเป็น 2,642 คนเท่าเดิม)
      - **สาเหตุ:** ปุ่ม "เลือกทั้งหมด" เดิมเลือกทุกคนในกลุ่มที่ซ้ำ **รวมคนแรกของกลุ่มด้วย** — พอกดลบเลยลบทั้งกลุ่มไม่เหลือใครเลย ไม่ใช่แค่ตัวซ้ำเกินมาตามที่ผู้ใช้ต้องการ (เจตนาจริงคือ "ล้างของซ้ำ" ไม่ใช่ "ลบทุกคนที่เคยซ้ำ")
      - **แก้ 2 ทาง:** (a) เพิ่มปุ่ม **"✅ เลือกเฉพาะรายการซ้ำส่วนเกิน (เก็บอันแรกไว้เสมอ)"** เป็นตัวเลือกหลักเมื่อเปิดฟิลเตอร์ซ้ำ — เก็บผู้ติดต่อที่สร้างก่อน (เก่าสุดตาม `created_at` จริงจาก Postgres — เพิ่มฟิลด์นี้ใน `contacts-cleanup.js`'s response ใหม่ เพราะ `contactFromRow()` เดิมคืนแค่ `contact_created_at` string ไทยที่ parse ไม่แน่นอน) ของแต่ละกลุ่มไว้เสมอ เลือกแค่ส่วนเกินมาลบ — ปุ่ม "เลือกทั้งหมด" เดิมยังอยู่แต่เปลี่ยนชื่อชัดเจนว่า "เลือกทั้งหมดที่เห็น — รวมตัวแรกของแต่ละคู่ด้วย" (b) เพิ่มการยืนยันสองชั้นสำหรับลบเกิน 20 รายการ — ต้องพิมพ์ตัวเลขจำนวนที่จะลบให้ตรงเป๊ะก่อน (เหมือน pattern เดียวกับ `delete-shop.js` ที่ต้องพิมพ์ชื่อร้าน) แทนที่จะพึ่ง `confirm()` ธรรมดาที่คลิกผ่านง่ายไป
      - **ทดสอบยิงจริง:** สร้าง 2 contact ชื่อ+เบอร์ซ้ำกัน (ห่างกันสร้าง ~1.2 วินาที) ยืนยันว่า `created_at` เรียงลำดับถูกต้อง (endpoint คืนค่าตรงกับลำดับสร้างจริง ทำให้ปุ่มเลือกอัตโนมัติใหม่จะเก็บตัวแรกไว้ถูกต้องเสมอ) — ลบข้อมูลทดสอบสะอาดหลังทดสอบ
    - **(2) บั๊กนำเข้าสินค้า Excel ใส่ VAT เดียวกันหมดทุกแถว:** ผู้ใช้แนบไฟล์จริง (`บริษัท สยาม โกลบอล เน็ทเวิร์ค เอ็นเตอร์ไพรส์ จำกัด_product.xlsx`) ชี้ว่า "สินค้าบริการตัวไหนไม่มี VAT ไม่ใช่จับใส่รวม VAT หมดทุกอัน" — ตรวจสอบไฟล์จริงด้วย `xlsx` library โดยตรง (ยืนยันว่าไม่มีบั๊ก encoding ใดๆ ทั้งสิ้น การอ่านภาษาไทยถูกต้อง 100% — "ภาษาอะไรไม่รู้" ที่ผู้ใช้บ่นถึงคือ dropdown จับคู่คอลัมน์แสดงชื่อ header ดิบภาษาอังกฤษล้วนจากไฟล์ (BarCode/UnitPriceWithVat ฯลฯ) ไม่ใช่ข้อมูลเพี้ยน) พบรูปแบบชัดเจน: ไฟล์มีคอลัมน์ราคาคู่กันเสมอ (`UnitPrice`/`UnitPriceWithVat`) — รายการขายจริง 28 รายการมีอัตราส่วน `UnitPriceWithVat/UnitPrice` = 1.07 เป๊ะ (VAT 7%) ส่วนรายการ **"ค่ามัดจำถังก๊าซ" 6 รายการมี `UnitPrice === UnitPriceWithVat` เป๊ะ** (ไม่มี VAT เพราะเป็นเงินมัดจำที่คืนได้ ไม่ใช่การขาย) — เดิม `pos.js` ใช้ปุ่ม VAT เดียวกันกับทุกแถวในไฟล์ (ตั้งตามคอลัมน์ราคาที่จับคู่ไว้ครั้งเดียว) ทำให้รายการมัดจำถูกตั้งเป็น "รวม VAT แล้ว" ผิดไปด้วย
      - **แก้:** เพิ่ม `findVatCounterpartHeader()` หาคอลัมน์ "ก่อน VAT" คู่กับคอลัมน์ราคาที่จับคู่ไว้อัตโนมัติ (ตัดคำว่า "withvat" ออกจากชื่อคอลัมน์ปกติแล้วหาคอลัมน์ที่ชื่อเหลือตรงกัน) + `detectRowVatType()` เทียบอัตราส่วนราคาก่อน/หลัง VAT **ต่อแถว**: ~1.00 = "ไม่มี VAT", ~1.07 = "รวม VAT แล้ว", เทียบไม่ได้ (ไม่มีคอลัมน์คู่/ราคาเป็น 0) = ใช้ปุ่มด้านบนเป็นค่า fallback เหมือนเดิม — ปุ่ม VAT เดิมเปลี่ยนความหมายจาก "บังคับใช้ทุกแถว" เป็น "ค่าเริ่มต้นเฉพาะแถวที่เทียบอัตโนมัติไม่ได้เท่านั้น" (label ในหน้าจอแก้ให้ตรงด้วย) — เพิ่มสรุปผลให้เห็นก่อนกดนำเข้าจริง (กี่รายการมี VAT/ไม่มี VAT/ใช้ค่าเริ่มต้น) แทนข้อความเดิมที่บอกแค่เหตุผลของค่าเดียวทั้งไฟล์
      - **ทดสอบยิงจริงกับไฟล์ที่ผู้ใช้แนบมาโดยตรง** (จำลอง pipeline ทั้งหมดนอก React ด้วย Node script เรียกฟังก์ชันชุดเดียวกับใน `pos.js`): ตรวจถูกต้องครบ 6 รายการมัดจำ = "ไม่มี VAT", 28 รายการขายจริง = "รวม VAT แล้ว", 1 รายการ "ส่วนลดท้ายบิล" (ราคา 0 เทียบอัตราส่วนไม่ได้) = fallback ไปใช้ค่าเริ่มต้นถูกต้องตามที่ออกแบบไว้
    - **Deploy production แล้ว (2026-08-05)** — revision จริง `smileslip-dashboard-00292-v58` (ข้อความ deploy โชว์ revision เดิม `00291-bws` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — verified บน production จริงว่า `contacts-cleanup.js` คืนฟิลด์ `created_at` ถูกต้องแล้ว

60. **3 บั๊กที่ผู้ใช้เจอทันทีตอนใช้งานจริง — เพิ่มผู้ติดต่อไม่ได้, นำเข้าผู้ติดต่อ "ภาษาอะไรไม่รู้" เหมือนสินค้า, นำเข้าแบบ Excel ไม่ได้:**
    - **(1) เพิ่มผู้ติดต่อไม่ได้ — error ดิบจาก Postgres:** ผู้ใช้รายงาน "ขึ้นเรื่องเลขอะไรไม่ตรงซักอย่าง" — ทดสอบยิงจริงจำลอง payload เดียวกับที่ฟอร์มเว็บส่งจริง (จาก `emptyContactForm()`) ยืนยัน error ตรงกับที่ผู้ใช้เจอ 100%: `invalid input syntax for type numeric: ""` — **สาเหตุ:** `emptyContactForm()` ตั้งค่าเริ่มต้นของช่องตัวเลข (`debt`/`cylinders`/`cylinder_limit`) เป็นสตริงว่าง `''` เสมอถ้าไม่ได้กรอก แต่ `api/pos/contacts.js`'s POST/PATCH ส่งค่าดิบเข้า Postgres (คอลัมน์ numeric) ตรงๆ โดยไม่แปลงก่อน — Postgres ปฏิเสธค่าว่างสำหรับ numeric column ด้วย error ภาษาอังกฤษล้วนที่ผู้ใช้ทั่วไปอ่านไม่เข้าใจ (bulk-import path เดิมไม่มีปัญหานี้เพราะใช้ `c.debt || 0` อยู่แล้ว — บั๊กนี้มีแค่ใน POST/PATCH เดี่ยว)
      - **แก้:** แปลงเป็นตัวเลขจริงด้วย `parseFloat(x) || 0` ก่อนเช็ค validation และก่อน insert/update เสมอทั้ง 2 endpoint — ทดสอบยิงจริงกับ dev server ยืนยันว่าค่าว่างบันทึกเป็น 0 ถูกต้อง ไม่ error อีกต่อไป (ทดสอบทั้ง POST เพิ่มใหม่ และ PATCH แก้ไข)
    - **(2)+(3) นำเข้าผู้ติดต่อ:** ตัวนำเข้าผู้ติดต่อ (`showImportModal`/`handleImportFile` ใน `pos.js`) เป็นโค้ดคนละชุดที่เก่ากว่าตัวนำเข้าสินค้าที่เพิ่งสร้างในข้อ 58/59 มาก — ไม่เคยได้รับการอัปเดตตาม จึงมีบั๊กเดิมทั้งสองแบบที่เพิ่งแก้ไปแล้วฝั่งสินค้า:
      - **dropdown จับคู่คอลัมน์แสดงชื่อ header ดิบภาษาอังกฤษ** ไม่มีตัวอย่างค่ากำกับ (สาเหตุเดียวกับที่ผู้ใช้บ่นตอนนำเข้าสินค้าในข้อ 59) — แก้โดยเพิ่มตัวอย่างค่าจริงจากแถวแรกกำกับชื่อคอลัมน์ เหมือนที่แก้ไปแล้วในหน้านำเข้าสินค้าเป๊ะ
      - **ไม่รองรับไฟล์ Excel เลย** — `handleImportFile` เดิมรองรับแค่ VCF (รายชื่อโทรศัพท์) กับ CSV เท่านั้น ใช้ `parseCSVText` ของตัวเองที่สมมติว่าแถวแรกสุดคือ header เสมอ (ไม่รองรับไฟล์ export จากระบบบัญชีที่มีแถวหัวเรื่อง/แถวว่างนำหน้าแบบ FlowAccount) — แก้โดยเปลี่ยนมาใช้ตัว parser ร่วมกับสินค้า (`buildProductImportFromRawRows`/`csvTextToRawRows` — ชื่อฟังก์ชันเหลือคำว่า "Product" จากตอนสร้างให้สินค้าก่อน แต่ตัว logic เป็น generic ล้วนๆ ไม่ผูกกับสินค้าเลย) เพิ่ม branch อ่านไฟล์ Excel ผ่าน dynamic import `xlsx` แบบเดียวกับหน้านำเข้าสินค้า — ลบ `parseCSVText` เดิมที่กลายเป็น dead code ออก (verified ไม่มีที่เรียกใช้อื่นก่อนลบ)
      - **ทดสอบ:** จำลองไฟล์ Excel สังเคราะห์ (title row + blank row + header ภาษาอังกฤษ + ข้อมูลไทย ตรงกับรูปแบบไฟล์จริงที่เจอในข้อ 58/59) ผ่าน pipeline เดียวกับที่ `pos.js` เรียกจริง (`XLSX.read` → `sheet_to_json({header:1})` → `buildProductImportFromRawRows` → `autoDetectMapping`) — parse ถูกต้องครบ ข้าม title/blank row อัตโนมัติ ชื่อไทยไม่เพี้ยน จับคู่คอลัมน์ถูกต้อง
    - **Deploy production แล้ว (2026-08-05)** — revision จริง `smileslip-dashboard-00293-4tn` (ข้อความ deploy โชว์ revision เดิม `00292-v58` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — **verified บน production จริง**: ยิงเคสบั๊กเดิม (เพิ่มผู้ติดต่อด้วย payload ที่มีช่องตัวเลขเป็นค่าว่าง เหมือนที่ฟอร์มเว็บส่งจริง) กับ production URL ตรง สำเร็จ 200 ไม่มี error อีกต่อไป — ลบข้อมูลทดสอบออกสะอาด

61. **QA Audit ทั้งระบบ (ทุกหน้า ไม่ใช่แค่ POS) ตามคำขอผู้ใช้ "เช็คทั้งระบบจุดไหนมีปัญหาไม่เชื่อมต่อ เหมือนเป็นผู้ใช้ใหม่ ตั้งแต่ซื้อแพ็กเกจจนถึงใช้แดชบอร์ดจริง" — เจอ 9 บั๊ก แก้ครบทุกข้อ deploy แล้ว:**
    - **วิธีตรวจ:** เบราว์เซอร์ทดสอบของเครื่องมือติด `router.isReady` ไม่ทำงานสำหรับหน้าที่ผูก query string ซ้ำอีกครั้ง (ทดสอบทั้ง direct navigate และ client-side click ก็ติดเหมือนกัน — ยืนยันเป็นข้อจำกัดระดับเครื่องมือ ไม่ใช่บั๊กแอป) ตรวจผ่านโค้ด + cross-reference API endpoint แทนทั้งหมด ยกเว้น landing/pricing ที่ทดสอบผ่านเบราว์เซอร์จริงได้ (สองหน้านี้ไม่ผูก query string)
    - **🔴 ข้อ 1 (ร้ายแรงสุด) — แดชบอร์ด "บัญชี" ไม่เห็นรายการใหม่เลยสักรายการ:** Phase 3 Tier 6 (ข้อ 55) ตัดบอทไม่ให้เขียน Google Sheets แล้ว (เขียน `ledger_transactions` อย่างเดียว) แต่ 4 endpoint ฝั่งเว็บยังอ่าน/เขียน Sheets ตรงๆ ไม่เคยถูกย้ายตาม: `api/sheets/transactions.js` (ตาราง "บัญชี"), `api/export/excel.js`, `api/sheets/tax-report.js`, `api/sheets/add-transaction.js` (ปุ่ม "บันทึกเอง" เขียน Sheets อย่างเดียว ไม่เข้า `ledger_transactions` เลยด้วยซ้ำ — คนละที่เก็บกับที่บอทใช้) — มีแค่ `api/sheets/update-transaction.js` (แก้ไขรายการเดี่ยว) ที่ถูกย้ายไปแล้วตั้งแต่ Tier 5.5 — **แก้:** เพิ่ม `lib/ledger-supabase.js` (helper กลาง: `bangkokYearMonthRangeISO` กันบั๊ก timezone double-shift แบบเดียวกับที่บอทเจอมาก่อน, `rowToLedgerRow`, `fetchLedgerRows`, `parseThaiDateTimeToUTC`) ย้ายทั้ง 4 endpoint มาอ่าน/เขียน `ledger_transactions` ที่เดียวกับบอท + เพิ่ม owner-session auth ที่ `add-transaction.js` ขาดไป — **ตั้งใจไม่ backfill ข้อมูลเก่า** (ธรรมเนียมเดียวกับทุกตารางที่ย้ายมาก่อนหน้านี้) รายการก่อน Tier 6 deploy จะไม่โผล่ในเว็บอีกต่อไป ยังดูได้จาก Google Sheet ตรงๆ ผ่านลิงก์เดิมเสมอ — ทดสอบยิงจริงกับ dev server ครบ 21 จุด (บันทึกเอง → โผล่ในตาราง → export Excel ถูกต้อง → รายงานภาษี JSON/CSV/XLSX/PDF คำนวณถูกต้อง → เปิดแก้ไขผ่าน `update-transaction.js` เจอ cross-check ความสอดคล้อง)
    - **🔴 ข้อ 2 — ปุ่ม "ลบร้าน" ของแอดมินบริษัทไม่ยกเลิก Stripe + ไม่ลบข้อมูล POS เลย:** `api/admin/update-shop.js`'s `delete_shop` action มี logic แยกของตัวเอง ลบแค่ 5 ตาราง (`shop_admins`/`shop_credits`/`shop_google_configs`/`shop_bank_accounts`/`shop_branches`) ไม่แตะ Stripe เลย ต่างจาก `api/shop/delete-shop.js` (ที่ลูกค้าลบร้านตัวเองใน `register.js` ใช้ — ยกเลิก Stripe + ลบ 17 ตาราง) — ไม่เคยอัปเดตตามหลังระบบ POS ถูกสร้าง **ผลคือถ้าแอดมินกดลบร้านที่ยังมี subscription active ลูกค้าจะโดนเก็บเงินต่อไปเรื่อยๆ ทั้งที่ร้านหายไปแล้ว แถมข้อมูล POS ทั้งหมดค้างอยู่ในระบบตลอดไป** — ตรวจเพิ่มพบว่าแม้ `delete-shop.js` เองก็ล้าสมัยแล้ว (เขียนก่อน POS migration เสร็จสมบูรณ์ ไม่มี `pos_products`/`pos_sales`/`ledger_transactions` ฯลฯ ในลิสต์) — **แก้:** เพิ่ม `lib/shop-deletion.js` เป็นจุดเดียวที่ทั้ง 2 endpoint เรียกใช้ร่วมกัน อัปเดตรายชื่อตารางเป็น 33 ตาราง (grep หาทุก `.from('pos_*'/'shop_*'/...)` จริงในโค้ดแทนเดาจากความจำ) — ทดสอบยิงจริง: สร้างร้านทดสอบ หว่านข้อมูลใน 6 ตารางตัวแทน (เก่า+ใหม่) เรียก `deleteShopCompletely()` ตรงๆ ยืนยันลบครบทุกตารางถูกต้อง
    - **🟡 ข้อ 3 — ปุ่ม "ดูรายการ" ในหน้า Home ของแดชบอร์ดกดแล้วไม่ไปไหน:** `dashboard.js` เรียก `setActiveTab('ledger')` แต่ไม่มีแท็บชื่อนี้จริง (ต้องเป็น `'accounts'` — `'ledger'` เป็นแค่ชื่อ sub-tab ข้างใน) — แก้เป็น `setActiveTab('accounts')`
    - **🟡 ข้อ 4 — แท็บ "ลูกค้า" ในระบบ Delivery (`/delivery`) เข้าไม่ถึงเลย:** มีโค้ดทั้งแท็บ (ค้นหา+ดูประวัติออเดอร์ลูกค้า) สมบูรณ์แต่ไม่มีปุ่มไหนพาไปถึง — เพิ่มปุ่ม `['customers','👥 ลูกค้า']` เข้ารายการแท็บ
    - **🟡 ข้อ 5 — ออกจากระบบ (`/logout`) ไม่ล้าง owner-session:** ล้างแค่ LIFF session ของ LINE ไม่เคยเรียก `clearOwnerSessionToken()` เลย (ฟังก์ชันมีอยู่แล้วแต่ไม่มีใครเรียกทั้งโปรเจกต์) — เพิ่ม `clearAllOwnerSessionTokens()` (ล้างทุก `owner_session_*` key ใน localStorage เพราะหน้า logout ไม่รู้ shopId เจาะจง) เรียกใน `logout.js`
    - **🟡 ข้อ 6 — หน้าตัดสินใจตอนสมัครซ้ำไม่บันทึก session ตอนกดเข้าร้านเดิม:** `register.js`'s `goToExistingShop()` ไม่เรียก `setOwnerSessionToken()` ต่างจาก `login.js`'s `goToRole()` ที่ทำถูกต้อง — เพิ่มให้ตรงกัน
    - **🟢 ข้อ 7-9 (เล็กน้อย):** หน้า "ช่วยเหลือ" ในแดชบอร์ดขาดคำสั่ง `#สมัครพนักงาน`/`#สมัครผู้จัดการสาขา` (เพิ่มแล้ว), ลบ `api/admin/pos-stats.js` (dead code ไม่มีใครเรียกใช้), แก้ข้อความใน `transaction/edit.js` ที่ยังอ้างอิง "Google Sheets" ทั้งที่ย้ายไป Supabase แล้ว (Tier 5.5)
    - **ยืนยันว่าไม่มีปัญหา (ตรวจแล้ว):** landing/pricing (ทดสอบเบราว์เซอร์จริง), `login.js` (เขียนดีที่สุดในบรรดาหน้า auth ใช้เป็นต้นแบบได้), โครงสร้างแท็บ/tier-gating ของ `pos.js`/`pos-staff.js`, การย้าย Sheets→Supabase ของโมดูล POS เอง (`reports.js`/`export.js` ไม่มี Sheets เหลือแล้วจริง — คนละปัญหากับข้อ 1 ที่เป็นระบบเก่า/บัญชีหลักของบอทคนละส่วน), `order.js`, endpoint ทุกจุดที่หน้าเว็บเรียกมีไฟล์จริงรองรับครบ
    - **Deploy production แล้ว (2026-08-06)** — commit แยก 3 ก้อน (ข้อ 3-9, ข้อ 2, ข้อ 1) push ครบก่อน deploy รวมทีเดียว — revision จริง `smileslip-dashboard-00294-96z` (ข้อความ deploy โชว์ revision เดิม `00293-4tn` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` พบว่า traffic ยังค้างที่ revision เก่าแม้ build ใหม่สำเร็จแล้ว ต้อง `gcloud run services update-traffic` เองอีกตามเคย) — **verified บน production จริง**: `transactions.js` คืน `connected:true` เสมอแล้ว (อ่านจาก Supabase ไม่ต้องเชื่อม Google), `admin/pos-stats.js` หายไปจริง (404), `delete-shop.js` ยัง validate ถูกต้องหลัง refactor

62. **โอนย้ายสต็อกข้ามสาขา (Cross-Branch Stock Transfer) — ผู้ใช้ขอ "ทำเต็มรูปแบบเลยแต่ต้องแบ่งเป็นเฟสเป็นเฟสไปเพราะว่าจะได้ไม่กระทบ ต้องดูทุกโลจิกอย่าให้มันผิดพลาด" — Phase 0-5 เสร็จสมบูรณ์ครบทุกเฟส deploy production แล้ว ไม่เหลือเฟสค้าง:**
    - **ปัญหาที่พบก่อนเริ่ม:** ระบบไม่มีแนวคิด "สต็อกแยกตามสาขา" เลยมาก่อน — `pos_products.stock`/`at_customer`/`empty_waiting` เป็นตัวเลขก้อนเดียวต่อ SKU ต่อร้าน ไม่ว่าจะขาย/รับสินค้า/ยืมคืนที่สาขาไหนก็หักจากตัวเลขเดียวกันหมด (คอลัมน์ `branches` เดิมเป็นแค่ "ขายได้ที่สาขาไหนบ้าง" ไม่ใช่จำนวนคงเหลือต่อสาขา — คนละเรื่อง) — ผู้ใช้เลือกทำแบบเต็มรูปแบบ (สต็อกแยกจริง ไม่ใช่แค่ log การโอนที่ตัวเลขไม่ตรงความจริง) โดยแบ่งเฟส วางแผนละเอียดผ่าน Plan Mode ก่อนเริ่ม (ไฟล์แผน `partitioned-coalescing-candle.md`)
    - **สถาปัตยกรรม:** ตารางใหม่ `pos_product_stock` (shop_id, sku, branch_name, qty, at_customer, empty_waiting) เป็น source of truth ต่อสาขา — `branch_name=''` (สตริงว่าง) = "ยังไม่ระบุสาขา/กองกลาง" ตรงกับที่แอปส่ง `selectedBranch?.branch_name || ''` มาตลอดอยู่แล้ว ร้านสาขาเดียว (ไม่มีแถวใน `shop_branches` — กรณีส่วนใหญ่เพราะ tier normal/pro จำกัดแค่ 1 สาขา) ใช้แถวนี้แถวเดียวตลอดไป แทบไม่ถูกกระทบเลย — `pos_products.stock/at_customer/empty_waiting` ยังอยู่เป็น **cache ผลรวม (SUM) ข้ามสาขา** ทุกจุดที่อ่านอย่างเดียว (reports.js, export.js, customer-orders.js, การ์ดสินค้าหน้าขาย) ไม่ต้องแก้เลย
    - **ตัดสินใจสำคัญที่สุด — ไม่ใช้ Postgres trigger sync cache:** วางแผนแรกจะใช้ trigger คำนวณ SUM อัตโนมัติ แต่ Plan agent ที่ขอ critique ก่อนเริ่มจับได้ว่าระหว่างช่วง rollout ที่ยังแก้ไม่ครบทุกจุด trigger จะ SUM จากจุดที่ยังไม่รู้จักการเปลี่ยนแปลงแล้วเขียนทับตัวเลขที่จุดอื่นเพิ่งเขียนไปเงียบๆ — เป็นบั๊ก "logic อยู่คนละที่ไม่ sync กัน" แบบเดียวกับที่เจอมาแล้ว 2 ครั้งในโปรเจกต์นี้ (ledger Sheets/Supabase, admin/customer delete-shop) แค่ย้ายไปเป็นเวอร์ชัน DB-level ที่ debug ยากกว่า — แก้เป็น **`lib/pos-stock.js`'s `adjustBranchStock()` เป็นจุดเดียว (choke point)** ที่ทั้งเขียนแถว branch และ sync cache กลับ `pos_products` ในคำเรียกเดียวกัน (app-level ไม่มี trigger) จุดที่ยังไม่ถูกแก้ในช่วง rollout ก็แค่ยังเขียน `pos_products` ตรงๆ แบบเดิม ไม่มีอะไรมาเขียนทับ ปลอดภัยตลอด rollout
    - **Phase 0 (schema + helper, ไม่กระทบพฤติกรรมเดิมเลย):** `scripts/branchstock-01-create-tables.sql` (`pos_product_stock` + `pos_stock_transfers` log table) + `scripts/branchstock-02-backfill.sql` (1 แถวต่อ SKU ที่มีอยู่จริงเข้า `branch_name=''` ด้วยค่าปัจจุบันเป๊ะ, `ON CONFLICT DO NOTHING` กันรันซ้ำทับข้อมูลจริง) — ผู้ใช้รันแล้ว, verified กับ D Gas จริง 23/23 SKU ตรงกันเป๊ะ — `lib/pos-stock.js` (ใหม่): `getBranchStock`, `getStockBreakdown`, `syncProductCache`, `adjustBranchStock(shopId,sku,branchName,{qtyDelta,atCustomerDelta,emptyWaitingDelta})` (clamp `Math.max(0,...)` เหมือนโค้ดเดิมทุกจุด)
    - **Phase 1 (แก้ 6 ไฟล์ที่เขียนสต็อกทั้งหมดให้แยกตามสาขา):** `sales.js`/`receives.js` (มี `branch` ใน request อยู่แล้ว), `products.js` (`receive-back`/`refill`/PATCH ทั่วไป — **ต้องเพิ่ม `branch` field ใหม่** เพราะไม่เคยมีเลย, `pos.js` ส่งจาก `selectedBranch` เดียวกับที่ทั้งแอปใช้อยู่แล้ว), `delivery.js`/`collections.js` (ไม่มี `branch` ในตัวเอง — ใช้ `pos_staff.branch_name` ของพนักงานที่ยืนยันแทน ผ่าน `getSessionStaffId(req)` จาก signed staff-session ปลอมไม่ได้), `loans.js` (POST มี `branch` แล้ว, PATCH คืนของใช้ `branch_name` ที่บันทึกไว้ตอนสร้างใบยืม ไม่ใช่ branch ปัจจุบันของ request)
      - **บั๊กจริงที่เจอระหว่างทำ (ไม่ได้อยู่ในแผนเดิม แต่จำเป็นต้องแก้):** `products.js`'s POST (ทั้งสร้างเดี่ยวและ bulk import) เขียน `stock` เริ่มต้นเข้า `pos_products` ตรงๆ โดยไม่เคย seed แถว `pos_product_stock` คู่กัน — เพราะ `syncProductCache` ถือว่า `pos_product_stock` เป็น source of truth เดียวเสมอ สินค้าที่สร้างใหม่พร้อมสต็อกเริ่มต้นจะถูกรีเซ็ตเป็น 0 เงียบๆ ทันทีที่มีการเขียนสต็อกแบบ branch-aware ครั้งแรก (เช่น ขายครั้งแรก) — แก้โดยเพิ่ม seed logic ทั้ง 2 จุด
      - **ทดสอบ:** 46 assertions ครอบคลุมทั้งร้านหลายสาขา (แยกกันจริง ไม่ปนกัน) และร้านสาขาเดียว (ตัวเลขตรงเป๊ะเหมือนก่อนแก้) — deploy production แล้ว (revision `smileslip-dashboard-00295-99s`), production-verified ด้วยชุดทดสอบแยกต่างหาก
    - **Phase 2 (ฟีเจอร์โอนย้ายจริง — ทำหลัง Phase 1 ครบเพราะถ้าทำก่อนตัวเลขต่อสาขาจะไม่ตรงความจริงสำหรับ SKU ที่ยังผ่านจุดแบบเก่าอยู่):**
      - `transferBranchStock()` ใหม่ใน `lib/pos-stock.js` — ต่างจาก `adjustBranchStock()` ตรงที่ **บล็อกถ้าสต็อคต้นทางไม่พอ** (ไม่ clamp เงียบๆ แบบขาย เพราะเป็น action ของแอดมิน ความถี่ต่ำ และ "ย้ายของที่ไม่มีจริง" เป็นไปไม่ได้ทางกายภาพ ไม่ใช่การตัดสินใจทางธุรกิจแบบการขาย) และใช้ **optimistic locking** (`UPDATE ... WHERE qty = <ค่าที่เพิ่งอ่าน>` retry สูงสุด 5 ครั้งถ้าชนกับคำขออื่นที่แก้สต็อคสาขาเดียวกันพร้อมกัน) เฉพาะฝั่งหักสาขาต้นทาง — ฝั่งเพิ่มปลายทางเรียก `adjustBranchStock()` ตามปกติ (บวกเข้าไม่มีทางติดลบ/ชนกันจนพังได้)
      - `pages/api/pos/stock-transfers.js` (ใหม่) — GET: ประวัติการโอน + breakdown สต็อกปัจจุบันต่อสาขาถ้าระบุ `sku`; POST: insert แถว `pos_stock_transfers` สถานะ `pending` ก่อนเสมอ → เรียก `transferBranchStock` → `committed` ถ้าสำเร็จ, `failed` ถ้าสต็อคไม่พอ (log ไว้เป็น audit trail แม้ถูกบล็อก), ปล่อยค้าง `pending` ถ้าล้มเหลวกลางทาง (เช่น ชนกันเกิน retry limit) ให้ตรวจสอบด้วยมือแทนการเดาว่าล้มเหลวสะอาดๆ
      - UI ใหม่ในแท็บ "สินค้า" ของ `pos.js` — ปุ่ม "🔄 โอนย้าย" ต่อสินค้า (เฉพาะร้านที่มีมากกว่า 1 สาขาและสินค้าประเภท "นับสต็อค") → modal เลือกสาขาต้นทาง/ปลายทาง/จำนวน/หมายเหตุ + breakdown ต่อสาขา + ประวัติการโอนแบบพับเก็บได้
      - **ทดสอบ:** 16 assertions (โอนปกติ, บล็อกสต็อคไม่พอ, ปฏิเสธสาขาเดียวกัน, invariant "ยอดรวมทั้งร้านไม่เปลี่ยนเลยก่อน-หลังโอน", เคส sentinel `''`) — commit+push สำเร็จ
      - **เหตุการณ์ตอน deploy:** `gcloud run deploy` ล้มเหลวด้วย `429 RESOURCE_EXHAUSTED` จาก Cloud Build API เอง (ชนโควต้า "Build and Operation Get requests per minute" เพราะ deploy ติดกันหลายรอบในเซสชันเดียว) — ตรวจสอบพบว่า **build จริงสำเร็จ (SUCCESS) อยู่เบื้องหลัง** เป็นแค่ CLI ที่ polling สถานะไม่ได้เท่านั้น ไม่ใช่ build พังจริง — deploy ซ้ำด้วย `--image=<digest จาก build ที่สำเร็จ>` (ข้าม rebuild) สร้าง revision ใหม่ `smileslip-dashboard-00296-fbz` จริง **แต่ข้อความ "Done...serving 100 percent" ของ deploy รอบนี้โชว์ชื่อ revision เก่าผิดอีกครั้ง** (`00295-99s` ของ Phase 1) — หลงเชื่อไปครั้งแรกจน production verification เจอ 404 (route ไม่มีจริงเพราะ traffic ยังชี้ revision เก่า) ก่อนจะเช็ค `gcloud run revisions list`/`gcloud run revisions describe` เทียบ image digest จริงแล้วพบว่า revision ใหม่ถูกสร้างจริงแค่ traffic ไม่ได้ตามไปด้วย — pin traffic เองด้วย `gcloud run services update-traffic` แล้ว production verification ผ่านครบ (15/15 assertion ที่ตั้งไว้ถูกต้อง ยกเว้น 1 จุดที่เป็นบั๊กในสคริปต์ทดสอบเองไม่ใช่แอป — คาดว่า transfer ที่ถูกบล็อกจะไม่ถูกบันทึกในประวัติเลย แต่จริงๆ ระบบบันทึกไว้เป็น `status:'failed'` ถูกต้องแล้วเพื่อ audit trail)
      - **บทเรียนย้ำอีกครั้ง (ตรงกับข้อควรระวัง #35 เป๊ะ แต่คราวนี้ซับซ้อนกว่าเดิม):** นอกจาก "traffic ค้างที่ revision เก่า" แล้ว ยังเจอเคส **revision ใหม่ถูกสร้างจริงแต่ deploy command เองรายงาน error ผิด (429 จาก polling ไม่ใช่จาก deploy จริง)** — ต้องตรวจทั้ง `gcloud builds describe <id> --format="value(status)"` (ดูว่า build จริงสำเร็จไหม) และ `gcloud run revisions list --sort-by=~metadata.creationTimestamp` (ดูว่ามี revision ใหม่จริงไหม) คู่กันเสมอเมื่อ deploy command แสดง error ที่ดูเหมือนเป็นปัญหาเครือข่าย/quota ไม่ใช่ error จากโค้ด — ห้ามด่วนสรุปว่า deploy ล้มเหลวจริงแล้วรีบ retry ทันที (เสี่ยง build ซ้ำซ้อนโดยไม่จำเป็น)
    - **Deploy production แล้วทั้ง Phase 1+2 (2026-08-06)** — revision จริงล่าสุดตอนนั้น `smileslip-dashboard-00296-fbz`, traffic pin 100% แล้ว — verified บน production จริงทั้งคู่ด้วยร้านทดสอบชั่วคราว (สร้าง+ลบหลังทดสอบทุกครั้ง)
    - **Phase 3 (`pos-staff.js` จัดการสต็อกแยกตามสาขาจริง + แก้บั๊กแฝงที่เจอระหว่างทำ):**
      - เพิ่ม `getBranchStockMap(shopId, branchName)` ใน `lib/pos-stock.js` — bulk query คืน `Map<sku,{qty,at_customer,empty_waiting}>` ของทุก SKU ที่สาขาหนึ่งๆ (กัน N+1 query เวลาต้องแสดงสต็อกทั้งลิสต์พร้อมกัน) — ใช้แทนโค้ด inline เดิมใน `products.js`'s `branchStock` overlay ด้วย (refactor ให้เรียกใช้ร่วมกัน)
      - `products.js` GET เพิ่ม query param `branchStock` (รับค่าว่างได้ = กองกลาง) — แทนที่ `stock/at_customer/empty_waiting` ในผลลัพธ์ด้วยตัวเลขเฉพาะสาขานั้น + คืน `shop_total_stock` (ยอดรวมทั้งร้านเดิม) แนบมาด้วยเผื่อ UI ต้องใช้ — **ไม่ส่ง param นี้ = พฤติกรรมเดิมทุกประการ** (caller เดิมทั้งหมดไม่ถูกกระทบ)
      - `pos-staff.js`'s หน้า "📦 จัดการสต็อกสินค้า" — `openManage('stock')` ดึงด้วย `branchStock=${staffBranch}` แทนยอดรวมทั้งร้าน, `saveManageStock()` ส่ง `branch: staffBranch` ไปกับ PATCH, เพิ่ม label "📍 แสดง/แก้ไขสต็อกที่สาขา: X" ให้ชัดเจน — แก้ optimistic update หลังบันทึกสำเร็จด้วย (เดิมใช้ `d.stock` ที่ API คืนมาซึ่งเป็นยอดรวมทั้งร้าน ไม่ใช่ยอดสาขา ทำให้ตัวเลขกระโดดผิดทันทีหลังบันทึก — เปลี่ยนเป็นใช้ค่าที่พิมพ์เข้าไปเองแทน)
      - **บั๊กแฝงจริงที่เจอในฝั่งเจ้าของร้าน (`pos.js`) ระหว่างตรวจสอบ pattern เดียวกัน — แก้พร้อมกัน:** `openEditProd()` (ฟอร์มแก้ไขสินค้า) label บอก "สาขา: {selectedBranch}" แต่ prefill ค่า `stock` จาก `prod.stock` ซึ่งเป็นยอดรวมทั้งร้าน (จาก Phase 1 ที่ผ่านมา ไม่เคยแก้จุดนี้) ทำให้ร้านหลายสาขากดแก้ไขแล้วบันทึก จะคำนวณ delta ผิด (เทียบค่าที่พิมพ์กับยอดรวมทั้งร้าน ไม่ใช่ยอดของสาขานั้นจริง) สต็อกสาขานั้นเพี้ยนไปเงียบๆ — แก้โดยดึงยอดจริงต่อสาขาจาก `GET /api/pos/stock-transfers?shopId&sku` (breakdown ที่มีอยู่แล้วจาก Phase 2) มา prefill ก่อนเสมอถ้าร้านมีมากกว่า 1 สาขา (ร้านสาขาเดียวไม่กระทบเลย เพราะ `prod.stock` = สาขา `''` อยู่แล้วเป๊ะ)
      - **ทดสอบ:** 13 assertions (overlay ถูกต้องทั้งมี/ไม่มี `branchStock`, PATCH branch-aware ไม่กระทบสาขาอื่น, cache resync ถูกต้อง, breakdown endpoint ที่ `pos.js` ใช้ยืนยันค่าตรงกัน) — ผ่านครบก่อน commit
    - **Phase 4 (`reports.js`/`export.js` ตัวกรองสาขาให้รายงาน "สินค้าคงเหลือ" จริง):**
      - ตรวจพบว่า `reportBranch` state ใน `pos.js` **ไม่เคยมี UI ไหนเรียก `setReportBranch` เลยทั้งไฟล์** (ไม่ใช่แค่ inventory report — ตัวกรอง `branch` ที่มีอยู่แล้วใน backend ของ `sales`/`credit`/`loans`/`expenses`/`vat` types ก็ไม่เคยเข้าถึงได้จาก UI มาก่อนเลยเช่นกัน) — เพิ่ม dropdown "สาขา" ในการ์ดตัวกรองวันที่ของแท็บรายงาน (โผล่เฉพาะร้านที่มีมากกว่า 1 สาขา) ทำให้ตัวกรอง branch ของทุก report type ใช้งานได้จริงเป็นครั้งแรกพร้อมกันหมด ไม่ใช่แค่ inventory
      - `reports.js`/`export.js` type=`inventory` เพิ่มการเช็ค `branch !== undefined` (แยกจาก "ไม่ส่งมาเลย") — ถ้าระบุ (รวมค่าว่าง = กองกลาง) overlay สต็อกด้วย `getBranchStockMap()` ก่อนคำนวณ summary/low_stock/out_of_stock — **เจอ pattern บั๊กเดียวกับ Phase 3 อีกจุด:** `export.js`'s ทุกชีตมี label "สาขา: {branchLabel}" ที่หัวชีตอยู่แล้ว (`branchLabel = branch || branchName || shopName`) แต่ชีต "สินค้าคงเหลือ" ไม่เคยกรองตัวเลขตามนั้นจริงเลย (แสดงยอดรวมทั้งร้านเสมอไม่ว่า label จะบอกสาขาไหนก็ตาม) — แก้ให้ตรงกับ label จริง
      - `pos.js` เพิ่ม label "📍 แสดงสต็อกของ: X" ในหน้ารายงาน inventory และในหัว export modal ให้ตรงกับตัวกรองที่เลือกอยู่เสมอ
      - **ทดสอบ:** 21 assertions (overlay ถูกต้องทั้ง reports.js/export.js xlsx, summary/low_stock คำนวณถูกต้องตามสาขาที่กรอง, ยืนยัน `shop_total_stock` ยังอยู่ครบ, ร้านสาขาเดียว parity ไม่กระทบ)
    - **Deploy production Phase 3+4 แล้ว (2026-08-07)** — revision จริงตอนนั้น `smileslip-dashboard-00297-gx2` (ข้อความ deploy โชว์ revision เก่าผิดอีกตามเคย `00296-fbz` ของ Phase 1+2 — เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เองอีกครั้งตามธรรมเนียม) — **verified บน production จริงครบ 16/16**: `branchStock` overlay, PATCH branch-aware, `reports.js`/`export.js` inventory branch filter, และ regression-test Phase 2 (`stock-transfers.js`) ว่ายังทำงานถูกต้องหลัง deploy ทับ
    - **Phase 5 (แจ้งเตือนสต็อคไม่พอแยกตามสาขา — ผู้ใช้ขอ "ทำเลย" ให้ทำต่อทันทีหลัง Phase 4 เสร็จ):**
      - **ตัดสินใจสำคัญ (ไม่ถามผู้ใช้ ตัดสินใจเองจากหลักฐานในโค้ด):** เลือก **เตือนไม่บล็อค** ทั้งฝั่งขายและฝั่งเว็บสั่งซื้อ แทนการ "บล็อกการขายถ้าสต็อคไม่พอ" ตามที่ระบุไว้ในแผนเดิมตรงตัว — เหตุผล: `sales.js` มี pattern `warnings[]` ที่ใช้กับ below-cost pricing/เพดานเปล่ารอรีฟิล/วงเงินยืมอยู่แล้วทุกจุด **ไม่เคยบล็อคการขายเลยสักจุดในทั้งระบบ** (ตรงกับข้อ 39/40 ที่ตัดสินใจเรื่องราคาปลอมไว้แล้ว) และ `customer-orders.js` เป็นคิว "รอตรวจสอบ" ที่มีแอดมินรีวิวก่อนเสมออยู่แล้ว การบล็อกจะขัดกับ philosophy เดิมทั้งระบบและเสี่ยงพังธุรกิจจริงถ้าตัวเลขสต็อคที่บันทึกไว้ผิด (เช่น ลืมคีย์ของเข้า)
      - **`pos.js` แท็บขาย:** เพิ่ม `saleBranchStock` (state ใหม่, `Map<sku,{qty,at_customer,empty_waiting}>`) แยกจาก `products` (ยังคงยอดรวมทั้งร้านไว้เหมือนเดิมทุกจุดที่ไม่ใช่แท็บขาย — แท็บจัดการสินค้า/modal รับสินค้าคืน/modal โอนย้าย ไม่ถูกกระทบเลย) — `useEffect` sync ทุกครั้งที่เปลี่ยนสาขา/ร้าน/สินค้าเปลี่ยน — overlay เข้า `displayProducts` เฉพาะ `tab==='sell'` เท่านั้น ทำให้การ์ดสินค้าในแท็บขาย + `addToCart()`'s out-of-stock check ใช้ตัวเลขจริงของสาขาที่กำลังทำงานอยู่ (ไม่ใช่ยอดรวมทั้งร้าน) เป็นครั้งแรก
      - **`sales.js`:** ย้าย `const warnings = []` ขึ้นมาก่อน loop คำนวณ `actual_stock_deducted` (เดิมประกาศทีหลัง คนละจุด) เพิ่ม warning `"⚠️ ...สต็อคที่สาขา X มีไม่พอ (มี Y ขาย Z)"` เมื่อ `branchStock.qty < wantQty` — ยังคง clamp ที่ 0 เหมือนเดิมทุกประการ (ไม่บล็อค ไม่เปลี่ยนพฤติกรรมการขาย)
      - **`customer-orders.js`:** แต่ละ item ที่บันทึกเข้าคิวรอตรวจสอบแนบ `low_stock: true/false` (เทียบ `qty` กับ `getBranchStock()` ของสาขาที่ลูกค้าเลือก) — `pos.js` แสดงเป็นข้อความเตือนสีเหลืองในการ์ดคำสั่งซื้อที่แอดมินตรวจสอบ ไม่บล็อคลูกค้าจากการสั่งซื้อเลย
      - **ตั้งใจไม่ทำ:** LINE push notification สำหรับสต็อคต่ำ — ไม่มี pattern แบบนี้อยู่ในระบบมาก่อนเลย (ต่างจากวงเงินเครดิต/เพดานเปล่ารอรีฟิลที่เป็นแค่ warning ในหน้าเว็บเหมือนกัน) ถือว่าอยู่นอกสโคปของ Phase 5 เดิม — การแสดงผลแบบ branch-aware ในแท็บขาย (real-time) + รายงานสินค้าคงเหลือ (Phase 4, real-time ถ้ากรองสาขา) ถือว่าครอบคลุม "แจ้งเตือนสต็อคต่ำแยกตามสาขา" ตามที่แผนเดิมระบุไว้แล้ว
      - **ทดสอบ:** 12 assertions local (ขายเกินสต็อคสาขา → เตือน+clamp เป็น 0 ไม่ติดลบ, ขายพอดี → ไม่เตือน, สั่งซื้อออนไลน์เกินสต็อคสาขา → รับออเดอร์+flag ถูกต้อง, สั่งซื้อพอดี → ไม่ flag, ร้านสาขาเดียว parity ไม่มี false-positive) — commit+push สำเร็จ
    - **Deploy production Phase 5 แล้ว (2026-08-07)** — revision จริง `smileslip-dashboard-00298-2xz` (ข้อความ deploy โชว์ revision เก่าผิดอีกตามเคย `00297-gx2` ของ Phase 3+4 — เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เองอีกครั้งตามธรรมเนียม) — **verified บน production จริง 5/6** (1 จุดที่ "fail" เป็นบั๊กในสคริปต์ทดสอบเอง ไม่ใช่แอป: ทดสอบขายเกินสต็อคสาขา A ไปก่อนแล้วสต็อคสาขา A ถูกหักจริงเหลือ 0 ตามที่ควรจะเป็น แต่ assertion ถัดไปลืมอัปเดต expected value ยังคาดหวัง 2 เดิม) — ยืนยัน warning/flag ทำงานถูกต้องบน production จริง + regression-test Phase 3/4 ผ่านครบ
    - **สรุปสถานะสุดท้าย: โอนย้ายสต็อกข้ามสาขาเสร็จสมบูรณ์ครบทุกเฟสแล้ว (Phase 0-5) ตามที่ผู้ใช้ขอทั้งหมด** — ไม่เหลือเฟสใดค้างอยู่

63. **ปิดช่องโหว่ deep-link/owner-session ที่ค้างจากข้อ 57 + delivery.js หักสต็อคสินค้าปกติ — ผู้ใช้ยืนยัน scope ของ White-Label (ตรวจแล้วตรงตามที่คุยไว้ ไม่ต้องแก้), สั่งทำ 3 รายการความปลอดภัย + delivery.js fix ทันที ("ทำพวกนี้ก่อนที่เหลือค่อยว่ากัน"):**
    - **ยืนยัน White-Label ตามที่ผู้ใช้จำไว้ถูกต้อง (ตรวจโค้ดจริง ไม่ต้องแก้):** `lib/tier-features.js:27` — `white_label` gate ที่ `level >= TIER_LEVEL.enterprise` เท่านั้น เฉพาะ Enterprise/`super` ไม่มีชื่อ "Smile Slip Pro" ในเอกสาร (ใบเสร็จ/ใบกำกับภาษี PDF/รายงาน VAT PDF/ชื่อไฟล์+เนื้อหา Excel) แพ็กเกจอื่นทั้งหมดใส่ branding เป็นค่าเริ่มต้นเสมอ
    - **`delivery.js` confirm_delivery หักสต็อคสินค้าประเภท "นับสต็อค" ด้วย** — เดิมหักแค่สินค้าหมุนเวียน (`type==='หมุนเวียน'`) ตั้งแต่สร้างโมดูล delivery มา สินค้าทั่วไปไม่เคยถูกตัดสต็อคตอนยืนยันจัดส่งสำเร็จเลยสักครั้ง (ตอนสร้างออเดอร์ก็ไม่หัก ตั้งใจให้หักตอน confirm เหมือนสินค้าหมุนเวียน) — แก้โดยแยก branch ใหม่ในลูปเดิม: `หมุนเวียน` ใช้ตรรกะเดิมทุกประการ (at_customer/empty_waiting/audit log), ประเภทอื่น (ที่ไม่ใช่ `ไม่นับสต็อค`) หักตรงๆ ด้วย `adjustBranchStock({qtyDelta:-qty})` เท่านั้น ไม่มี audit log แบบหมุนเวียนเพราะไม่ใช่แนวคิด "แลกเปลี่ยน/ยืม" — ทดสอบ 8 assertions (หัก normal ถูกต้อง, cyclical ไม่ regression, service-type ไม่ถูกแตะ)
    - **Deep-link ของบอท LINE (~11 จุดใน `smileslip-pro/index.js`) เปลี่ยนจาก `?userId=${owner_line_id}` เปลือยๆ เป็นแนบ signed owner-session token คู่กัน:**
      - `lib/owner-session-sign.js` (ใหม่, บอท) — `issueDeepLinkToken(shopId, ownerLineId)`/`ownerDeepLink(shop, path, extraParams)` ใช้อัลกอริทึมเดียวกับ `lib/owner-session.js` ของ dashboard เป๊ะ (HMAC-SHA256, base64url, payload `{shopId,ownerId,role,exp}`) — ต้องแชร์ `OWNER_SESSION_SECRET` เดียวกันทั้งสองฝั่ง (คัดลอกจาก dashboard .env ไปใส่บอท .env แล้ว ไม่เคยพิมพ์ค่าจริงออกมาที่ไหนเลยระหว่างทำ) — TTL 24 ชม. (สั้นกว่า login-session 30 วัน เพราะเป็นลิงก์ที่ตั้งใจให้กดใช้ทันทีหลังบอทส่งข้อความ) — cross-verified แล้วว่า token ที่บอทออกผ่าน `verifyOwnerSession()` ของ dashboard ได้จริง (คนละ codebase ต้อง byte-for-byte เข้ากันได้)
      - **ค้นพบระหว่างทำ (ไม่ใช่แค่ปิดช่องโหว่ ยังแก้บั๊กจริงที่กระทบผู้ใช้จริงตอนนี้ด้วย):** `pos.js` (ไม่เหมือน `dashboard.js`) **ไม่เคยรองรับ consume `ownerSession` query param เลย** — แปลว่าเจ้าของร้านตัวจริงที่คลิกลิงก์บอทไป `/pos?userId=X` (ไม่เคย login ผ่าน `/login` มาก่อนในเครื่องนั้น) จะโดน redirect ไป `/login` ทันที (จาก guard ที่เพิ่มไว้ตั้งแต่ข้อ 57 Phase B) ทั้งที่ตั้งใจให้เปิด `/pos` ตรงได้เลย — แก้โดยเพิ่ม pattern เดียวกับ `dashboard.js` (`pendingOwnerSessionToken` → `setOwnerSessionToken` เมื่อรู้ `shopId` แล้ว) เข้า `pos.js` ด้วย
    - **4 endpoint ที่ไม่เคยบังคับ owner-session เพราะ chicken-and-egg (ข้อ 57) — วิเคราะห์แล้วตัดสินใจทำแบบ "validate-if-present" (`enforce:false`) ไม่ใช่ hard-`enforce:true`:**
      - **เหตุผลที่ไม่ทำ `enforce:true` เต็มรูปแบบ:** ไล่โค้ดจริงพบว่า `shop/data.js` (เรียกตอนยังไม่รู้ shopId เลย ใช้ userId lookup ก่อน) และ `pos/setup.js`/`pos/pos-config.js` GET (เรียกซิงโครนัสภายใน `loadShopBody()`/`init()` ก่อน React re-render ติดตั้ง fetch-override) ถ้า hard-block ตอนไม่มี token จะทำให้ cold-boot (bookmark ตรง หรือเบราว์เซอร์ใหม่ไม่มี token เลย) ค้างที่หน้า loading ตลอดไป **แทนที่จะ redirect ไป login ได้อย่างสะอาดแบบที่ทำได้อยู่ทุกวันนี้** — การแก้ให้ปลอดภัยเต็มที่ต้องรื้อ `init()`/`fetchData()`/`loadShopBody()` ให้ดัก 401 เองแทนพึ่ง effect ที่ผูกกับ `shopId` (ซึ่งไม่มีทาง set ได้ถ้า endpoint แรกสุดพังไปแล้ว) — ประเมินว่าเสี่ยงเกินไปที่จะทำแบบเร่งด่วนกับระบบ production ที่ผู้ใช้กำลังทดสอบจริงอยู่ ไม่ใช่แค่เขียนโค้ดเสร็จแล้วจบ
      - **สิ่งที่ทำแทน (ปลอดภัย 100%, ปิดช่องโหว่จริงที่สำคัญที่สุด):** เพิ่ม `requireOwnerAuth(req,res,shopId,{enforce:false})` ใน `shop/data.js`, `shop/referral.js`, `pos/setup.js` GET, `pos/pos-config.js` GET — "ไม่มี token = ผ่านเหมือนเดิมทุกประการ" (ไม่กระทบ cold-boot เลย) **แต่ "มี token ที่ shopId ไม่ตรงกับร้านที่ขอ" จะถูกบล็อกเสมอ (403)** — ปิดช่องโหว่หลักจริง: token ของร้าน A ใช้อ่านข้อมูลอ่อนไหวของร้าน B ไม่ได้อีกต่อไป (`shop/data.js` คืนบัญชีธนาคารเต็ม + `google_refresh_token` จริง, `pos-config.js` คืน `kbank_api_key`/`scb_api_key` จริง — สองไฟล์นี้อ่อนไหวกว่าที่คาดตอนแรกมาก)
      - **ทดสอบ:** 12 assertions ครบทุกไฟล์ (no-token=ผ่าน, matching-token=ผ่าน, cross-shop-token=403) + regression ยืนยันไม่กระทบ cold-boot
    - **`staff-setpin.js` — ต้องมี signed one-time-ish token คู่กับ `shopId+staff_id` เท่านั้น (ต่างจาก 4 endpoint ข้างบน ทำได้เป็น hard-required เต็มรูปแบบ เพราะไม่มีปัญหา cold-boot — ทางเดียวที่จะมาถึง endpoint นี้คือคลิกลิงก์ที่เพิ่งออกใหม่เสมอ):**
      - `lib/staff-setpin-token.js` (ใหม่) — `issueSetpinToken`/`verifySetpinToken` ใช้ `STAFF_SESSION_SECRET` เดียวกับ `lib/staff-session.js` (คนละ trust domain แต่ใช้ secret ร่วมกันแทนเพิ่มตัวใหม่โดยไม่จำเป็น) **แต่แยกฟังก์ชัน verify ต่างหากเด็ดขาด ห้ามใช้ `verifyStaffSession()` ปนกัน** เพราะ token นี้ออกให้ "ก่อน" มี PIN จริงด้วยซ้ำ ใส่ `purpose:'setpin'` กำกับกันสับสน/ใช้แทนกันในโค้ดจุดอื่นที่อาจเพิ่มในอนาคต — TTL 7 วัน (ยาวกว่า deep-link เพราะลิงก์ตั้ง PIN อาจค้างไม่ได้เปิดในแชทนานกว่า)
      - `staff.js`/`staff-requests.js`'s `sendPinSetupLink()` (คนละไฟล์ ฟังก์ชันซ้ำกันตามธรรมเนียมโปรเจกต์) แนบ `&token=` เข้าลิงก์ — `pos-staff.js` อ่าน `token` จาก query แล้วส่งต่อไปกับ POST body — `staff-setpin.js` **บังคับต้องมี token ที่ผ่าน verify เท่านั้น ไม่มี fallback** (ลิงก์เก่าก่อนแก้ใช้ไม่ได้แล้ว ตั้งใจ ไม่ใช่บั๊ก — แก้โดยกดปุ่ม "ส่งลิงก์ตั้ง PIN" ใหม่จากหน้าตั้งค่าพนักงาน)
      - **ทดสอบ 8 assertions ครอบคลุมช่องโหว่ที่ปิดตรงๆ:** ไม่มี token เลย → 401, token ถูกต้องของตัวเอง → ผ่าน+บันทึก PIN จริง, **token ที่ออกให้พนักงานคนอื่น (ไม่ใช่คนที่ระบุใน request) → 401 (นี่คือช่องโหว่หลักที่ปิด)**, token หมดอายุ → 401, `purpose`ผิด (จำลอง token ปลอมที่เดา field ผิด) → 401, ลายเซ็นถูกแก้ไข → 401
    - **Deploy production แล้วทั้งบอทและ dashboard (2026-08-07)** — บอท revision จริง `smileslip-service-00229-stk` (auto-deploy จาก CI/CD ที่แก้ไว้ตั้งแต่ข้อ 48 ทำงานถูกต้อง แต่ต้องเพิ่ม `OWNER_SESSION_SECRET` เข้า Cloud Run env vars ด้วยมือแยกต่างหากอีกที เพราะ CI/CD ไม่เคยรู้จัก secret ใหม่ที่ยังไม่เคยตั้งบน Cloud Run มาก่อน — ใช้ `--update-env-vars` ไม่ใช่ `--set-env-vars` กัน env vars เดิมหาย), dashboard revision จริง `smileslip-dashboard-00299-zrx` — ข้อความ deploy โชว์ revision เดิมผิดอีกตามเคยทั้งคู่ เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เองทั้งคู่ตามธรรมเนียม — **verified บน production จริงครบ 11/11**: cross-shop token blocked บนทั้ง `shop/data.js`/`pos-config.js`, cross-staff token attack blocked บน `staff-setpin.js`, regression ยืนยัน `delivery.js` หักสต็อคสินค้าปกติถูกต้องจริงบน production
    - **ปลด min-instances จาก 1 เป็น 0 บน `smileslip-service` (บอท)** — ผู้ใช้ตัดสินใจว่า cold start ไม่กี่วินาทีไม่กระทบ ประหยัดค่าใช้จ่ายดีกว่า — เปลี่ยนแค่ service-level scaling annotation ไม่สร้าง revision ใหม่เลย (ยืนยันจาก creation timestamp เดิมของ revision ที่ยังคง active อยู่)
    - **สถานะ testimonials:** ผู้ใช้แจ้งว่าลงข้อมูลจริงในหน้าเซลเพจแล้วเอง — ไม่ต้องทำอะไรเพิ่ม
    - **ยังไม่ได้ทำ (out of scope ของ Approach A ข้างบน โดยตั้งใจ, ต้องการ redesign เพิ่มถ้าจะทำ):** `enforce:true` เต็มรูปแบบสำหรับ 4 endpoint ที่มีปัญหา chicken-and-egg — ต้องรื้อ `init()`/`fetchData()`/`loadShopBody()` ให้ดัก 401 เองก่อน ไม่ใช่พึ่ง effect ที่ผูกกับ `shopId` state

64. **แก้บั๊ก `/login` ค้างที่ "กำลังโหลด..." ถ้า navigate มาจาก `/logout` — เจอระหว่างที่ผู้ใช้ทดสอบสมัครสมาชิกจริงด้วยไลน์ไอดีจริง (ไม่ใช่บัญชีทดสอบเดิม):**
    - **สิ่งที่ผู้ใช้เจอ:** (1) สมัครสมาชิกแล้วเด้งเข้า "หน้าแอดมิน" แทนที่จะได้กรอกฟอร์มสมัครร้านใหม่ (2) กด "ออกจากระบบ" แล้วหน้าค้าง ต้อง refresh เอง
    - **ข้อ 1 ไม่ใช่บั๊ก — เป็นพฤติกรรมตั้งใจจากข้อ 44 ที่ทำให้ผู้ใช้เข้าใจผิดได้ง่าย:** ไลน์ไอดีที่ใช้ทดสอบมีบทบาท "แอดมิน" ผูกกับร้านแก๊สเดิมอยู่แล้ว (จากที่เคยทดสอบ `#สมัครแอดมิน` มาก่อนในโปรเจกต์นี้) — หน้า "พบบัญชี LINE นี้อยู่ในระบบแล้ว" ของ `register.js` เลยแสดงปุ่ม "เข้าร้าน [ชื่อร้านเดิม]" เป็นปุ่มแรก ทำให้กดพลาดได้ง่าย ต้องกดปุ่ม "สมัครร้านใหม่แยกต่างหาก" ต่างหากถึงจะได้ฟอร์มสมัครร้านใหม่จริง — อธิบายให้ผู้ใช้ทราบแล้ว ไม่ได้แก้โค้ด (ปุ่มมีอยู่ครบถูกต้องตามดีไซน์เดิม)
    - **ข้อ 2 เป็นบั๊กจริง พบ root cause แล้ว:** `next/script` ไม่ยิง `onLoad` ซ้ำถ้า script `src` เดียวกันโหลดสำเร็จไปแล้วจากหน้าก่อนหน้าในเว็บเดียวกัน (client-side navigation) — `logout.js` โหลด LIFF SDK ไปแล้ว พอ `router.replace('/login')` มา `login.js`'s `initLiff()` (ที่ผูกกับ `onLoad` เพียงอย่างเดียว) ไม่เคยถูกเรียกเลย หน้าค้างที่ `view='loading'` ตลอดไปจนกว่าจะ hard refresh — `logout.js`/`pos-staff.js` (อีก 2 ไฟล์ที่โหลด LIFF SDK เหมือนกัน) ไม่โดนบั๊กนี้เพราะมี fallback timer อิสระอยู่แล้ว (`logout.js` 3 วิ, `pos-staff.js` 10 วิ ตามข้อ 45) — `login.js` เป็นไฟล์เดียวที่ fallback timer (8 วิ) ผูกอยู่ *ข้างใน* `initLiff()` เอง จึงไม่ทำงานเลยถ้า `initLiff()` ไม่เคยถูกเรียก
    - **แก้:** เพิ่ม `useEffect` เช็ค `window.liff` ตรงๆ ตอน mount แล้วเรียก `initLiff()` เองถ้ามีอยู่แล้ว (ไม่ต้องรอ `onLoad`) + เพิ่ม `liffInitStarted` ref กันเรียกซ้ำถ้าทั้ง `onLoad` และ `useEffect` นี้ทำงานพร้อมกัน
    - **Deploy production แล้ว (2026-08-07)** — revision จริง `smileslip-dashboard-00300-wg8` (ข้อความ deploy โชว์ revision เดิมผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — ยังไม่ได้ทดสอบผ่านเบราว์เซอร์จริงเพราะ LIFF SDK โหลดจาก CDN ภายนอกถูกบล็อกในเบราว์เซอร์ทดสอบของเครื่องมือ (ข้อจำกัดเดิมที่เจอซ้ำหลายรอบในโปรเจกต์นี้) — ตรวจสอบผ่าน code review เชิงลึก (เข้าใจกลไก `next/script` ที่ทำให้เกิดบั๊กนี้ชัดเจน) + build ผ่านเท่านั้น — รอผู้ใช้ยืนยันจากการทดสอบจริงต่อ

65. **ตรวจสอบหน้าเซลเพจ (`pages/index.js`) ให้ตรงกับความเป็นจริง + คำถามเรื่องแพ็คเสริมสาขา:**
    - **ตรวจสอบครบทั้งไฟล์:** จำนวนสาขาต่อแพ็กเกจ (5/10/20 — Advance/Business/Enterprise), testimonials (fetch จริงจาก `/api/testimonials` มี fallback เป็น placeholder ถ้ายังไม่มีข้อมูลจริง — ผู้ใช้ยืนยันแล้วว่าลงข้อมูลจริงแล้วในข้อ 63), สแกน ∞ ของ Enterprise ฯลฯ — **ตรงกับระบบจริงทั้งหมดอยู่แล้ว ไม่ต้องแก้**
    - **พบ 1 จุดไม่ตรง แก้แล้ว:** การ์ดแพ็กเกจ Shop Pro ใน `index.js` ไม่มีคำเตือนว่าไม่รวมสต็อกหมุนเวียน/แอปพนักงานส่งของ/ระบบเงินเชื่อ ทั้งที่ `pricing.js` (หน้าราคาเต็มรูปแบบ) มีคำเตือนนี้อยู่แล้วตั้งแต่ข้อ 33 — ลูกค้าที่เห็นหน้าแรกก่อนอาจสมัคร Shop Pro แล้วคาดหวังฟีเจอร์ที่เคยมีตอน Starter trial (Starter มีครบ) แล้วงงว่าหายไป — เพิ่มคำเตือนให้ตรงกับ `pricing.js` ทั้ง Shop Pro ("⚠️ ไม่รวม...") และ Advance ("🔓 ปลดล็อก...") — ทดสอบผ่านเบราว์เซอร์จริงแล้ว (หน้านี้ไม่ผูก query string เหมือน `/pos` เลยทดสอบได้ปกติ) — deploy production แล้ว revision จริง `smileslip-dashboard-00301-hl8`
    - **คำถาม/ข้อเสนอเรื่องแพ็คเสริมสาขา (ยังไม่ได้ตัดสินใจ/ยังไม่ทำ — รอผู้ใช้ตอบ):** ผู้ใช้เสนอให้คง cap 20 สาขาที่ Enterprise ไว้เหมือนเดิม (`MAX_BRANCHES` ปัจจุบัน) แล้วเพิ่มระบบซื้อ "แพ็คเสริมสาขา" แยกต่างหากสำหรับร้านที่ต้องการมากกว่า 20 — เหตุผล: กันต้นทุนจัดเก็บข้อมูลบานปลายถ้าปล่อยสาขาไม่จำกัดในราคาคงที่ + กันคนยืม POS ไปใช้ฟรีข้ามธุรกิจที่ไม่เกี่ยวข้องกัน — **ให้ความเห็นแล้วว่าเห็นด้วยกับแนวคิด แต่แนะนำให้รอสร้างระบบซื้อแพ็คเสริมแบบ self-service เต็มรูปแบบ** (ต้องมี Stripe price ใหม่ + คอลัมน์เก็บจำนวนแพ็คที่ซื้อ + เปลี่ยน limit-check เป็น dynamic) **จนกว่าจะมีลูกค้าจริงมากกว่า 1-2 รายที่ต้องการเกิน 20 สาขาจริงๆ** (ตอนนี้มีลูกค้าจริงรายเดียว 2 สาขา ห่างไกล 20 มาก) — ระหว่างนี้ถ้ามีคนขอเกิน 20 สาขาจริง แนะนำจัดการแบบ manual ผ่านหน้าแอดมิน (ปรับ limit เฉพาะราย + คุยราคาเอง) ไปก่อน

66. **แก้บั๊กจริงที่ร้ายแรงที่สุดที่เจอระหว่างทดสอบ: กด "สมัครสมาชิก" ไม่มีทางสมัครร้านใหม่ได้เลยถ้าไลน์ไอดีมีบทบาทอยู่แล้วแม้แค่ 1 ร้าน — เจอระหว่างผู้ใช้ทดสอบสมัครจริงต่อจากข้อ 64 (ยังไม่ใช่ปัญหาเรื่องกดปุ่มผิดตามที่เข้าใจตอนแรก):**
    - **สิ่งที่ผู้ใช้เจอ:** "พอกดสมัครสมาชิก มันก็ขึ้นเข้าสู่ระบบด้วยไลน์ แล้วมันก็เด้งไปหน้าเข้าใช้งานเลย" — ไม่เจอหน้าตัดสินใจ (เข้าร้านเดิม/สมัครร้านใหม่) ที่ควรจะมีตามข้อ 44 เลยแม้แต่วินาทีเดียว
    - **Root cause ที่แท้จริง (ไล่โค้ดจริงจนเจอ ไม่ใช่แค่เดา):** ปุ่ม "สมัครสมาชิก" บนหน้าแรกพาไป `/register` (bare, ไม่มี `userId`) → `register.js` ยังไม่มี `userId` เลยแสดงปุ่ม "เข้าสู่ระบบด้วย LINE" (ตรงกับที่ผู้ใช้เห็น) → ปุ่มนี้ยิงไป `/api/auth/line` (LINE OAuth **redirect-based**, ไม่ใช่ LIFF) → LINE ยืนยันตัวตนสำเร็จ → `/api/auth/callback/line.js` เช็ค `getRolesForLineId()` แล้ว **ถ้าเจอพอดี 1 บทบาท (`roles.length===1`) จะ `res.redirect()` ตรงไปยัง `/dashboard` ทันทีเสมอ ไม่เคยส่งไปที่ `/register?userId=...` เลย** — หน้าตัดสินใจของ `register.js` (ที่มีปุ่ม "สมัครร้านใหม่แยกต่างหาก" ถูกต้องอยู่แล้วตามข้อ 44) จะทำงานได้ก็ต่อเมื่อ `userId` มากับ URL อยู่แล้วเท่านั้น — แต่ไม่มี entry point ไหนในระบบเลยที่จะส่ง `userId` ไปที่ `/register` สำหรับเคส "มีบทบาทอยู่แล้วพอดี 1 ร้าน" ทั้ง `callback/line.js` (OAuth) และ `login.js`'s `proceedWithProfile()` (LIFF) ต่างก็ auto-redirect ตรงไปหาบทบาทเดียวที่เจอเสมอ **สรุป: โค้ดหน้าตัดสินใจของข้อ 44 เป็น dead code สำหรับเคส 1-role มาตลอดตั้งแต่สร้าง — เข้าถึงได้แค่ทาง URL ที่พิมพ์ `userId` เองตรงๆ เท่านั้น ไม่มีทางเข้าถึงจาก user flow จริงเลย**
    - **ทำไมแก้ยาก:** `/api/auth/line` (endpoint เดียวกัน) ถูกเรียกจากทั้ง 2 เจตนา — ปุ่ม "สมัครสมาชิก" (`register.js`) และปุ่ม "เข้าสู่ระบบ" (`login.js`'s fallback เมื่อ LIFF SDK โหลดไม่ทัน) — `callback/line.js` ไม่มีทางรู้เลยว่าไลไอดีที่เจอบทบาทเดียวพอดีนี้ ตั้งใจจะ "เข้าร้านเดิม" (login ปกติ ไม่ควรมีอะไรมาขวาง) หรือ "สมัครร้านใหม่แยกต่างหาก" (register ตั้งใจ ต้องได้เห็นตัวเลือก) — ถ้า auto-redirect ไป `/register` เสมอไม่ว่าเจตนาไหน จะเพิ่มความยุ่งยากให้คน login ปกติ (กรณีส่วนใหญ่) โดยไม่จำเป็น
    - **แก้:** ใช้ query param `state` ของ LINE OAuth (มีอยู่แล้วเดิมแต่เป็นแค่ค่าสุ่มไม่มีความหมาย ไม่เคยถูกตรวจสอบฝั่ง callback เลยด้วยซ้ำ) พกเจตนาข้ามรอบ OAuth แทน — `api/auth/line.js` (GET) รับ `?intent=register` แล้วเข้ารหัสเป็น `state=register:<random>` (ไม่ระบุ/ค่าอื่นใดๆ = `state=login:<random>` ค่าเริ่มต้น) — `register.js`'s ปุ่ม "เข้าสู่ระบบด้วย LINE" เปลี่ยนเป็นยิง `/api/auth/line?intent=register` — `callback/line.js` parse `state` ก่อนเช็ค roles แล้วถ้า `roles.length===1 && intent==='register'` ส่งไปหน้าตัดสินใจ (`/register?userId=...`) แทนที่จะ auto เข้าร้านเดิม — เคส login ปกติ (ไม่ส่ง intent หรือ state ผิดรูปแบบ) **พฤติกรรมเหมือนเดิมทุกประการ ไม่มีความยุ่งยากเพิ่ม** (fallback เป็น `'login'` เสมอถ้า parse ไม่ได้ — ปลอดภัยกว่าเดาผิดเป็น register)
    - **ทดสอบ:** 8 unit-test cases สำหรับ intent-derivation logic ครอบคลุม edge case ครบ (state ปกติทั้ง 2 แบบ, undefined, string ว่าง, ไม่มี colon, case ผิด, ลงท้ายด้วย colon เฉยๆ, ไม่ใช่ string) ผ่านหมด + ยิงตรงกับ dev server จริงยืนยันว่า `state` ถูกเข้ารหัสถูกต้องใน URL ที่ redirect ไป LINE จริง (`state=register:xxx` เมื่อ `intent=register`, `state=login:xxx` เมื่อไม่ระบุ/ระบุ `login`) — **ไม่สามารถทดสอบ callback แบบ end-to-end เต็มรูปแบบได้** เพราะต้องมี LINE OAuth `code` จริงจากการ login จริง (ไม่มีทาง mock ได้โดยไม่มี credential จริง) ตรวจสอบด้วย code review เชิงลึก + trace logic ทีละบรรทัดแทน
    - **Deploy production แล้ว (2026-08-07)** — revision จริง `smileslip-dashboard-00302-b42` — verified บน production จริงว่า `GET /api/auth/line?intent=register` เข้ารหัส `state=register:xxx` ถูกต้อง (redirect ไป LINE OAuth จริง เห็น query string ตรงตามที่ตั้งใจ)
    - **ยังไม่ได้แก้ (out of scope รอบนี้, พฤติกรรมเดิมไม่เปลี่ยน):** `login.js`'s LIFF flow (`proceedWithProfile()`) ยังคง auto-redirect ตรงเมื่อเจอ 1 บทบาทเสมอ ไม่ผ่านหน้าตัดสินใจ — ตั้งใจไม่แตะเพราะปุ่ม "สมัครสมาชิก" บนหน้าแรกไม่เคยใช้ LIFF เลย (ไม่มี `<Script>` LIFF ใน `register.js` เลยสักบรรทัด) จึงไม่ใช่เส้นทางที่บั๊กนี้กระทบ — ถ้าในอนาคตมีปุ่ม "สมัครสมาชิก" ที่ใช้ LIFF ต้องแก้จุดนี้เพิ่มด้วย pattern เดียวกัน

67. **"พนักงานใน pos หาย" — กู้คืนข้อมูลพนักงาน 2 คนของร้าน D Gas ที่หายไปจาก Tier F migration (ข้อ 54) เพราะไม่เคย backfill:**
    - **สาเหตุ:** ไม่ใช่บั๊กใหม่ — `scripts/tierF-01-create-staff.sql`'s comment เขียนไว้ชัดเจนตั้งแต่วันที่ deploy (2026-07-29) ว่า "ตามธรรมเนียม migration นี้ทั้งหมด: ไม่ backfill ข้อมูลเก่า (ชอบ D Gas 2 คนที่มีอยู่แล้วใน Sheets จะไม่ตามมาอัตโนมัติ)" — ตาราง `pos_staff` เริ่มว่างเปล่าตั้งแต่วันนั้น พนักงาน "Testing" (พนักงานส่ง) และ "ดีแก๊ส2024" (แคชเชียร์) ที่เคยตั้งไว้ในแท็บ "พนักงาน" ของ POS Sheet เดิมเลยหายไปจากหน้าจัดการพนักงานตั้งแต่นั้น — เพิ่งเจอตอนนี้เพราะเป็นครั้งแรกที่มีคนเปิดหน้านี้ดูอีกครั้งหลัง migration
    - **กู้คืนสำเร็จ 100% ไม่ต้องให้พนักงานตั้ง PIN ใหม่:** `pos_configs.pos_sheet_id` ของร้านนี้ (ตั้งมาตั้งแต่ 2026-07-03 ก่อน cutover) ยังชี้ไปสเปรดชีตเดิมที่ยังไม่ถูกลบ + `shop_google_configs.google_refresh_token` ยังใช้งานได้จริง — อ่านแท็บ "พนักงาน" ตรงผ่าน Google Sheets API ได้ข้อมูลครบทั้ง 21 คอลัมน์ (รวม PIN ที่เก็บเป็นข้อความล้วนเหมือนกันทั้ง Sheets เดิมและ `pos_staff` ปัจจุบัน จึงคัดลอกข้ามได้ตรงๆ ไม่ต้องรีเซ็ต) → insert กลับเข้า `pos_staff` ตรงๆ ให้ตรงกับข้อมูลเดิมทุกฟิลด์ (ชื่อ/เบอร์/LINE ID/บทบาท/สาขา/PIN/สิทธิ์ 12 ตัว) — verified บน production จริงผ่าน `GET /api/pos/staff` ว่าเห็นพนักงานครบ 2 คนพร้อม `has_pin:true` ทั้งคู่
    - **ไม่ใช่โค้ดที่ต้องแก้ — เป็นแค่การกู้ข้อมูล (data recovery) ครั้งเดียว** ไม่มีการ deploy ใดๆ ในข้อนี้ ลบสคริปต์ชั่วคราวออกจากโปรเจกต์หมดแล้ว
    - **ข้อควรระวังสำหรับอนาคต:** ร้านอื่นที่เคยตั้งพนักงานไว้ใน POS Sheet เดิมก่อน 2026-07-29 (ถ้ามี) จะเจอปัญหาเดียวกันทุกร้าน — ถ้ามีคนแจ้งพนักงานหาย ให้เช็ค `pos_configs.pos_sheet_id` ของร้านนั้นก่อนว่ายังมี (ร้านที่สมัครหลัง Tier 143 cleanup จะไม่มี `pos_sheet_id` เลย กู้จาก Sheets ไม่ได้) แล้วอ่านแท็บ "พนักงาน" ตรงด้วย pattern เดียวกับที่ทำในข้อนี้

68. **ต่อจากข้อ 67 — ผู้ใช้แจ้งเพิ่มว่า "สมัครพนักงานใหม่แล้วแต่ไม่ขึ้นแสดง" (คนละปัญหาจากข้อ 67 — นี่คือบั๊กจริงในโค้ด ไม่ใช่แค่ข้อมูลหาย) — พบว่า `#สมัครผู้จัดการสาขา` อนุมัติแล้วไม่เคย sync เข้า `pos_staff` เลยตั้งแต่ไหนแต่ไร:**
    - **สาเหตุ:** `api/pos/staff-requests.js`'s PATCH (approve) เดิมเช็คแค่ `if (reqRow.role === 'delivery_staff')` ถึงจะสร้างแถวใน `pos_staff` — ถ้า role เป็น `branch_manager` (มาจาก `#สมัครผู้จัดการสาขา`) จะข้าม block นี้ไปเลย เหลือแค่เปลี่ยน `status` เป็น `approved` เฉยๆ ไม่มีผลอะไรกับหน้าจัดการพนักงานทั้งสิ้น — เป็น known gap เก่าที่บันทึกไว้ตั้งแต่ข้อ 11 (ยุคที่ยังไม่มีระบบสิทธิ์ 12 ตัว, "ยังไม่มีระบบสิทธิ์แยกตามสาขาจริงจัง") ที่ไม่เคยถูกอัปเดตตามตอนระบบสิทธิ์พนักงานเต็มรูปแบบถูกสร้างขึ้นทีหลัง (ข้อ 44/56) — พิสูจน์จริงด้วยการ query `branch_role_requests` เจอคำขอ `branch_manager` ของ "ดีแก๊ส2024" (LINE ID เดิมที่เคยได้รับอนุมัติเป็น `delivery_staff` ไปแล้วก่อนหน้า) ถูก approve เมื่อ 2026-08-08 08:09 แต่ไม่มีร่องรอยใน `pos_staff` เลย
    - **เจอบั๊กที่สองซ้อนอยู่ระหว่างตรวจ:** `delivery_staff` เองก็มีปัญหา — สร้างแถว `pos_staff` จริง แต่ **ไม่เคยให้สิทธิ์อะไรเลยสักอย่าง** (ไม่มี `perm_manage_delivery`) ทั้งที่ `delivery.js`/`collections.js` ทุก endpoint ถูกบังคับด้วยสิทธิ์นี้มาตั้งแต่ข้อ 56 — พนักงานที่สมัครผ่าน `#สมัครพนักงาน`/`#สมัครพนักงานขนส่ง` แล้วตั้ง PIN เข้าใช้งาน จะยืนยันจัดส่ง/เก็บเงินไม่ได้เลยจนกว่าเจ้าของร้านจะไปเปิดสิทธิ์ให้เองทีหลัง (ซึ่งไม่มีใครรู้ว่าต้องทำ)
    - **แก้:** เพิ่ม `ROLE_APPROVAL_CONFIG` ใน `staff-requests.js` ใช้ preset สิทธิ์ชุดเดียวกับ `STAFF_PRESETS` ที่มีอยู่แล้วในฟอร์มเพิ่มพนักงานของ `pos.js` (`delivery_staff`→`perm_manage_delivery`, `branch_manager`→ 9 สิทธิ์ของผู้จัดการสาขา) ให้สิทธิ์ถูกต้องทันทีที่อนุมัติทั้งสองบทบาท — เพิ่ม logic "เลื่อนตำแหน่ง": ถ้าไลไอดีนั้นเป็นพนักงานอยู่แล้วในร้าน (ยังไม่ถูกลบ) จะ `UPDATE` แถวเดิม (เปลี่ยน role + รวมสิทธิ์ใหม่เข้ากับของเดิมแบบ union ไม่ลบสิทธิ์ที่เคยมี) แทนสร้างพนักงานซ้ำคนละแถว — ตรงกับเคสจริงของ "ดีแก๊ส2024" ที่สมัคร 2 บทบาทต่อเนื่องกัน (delivery_staff ก่อน แล้วมา branch_manager ทีหลัง)
    - **ทดสอบยิงจริงกับ dev server ก่อน deploy** (พนักงานทดสอบชั่วคราว, ลบทิ้งหลังทดสอบ): อนุมัติ `branch_manager` ใหม่ → สร้างแถวพร้อม 9 สิทธิ์ถูกต้อง + `perm_manage_staff` ไม่ถูกให้มาด้วย (ไม่ได้อยู่ใน preset) ถูกต้อง, อนุมัติ `delivery_staff` ซ้ำให้ไลไอดีเดิม → ไม่สร้างแถวใหม่ซ้ำ (ยังมีแค่ 1 แถว) role เปลี่ยนเป็น "พนักงานส่ง" แต่สิทธิ์จาก manager preset เดิม (เช่น `perm_view_revenue`) ยังคงอยู่ไม่ถูกลบ ถูกต้องตามที่ออกแบบ, reject action ยัง regression-safe
    - **Deploy production แล้ว (2026-08-08)** — revision จริง `smileslip-dashboard-00303-56d` (ข้อความ deploy โชว์ revision เดิม `00302-b42` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — **ยิงซ้ำคำขอ `branch_manager` ที่ค้างจริงของ "ดีแก๊ส2024" ผ่าน production ตรงๆ หลัง deploy สำเร็จ** (endpoint ไม่มี guard กันอนุมัติซ้ำคำขอที่ `approved` อยู่แล้ว — เรียกซ้ำได้อย่างปลอดภัย) verified ผ่าน `GET /api/pos/staff` ว่า "ดีแก๊ส2024" ขึ้นเป็น "ผู้จัดการสาขา" พร้อมสิทธิ์ครบแล้วจริงบน production

69. **"ทำไมเพิ่มผู้ติดต่อไม่ได้ หรือต้องรีเพรช" + ขอเพิ่มผู้ติดต่อจากหน้าขายได้เลยไม่ต้องออกไปหน้าผู้ติดต่อ:**
    - **ตรวจสอบพบว่า POST เพิ่มผู้ติดต่อไม่ได้พังจริง** (ยืนยันจาก DB ตรงว่ารายการล่าสุดที่ผู้ใช้เพิ่งเพิ่มก่อนหน้านี้บันทึกสำเร็จจริง) — **สาเหตุที่แท้จริงคือ "มองไม่เห็น" ไม่ใช่ "เพิ่มไม่ได้"** สองทางซ้อนกัน: (1) รายชื่อเรียงเก่า→ใหม่ + แบ่งหน้า 20 รายการ/หน้า ค้างที่หน้า 1 เสมอ — ร้านที่มีผู้ติดต่อหลักพันคน (D Gas 2,237 คน ≈ 112 หน้า) ผู้ติดต่อใหม่จะตกไปอยู่หน้าสุดท้ายเสมอ ไม่มีทางเห็นถ้าไม่รู้ว่าต้องเลื่อนไปหน้าไหน (2) ตัวกรองประเภท (ลูกค้า/ผู้จำหน่าย) ค้างอยู่ ถ้าผู้ติดต่อใหม่ไม่ตรงกับตัวกรองที่เปิดค้างไว้จะไม่โผล่เลย — แก้ `saveContact()` ให้หลังบันทึกสำเร็จ เซ็ตตัวกรองเป็น "ทั้งหมด" + ค้นหาด้วยชื่อที่เพิ่งบันทึกทันที รับประกันเห็นผลลัพธ์เสมอไม่ว่าจะตกหน้าไหน/ประเภทไหน
    - **เพิ่มปุ่ม "✚ เพิ่มผู้ติดต่อใหม่เข้าระบบ" ในตัวเลือกลูกค้า 3 จุดของหน้าขาย** (เปิดบิลใหม่/checkout ขายเชื่อ/สร้างออเดอร์จัดส่ง) เมื่อค้นหาชื่อ/เบอร์แล้วไม่เจอ — เดิมแต่ละจุดจัดการคนละแบบไม่สม่ำเสมอกัน: จุดเปิดบิลใหม่ไม่มีทางเลือกอะไรเลย ("ไม่พบลูกค้า" เฉยๆ), checkout ขายเชื่อมีแค่ "+ ใช้เป็นชื่อลูกค้าใหม่ (ไม่บันทึกราคาประจำตัว)" ซึ่งเป็นแค่สตริงชื่อลอยๆ สำหรับบิลนั้นครั้งเดียว ไม่ใช่ผู้ติดต่อจริง, จุดจัดส่งมีลิงก์ "เพิ่มลูกค้าใหม่" ที่กด **ปิด modal ทิ้งทันทีแล้วเด้งไปหน้าผู้ติดต่อ เสียตะกร้า/ขั้นตอนจัดส่งที่ทำค้างอยู่ทั้งหมด** — ตรงกับที่ผู้ใช้ขอเป๊ะ
    - เพิ่มฟอร์ม inline เล็กๆ (ชื่อ+เบอร์เท่านั้น, ใช้ซ้ำได้ทั้ง 3 จุดผ่าน `renderQuickAddContactForm(target)`) เดา name/phone อัตโนมัติจากคำค้นหาที่พิมพ์ไว้ (ตัวเลขล้วน ≥6 หลัก = เบอร์โทร, อย่างอื่น = ชื่อ) บันทึกจริงผ่าน `POST /api/pos/contacts` (`contact_type:'ลูกค้า'` เสมอ เพราะทั้ง 3 จุดนี้คือตัวเลือก "ลูกค้า" ทั้งหมด) แล้วเลือกใช้ผู้ติดต่อที่เพิ่งสร้างให้อัตโนมัติทันที **ไม่ปิด modal หลักเลย ไม่เสียตะกร้า/ขั้นตอนที่ทำค้างอยู่** — เก็บตัวเลือกเดิมของ checkout ("ใช้ชื่อลอยๆ ไม่บันทึก") ไว้คู่กันด้วย (เผื่ออยากขายด่วนไม่อยากบันทึกถาวร)
    - **ทดสอบยิงจริงกับ dev server** (จำลอง `saveQuickAddContact()` เป๊ะผ่าน POST ตรง) ยืนยันบันทึกถูกต้อง+ปรากฏในตัวกรอง `customers` (contact_type==='ลูกค้า') ที่ picker ทั้ง 3 จุดใช้จริง — ทดสอบ heuristic เดาเบอร์/ชื่อจากคำค้นหา 7 เคส (มี/ไม่มีขีด, มีวรรค, ชื่อผสมตัวเลข) ถูกต้องครบ
    - **Deploy production แล้ว (2026-08-08)** — revision จริง `smileslip-dashboard-00304-vls` (ข้อความ deploy โชว์ revision เดิมผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — verified บน production จริงว่า `POST /api/pos/contacts` ยังทำงานถูกต้องหลัง deploy

70. **3 เรื่องหน้าพนักงานส่ง (`pos-staff.js`) — แก้หมุด/เพิ่มหมุดได้, เบอร์กดโทรได้เลย, เลิก hardcode "ถัง":**
    - **แก้หมุดแผนที่ได้จากหน้าคนขับเอง** — ก่อนหน้านี้หมุด/ที่อยู่ถูกกำหนดตายตัวตอนแอดมินสร้างออเดอร์เท่านั้น คนขับเห็นแค่ลิงก์เปิดแผนที่แบบอ่านอย่างเดียว ถ้าหมุดผิดจุด (ลูกค้ามีทั้งบ้าน+ร้าน หรือมีหลายที่อยู่) ไม่มีทางแก้เองได้เลยต้องรอแอดมิน — เพิ่มปุ่ม "✏️ แก้ที่อยู่/หมุด" ในหน้ายืนยันจัดส่ง เปิดฟอร์มแก้ข้อความที่อยู่ + ปักหมุดใหม่ผ่าน `MapPickerModal` (ก็อปจาก `pos.js` มาทั้งดุ้นตามธรรมเนียมโปรเจกต์ที่ไม่แชร์ component ข้ามหน้า — Leaflet + OpenStreetMap ฟรีเหมือนเดิม) บันทึกผ่าน `PATCH /api/pos/delivery` ที่รองรับ `address`/`maps_link` อยู่แล้วเดิม (ไม่ต้องแก้ backend เลยสักบรรทัด แค่เพิ่มผู้เรียกใหม่) — คนขับมีสิทธิ์ `perm_manage_delivery` อยู่แล้วเป็นทุนเดิม (endpoint เดียวกับที่ใช้ยืนยันจัดส่งสำเร็จ)
    - **เบอร์โทรลูกค้ากดโทรออกได้ทันที** — เดิมทั้ง 4 จุด (รายการงานจัดส่ง/รายละเอียดงานจัดส่ง/รายการงานเก็บเงิน/รายละเอียดงานเก็บเงิน) แสดงเป็น `<div>` ข้อความเฉยๆ ผู้ใช้แจ้งว่ากดคัดลอกก็ไม่ได้ — เปลี่ยนเป็น `<a href="tel:...">` ทั้งหมด (ตัดอักขระที่ไม่ใช่ตัวเลข/`+` ออกก่อนใส่ href กัน tel: parse พัง แต่ยังโชว์ข้อความเบอร์รูปแบบเดิมที่มีขีดให้อ่านง่าย)
    - **เลิก hardcode "ถัง" เป็นหน่วยตายตัว** — ตรวจ `pos-staff.js` พบว่าจุดยืม/คืนสินค้าหมุนเวียนใช้ `item.unit` แบบไดนามิกอยู่แล้วตั้งแต่ข้อ 25 (ไม่มีบั๊กจริงในไฟล์นี้) — ตัวจริงที่ hardcode อยู่คือ **`pos.js`** (แดชบอร์ดหลัก) 6 จุด: การ์ดสรุปยอด/ป้ายกำกับในหน้าผู้ติดต่อ (2 จุด), badge จำนวนถังต่อรายชื่อ, label ฟอร์มแก้ไขผู้ติดต่อ 2 ช่อง (`ถังอยู่กับลูกค้า`, `วงเงินยืมสูงสุด (ชิ้น)`), ชื่อ modal รับคืน/รีฟิลสินค้าหมุนเวียน — เพิ่ม `cyclicalUnitLabel` (เดาหน่วยจากสินค้าประเภท "หมุนเวียน" ที่มีจริงในร้าน — หน่วยเดียวกันหมดใช้หน่วยนั้น, หลายหน่วยปนกัน fallback "หน่วย" กลางๆ กันเดาผิด) แทนที่ทั้ง 6 จุด — จุดที่เป็นแค่ตัวอย่าง/placeholder (เช่น "เช่น ถังแก๊ส" ในคำแนะนำนำเข้าไฟล์, `desc: 'ถัง/กล่อง'`) ปล่อยไว้เหมือนเดิมเพราะเป็นตัวอย่างประกอบ ไม่ใช่ label บังคับ
    - **ทดสอบยิงจริงกับ dev server** (สร้างออเดอร์จัดส่งทดสอบจริง → PATCH แก้ address/maps_link ผ่าน staff-session ของพนักงาน "Testing" → ยืนยันค่าใน DB ตรง + status ไม่ถูกกระทบ → ทดสอบ regression staff-session ปลอม/ไม่มีสิทธิ์ยังโดนบล็อก 403 เหมือนเดิม) + ทดสอบ regex ตัดอักขระเบอร์โทรสำหรับ `tel:` ครบ 4 รูปแบบ (มีขีด/วรรค/บวกหน้า)
    - **Deploy production แล้ว (2026-08-08)** — revision จริง `smileslip-dashboard-00305-kwt` (ข้อความ deploy โชว์ revision เดิมผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — verified บน production จริงด้วยออเดอร์ทดสอบชั่วคราว: สร้าง→PATCH แก้ address/maps_link ผ่าน staff-session จริงสำเร็จ ยืนยันค่าใน DB ตรง แล้วลบทิ้ง
    - **ยังไม่ได้ทดสอบ UI ผ่านเบราว์เซอร์จริง** (ปุ่ม/ฟอร์ม inline ทั้งหมด) — ลองผ่าน browser tool อีกครั้งแล้วยืนยันว่ายังติดปัญหาเดิม (`router.query` ไม่ hydrate สำหรับ `/pos`/`/pos-staff` ที่ผูก query string ในเบราว์เซอร์ sandboxed ของเครื่องมือ) ตรวจสอบผ่าน code review เชิงลึก + `npx next build` ผ่าน + ทดสอบ backend/logic ทุกจุดที่ทำได้แล้วเท่านั้น

71. **ทำไมเอา Biller ID ใส่เข้าไปแล้วสแกนเงินไม่ได้ ("QR ไม่ถูกต้อง"):**
    - **สาเหตุ:** `generateBillPaymentPayload()` เดิม `replace(/\D/g,'')` ตัดตัวอักษรทิ้งเงียบๆ แล้วเอาตัวเลขที่เหลือไปสร้าง QR ตรงๆ ไม่เช็คความยาว — ถ้ากรอก "เลขอ้างอิง" ที่แอปธนาคารโชว์ (มีตัวอักษรปนบ่อย ตรงตามเคสจริงที่เคยเจอมาก่อนในข้อ 5) แทน Biller ID จริง จะได้ QR ที่ syntax ถูกต้อง (CRC/TLV ผ่านหมด — ยืนยันแล้วว่า CRC-16/CCITT-FALSE implementation ถูกต้องตรงกับ test vector มาตรฐาน `29B1`) แต่ข้อมูลข้างในเป็นเลขมั่วที่ไม่มี biller จริงรองรับ ธนาคารปฏิเสธตอนสแกนด้วย error กำกวม "QR ไม่ถูกต้อง"
    - **แก้ 3 ชั้น:** (1) `validateBillerId()` ฝั่ง UI แสดงเตือนสด + บล็อคปุ่มบันทึกถ้าไม่ใช่ตัวเลขล้วน 15 หลัก พร้อมคำเตือนชัดเจนว่าเลขที่โชว์หน้าจอ QR ของแอปธนาคารมักไม่ใช่ Biller ID จริง ต้องไปหาในเมนู "แก้ไขข้อมูลร้านค้า"/Merchant Settings แทน (2) `pos-config.js` PATCH validate ซ้ำฝั่ง server กันเซฟค่าผิดเข้าระบบทาง API ตรง (3) `thai-qr-billpayment.js` เพิ่ม safety-net เดียวกันเป็นด่านสุดท้ายกันข้อมูลเก่าที่อาจหลุดผ่านมาก่อนหน้านี้
    - **ทดสอบยิงจริงกับ dev server**: เคสตัวอักษรปน → 400 ถูกต้อง, เคสความยาวผิด (13 หลัก) → 400 ถูกต้อง, เคส Biller ID จริง 15 หลัก → บันทึกสำเร็จ + สร้าง QR จริงได้ (`method:'billpayment'`, มี dataURL) — คืนค่า D Gas กลับเป็นว่างหลังทดสอบ (ไม่ได้ตั้ง Biller ID จริง)
    - **Deploy production แล้ว (2026-08-08)** — revision จริง `smileslip-dashboard-00306-fqs` (deploy ครั้งแรกเจอ `429 RESOURCE_EXHAUSTED` จาก Cloud Build API เอง — เช็คด้วย `gcloud builds describe` พบว่า build จริงสำเร็จเบื้องหลัง เป็นแค่ CLI polling ชนโควต้าเท่านั้น ไม่ใช่ build พังจริง ตรงกับที่เคยเจอมาแล้วในข้อ 62 — deploy ซ้ำรวมกับงานข้อ 72 ด้านล่างสำเร็จในรอบเดียว)

72. **บั๊กสินค้าหมุนเวียน "ยอดหายไม่เปลี่ยนเป็นเปล่า" + รายงานยอดขายไม่รวมออเดอร์จัดส่ง — เจอระหว่างผู้ใช้เทสระบบขายจริงวันนี้:**
    - **บั๊ก 1 — รับสินค้าประเภทหมุนเวียนเดิม "หัก เปล่ารอรีฟิล" เสมอไม่มีเงื่อนไข** ไม่ว่าจะเป็นการซื้อของใหม่เพิ่มหรือรีฟิลของเดิมจริงๆ ก็ตาม — ทำให้ตัวเลขเปล่ารอรีฟิลเพี้ยนลงเรื่อยๆ ทุกครั้งที่รับสินค้า (ยืนยันจริงกับข้อมูล D Gas: SKU เนื้อก๊าซ 2 ตัวที่มีการยืนยันจัดส่ง+รับสินค้าวันนี้ empty_waiting ถูกดันลงเหลือ 0 ทั้งคู่) — แก้โดยเพิ่มตัวเลือก `isRefill` ต่อรายการตอนรับสินค้า (ค่าเริ่มต้น = รีฟิล สำหรับสินค้าหมุนเวียน ตรงกับพฤติกรรมทั่วไปของธุรกิจแบบนี้ แต่แก้เป็น "ซื้อใหม่" ได้ถ้าไม่ใช่การรีฟิลจริง) — persist `isRefill` ลงในบันทึกใบรับสินค้าด้วย แล้วแก้รายงาน "🔄 สินค้าหมุนเวียน" (ต้นทุนรีฟิล vs ซื้อใหม่) ให้อ่านค่านี้แทนเดาจากประเภทสินค้าอย่างเดียว (ใบรับเก่าก่อนมีฟิลด์นี้ fallback เป็นรีฟิลตามพฤติกรรมเดิม กันตัวเลขย้อนหลังเปลี่ยน)
    - **ปิดปุ่ม "รีฟิล" ด่วนแบบเดิมที่ไม่มีใบรับสินค้า/ต้นทุนกำกับเลยออก** — ผู้ใช้ชี้ว่า "กดปุ่มรีฟิวแต่ไม่มีบิล มันไม่ได้ ต้องมีที่มาสินค้าด้วย" — เปลี่ยนปุ่มนี้เป็นพาไปหน้า "รับสินค้า" แทน พร้อมสินค้าใส่ให้อัตโนมัติ (เก็บปุ่ม "รับคืน" ไว้เหมือนเดิม เพราะเป็นแค่การย้ายระหว่าง at_customer/empty_waiting ไม่มีต้นทุน ไม่ใช่การรับสินค้าจริง)
    - **เพิ่ม `at_customer`/`empty_waiting` เป็นช่องแก้ไขตรงในฟอร์มสินค้า** (เฉพาะแก้ไขสินค้าหมุนเวียนที่มีอยู่แล้ว) — backend รองรับ absolute-value-to-delta อยู่แล้วจาก Phase 3 stock transfer (ข้อ 62) แค่ไม่เคยมี UI ให้ใช้ — ผู้ใช้ขอเพราะไม่มีทางรู้/แก้ไขจำนวนเปล่าที่ถูกต้องได้เลยถ้าตัวเลขเพี้ยนจากบั๊กข้อ 1 — **ตั้งใจไม่ auto-fix ตัวเลขที่เพี้ยนไปแล้วของ D Gas เอง เพราะไม่มีทางรู้จำนวนถังเปล่าจริงทางกายภาพที่หน้าร้าน** ให้ผู้ใช้ใช้ช่องนี้แก้เองตามที่นับได้จริง
    - **บั๊ก 2 — รายงานยอดขาย/กำไรขาดทุน/สินค้าขายดี (`type=sales`/`pl`/`topsellers`) เดิมอ่านจาก `pos_sales` (ขายหน้าร้าน) อย่างเดียว ไม่เคยรวมออเดอร์จัดส่งที่ยืนยันสำเร็จแล้วเลย** — รู้อยู่แล้วว่า `type=credit` แก้จุดนี้ไปแล้วก่อนหน้า (ข้อ 22) แต่ sales/pl/topsellers ตกหล่น — ร้านที่ขายผ่านช่องทางจัดส่งเป็นหลัก (ไม่มีขายหน้าร้านเลยแบบ D Gas วันนี้) เห็นรายงานว่างเปล่าตลอดทั้งที่มียอดขายจริง — เพิ่ม `deliveryOrderToSaleShape()` แปลงออเดอร์ที่ "ส่งแล้ว" เท่านั้น (ยังไม่นับออเดอร์รอจัดส่ง เพราะยังไม่เกิดรายได้จริง) ให้อยู่ในรูปเดียวกับยอดขายหน้าร้าน รวม status/payment_method mapping ให้ตรงกับที่ `pos_sales` ใช้ (ค้างจ่ายยังไม่นับเป็นรายได้จนกว่าจะเก็บเงินได้จริง ตรงกับตรรกะเดิมของขายเชื่อหน้าร้าน)
    - **`export.js` (Excel export) มีช่องว่างเดียวกันแต่ยังไม่ได้แก้ในรอบนี้** — flag ไว้เป็นงานต่อ
    - **ตอบคำถามผู้ใช้ระหว่างทาง (ตรวจโค้ดยืนยัน ไม่ใช่บั๊ก):** "หน้าขายปรับราคาต่อชิ้นได้ไหม" — ได้ (checkout modal มีช่องแก้ราคาต่อรายการอยู่แล้วจากข้อ 12) "ระบบจำราคาต่อลูกค้าไหม" — จำ แต่ auto-fill เฉพาะตอน**จัดส่ง** เท่านั้น (`openDelivery()`/เลือกลูกค้าในหน้าจัดส่งเรียก `fetchCustomerPrices()`) ขายหน้าร้านปกติใช้ราคากลางเสมอไม่ auto-fill (ตัดสินใจไว้แล้วตั้งแต่ข้อ 24 กันราคาที่แก้เองถูกเขียนทับ)
    - **เจอปัญหา timing ระหว่าง deploy:** ครั้งแรก `gcloud run deploy` ล้มเหลวด้วย `429 RESOURCE_EXHAUSTED` จาก Cloud Build API เอง (ชนโควต้า polling เพราะ deploy ติดกันหลายรอบ) — เช็คด้วย `gcloud builds describe` ยืนยันว่า build จริงสำเร็จเบื้องหลัง (ตรงกับ pattern ที่เคยเจอในข้อ 62) — ตัดสินใจไม่พยายามกู้ image เดิมมาใช้ต่อ (ยุ่งยากไม่คุ้ม) แต่รวมโค้ดใหม่ของข้อนี้เข้าไปแล้ว deploy ใหม่ทีเดียวพร้อมกันแทน
    - **ทดสอบยิงจริงกับ dev server (`TZ=UTC` จำลอง production) ครบทุกจุดด้วยข้อมูลจริงของร้าน D Gas** — **บทเรียนระหว่างทดสอบ:** เจอผลลัพธ์ 0 ที่ทำให้เข้าใจผิดว่าโค้ดพัง ตอนแรกทดสอบโดยไม่ตั้ง `TZ=UTC` (เครื่อง dev เป็น Asia/Bangkok) ทำให้ `parseThaiBEDate()`'s `new Date(year,m-1,d)` (local midnight) เทียบกับ `dateFrom` (UTC เสมอสำหรับ date-only string) เพี้ยนกัน 7 ชม. จนดูเหมือนไม่มีข้อมูลเลย — ตรงกับ pattern ที่เคยเจอมาแล้วหลายรอบในโปรเจกต์นี้ (ต้องรัน dev server ด้วย `TZ=UTC` เสมอเวลาทดสอบ endpoint ที่เกี่ยวกับช่วงวันที่) — พอตั้ง `TZ=UTC` แล้วเห็นข้อมูลถูกต้องครบ ไม่ใช่บั๊กจริง
    - **Deploy production แล้ว (2026-08-08)** — revision จริง `smileslip-dashboard-00306-fqs` (เดียวกับข้อ 71) — **verified บน production จริงครบ**: sales report รวมออเดอร์จัดส่งถูกต้อง (5 รายการ ทั้ง pos_sales + delivery), รับสินค้าแบบ "ซื้อใหม่" ไม่กระทบ empty_waiting ถูกต้อง, แก้ at_customer/empty_waiting ตรงจากฟอร์มสินค้าถูกต้อง — ลบข้อมูลทดสอบสะอาดหมดหลังทดสอบ

73. **5 ข้อจากผู้ใช้ทดสอบระบบขายจริงต่อเนื่อง — ยกเลิกใบรับสินค้าไม่ได้/ดูรูปแนบไม่ได้, "ยอดสะสม" งง, ภาษีขายแยกสาขาขึ้นแค่บิลเดียว + ไม่มี drill-down:**
    - **บั๊กที่แก้แล้ว — ประวัติรับสินค้าลบ/ยกเลิกไม่ได้เลย:** `api/pos/receives.js` ไม่เคยมี DELETE endpoint ตั้งแต่สร้างฟีเจอร์มา — เพิ่ม `DELETE /api/pos/receives {receive_no}` (ต้องมีสิทธิ์ `perm_manage_receiving`) คืนสต็อคย้อนกลับตาม `isRefill` ของแต่ละรายการ (สินค้าหมุนเวียนที่เคยรีฟิล → คืน `empty_waiting` กลับ, ที่เป็นซื้อใหม่ → ไม่แตะ `empty_waiting`) ผ่าน `adjustBranchStock()` เดียวกับทุกจุด แล้วลบแถวใน `pos_receives` + ลบแถวคู่ใน `ledger_transactions` (จับคู่ด้วย `raw_data->>receive_no` — เจอบั๊กระหว่างเขียนสคริปต์ cleanup ของตัวเองว่า `.ilike('items','%text%')` บนคอลัมน์ JSONB ไม่ทำงานเงียบๆ ไม่ error แค่ไม่ match อะไรเลย ต้องใช้ operator `->>` ของ jsonb ตรงๆ ถึงจะแมตช์ได้จริง — เจอเพราะสคริปต์ cleanup ของตัวเองก่อนหน้านี้ทิ้งข้อมูลทดสอบ 3 แถวไว้ใน `pos_receives` ของร้าน D Gas จริงโดยไม่รู้ตัว ตรวจพบและลบออกผ่าน DELETE endpoint ใหม่นี้เรียบร้อยแล้ว) — **ไม่ย้อนต้นทุนถ่วงน้ำหนัก (`pos_products.cost`) ให้** เพราะขึ้นกับสต็อค ณ ตอนรับจริงซึ่งอาจเปลี่ยนไปแล้วจากการรับสินค้ารอบอื่นคั่นกลาง ย้อนกลับให้ "ถูกต้อง" ไม่ได้เสมอไป (known limitation ที่ยอมรับไว้)
    - **บั๊กที่แก้แล้ว — ยกเลิกบิลขายพังสำหรับบิลที่มาจากออเดอร์จัดส่ง:** ผลข้างเคียงจากข้อ 72 ที่เพิ่งรวมออเดอร์จัดส่งเข้ารายงานยอดขายเดียวกัน — ปุ่ม "ยกเลิก" เดิมยิง `DELETE /api/pos/sales` เสมอ ซึ่งหา `bill_no` ที่จริงๆ เป็นเลขออเดอร์จัดส่ง (`DEL...`) ไม่เจอ — แก้โดยซ่อนปุ่มยกเลิกสำหรับแถวที่มาจากจัดส่ง (`source==='delivery'`) แสดงข้อความ "🚚 ดูที่แท็บจัดส่ง" แทน (ตั้งใจไม่ผูกกับ DELETE ของ `delivery.js` เพราะออกแบบไว้สำหรับออเดอร์ที่ยังไม่ยืนยันเท่านั้น ไม่ย้อนผลกระทบสต็อค/หนี้ของออเดอร์ที่ยืนยันแล้ว)
    - **เพิ่ม UI — ดูรูปแนบใบรับสินค้า** ในประวัติรับสินค้า: ลิงก์ "🖼️ ดูรูปบิล/ใบส่งของ" (จาก `photo_url` ที่มีอยู่แล้วในข้อมูล — รวมถึงรูปที่รับผ่าน `#รับสินค้า` ทาง LINE ที่แนบมาด้วยเสมออยู่แล้ว) + ปุ่ม "ยกเลิก" ต่อรายการ
    - **"ยอดสะสม" ไม่ใช่บั๊ก — แค่ label งง:** เป็นค่าที่คำนวณอัตโนมัติจากผลรวมสะสมของตารางอยู่แล้ว (ไม่ต้องกรอกเอง) เพิ่ม `title` tooltip + คำอธิบาย "(คำนวณอัตโนมัติ)" กำกับหัวตารางให้ชัดเจนขึ้น
    - **ตรวจสอบแล้วแต่ยังไม่แก้ (compliance-sensitive, รอผู้ใช้ตัดสินใจ) — ภาษีขายแยกตามสาขา/Input VAT drill-down:** `api/sheets/tax-report.js` เป็นคนละระบบกับ `POS → รายงาน → 🧾 ภาษี VAT` (ตามที่บันทึกไว้ตั้งแต่ข้อ 35 — อ่านจาก `ledger_transactions` ทั้งบัญชี ไม่ใช่ POS Sheets) — ตรวจโค้ดพบว่ารายงานนี้กรองเฉพาะแถวที่มี `tax_id`+`tax_amount>0` เท่านั้น (บรรทัด 51) แต่ทั้ง 3 จุดที่ POS dual-write เข้า `ledger_transactions` (`sales.js`/`receives.js`/`expenses.js`) **ไม่เคยใส่ข้อมูลภาษี (`tax_id`/`taxpayer_name`/`tax_amount`/`tax_address`) เข้าไปเลยสักจุด** ทำให้ยอดขาย/ซื้อของ POS แทบทั้งหมดถูกกรองออกจากรายงานนี้ไปโดยอัตโนมัติ (เห็นแค่บิลที่มีคนออกใบกำกับภาษีทางการแยกต่างหากจริงๆ) — **ตั้งใจไม่แก้เองในรอบนี้** เพราะการนับ "ภาษีขาย" ของยอดขายหน้าร้าน POS ทั่วไป (ที่ไม่ได้ออกใบกำกับภาษีทางการทุกใบ) เป็นการตัดสินใจเชิงบัญชี/ภาษีที่ต้องคุยกับผู้ใช้ก่อนว่าควรนับยังไง ไม่ใช่แค่บั๊กโค้ดที่แก้เองได้ทันที (ดู drill-down ของ Input VAT ก็ติดปัญหาเดียวกัน — ไม่มีข้อมูลภาษีให้ drill ลงไปเพราะไม่เคยถูกบันทึกไว้ตั้งแต่ต้น)
    - **`export.js` (Excel export) ยังมีช่องว่างเดียวกันกับข้อ 72 (ไม่รวมยอดขายจากออเดอร์จัดส่ง) — ยังไม่ได้แก้ในรอบนี้เช่นกัน** — flag ไว้เป็นงานต่อ
    - **ทดสอบยิงจริงกับ dev server (`TZ=UTC`) + production จริง (`_verify_prod_receives_delete.mjs`, ข้อมูลทดสอบชั่วคราวลบทิ้งหลังทดสอบ)** — DELETE คืนสต็อค/`empty_waiting` ถูกต้องตาม `isRefill`, ลบแถว `ledger_transactions` คู่กันสำเร็จ (ยืนยัน `->>` operator ทำงานถูกต้องหลังพบว่า `ilike` ใช้ไม่ได้), สิทธิ์พนักงาน `perm_manage_receiving` บังคับถูกต้อง (403 ถ้าไม่มี)
    - **Deploy production แล้ว (2026-08-08)** — revision จริง `smileslip-dashboard-00307-km7` (ข้อความ deploy โชว์ revision เดิมผิดอีกตามเคย เช็คด้วย `gcloud run revisions list` แล้ว pin traffic เอง) — verified บน production จริงว่า DELETE ทำงานถูกต้อง (สร้าง+ลบใบรับสินค้าทดสอบ ยืนยันสต็อคคืนถูกต้อง)

74. **ตามคำถามผู้ใช้ยืนยันหลักการ VAT ที่ถูกต้องตามกฎหมาย ("ร้านจด VAT ทุกครั้งที่ขายก็ต้องบันทึกภาษีขายอยู่แล้ว ไม่งั้นจะคำนวณภาษียังไง") — แก้ช่องว่างที่ตั้งใจเลื่อนไว้ในข้อ 73 (ภาษีขายแยกสาขา/Input VAT drill-down) แล้ว + เจอบั๊กจริงเพิ่มระหว่างแก้:**
    - **ยืนยันหลักการถูกต้องแล้ว:** ร้านที่จดทะเบียน VAT ต้องนับภาษีขายจากยอดขายปลีกทุกบิลตามกฎหมาย (ภ.พ.30 นับยอดขายรวมทั้งเดือน ไม่ใช่แค่บิลที่ลูกค้าขอใบกำกับภาษีเต็มรูปพร้อมเลขผู้ซื้อ — ใบเสร็จอย่างย่อก็นับเป็นภาษีขายได้) — เดิม `api/sheets/tax-report.js` กรองแถวที่ต้องมี "เลขภาษีคู่ค้า" ควบคู่กับยอดภาษีเสมอ (บรรทัด 51 เดิม) ซึ่งผิดหลักการนี้ และยิ่งซ้ำร้ายเพราะ 3 จุดที่ POS dual-write เข้า `ledger_transactions` (`sales.js`/`receives.js`/`expenses.js`) ไม่เคยส่งค่า `tax_amount` เข้าไปเลยสักจุดตั้งแต่ต้น (ทั้งที่คำนวณ VAT ต่อรายการไว้แล้วจริงสำหรับรายงาน VAT ของ POS เอง ผ่าน `computeVatBreakdown()`/`splitVat()`) แก้ทั้ง 2 ชั้นพร้อมกัน:
      1. **`sales.js`/`receives.js`/`expenses.js`** — เพิ่ม query `pos_configs.vat_registered` เข้า `getConfig()`/`getMainLedgerConfig()` ที่มีอยู่แล้วทุกไฟล์ แล้ว dual-write `tax_amount` (= VAT ที่คำนวณไว้แล้วต่อธุรกรรม) เข้า `ledger_transactions` **เฉพาะร้านที่ `vat_registered=true` เท่านั้น** (ร้านไม่จด VAT จะไม่มีอะไรเปลี่ยนเลย) — `receives.js` เพิ่มอีกชั้น: ถ้า `supplierId` ผูกกับผู้ติดต่อที่มีเลขภาษี (`pos_contacts.tax_id`) จริง จะดึงเลขภาษี/ชื่อบริษัท/ที่อยู่ผู้จำหน่ายมาใส่ให้ครบ (ให้ Input VAT drill-down ระบุคู่ค้าได้จริง ไม่ใช่แค่ยอดลอยๆ) — ไม่มีก็ยังบันทึกยอด VAT ได้ปกติ (แค่ไม่มีเลขภาษีกำกับ)
      2. **`tax-report.js`** — ตัดเงื่อนไข "ต้องมีเลขภาษีคู่ค้า" ออก เหลือแค่ `taxAmount > 0` — แถวที่ไม่มีเลขภาษีจริง (ขายปลีกหน้าร้าน/ซื้อจากผู้จำหน่ายที่ไม่มีเลขภาษีในระบบ) จะถูกจัดกลุ่ม (drill-down) รวมตามหมวดหมู่แทนเลขภาษี (เช่น "ขายหน้าร้าน (ไม่มีเลขภาษีผู้ซื้อ)", "ซื้อสินค้าเข้าสต็อค (POS) (ไม่มีเลขภาษีผู้ขาย)") กดขยายดูรายการย่อยได้เหมือนกลุ่มที่มีเลขภาษีจริงทุกประการ (แก้ตามข้อ 73 จุดที่ 4 ที่ขอไว้ด้วย: "กดดูได้ว่ามาจากรายการอะไร") — ใช้ `_groupKey` ภายในแยกจาก `taxId` ที่ export ออกไป กัน CSV/Excel เห็นสตริงกำกับภายในหลุดเข้าคอลัมน์ "เลขประจำตัวผู้เสียภาษี" — แก้ `dashboard.js`'s toggle ขยาย/ย่อกลุ่มจากอิง `s.taxId` (ชนกันได้ถ้าหลายกลุ่มไม่มีเลขภาษีพร้อมกัน — ทุกกลุ่มจะกลายเป็น `'-'` เหมือนกันหมด) เป็นอิง index แทน
      3. **`receives.js`/`expenses.js` — ปรับปรุง note ให้ drill-down มีประโยชน์จริง:** `receives.js` เดิม note เป็นแค่ "รับสินค้าเข้าสต็อค POS" ลอยๆ ไม่บอกว่ารับอะไรมา — เพิ่มเลขที่ใบรับ + รายการสินค้า (name×qty) ต่อท้าย; `expenses.js` เดิม note เป็น "รายจ่าย POS" ลอยๆ ซ้ำกับคอลัมน์หมวดหมู่ที่มีอยู่แล้ว ไม่ได้ช่วยอะไร — เปลี่ยนเป็นชื่อรายการ (label) จริงแทน
    - **🐛 เจอบั๊กจริงระหว่างแก้ (ไม่เกี่ยวกับ VAT โดยตรง แต่ร้ายแรงกว่า):** `receives.js`'s DELETE endpoint ที่เพิ่งสร้างในข้อ 73 (บันทึก `receive_no: receiveNo` ลง `raw_data` ของ `ledger_transactions` เพื่อให้ DELETE จับคู่แถวเจอ) อ้างอิงตัวแปร `receiveNo` ที่**ไม่เคยถูกส่งเข้าฟังก์ชัน `writeReceiveToMainSheets()` เป็นพารามิเตอร์เลย** (ฟังก์ชันนี้เป็น top-level function แยกจาก POST handler ไม่มี closure เข้าถึงตัวแปรท้องถิ่นของ handler ได้) — เป็น `ReferenceError` ที่ถูก `dualWrite()`'s try/catch ภายในกลืนเงียบๆ ทุกครั้ง (log แค่ `[dual-write:receives-mainledger] secondary (Supabase) write failed`) ทำให้ **ทุกใบรับสินค้าตั้งแต่ deploy ข้อ 73 ไม่เคยมีแถวใน `ledger_transactions` เลยสักใบ** (Sheets ฝั่งบัญชีหลักยังเขียนสำเร็จปกติเพราะ primary write ไม่แตะตัวแปรนี้ — กระทบแค่ secondary/Supabase) — ตอนทดสอบ DELETE ในข้อ 73 ไม่จับได้เพราะทดสอบด้วยการ insert แถว ledger ปลอมเข้าไปเองตรงๆ ข้ามการเรียกฟังก์ชันจริง ไม่เคยเดินโค้ดจริงจนสุดทาง — แก้โดยเพิ่ม `receiveNo` เป็นพารามิเตอร์จริงของฟังก์ชัน (พร้อม `vatAmount`/`itemsSummary`/เลขภาษีผู้จำหน่ายที่เพิ่มไปพร้อมกันในงานนี้)
    - **ทดสอบยิงจริงกับ dev server (`TZ=UTC`) ด้วยข้อมูลจริงของร้าน D Gas (`vat_registered=true` อยู่แล้วจริง)** — สร้างบิลขาย+รายจ่าย+ใบรับสินค้าทดสอบจริงผ่าน API ครบ 3 endpoint ยืนยัน `ledger_transactions.tax_amount` ตรงกับยอด VAT ที่คำนวณต่อธุรกรรมทุกจุด (20/20 assertion ผ่าน), ยืนยัน `tax-report.js` รวมยอดทั้ง 3 รายการเข้า `salesVat`/`purchaseVat` ถูกต้อง + จัดกลุ่ม drill-down แยกตามหมวดหมู่ถูกต้อง (ไม่มี `_groupKey` หลุดออกไปใน response), ยืนยัน `ledger_transactions` ของ `receives.js` เขียนสำเร็จจริงแล้ว (พิสูจน์ว่าบั๊ก ReferenceError แก้แล้ว) — ลบข้อมูลทดสอบ + คืนค่าสต็อค/ต้นทุนที่กระทบกลับที่เดิมสะอาดหลังทดสอบ
    - **ยังไม่ได้ทำ (out of scope รอบนี้, flag ไว้เป็นงานต่อเนื่องจากข้อ 73):** `export.js` (Excel export) ยังมีช่องว่างเดียวกันกับ VAT/delivery-revenue ที่ `reports.js`/`tax-report.js` เพิ่งแก้ — ยังไม่ได้ไล่แก้
    - **Deploy production แล้ว (2026-08-09)** — revision จริง `smileslip-dashboard-00308-x5b` (ข้อความ deploy โชว์ revision เดิม `00307-km7` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง) — verified บน production จริงครบ 6/6: ยิงบิลขายจริงกับร้าน D Gas → `ledger_transactions.tax_amount` ตรงกับ VAT ที่คำนวณ → `tax-report.js` เห็นยอดถูกต้องและจัดกลุ่ม drill-down "ขายหน้าร้าน (ไม่มีเลขภาษีผู้ซื้อ)" ถูกต้อง — ลบข้อมูลทดสอบสะอาดหลังทดสอบ

75. **หน้ากราฟวิเคราะห์ยังโหลดจาก Google Sheets (endpoint ที่ 5 ที่ตกหล่นจากข้อ 61) — ไม่แสดงข้อมูลใหม่เลย + ตั้งค่าสาขาขาดเบอร์โทร/บัญชีธนาคาร:**
    - **บั๊ก — `api/shop/analytics.js` ไม่เคยถูกย้ายมา Supabase:** ข้อ 61 ไล่แก้ 4 endpoint ที่ยังอ่าน/เขียน Google Sheets ตรงๆ หลัง Phase 3 Tier 6 ตัดบอทไม่ให้เขียน Sheets แล้ว (`transactions.js`/`export.js`/`tax-report.js`/`add-transaction.js`) แต่พลาด `shop/analytics.js` (หน้า "กราฟวิเคราะห์") ไปจุดหนึ่ง — ยังคงแลก Google access token + อ่าน Sheets range `A:P` ตรงๆ (คอลัมน์ตรงกับ schema เก่า 16 คอลัมน์) ทำให้กราฟไม่เห็นรายการใหม่เลยตั้งแต่บอทเลิกเขียน Sheets — แก้โดยเปลี่ยนมาอ่าน `ledger_transactions` ผ่าน `fetchLedgerRows()` (helper กลางเดียวกับที่ endpoint อื่นใช้ตั้งแต่ข้อ 61) ไม่ต้องเชื่อมต่อ Google เลยด้วย (เดิมบังคับ ขึ้น error "ยังไม่ได้เชื่อมต่อ Google Sheets" ถ้าไม่เชื่อม) — เพิ่ม `transactionIsoDate` (ISO YYYY-MM-DD จากวันที่ธุรกรรมจริง ปฏิทินกรุงเทพ) ใน `rowToLedgerRow()` (`lib/ledger-supabase.js`) ให้ analytics.js group รายวัน/รายเดือนได้ — เปลี่ยนความหมายการ group จาก "วันที่บันทึกเข้าระบบ" (Sheets เดิม) เป็น "วันที่ธุรกรรมจริง" (รองรับ backdate ถูกต้องกว่า)
    - **🐛 เจอบั๊กแฝงเพิ่มระหว่างทดสอบ (มีมาตั้งแต่สร้างไฟล์ ไม่เกี่ยวกับการย้าย Sheets→Supabase):** `Object.values(monthlyMap)` ที่ใช้สร้าง `monthlyData` array ผิดลำดับมาตลอด — คีย์ `'10'`/`'11'`/`'12'` เข้าเงื่อนไข "canonical integer string" ตามสเปก JS ทำให้ JS engine เรียงคีย์เหล่านี้ขึ้นก่อนคีย์ `'01'`-`'09'` เสมอไม่ว่าจะ insert ตามลำดับไหน (พิสูจน์ตรงด้วย `Object.keys()`: ได้ `['10','11','12','01',...,'09']`) ผลคือกราฟรายเดือนแสดงเรียง ต.ค.,พ.ย.,ธ.ค.,ม.ค.,...,ก.ย. มาตลอดแทนที่จะเป็น ม.ค.-ธ.ค. — แก้โดย build `monthlyData` เป็น array ตรงๆ ตามลำดับเดือน 1-12 แทนพึ่ง `Object.values()`
    - **ตั้งค่าสาขา (`dashboard.js` → จัดการสาขา) เพิ่มเบอร์โทรสาขา + บัญชีธนาคารสาขา:** ผู้ใช้ชี้ว่าสาขามีแค่ชื่อ+แบรนด์ ไม่มีเบอร์โทร/บัญชีธนาคารแยกต่อสาขา (สำหรับร้านที่แต่ละสาขามีบัญชีรับเงินคนละบัญชี) — เพิ่ม:
      - **`shop_branches.phone`** — inline-edit เหมือน `address`/`brand_name` เดิม (ว่าง = ใช้เบอร์ร้านหลัก) — `api/shop/branches.js` POST/PATCH เขียนแบบ best-effort แยกจาก insert/update หลักเหมือน pattern `address` (กันพังถ้ายังไม่ได้รัน SQL)
      - **`shop_bank_accounts.branch_id`** (nullable FK → `shop_branches.id`, `ON DELETE SET NULL`) — บัญชีที่ `branch_id=null` คือบัญชีรวมของร้าน (พฤติกรรมเดิมทุกบัญชีที่มีอยู่แล้ว ไม่กระทบ) บัญชีใหม่ผูกกับสาขาได้ผ่านฟอร์มเล็กๆ ในการ์ดของแต่ละสาขาโดยตรง (คนละที่จาก "บัญชีธนาคารรับเงิน" ของแท็บตั้งค่าที่จัดการบัญชีรวมของร้าน) — `api/shop/bank-accounts.js` POST ลอง insert พร้อม `branch_id` ก่อนเสมอถ้าระบุมา แล้ว fallback เป็น insert แบบไม่มี `branch_id` ถ้า error (กันพังถ้ายังไม่ได้รัน SQL) + GET รองรับ `?branchId=` กรอง
      - **ยังไม่ได้ทำ (ตั้งใจไม่ทำตอนนี้ เพราะยังไม่มีจุดไหนในระบบใช้ต่อ):** เชื่อมบัญชีธนาคาร/เบอร์โทรของสาขาเข้ากับ `detectTypeFromBankAccounts()` ของบอท (ตอนนี้ตรวจ income/expense จากชื่อบัญชีระดับร้านทั้งหมดเท่านั้น ไม่แยกตามสาขา) — ถ้าต้องการให้บอทรู้ว่าสลิปที่ส่งเข้ากลุ่มไลน์สาขาไหน ควรตรงกับบัญชีธนาคารของสาขานั้นเท่านั้น ต้องทำเพิ่มเป็นงานแยก
    - **ทดสอบยิงจริงกับ dev server (`TZ=UTC`) ด้วยข้อมูลจริงของร้าน D Gas:** `analytics.js` — insert แถว ledger ทดสอบตรง ยืนยัน `summary.totalIncome`/`monthlyData[เดือนปัจจุบัน]`/`dailySummary` สะท้อนถูกต้อง + ยืนยัน `monthlyData[0].month==='ม.ค.'` และ `[11].month==='ธ.ค.'` (แก้ลำดับถูกต้องแล้ว) + ไม่มี `notConnected` flag อีกต่อไป (8/8 ผ่าน) — `branches.js`/`bank-accounts.js` — PATCH เบอร์โทร + POST บัญชีธนาคารผูกสาขาไม่ error แม้ SQL ยังไม่ได้รัน (fail-safe ทำงานถูกต้อง, ยืนยันด้วยการทดสอบ GET กรอง `branchId` คืนค่าว่างเปล่าตามคาด เพราะคอลัมน์ยังไม่มี) — ลบข้อมูลทดสอบสะอาดหลังทดสอบทุกจุด
    - **ยังไม่ได้รัน SQL** — เพิ่มคอลัมน์ `shop_branches.phone` และ `shop_bank_accounts.branch_id` (ดูหัวข้อ "ต้องทำด้วยมือ — Supabase SQL" ด้านล่าง) — ก่อนรัน ทุกอย่างยังทำงานปกติทุกประการ (เบอร์โทร/บัญชีธนาคารสาขาแค่ไม่ถูกบันทึกจริง ไม่ error)
    - **Deploy production แล้ว (2026-08-09)** — revision จริง `smileslip-dashboard-00309-zwl` (ข้อความ deploy โชว์ revision เดิม `00308-x5b` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง) — verified บน production จริงครบ 5/5: กราฟวิเคราะห์อ่าน `ledger_transactions` ถูกต้อง (insert แถวทดสอบจริงแล้วยืนยัน `summary`/`monthlyData` สะท้อนถูกต้อง) + ลำดับเดือน ม.ค.-ธ.ค. ถูกต้องแล้ว — ลบข้อมูลทดสอบสะอาดหลังทดสอบ

76. **ผู้ใช้ขอระบบบัญชีเต็มรูปแบบแบบ FlowAccount (ภาษีเงินได้นิติบุคคล/บุคคลธรรมดาปลายปี, เงินเดือน+ประกันสังคม, ยืนยันหลักการ "VAT ไม่ใช่รายได้/รายจ่ายของกิจการ") — ตกลงแบ่ง 3 เฟสตามลำดับ + รองรับทั้ง 2 ประเภทนิติบุคคล (`user_type` ต่อร้าน) — **เฟส 1-3 เสร็จสมบูรณ์ครบทุกเฟสแล้ว:**
    - **เฟส 1 (เสร็จแล้ว) — แก้ P&L/สินค้าขายดีให้แยก VAT ออกจากรายรับ/รายจ่ายจริง:** ตรวจโค้ดพบว่า `api/pos/reports.js`'s `type=pl`/`type=topsellers` คำนวณ "รายได้" จาก `item.price * qty` ตรงๆ (รวม VAT อยู่แล้วถ้าสินค้าตั้งราคาแบบ "รวม VAT แล้ว") ทั้งที่ "ต้นทุน" (`pos_products.cost`, ต้นทุนถ่วงน้ำหนักจาก `receives.js`) เป็นฐานก่อน VAT อยู่แล้วเสมอ — ทำให้ margin/กำไรที่โชว์เพี้ยนไปในทางที่ดูแย่กว่าจริง (รายได้สูงเกินจริงเทียบกับต้นทุนที่ถูกต้องอยู่แล้ว) ตรงกับหลักการที่ผู้ใช้อธิบาย: "VAT ไม่ใช่รายได้ของบริษัท สรรพากรมองว่าเป็นแค่ตัวกลางสลับกระเป๋าเงิน"
      - เพิ่ม `lineRevenueBase(price, qty, vatType)` (helper กลางใน `reports.js`, ตรรกะเดียวกับ `computeVatBreakdown()` ใน `google-pos.js` แค่ทำต่อรายการเดียวแทนอาเรย์รวม) + `getVatRegistered(shopId)` — gate การแยก VAT ด้วย `pos_configs.vat_registered` เสมอ (ร้านไม่จด VAT ไม่ได้แยก/ไม่ได้รับสิทธิ์เรียกคืนภาษีซื้อ ราคาที่บันทึกไว้คือต้นทุน/รายได้จริงทั้งก้อน พฤติกรรมเดิมไม่เปลี่ยน)
      - `type=pl`: แก้ `byCategory` loop ให้ใช้ `lineRevenueBase()` แทน `item.price*qty` ตรงๆ (เมื่อจด VAT) + แก้ `totalExpenses` ให้ใช้ `e.subtotal` (ฐานก่อน VAT, มีอยู่แล้วใน `pos_expenses`) แทน `e.total` (ยอดรวม VAT) เมื่อจด VAT — เหตุผล: ภาษีซื้อของร้านที่จด VAT เรียกคืนได้ ไม่ใช่ต้นทุนจริง ร้านไม่จด VAT เรียกคืนไม่ได้จึงยังนับเป็นต้นทุนจริงเหมือนเดิม
      - `type=topsellers`: แก้ตรรกะเดียวกันสำหรับ `revenue` ต่อ SKU และ `summary.total_revenue`
      - **Known limitation ที่ไม่ได้แก้ (มีอยู่ก่อนแล้ว ไม่เกี่ยวกับ VAT):** ทั้ง `byCategory`/`top_sellers` คำนวณจากราคาต่อรายการตรงๆ ไม่หักส่วนลดระดับทั้งบิล (bill-level discount) มาตั้งแต่สร้างฟีเจอร์ — ตั้งใจไม่แก้พร้อมกันเพราะเป็นคนละปัญหาจาก VAT (ไม่อยากขยายสโคป)
      - **ทดสอบยิงจริงกับ dev server (`TZ=UTC`) ด้วยข้อมูลจริงของร้าน D Gas:** ขายสินค้าจริงราคา ฿490 (vat_type='รวม VAT แล้ว') → ยืนยัน `pl.categories[].revenue` และ `topsellers[].revenue` ตรงกับ `490/1.07=457.94` เป๊ะ (ไม่ใช่ 490) + สร้างรายจ่ายจริง ฿107 (vat_type='รวม VAT แล้ว') → ยืนยัน `pl.summary.total_expenses` ใช้ `subtotal=100` ไม่ใช่ `total=107` — **บทเรียนระหว่างทดสอบ:** เจอผลลัพธ์ที่ดูเหมือนบั๊ก (แถวทดสอบไม่โผล่ในรายงานที่กรองด้วย `dateFrom=วันนี้`) แต่พิสูจน์แล้วว่าเป็นบั๊กในสคริปต์ทดสอบเอง ไม่ใช่แอป — `resolveRecordDateTime()` (โค้ดแอป) ประทับเวลาธุรกรรมเป็นเวลาไทย (UTC+7) เสมอโดยเจตนา (ระบุ `timeZone:'Asia/Bangkok'` ตรงๆ ไม่สนใจ `TZ` env ของ process) ในขณะที่สคริปต์ทดสอบคำนวณ "วันนี้" จาก `toISOString()` (UTC เสมอ) — ช่วงเวลาใกล้เที่ยงคืน UTC ทำให้ปฏิทินไทย (UTC+7) ข้ามวันไปแล้วแต่ปฏิทิน UTC ยังไม่ข้าม ทำให้ตัวกรองวันที่ของสคริปต์ผิดวันไป 1 วัน — แก้สคริปต์ทดสอบให้คำนวณ "วันนี้" ด้วย `Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Bangkok'})` แทนแล้วผ่านครบ (4/4 + 4/4) — ลบข้อมูลทดสอบสะอาดหลังทดสอบทุกจุด
    - **เฟส 1 Deploy production แล้ว (2026-08-09)** — revision จริง `smileslip-dashboard-00310-qwq` (ข้อความ deploy โชว์ revision เดิม `00309-zwl` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง) — verified บน production จริงครบ 3/3: ขายสินค้าจริง ฿490 (vat_type=รวม VAT แล้ว) → pl/topsellers revenue ตรงกับ 490/1.07 เป๊ะ — ลบข้อมูลทดสอบสะอาดหลังทดสอบ
    - **เฟส 2 (ระบบเงินเดือน) — เสร็จแล้ว:** สร้างโมดูลใหม่ทั้งหมด — `lib/payroll.js` (คำนวณ), `api/pos/payroll-employees.js`/`api/pos/payroll-runs.js` (CRUD), 2 ตารางใหม่ (`pos_payroll_employees`/`pos_payroll_runs`), แท็บใหม่ "💰 เงินเดือน" ใน `pos.js`
      - **พนักงานเงินเดือนแยกจาก `pos_staff` โดยเจตนา** (คนละแนวคิด — พนักงานเงินเดือนไม่จำเป็นต้องเข้า POS ด้วย PIN เช่นแม่ครัว/คนทำความสะอาด และพนักงาน POS บางคนก็อาจเป็นแค่ part-time ไม่อยู่ payroll)
      - **ประกันสังคม (มาตรา 33):** 5%/5% ฐานเงินเดือน 1,650-15,000 บาท (สูงสุด ฿750/เดือน/ฝั่ง) — `calcSSO()` clamp ฐานเงินเดือนก่อนคูณอัตรา
      - **ประมาณการณ์ภาษีหัก ณ ที่จ่าย** — วิธี annualization มาตรฐานของ PND1 (เงินเดือน×12 → หักค่าใช้จ่าย 50% เพดาน 100,000 + ค่าลดหย่อนส่วนตัว 60,000 (สมมติโสด ไม่มีคู่สมรส/บุตร/ลดหย่อนอื่น) → คำนวณภาษีขั้นบันได 8 ระดับ → หาร 12) — `estimateMonthlyWithholding()`
      - **แก้ไขตัวเลขที่คำนวณให้ได้เสมอก่อนบันทึกจริง** (ช่อง override ทั้ง SSO พนักงาน + ภาษีหัก ณ ที่จ่าย ในหน้า "รันจ่ายเงินเดือน") — ไม่บังคับใช้ตัวเลขประมาณการณ์ เพราะรู้ว่าไม่ตรง 100% ทุกกรณี (พนักงานมีคู่สมรส/บุตร/ค่าลดหย่อนอื่นที่ระบบไม่รู้)
      - **บันทึกเป็นรายจ่าย "เงินเดือนพนักงาน" เข้าบัญชีหลักอัตโนมัติ** (`writePayrollToMainSheets`, pattern เดียวกับ `expenses.js`/`receives.js`) — **ยอดที่บันทึกคือ `gross_pay + sso_employer`** (ต้นทุนรวมที่บริษัทจ่ายออกจริง ไม่ใช่แค่ `net_pay` ที่พนักงานได้รับ เพราะเงินสมทบนายจ้างเป็นต้นทุนเพิ่มต่างหากนอกเหนือจากเงินเดือน)
      - **ยกเลิกรอบจ่าย (DELETE)** — soft-delete แถว `pos_payroll_runs` + ลบแถวคู่ใน `ledger_transactions` (จับคู่ด้วย `raw_data->>run_no` เหมือน pattern ที่ใช้กับ `receives.js`/`expenses.js`)
      - **พิมพ์สลิปเงินเดือน** — HTML string ธรรมดาผ่าน `openPrintWindow()` (helper เดิมที่ใช้กับใบเสร็จ) ไม่ใช้ `buildReceiptHtml()` เพราะโครงสร้างข้อมูลต่างกันเยอะ (เขียนแยกเฉพาะ) — มีข้อความกำกับชัดเจนว่ายอดภาษีหัก ณ ที่จ่ายเป็นตัวประมาณการณ์
      - **ความปลอดภัย:** ข้อมูลเงินเดือน/เลขบัตรประชาชนเป็นข้อมูลอ่อนไหวระดับ HR — บล็อก staff session ทุกกรณีไม่ว่าจะมีสิทธิ์อะไรก็ตาม (`blockAllStaffSessions()`, pattern เดียวกับ `pos-config.js`) เฉพาะเจ้าของร้าน/แอดมินร้านเท่านั้นที่เข้าถึงได้ ไม่โชว์แท็บนี้ในโหมดแคชเชียร์และไม่มีใน `pos-staff.js` เลย
      - **ทดสอบยิงจริงกับ dev server (`TZ=UTC`) ด้วยข้อมูลจริงของร้าน D Gas ครบ 23/23:** สร้าง/แก้ไขพนักงานทดสอบ, ยืนยัน staff-session ถูกบล็อก 403 ถูกต้อง, รันจ่ายเงินเดือนฐาน ฿20,000 → SSO capped ที่ 750 ถูกต้อง (เกิน 15,000) + WHT ประมาณการณ์ = 0 ตรงกับคำนวณมือ (annual 240k เข้าขั้นบันไดยกเว้นภาษีพอดี) + net_pay ถูกต้อง, ยืนยัน `ledger_transactions.amount = gross+sso_employer` (20,750) ถูกต้อง, ทดสอบ override SSO/WHT เองก่อนบันทึกทำงานถูกต้อง, ยกเลิกรอบจ่าย → ลบแถว ledger คู่กันสำเร็จ — **ทดสอบสูตรแยกต่างหาก** (`estimateMonthlyWithholding(50000)` → 1,791.67 ตรงกับคำนวณมือที่ขั้นบันได 5%/10% เริ่มมีผลจริง, `calcSSO()` ครบ 3 เคส: ต่ำกว่าเพดานล่าง (clamp 1,650), อยู่ในช่วงปกติ, ไม่เข้าประกันสังคม) — ลบข้อมูลทดสอบสะอาดหลังทดสอบทุกจุด
      - **เฟส 2 Deploy production แล้ว (2026-08-09)** — revision จริง `smileslip-dashboard-00311-bdv` (ข้อความ deploy โชว์ revision เดิม `00310-qwq` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง) — verified บน production จริงครบ 7/7 (สร้างพนักงาน+รันจ่ายเงินเดือนจริง SSO/net ถูกต้อง + ledger dual-write สำเร็จ) — ลบข้อมูลทดสอบสะอาดหลังทดสอบ
    - **เฟส 3 (รายงานภาษีปลายปีนิติบุคคล/บุคคลธรรมดา) — เสร็จแล้ว:** เพิ่ม `lib/tax-estimate.js` (ใหม่, `CORPORATE_TAX_BRACKETS`/`calcCorporateTax()` + `estimateAnnualTax(userType, netProfit)` เรียก `calcAnnualPIT()` จาก `lib/payroll.js` สำหรับบุคคลธรรมดา) + `type=annual_tax` ใหม่ใน `reports.js` (`&year=YYYY`) — เช็คจาก `shop_profiles.user_type` เองว่าใช้สูตรไหน (`corporate` = อัตรา SME ยกเว้น 300k แรก/15% ของ 300k-3M/20% ส่วนเกิน, อย่างอื่นถือเป็นบุคคลธรรมดา = หักค่าลดหย่อน 60,000 ก่อนเข้าขั้นบันได 8 ระดับ — ตั้งใจไม่ใช้การหักแบบ 50%/100,000 ของเงินได้ประเภทเงินเดือนซ้ำ เพราะกำไรสุทธิหักต้นทุน/ค่าใช้จ่าย/เงินเดือนไปแล้ว) — มีข้อความกำกับชัดเจนในทุกจุด (UI + API response's `note` field) ว่าเป็นตัวประมาณการณ์สำหรับวางแผนเท่านั้น ไม่ใช่เครื่องมือยื่นภาษีที่แม่นยำ 100% เพราะมีรายการปรับปรุงทางภาษีอีกหลายอย่าง (ค่าเสื่อมราคา, รายจ่ายต้องห้ามตามมาตรา 65 ตรี, ค่าลดหย่อนส่วนบุคคลอื่นๆ ฯลฯ) ที่ระบบไม่มีข้อมูลพอจะรู้เอง — ต้องตรวจสอบกับนักบัญชีก่อนยื่นจริงเสมอ
      - **🐛 เจอบั๊กจริงระหว่างทำ (เกี่ยวข้องกับเฟส 2 ที่ทำไปก่อนหน้า):** `type=pl` ไม่เคยรวมต้นทุนเงินเดือนเข้า `total_expenses` เลยตั้งแต่สร้างระบบเงินเดือน (payroll-runs.js เขียนเข้า `ledger_transactions` ตรงๆ ไม่ผ่าน `pos_expenses` ที่ `type=pl` อ่านอยู่) ทำให้กำไรสุทธิที่รายงานสูงเกินจริงสำหรับร้านที่มีพนักงานประจำ — แก้โดยดึงตรรกะ P&L ทั้งหมดไปรวมศูนย์ที่ `computePL(shopId, from, to)` (ฟังก์ชันใหม่) ให้ `type=pl` และ `type=annual_tax` เรียกใช้ร่วมกัน กันตรรกะเพี้ยนกันในอนาคต — เพิ่ม `fetchPayrollRuns()` + กรองด้วย `inRangeISO(r.paid_at, from, to)` (ไม่ใช่ `inRange()` เพราะ `paid_at` เป็น timestamptz จริง ไม่ใช่สตริงไทย) เพิ่ม `summary.total_payroll` แยกให้เห็นชัดในทั้ง 2 endpoint
      - **ทดสอบยิงจริงกับ dev server (`TZ=UTC`) ด้วยข้อมูลจริงของร้าน D Gas ครบ 12/12:** สลับ `user_type` ชั่วคราวทดสอบทั้ง 2 สูตร (คืนค่าเดิมหลังทดสอบ) — ยืนยัน `net_profit` เท่ากันไม่ว่าจะเป็นนิติบุคคลหรือบุคคลธรรมดา (มีแค่สูตรภาษีต่างกัน), ยืนยันภาษีที่คำนวณตรงกับการคำนวณมือทุกจุด, ยืนยัน `type=pl`/`type=annual_tax` ให้ตัวเลข `total_expenses` สอดคล้องกัน (parity cross-check) — **ทดสอบสูตรแยกต่างหากที่ยอดสูงพอจะเจอขั้นภาษีจริง** (นิติบุคคล ฿5,000,000 → ฿805,000 ตรงกับคำนวณมือ, บุคคลธรรมดา taxable ฿740,000 → ฿63,500 ตรงกับคำนวณมือ) เพราะกำไรจริงของ D Gas ปีนี้เล็กเกินกว่าจะเข้าขั้นภาษีที่ไม่ใช่ 0% — ลบ/คืนค่าข้อมูลทดสอบสะอาดหลังทดสอบทุกจุด
      - **Deploy production แล้ว (2026-08-09)** — เจอปัญหาเดิมซ้ำ (`429 RESOURCE_EXHAUSTED` จาก Cloud Build API เอง) แต่รอบนี้ build สำเร็จจริงแต่ไม่มี revision ใหม่เกิดขึ้นเลย (คนละเคสจาก pattern เดิมที่ traffic แค่ไม่ขยับ) — แก้โดยดึง image digest จาก build ที่สำเร็จ (`gcloud builds describe <id> --format="value(results.images[0].digest)"`) แล้ว `gcloud run deploy --image=...` ตรงๆ ข้าม rebuild — ได้ revision จริง `smileslip-dashboard-00312-kt2` (ข้อความ deploy โชว์ revision เดิม `00311-bdv` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง) — verified บน production จริงครบ 6/6
    - **สรุป: ระบบบัญชีเต็มรูปแบบ (เฟส 1-3) เสร็จสมบูรณ์ครบทุกเฟสตามที่ผู้ใช้ขอแล้ว** — VAT แยกออกจาก P&L ถูกต้อง, ระบบเงินเดือน+ประกันสังคมใช้งานได้จริง, รายงานภาษีปลายปีประมาณการณ์ทั้ง 2 ประเภทนิติบุคคลพร้อมใช้

77. **แก้ราคาในตะกร้าได้ตรงๆ + แยกตัวกรอง "รอยืนยันรับเงิน/ของ" ในหน้าออเดอร์จัดส่ง (client-side logic ล้วนๆ ไม่แตะ backend เลย):**
    - **บั๊ก UX — แก้ราคาต้องเปิด modal ชำระเงินก่อน:** เดิมช่องแก้ราคาต่อรายการ (`updatePrice()`) มีอยู่แล้วแค่ใน modal "ชำระเงิน" เท่านั้น ไม่มีในตะกร้าหลักบนหน้าขาย ทำให้ถ้าจะแก้ราคาก่อนกดส่งงานให้พนักงานจัดส่ง (`openDelivery()`) ต้องเปิด modal ชำระเงินก่อน (ซึ่งอ่าน/เขียน `cart` state เดียวกัน) แล้วปิดออกมาอ้อมๆ ถึงจะกดปุ่มจัดส่งได้ — เพิ่มช่องแก้ราคาเดียวกัน (เรียก `updatePrice()` ตัวเดียวกันเป๊ะ ไม่ได้เขียนใหม่) เข้าไปทั้งตะกร้า desktop sidebar และ mobile cart drawer โดยตรง
    - **บั๊ก UX — ออเดอร์ที่พนักงานยืนยันจัดส่งแล้วแต่แอดมินยังไม่ได้กดรับเงิน/รับของ ปนอยู่ในตัวกรอง "จัดส่งแล้ว":** ระบบมีปุ่ม "ยืนยันรับเงินเข้าร้าน"/"ยืนยันรับของคืนเข้าคลัง" อยู่แล้วในการ์ดออเดอร์ (สองชั้นกันเงิน/ของหาย จากข้อ 22) แต่ตัวกรองสถานะเดิมมีแค่ "ค้างจัดส่ง"/"จัดส่งแล้ว" (`o.status==='ส่งแล้ว'` เฉยๆ) ไม่แยกว่าออเดอร์นั้นแอดมินยืนยันรับเงิน/ของครบแล้วจริงหรือยัง ทำให้ต้องเลื่อนหาออเดอร์ที่รอยืนยันท่ามกลางออเดอร์ที่ปิดจบแล้วจริงๆ — เพิ่ม `isOrderFullyConfirmed(order)` (เช็คเงื่อนไขเดียวกับที่ปุ่มยืนยันในการ์ดออเดอร์ใช้อยู่แล้วเป๊ะ: `payment_method!=='ค้างจ่าย'` ต้องมี `cash_received`, มีรายการ `returned_qty>0` ต้องมี `goods_received` — pattern เดียวกับ `isFullyDone` ของแท็บ "เก็บเงิน/ของ" ที่มีอยู่แล้วจากข้อ 39) แยกตัวกรอง "จัดส่งแล้ว" (`o.status==='ส่งแล้ว'` เฉยๆ) ออกเป็น 2 อัน: **"💰 รอยืนยันรับเงิน/ของ"** (ส่งแล้วแต่ยังไม่ครบ) กับ **"✅ จัดส่งสำเร็จ"** (ครบทุกอย่างจริง)
    - **ทดสอบ:** ตรวจ logic ด้วยข้อมูลจริงของร้าน D Gas (read-only, ไม่มีการเขียนข้อมูล) ยืนยันว่า 3 กลุ่ม (ค้างจัดส่ง/รอยืนยัน/จัดส่งสำเร็จ) รวมกันครบเท่าจำนวนออเดอร์ทั้งหมดพอดี (10 ออเดอร์ → 1/0/9) ไม่มีออเดอร์ตกหล่นหรือซ้ำกลุ่ม — verified บน production จริงว่าโค้ดใหม่ถูก deploy จริงโดยเช็ค string ใหม่ในไฟล์ JS ที่เสิร์ฟจริง (`grep` หา "รอยืนยันรับเงิน"/"จัดส่งสำเร็จ" ใน bundle ที่ดาวน์โหลดจาก production ตรงๆ)
    - **Deploy production แล้ว (2026-08-09)** — เจอปัญหาเดิมซ้ำอีกครั้ง (`429` จาก Cloud Build ตอน deploy → build สำเร็จจริงแต่ไม่มี revision ใหม่เกิดขึ้น) แก้ด้วยวิธีเดิม (ดึง image digest จาก build ที่สำเร็จมา `gcloud run deploy --image=...` ตรงๆ ข้าม rebuild) — revision จริง `smileslip-dashboard-00313-n8w` (ข้อความ deploy โชว์ revision เดิม `00312-kt2` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง)
    - **บทเรียนใหม่จากรอบนี้:** ระหว่างรอ build ที่ค้าง เผลอรันคำสั่ง polling ผ่าน Bash tool (`gcloud builds describe ...`) ซึ่งใช้ไม่ได้บนเครื่องนี้ (Python not found — เหตุผลเดียวกับที่ต้องใช้ PowerShell สำหรับ `gcloud` ทุกคำสั่งในโปรเจกต์นี้มาตลอด) ทำให้ loop ค้างเงียบไม่มีการแจ้งเตือนเป็นเวลานานโดยไม่รู้ตัว (เข้าใจผิดว่ากำลังรอผลอยู่ปกติ) — **ต้องเช็คสถานะ Cloud Build/Cloud Run ผ่าน PowerShell เท่านั้นเสมอ ห้ามใช้ Bash tool เรียก `gcloud` แม้แต่คำสั่ง read-only อย่าง `describe`/`list` ก็ตาม**

78. **แก้บั๊กสำคัญ: `contact.debt`/`contact.cylinders` เพี้ยนหลังยืนยันจัดส่งสำเร็จ (ไม่ sync กับรายงาน) + เพิ่มปุ่ม "ส่งคนไปเก็บ"/เลือกวิธีชำระในหน้ารับชำระเงินเชื่อ + แก้ Biller ID ผิดค่าที่บันทึกไว้เดิม — ผู้ใช้แจ้งว่ายืนยันจัดส่งสำเร็จแล้วหน้าผู้ติดต่อยังขึ้น "ค้างสินค้า"/ค้างชำระไม่เป็น 0 ทั้งที่รายงาน "เงินเชื่อ" ถูกต้องดี (แปลว่าปัญหาอยู่ที่ฟิลด์แคชบนผู้ติดต่อ ไม่ใช่ข้อมูลธุรกรรมจริง):**
    - **Root cause (คอนเฟิร์มจากโค้ดจริง, `api/pos/delivery.js`):** ทั้ง `contact.debt` และ `contact.cylinders` ถูกเขียน **สองจุดซ้อนกันสำหรับออเดอร์เดียวกัน** — จุดแรกตอน **สร้างออเดอร์** (POST, สมมติว่าลูกค้าจะไม่คืนของเดิม/ไม่จ่ายเลยแบบเลวร้ายที่สุด) จุดที่สองตอน **ยืนยันจัดส่งสำเร็จ** (PATCH `confirm_delivery`, คำนวณยอดสุทธิจริงหลังหักคืน/หักที่จ่ายมาแล้ว) — เดิมทั้งสองจุดบวกเข้า contact ตรงๆ ไม่มีเงื่อนไขเชื่อมกันเลย ทำให้:
      - **ถัง (cylinders):** สร้างออเดอร์บวกเต็มจำนวนที่จะส่ง (`cylinders_delivered`) ทันที แล้ว confirm_delivery บวก "ยอดสุทธิหลังหักคืน" ซ้ำอีกรอบ — ถ้าลูกค้าไม่คืนของเดิมเลย (`returned_qty=0`, กรณีปกติที่สุดตามที่ผู้ใช้อธิบาย "ไม่ต้องกดรับถังคืนมา") ยอดถังจะเฟ้อเป็น **2 เท่า** ของความจริงทันที
      - **หนี้ (debt):** สร้างออเดอร์แบบ "ค้างจ่าย" บวกหนี้เต็มจำนวนทันที แล้ว `pos-staff.js`'s หน้ายืนยันจัดส่งพรีฟิลวิธีชำระเป็นค่าเดิมตอนสร้างออเดอร์เป๊ะ (`order.payment_method`) — ถ้าพนักงานไม่เปลี่ยนอะไรเลย (กรณีปกติที่สุด: ลูกค้ายังค้างเหมือนเดิม) จะบวกหนี้**ซ้ำสองเท่า**เงียบๆ และถ้าลูกค้าจ่ายเต็มตอนของถึงจริง (พนักงานเปลี่ยนวิธีชำระเป็นเงินสด/โอนตอนยืนยัน) หนี้เดิมที่บวกไว้ตั้งแต่สร้างออเดอร์ก็**ไม่เคยถูกล้างออกเลยสักครั้ง** (ค้างชำระไม่มีทางกลับมาเป็น 0 บาทได้แม้รับเงินเต็มจำนวนแล้วจริง — ตรงกับที่ผู้ใช้เจอ)
    - **แก้ถัง:** เลื่อนการคอมมิตค่าจริงไปไว้ที่ `confirm_delivery` จุดเดียว (เหมือนสต็อคสินค้าที่ก็ไม่ถูกแตะตอนสร้างออเดอร์อยู่แล้ว) — ตอนสร้างออเดอร์ **ไม่บวก `cust.cylinders`/ไม่เขียน audit log "ยืม" อีกต่อไป** เหลือแค่เตือนแบบประมาณการณ์ (ไม่ commit) ว่าถ้าไม่คืนของเดิมเลยจะเกินวงเงินยืมสูงสุดไหม — ผลพลอยได้: audit log ("บันทึกแลกเปลี่ยน") ที่ใช้คำนวณ auto-fill ในฟีเจอร์ "ส่งพนักงานไปเก็บ" (ข้อ 34 ข้อ 5) จะแม่นขึ้นด้วย เพราะไม่มี log รวม "สินค้าหมุนเวียน (รวม)" ไม่มี SKU มาปนกับ log ที่แยก SKU จริงจาก confirm_delivery อีกต่อไป (เคส "unreconciled ต้องกรอกเอง" ควรลดลง)
    - **แก้หนี้:** เขียนใหม่เป็นตรรกะ "กระทบยอด" (reconcile) แทนบวกแบบไม่มีเงื่อนไข — เทียบ `existing.payment_method`/`existing.total` (สถานะตอนสร้างออเดอร์) กับ `payment_method`/`partial_paid_amount` ที่ส่งมาตอน confirm แล้วคำนวณ **delta** ที่ต้องปรับ (`orderRemainingDebt - creationDebtAmount`) แทนการบวกยอดใหม่ทั้งดุ้นทุกครั้ง — ครอบคลุมครบ 4 เคส (ไม่เคยเป็นเครดิตเลย/เพิ่งกลายเป็นเครดิตตอนนี้/ยังคงเครดิตเหมือนเดิม ไม่ double-count/เปลี่ยนจากเครดิตเป็นจ่ายเต็ม ล้างหนี้เดิมออกให้ถูกต้อง) — API ยังคง return `debtAdded` ชื่อเดิม (ใช้แสดงในใบเสร็จ/หน้าสรุปพนักงาน) แต่เปลี่ยนความหมายเป็น "ยอดค้างชำระของออเดอร์นี้หลัง confirm" (`orderRemainingDebt`) แทนค่า delta ดิบ กัน `pos-staff.js` ต้องแก้ตาม
    - **ทดสอบยิงจริงกับ dev server ครบ 17 assertions** (สร้าง contact/order ทดสอบชั่วคราว ลบทิ้งหลังทดสอบทุกรอบ): ถังยืมเต็มไม่คืนเลย (ไม่เฟ้อ 2 เท่า), แลกเปลี่ยนเต็มจำนวน (net 0), คืนบางส่วน (net ถูกต้อง), หนี้ 4 เคสข้างบนครบทุกเคส, หนี้เดิมจากออเดอร์อื่นที่ไม่เกี่ยวกันไม่ถูกกระทบ
    - **เพิ่มปุ่ม "📤 ส่งพนักงานไปเก็บ"** ในหน้ารายงาน "🧾 เงินเชื่อ" (POS → รายงาน) ที่รายชื่อลูกค้าค้างชำระแต่ละคน (เดิมมีแค่ปุ่ม "✅ รับชำระ" ต่อบิล ไม่มีทางส่งพนักงานไปเก็บจากหน้านี้เลย ต้องย้ายไปหน้าผู้ติดต่อแทน) — จับคู่กับ contact จริงผ่าน `cust.customer_id` (มีอยู่แล้วใน `type=credit` response) แล้วเรียก `openCollectDispatch()` ตัวเดียวกับที่ปุ่มในหน้าผู้ติดต่อใช้อยู่แล้ว (ไม่เขียน dispatch logic ซ้ำ)
    - **เพิ่มตัวเลือกวิธีชำระ (เงินสด/โอน) ตอนกด "✅ รับชำระ"** — เดิมกด `confirm()` เฉยๆ ไม่มีทางระบุว่าลูกค้าเอาอะไรมาจ่าย (บันทึกเป็น "เงินสด" ในบัญชีหลักเสมอไม่ว่าจริงจะโอนมาก็ตาม) — เพิ่ม modal เลือก 💵 เงินสด / 🏦 โอน ก่อนยืนยันเสมอ ทั้ง `api/pos/sales.js` PATCH (ขายเชื่อหน้าร้าน) และ `api/pos/delivery.js` PATCH `credit_settled` (ค้างจ่ายจากออเดอร์จัดส่ง) รับ `collected_method` ใหม่ บันทึกลง `notes` ("ชำระด้วย: X") + ใช้ label ที่ถูกต้องตอนเขียนเข้าบัญชีหลัก (`writeToMainSheets`) — **ตั้งใจไม่เปลี่ยน `pos_sales.payment_method`/`pos_delivery_orders.payment_method` เอง** (ยังคงเป็น "เชื่อ"/"ค้างจ่าย" ตลอดไป) เพราะ `type=credit` ใน `reports.js` ใช้ค่านี้เป็นตัวกรองว่าบิลนี้เคยเป็นเงินเชื่อ (รวมที่ชำระแล้ว) — ถ้าเปลี่ยนจะทำให้บิลที่เพิ่งชำระหายจากรายงานเงินเชื่อไปเลย — **known gap ที่ตั้งใจไม่ทำรอบนี้:** ยังไม่ผูกเข้าระบบกะเงินสด (`computeExpectedCash()` กรอง `payment_method==='เงินสด'` ตรงๆ ซึ่งบิลเชื่อที่ชำระแล้วไม่เข้าเงื่อนไขนี้เพราะเหตุผลข้างบน) ต้องมีคอลัมน์แยกใหม่ก่อนถึงจะทำได้โดยไม่ชนกับตัวกรองรายงานเงินเชื่อ
    - **ทดสอบยิงจริงกับ dev server ครบ 9 assertions:** `payment_method` ไม่เปลี่ยน (ยังโผล่ในรายงานเงินเชื่อ "ชำระแล้ว" ปกติ), `notes` บันทึกวิธีชำระถูกต้องทั้ง sales.js/delivery.js, ยอดหนี้ลูกค้าเคลียร์ถูกต้อง
    - **แก้ Biller ID ที่บันทึกผิดค่าไว้เดิม (ร้าน D Gas)** — ผู้ใช้ส่งภาพหน้าจอ K SHOP ยืนยัน Biller ID จริง (ตรงกับที่เคยบันทึกไว้เป็นเคสตัวอย่างตั้งแต่ข้อ 5) แต่ค่าที่บันทึกจริงใน `pos_configs.scb_biller_id` ตอนตรวจสอบกลับเป็นคนละเลขกันเลย (ไม่ใช่ "เลขอ้างอิง" ที่มักกรอกผิดตามข้อ 71 ด้วยซ้ำ — ดูเหมือนข้อมูลทดสอบ/พิมพ์ผิดเก่าที่ค้างอยู่ ไม่เกี่ยวกับ Sheets→Supabase migration) — ยิง `PATCH /api/pos/pos-config` บันทึกค่าที่ถูกต้องแทนที่โดยตรง verified ทั้งการบันทึก (15 หลักผ่าน validation จากข้อ 71) และ `GET /api/pos/promptpay-qr` สร้าง QR แบบ Bill Payment สำเร็จจริง (`method:'billpayment'`) — ร้านสแกนจ่ายได้แล้วตอนนี้
    - **Deploy production แล้ว (2026-08-09)** — เจอ `429` จาก Cloud Build API ซ้ำอีกครั้งระหว่าง deploy (ตรงกับ pattern ที่เจอซ้ำหลายรอบ) แต่รอบนี้ deploy ผ่านสำเร็จในความพยายามเดียวหลัง retry — revision จริง `smileslip-dashboard-00314-p8h` (ข้อความ deploy โชว์ revision เดิม `00313-n8w` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง) — **verified บน production จริงครบ 6/6**: ยิง `POST`/`PATCH` `/api/pos/delivery` ตรงกับ production URL ยืนยันทั้งหนี้และถังไม่เฟ้อซ้ำ (900→900 ไม่ใช่ 1800, 4→4 ไม่ใช่ 8) + Biller ID ยังถูกต้อง — ลบข้อมูลทดสอบออกจากทั้ง dev และ production สะอาดครบทุกรอบ
    - **⚠️ แก้ไขบันทึกจากข้อนี้:** ตอนนั้นสรุปว่า "ร้านสแกนจ่ายได้แล้วตอนนี้" หลังแก้ค่า Biller ID ให้ถูกต้อง — **ยังไม่จริงทั้งหมด** ผู้ใช้สแกนจริงแล้วเจอ error "ข้อมูล QR/บาร์โค้ดไม่ถูกต้อง" ต่ออีก — สาเหตุที่แท้จริงคือขาดฟิลด์บังคับอีกตัว (Reference 1) แก้ต่อในข้อ 79

79. **ตามข้อ 78: QR Bill Payment ยังสแกนไม่ได้ต่อแม้ Biller ID จะถูกต้องแล้ว ("ข้อมูล QR/บาร์โค้ดไม่ถูกต้อง") — เจอ root cause ที่แท้จริงจากสเปกทางการของ ธปท.:**
    - **การสืบสวน:** parse payload ที่สร้างจาก `generateBillPaymentPayload()` แบบ TLV ด้วยมือ ยืนยันว่าโครงสร้าง/CRC ถูกต้อง 100% ตาม test vector มาตรฐาน (`123456789` → `29B1`, ตรงกับที่เคยยืนยันไว้แล้วในข้อ 71) — ไม่ใช่บั๊กจาก syntax/checksum — ดาวน์โหลดเอกสารทางการ **"Policy Guideline: Standardized Thai QR Code for Payment Transactions" ของธนาคารแห่งประเทศไทย (17 เม.ย. 2562)** มาตรวจตาราง 2.3 (Data structures) ของ Tag 30 (Bill Payment) โดยตรง พบว่า sub-tag ภายใน Tag 30 มี **Reference 1 (sub-tag "02") เป็น "M" (Mandatory — บังคับ)** ไม่ใช่แค่ทางเลือกเสริมแบบ Reference 2 ("O" — Optional) — โค้ดเดิม (`lib/thai-qr-billpayment.js`) **ไม่เคยส่งค่านี้เลยสักครั้งตั้งแต่สร้างฟีเจอร์** (`promptpay-qr.js` ไม่เคยมี `ref1` ให้ส่งต่อ) ทำให้ QR ทุกใบที่สร้างผ่านโหมด Bill Payment ขาดฟิลด์บังคับตามสเปกมาตลอด — แอปธนาคารบางเจ้า (ยืนยันจริงกับ K PLUS) ปฏิเสธทันทีตั้งแต่ขั้นตอนตรวจสอบโครงสร้าง QR ก่อนจะไปถึงขั้นเช็คว่า biller มีจริงไหมด้วยซ้ำ — อธิบายได้ว่าทำไม error ที่เจอเป็นคำ "ข้อมูล QR/บาร์โค้ดไม่ถูกต้อง" (โครงสร้างเสีย) ไม่ใช่ "ไม่พบผู้รับเงิน"/"biller ไม่ถูกต้อง" (ปฏิเสธระดับธุรกิจ) แบบที่เจอตอน Biller ID ผิดค่าในข้อ 71/78
    - **ค่าที่ควรใส่ใน Reference 1:** สเปกไม่ได้บังคับเนื้อหาตายตัว แต่จากภาพหน้าจอ "QR รับเงิน" ของ K SHOP ที่ผู้ใช้ส่งมา มี "เลขอ้างอิง" โชว์คู่กับ Biller ID เสมอ — เป็นค่าที่ธนาคารกำหนดมาให้ต่อร้านแล้ว ใช้ค่านี้กรอกลง Reference 1 ได้เลย (พิสูจน์ทางความยาว: ตรง 20 ตัวอักษรพอดี ซึ่งเป็นขนาดสูงสุดที่สเปกกำหนดไว้สำหรับฟิลด์นี้พอดี ไม่ใช่เรื่องบังเอิญ — รายละเอียดค่าจริงไม่บันทึกไว้ในเอกสารนี้เพราะเป็นข้อมูลระบุตัวตนทางธนาคารของร้าน)
    - **แก้:** เพิ่มคอลัมน์ `pos_configs.scb_biller_ref1` (ยังไม่ได้รัน SQL — ดู "ต้องทำด้วยมือ" ด้านล่าง) — `api/pos/pos-config.js` GET คืนค่านี้ (fail-safe แยก query เหมือนคอลัมน์ใหม่อื่นๆ), PATCH **บังคับ (validate 400)** ว่าต้องกรอก Reference 1 ที่ไม่ว่างเปล่าและไม่เกิน 20 ตัวอักษรทุกครั้งที่มีการตั้ง/แก้ Biller ID (กันสร้างค่าพังซ้ำแบบเดิมที่เพิ่งแก้ไปในข้อ 78) — `api/pos/promptpay-qr.js` อ่านค่านี้ (fail-safe แยก query) แล้วส่งเป็น `ref1` ให้ `generateBillPaymentPayload()` (ฟังก์ชันรองรับพารามิเตอร์นี้อยู่แล้วตั้งแต่สร้างไฟล์ แค่ไม่เคยมีใครส่งมา) — `pos.js` เพิ่มช่องกรอก "เลขอ้างอิง (Reference 1)" คู่กับ Biller ID ในหน้าตั้งค่า พร้อมคำอธิบายชัดเจนว่าเป็นฟิลด์บังคับและตัวอย่างค่าจากภาพหน้าจอจริงที่ผู้ใช้ส่งมา — `validateBillerId()` ขยายรับพารามิเตอร์ ref1 เพิ่ม ใช้ร่วมกันทั้งจุดแสดงผล error และปุ่มบันทึก
    - **ทดสอบยิงจริงกับ dev server ก่อน SQL (ยืนยัน fail-safe ทำงานถูกต้อง):** GET คืน `scb_biller_ref1:""` ไม่ crash แม้คอลัมน์ยังไม่มี, PATCH ไม่ใส่ ref1 (มี biller_id เดิมอยู่แล้ว) ถูกปฏิเสธ 400 ตามที่ออกแบบไว้ถูกต้อง, PATCH ใส่ ref1 ครบคืน 200 (เขียนคอลัมน์ใหม่ไม่ได้จริงเพราะยังไม่รัน SQL แต่ไม่ทำให้ endpoint พังหรือ field อื่นเซฟไม่ได้) — parse payload ที่สร้างจาก `generateBillPaymentPayload(billerId, {amount, ref1})` ด้วยมือแบบ TLV ยืนยันโครงสร้างครบถ้วนตามสเปกทางการแล้ว (AID + Biller ID + **Reference 1 ยาว 20 ตัวอักษรปรากฏอยู่จริง** ใน sub-tag "02" ของ Tag 30, ไม่มีไบต์เกิน/ขาด)
    - **ยังไม่ได้ทดสอบสแกนจริงกับแอปธนาคาร** (ต้องรอผู้ใช้รัน SQL แล้วกรอกค่าจริงในหน้าเว็บก่อน) — ความมั่นใจสูงมากว่าแก้ถูกจุดแล้ว (อ้างอิงจากเอกสารทางการของ ธปท. โดยตรง ไม่ใช่การเดา) แต่ยังไม่มีการยืนยันจากการสแกนจริงครั้งสุดท้าย — **แนะนำให้ผู้ใช้รัน SQL ด้านล่าง กรอกเลขอ้างอิงในหน้าตั้งค่า POS แล้วลองสแกนจ่ายจริงอีกครั้ง**
    - **Deploy production แล้ว (2026-08-09)** — revision จริง `smileslip-dashboard-00315-2dk` (ข้อความ deploy โชว์ revision เดิม `00314-p8h` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง) — **verified บน production จริง**: `GET /api/pos/pos-config` คืน `scb_biller_ref1` ถูกต้อง, `PATCH` ที่ไม่มี Reference 1 ถูกปฏิเสธ 400 ตามที่ออกแบบไว้ — **ยังรอผู้ใช้รัน SQL + กรอกเลขอ้างอิงจริงในหน้าเว็บก่อนถึงจะสแกนจ่ายได้จริง**

80. **แก้ 2 เรื่องด่วน: (1) ตัวอย่างในช่องกรอกดันเป็นเลขอ้างอิงจริงของผู้ใช้ — ลบออกจาก UI/เอกสารแล้ว (2) หน้าเว็บพังทั้งหน้า ("Application error") ทันทีที่กดเข้าแท็บ "เงินเดือน" — เจอ root cause จริงผ่านการทดสอบเบราว์เซอร์จริงบน production:**
    - **(1) ความเป็นส่วนตัว:** ตอนแก้ข้อ 79 เผลอเอา "เลขอ้างอิง" จริงของผู้ใช้ (จากภาพหน้าจอ K SHOP ที่ส่งมาให้ดูตัวอย่าง) ไปใส่เป็น placeholder ตัวอย่างในช่องกรอกของหน้าเว็บ + เขียนอ้างซ้ำไว้ในคำอธิบาย error message และเอกสาร CLAUDE.md หลายจุด — ผู้ใช้ทักท้วงถูกต้องว่าเป็นข้อมูลระบุตัวตนทางธนาคารของร้าน ไม่ควรถูกนำไปแสดง/บันทึกไว้ที่ไหนอีก — ลบค่าจริงออกจากทุกจุด (placeholder ในฟอร์ม, error message ฝั่ง backend, ทุกย่อหน้าที่เขียนไว้ในข้อ 78/79 ของเอกสารนี้ รวมถึง Biller ID จริงที่เผลอใช้เป็น placeholder อีกจุดด้วย) แทนที่ด้วยคำอธิบายทั่วไปไม่มีตัวเลขจริงปน — **หมายเหตุสำคัญ:** ค่าที่ถูกลบออกยังคงอยู่ใน git history ของ commit ก่อนหน้า (bb064c1) เพราะ push ขึ้น GitHub ไปแล้วก่อนจะรู้ตัว การลบออกจาก history จริงต้องทำ force-push rewrite ประวัติ (destructive, ต้องขอผู้ใช้ยืนยันก่อนเสมอ) — ยังไม่ได้ทำส่วนนี้ รอผู้ใช้ตัดสินใจว่าต้องการให้ทำหรือไม่
    - **(2) หน้าเว็บพังทั้งหน้า:** ไล่โค้ดในไฟล์ pos.js อย่างละเอียดหาสาเหตุไม่เจอนานพอสมควร (ตรวจ `payrollEmployeeFromRow()`, `saveEmployee()`, `runPreview` ทุกจุดที่แตะ `base_salary` ล้วน guard ด้วย `Number(...)||0`/`parseFloat(...)||0` ครบถ้วนแล้ว ไม่น่าเป็นสาเหตุ) — **เปลี่ยนวิธีเป็นทดสอบเบราว์เซอร์จริงกับ production โดยตรงแทนการไล่อ่านโค้ดเดา** (สร้าง owner-session token ที่เซ็นชื่อเองแนบไปกับ URL `/pos?userId=...&ownerSession=...` — **ค้นพบใหม่ที่สำคัญ: วิธีนี้ทำให้เบราว์เซอร์ทดสอบของเครื่องมือ (sandboxed) เข้าหน้า `/pos` ได้สำเร็จเป็นครั้งแรกในโปรเจกต์นี้** ต่างจากที่เคยเจอปัญหา `router.query`/`router.isReady` ไม่ hydrate มาตลอดหลายสิบรอบก่อนหน้านี้ตลอดทั้งโปรเจกต์ — **แนวทางนี้ควรใช้ทดสอบ `/pos` ผ่านเบราว์เซอร์จริงต่อไปในอนาคต แทนที่จะข้ามไปใช้แค่ backend API test อย่างเดียวเหมือนที่ทำมาตลอด**) — เปิด console errors เจอ `ReferenceError: Edit3 is not defined` ทันที
    - **สาเหตุจริง:** `pos.js` ทั้งไฟล์ **ไม่เคย import จาก `lucide-react` เลยสักบรรทัดเดียว** (ต่างจาก `dashboard.js`/`admin.js`/ไฟล์อื่นที่ import ปกติ) แต่ตอนสร้างระบบเงินเดือนในข้อ 76 ใช้ `<Edit3 size={13}/>` และ `<Trash2 size={13}/>` (ปุ่มแก้ไข/ลบพนักงาน + ปุ่มยกเลิกรอบจ่ายเงินเดือน) โดยไม่ได้เพิ่ม import ให้ — `npx next build` ผ่านตลอดเพราะ JSX เขียนถูก syntax (React ไม่รู้จัก component จนกว่าจะ runtime ถึงจะโยน `ReferenceError`) มีแค่ 2 icon/3 จุดใช้งานทั้งไฟล์ (ยืนยันด้วย regex กวาดหาทุก JSX tag ตัวพิมพ์ใหญ่ในไฟล์) — แก้โดยเพิ่ม `import { Edit3, Trash2 } from 'lucide-react';` (patch อยู่แล้วเป็น dependency ของโปรเจกต์ ไม่ต้องติดตั้งเพิ่ม)
    - **ทดสอบยืนยันบน production จริงหลัง deploy** — เข้าหน้า `/pos` → กด "⋯ เพิ่มเติม" → กด "💰 เงินเดือน" ผ่านเบราว์เซอร์จริง เห็นรายชื่อพนักงานที่มีอยู่จริงของร้าน D Gas ("นิค ปัญญา — พนักงานขนส่ง · ฿15,000/เดือน · ประกันสังคม") แสดงผลถูกต้องไม่พังอีกต่อไป (หมายเหตุ: `read_console_messages` ของเครื่องมือทดสอบสะสม error log ข้ามการ navigate ไม่รีเซ็ต ทำให้เห็น error เก่าค้างอยู่แม้หน้าเว็บจะพังแล้วจริงหรือไม่ก็ตาม — ต้องอ่านเนื้อหาหน้าจริงผ่าน `get_page_text` เพื่อยืนยัน ไม่ใช่เชื่อ console log เฉยๆ)
    - **Deploy production แล้ว (2026-08-09)** — revision จริง `smileslip-dashboard-00316-hmw` (ข้อความ deploy โชว์ revision เดิม `00315-2dk` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง)

81. **ขยายระบบเงินเดือน (Phase 2 เดิม, ข้อ 76) ให้ครอบคลุมแบบพนักงานที่ไม่ใช่รายเดือน + นโยบายวันหยุด/หัก/OT + ที่อยู่-เบอร์ติดต่อ — ผู้ใช้ขอ "ช่วยคิด" ก่อนแล้วยืนยัน 3 คำถามออกแบบผ่าน AskUserQuestion:**
    - **ตัดสินใจออกแบบ (ยืนยันจากผู้ใช้ 2026-08-10):** (1) พนักงานรายวัน/แทรค N วัน อยู่ในระบบเงินเดือนเดียวกับรายเดือน เลือกประเภทตอนเพิ่มพนักงาน ไม่แยกหน้า (เพราะทุกแบบยังเป็นค่าใช้จ่ายสาขาที่ต้องเข้า P&L เดียวกัน) (2) นโยบายวันหยุด/หักเงินตั้งเป็นค่ากลางของร้าน ปรับต่อคนได้ถ้าจำเป็น (3) ระบบลงชื่อเข้า-ออกงานทำแค่ช่องกรอกมือก่อน (ยังไม่สร้างระบบสแกน/GPS จริง)
    - **`pay_type` ต่อพนักงาน** — 'monthly' (รายเดือน, เดิม) / 'daily' (รายวัน, มี `daily_rate`) / 'cycle' (แทรค N วัน เช่น "แทรค 10 วัน" ของธุรกิจขนส่ง/แก๊ส, มี `cycle_days`+`cycle_rate`) — คนละสูตรจ่ายเงินกัน ไม่ใช่แค่ optional field ของกันและกัน (พนักงานรายวัน "เงินเดือน" ไม่มีความหมาย ต้องคำนวณจากจำนวนวันที่ทำจริง × อัตรา)
    - **นโยบายวันหยุด/หักเงินกรณีหยุดเกินสิทธิ์ (เฉพาะพนักงานรายเดือน)** — สูตรตามที่ผู้ใช้อธิบายเอง: อัตราต่อวัน = เงินเดือน ÷ จำนวนวันจริงในเดือนนั้น (28-31 ตามเดือนจริง ไม่ใช่ 30 คงที่) หักเฉพาะจำนวนวันที่เกินสิทธิ์ — สิทธิ์คำนวณจาก "วันหยุดต่อสัปดาห์" (ค่ากลางร้านใหม่ `pos_configs.payroll_days_off_per_week`, override ต่อคนได้ผ่าน `employee.days_off_per_week_override`) เทียบสัดส่วนกับจำนวนวันจริงในเดือนนั้น (`allowedThisMonth = daysOffPerWeek × daysInMonth / 7`)
    - **OT** — จงใจ **ไม่คำนวณอัตราให้อัตโนมัติ** (กฎหมายแรงงานไทยมีหลายอัตราต่างกันตามวันธรรมดา/วันหยุด/กลางคืน คำนวณผิดกฎหมายโดยไม่รู้ตัวได้ง่ายถ้าเดาสูตรเอง) — ให้กรอกชั่วโมง+อัตรา/ชม.ที่ตกลงกับพนักงานเองเสมอ ระบบคูณให้แค่นั้น
    - **ระบบลงชื่อเข้า-ออกงานในอนาคต** — ตามที่ผู้ใช้เลือก ("ทำแค่ช่องกรอกมือก่อน") ไม่มีระบบสแกน/ปุ่ม "เชื่อมระบบ" ในหน้าเว็บตอนนี้ แต่ออกแบบ data flow ให้พร้อมต่อยอด: ค่า "จำนวนวันที่หยุดจริง"/"ชั่วโมง OT" เป็นช่องกรอกที่ไหลเข้า `deductions`/`additions` (ฟิลด์จริงที่มีอยู่แล้วในระบบเดิม) ผ่านปุ่ม "รวมเข้ายอด..." — ถ้าจะต่อระบบสแกนจริงในอนาคต แค่เปลี่ยนแหล่งที่มาของตัวเลขสองค่านี้จากกรอกมือเป็น auto-fill จากระบบสแกน ไม่ต้องรื้อโครงสร้างเดิม
    - **ตัดสินใจสำคัญ: ไม่เพิ่มคอลัมน์ใหม่ใน `pos_payroll_runs` เลย** — ใช้ `additions`/`deductions`/`addition_note`/`deduction_note`/`notes` ที่มีอยู่แล้วทั้งหมด (ตัวคำนวณ OT/หักวันหยุดเกิน "เติม" ค่าเข้าไปในช่องเหล่านี้ผ่านปุ่ม ไม่ทับของเดิมที่อาจกรอกเหตุผลอื่นไว้ก่อน) — สำหรับรายวัน/แทรค ต่อท้าย context (เช่น "[รายวัน 15 วัน × ฿400]") เข้า `notes` อัตโนมัติให้ประวัติย้อนหลังดูออกว่าคิดจากฐานอะไร — ความเสี่ยง schema เปลี่ยนแปลงน้อยที่สุด ยังคง backward compatible กับพนักงานรายเดือนเดิม (เช่น "นิค ปัญญา" ที่มีอยู่จริง) โดยอัตโนมัติ (`pay_type` default เป็น 'monthly')
    - **เพิ่ม:** คอลัมน์ `pos_payroll_employees.{pay_type,daily_rate,cycle_days,cycle_rate,address,phone,days_off_per_week_override}` + `pos_configs.payroll_days_off_per_week` (ยังไม่ได้รัน SQL — ดู "ต้องทำด้วยมือ" ด้านล่าง, ทุกจุดเขียนแบบ fail-safe แยก query เหมือนคอลัมน์ใหม่อื่นๆ ในโปรเจกต์นี้เสมอ) — `lib/payroll.js` เพิ่ม `PAY_TYPES`, `computeDaysOffDeduction()`, `computeOtPay()`, `daysInYearMonth()` (pure function ทดสอบแยกได้) — ฟอร์มเพิ่ม/แก้ไขพนักงานใน `pos.js` มีตัวเลือกประเภทจ่าย + ฟิลด์ที่เปลี่ยนตามประเภท + ที่อยู่/เบอร์ + override วันหยุดต่อคน — หน้ารันจ่ายเงินเดือนมีส่วนคำนวณ OT + หักวันหยุดเกินสิทธิ์ (เฉพาะรายเดือน) + ช่อง "จำนวนวันที่ทำงาน"/"จำนวนรอบ" สำหรับรายวัน/แทรค (คำนวณ base salary ของรอบนั้นสดๆ)
    - **ทดสอบยิงจริงกับ dev server ครบ 14 assertions:** unit test สูตรคำนวณ `computeDaysOffDeduction`/`computeOtPay`/`daysInYearMonth` ด้วยมือ (เดือน 31 วัน เงินเดือน 15,500 → วันละ 500 พอดี, สิทธิ์ 1 วัน/สัปดาห์ → ~4.43 วัน/เดือน, หยุดจริง 8 วัน → เกิน 3.57 วัน → หัก 1,785 บาท ตรงกับคำนวณมือ, เคสไม่เกินสิทธิ์ → ไม่หัก) + ยิง `POST /api/pos/payroll-employees` ก่อนรัน SQL ยืนยัน fail-safe ทำงานถูกต้อง (แถวหลักสร้างสำเร็จแม้คอลัมน์ใหม่ยังไม่มีจริง ไม่ error ทั้ง endpoint) + `GET /api/pos/pos-config` คืน `payroll_days_off_per_week` default ถูกต้อง — ลบข้อมูลทดสอบสะอาดหลังทดสอบ
    - **Deploy production แล้ว (2026-08-10)** — revision จริง `smileslip-dashboard-00317-69c` (ข้อความ deploy โชว์ revision เดิม `00316-hmw` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง) — verified บน production จริงด้วย backend script (POST/GET/DELETE + regression นิค ปัญญาไม่ถูกกระทบ)
    - **ทดสอบผ่านเบราว์เซอร์จริงบน production สำเร็จครบ** ด้วยเทคนิคแนบ owner-session token (จากข้อ 80): เพิ่มพนักงานทดสอบประเภท "รายวัน" → ฟอร์มสลับฟิลด์ถูกต้อง (เงินเดือน/เดือน → ค่าแรง/วัน, ซ่อนช่อง override วันหยุดสำหรับพนักงานที่ไม่ใช่รายเดือน) → บันทึกสำเร็จ ("✅ เพิ่มพนักงานแล้ว") → เปิดหน้า "รันจ่ายเงินเดือน" ของ นิค ปัญญา (พนักงานรายเดือนจริง) เห็น**ตัวคำนวณหักวันหยุดเกินสิทธิ์ทำงานสด** ("เดือนนี้มี 31 วัน → สิทธิ์ ~4.43 วัน · อัตราหักวันละ ฿483.87" ตรงกับสูตร 15000/31 และ 1×31/7 เป๊ะ) และ**ตัวคำนวณ OT ทำงานสด** (กรอก 5 ชม. × ฿100 → "รวม OT = ฿500.00") โดยไม่ได้กดยืนยันจ่ายจริง (ไม่แตะข้อมูลจริง) → ยืนยันการ์ดตั้งค่า "📅 วันหยุดต่อสัปดาห์ (ค่าเริ่มต้นทั้งร้าน)" render ถูกต้องในแท็บตั้งค่า → ลบพนักงานทดสอบผ่าน API หลังทดสอบ (ปุ่มลบใน UI ต้องพึ่ง native `confirm()` dialog ที่เครื่องมือทดสอบกดไม่ได้ — ข้อจำกัดเดิมที่เคยเจอมาก่อน ไม่ใช่บั๊ก) — ยืนยันรายชื่อพนักงานกลับเหลือแค่ "นิค ปัญญา" คนเดียวถูกต้อง
    - **ผู้ใช้รัน SQL แล้ว (2026-08-10) — verified ครบ 14/14 บน production จริง:** `pos_configs.payroll_days_off_per_week` PATCH/GET บันทึกจริงถูกต้อง, พนักงานรายวัน/แทรค (`pay_type`/`daily_rate`/`cycle_days`/`cycle_rate`/`address`/`phone`) บันทึกจริงครบทุกฟิลด์, `days_off_per_week_override` PATCH บันทึกจริงถูกต้อง, regression "นิค ปัญญา" (พนักงานรายเดือนจริง) ไม่ถูกกระทบเลย (`pay_type='monthly'`, `base_salary=15000` เหมือนเดิม) — ลบข้อมูลทดสอบสะอาดหลังทดสอบ
    - **⚠️ แก้ไขบันทึกจากข้อนี้ — ไม่ได้ "ใช้งานได้เต็มรูปแบบไม่มีอะไรค้าง" ตามที่สรุปไว้ตอนแรก:** ทันทีที่ผู้ใช้ทดสอบใช้งานจริง ชี้ว่าสูตรวันหยุด (`payroll_days_off_per_week`) ออกแบบผิดสมมติฐาน — ดูรายละเอียดเต็มในข้อ 82 ถัดไป (แก้แล้ว)

82. **แก้บั๊กสูตรวันหยุดของข้อ 81 (สัปดาห์ → โควตาต่อเดือนตรงๆ) + ตรวจสอบ QR Bill Payment ละเอียดตามสเปกทางการ ธปท. — ผู้ใช้ขอ "แก้บัคทั้งหมดก่อน" รวม 4 จุดในรอบเดียว:**
    - **บั๊กสูตรวันหยุด (แก้แล้ว):** สูตรเดิม `allowedThisMonth = daysOffPerWeek × daysInMonth / 7` สมมติว่าหยุดเป็นรอบสัปดาห์ประจำ แต่ธุรกิจจริงของผู้ใช้ให้ "หยุด 6 วันต่อเดือน วันไหนก็ได้ตามใจพนักงาน" ไม่ผูกกับสัปดาห์เลย — แก้ `computeDaysOffDeduction()` ให้รับ `daysOffAllowedPerMonth` เป็นโควตาตรงๆ ไม่ต้องคูณ/หารแปลงจากอัตราต่อสัปดาห์อีกต่อไป — เปลี่ยนคอลัมน์ `pos_configs.payroll_days_off_per_week`→`payroll_days_off_per_month` (default 6, ตรงกับนโยบายจริงของร้านที่ทำให้พบบั๊กนี้) และ `pos_payroll_employees.days_off_per_week_override`→`days_off_per_month_override` ทั่วทั้ง `lib/payroll.js`/`api/pos/payroll-employees.js`/`api/pos/pos-config.js`/`pos.js` (ฟอร์มเพิ่ม/แก้ไขพนักงาน, การ์ดตั้งค่าร้าน, ตัวคำนวณในหน้ารันจ่ายเงินเดือน) — **คอลัมน์เก่าเหลือไว้เฉยๆ ไม่ลบ ไม่มีโค้ดอ่าน/เขียนแล้ว** (ไม่มีข้อมูลจริงใช้อยู่เลยตอนแก้ มีแค่ข้อมูลทดสอบที่ลบไปแล้วก่อนหน้า จึงแทนที่ตรงๆ ได้โดยไม่ต้อง migrate)
    - **บั๊ก `export.js` (Excel) ไม่รวมยอดขายจากออเดอร์จัดส่ง/ไม่รวม VAT (แก้แล้ว):** ช่องว่างเดียวกับที่ `reports.js` แก้ไปแล้วในข้อ 72/74 แต่ไม่เคยไล่แก้ไฟล์นี้จนกระทั่งตอนนี้ — sheet "ยอดขาย"/"เงินเชื่อ"/"สินค้าขายดี"/"กำไรขาดทุน"/"สรุปยอดขายแยกสาขา" อ่านแค่ `pos_sales` เท่านั้น ร้านที่ขายผ่านช่องทางจัดส่งเป็นหลักจะเห็นรายงานว่างเปล่า/ไม่ครบเสมอ — เพิ่ม `orderFromRow()`/`deliveryOrderToSaleShape()`/`lineRevenueBase()`/`getVatRegistered()`/`fetchDeliveryOrders()` (ก็อปจาก `reports.js` ตามธรรมเนียมโปรเจกต์ที่ duplicate ฟังก์ชันข้ามไฟล์แทน extract shared module) ทั้ง 5 sheet ตอนนี้รวมออเดอร์จัดส่งที่ "ส่งแล้ว" เข้าไปด้วย + sheet "สินค้าขายดี"/"กำไรขาดทุน" คำนวณรายได้จากฐานราคาก่อน VAT เสมอถ้าร้านจดทะเบียน VAT (เหมือน `reports.js`) — **`vat`/`vat30` sheet ไม่ได้แก้** เพราะ `pos_delivery_orders` ไม่มีคอลัมน์ VAT dual-write ของตัวเอง (คนละช่องว่างที่ใหญ่กว่า ต้องทำ VAT dual-write ให้ delivery orders ก่อน — นอกสโคปรอบนี้ เป็นช่องว่างเดียวกับที่ `reports.js`'s `type=vat` ก็ยังมีอยู่เหมือนกัน ไม่ใช่บั๊กเฉพาะไฟล์นี้)
    - **บั๊กกะเงินสดไม่นับเงินที่เก็บจากรับชำระเงินเชื่อ (แก้แล้ว):** known gap เดิมจากข้อ 78 — `computeExpectedCash()` เดิมกรอง `payment_method==='เงินสด'` เท่านั้น แต่บิลเชื่อที่เพิ่งชำระยังคง `payment_method==='เชื่อ'`/`'ค้างจ่าย'` ตลอดไปโดยตั้งใจ (กัน `type=credit` ในรายงานหาบิลที่ชำระแล้วไม่เจอ) ทำให้เงินที่เก็บได้จริงเป็นเงินสดไม่เคยถูกนับเข้ากะเลย — เพิ่มคอลัมน์ใหม่ `pos_sales.{collected_method,settled_shift_no}` และ `pos_delivery_orders.{collected_method,settled_shift_no}` (แยกจาก `payment_method`/`shift_no` เดิมของบิลโดยเจตนา เพราะบิลเชื่อมักถูกชำระคนละกะ/คนละวันจากตอนสร้างบิล) — `sales.js`/`delivery.js` PATCH บันทึกคอลัมน์ใหม่นี้ตอนกดรับชำระ (แยก update ต่างหาก + กันพัง เหมือนคอลัมน์ใหม่อื่นๆ) — `pos.js`'s `markCreditPaid()` แนบ `activeShift.shift_no` ไปด้วยเฉพาะตอนเลือก "เงินสด" (โอนไม่กระทบเงินสดในลิ้นชัก) — `computeExpectedCash()` เพิ่ม query ยอดที่เก็บได้จากลูกหนี้ที่ `settled_shift_no` ตรงกับกะปัจจุบันและ `collected_method='เงินสด'` มารวมเข้า `totalCashIn` ด้วย (แยก try/catch กันพังถ้ายังไม่ได้รัน SQL)
    - **ทดสอบยิงจริงกับ dev server (`TZ=UTC`) ด้วยข้อมูลจริง/สังเคราะห์ของร้าน D Gas ครบ:** unit test สูตรวันหยุดใหม่ 6 assertions (ตรงกับคำนวณมือ), `pos-config.js` คืนฟิลด์ใหม่ถูกต้อง, seed ออเดอร์จัดส่งทดสอบจริงแล้วดาวน์โหลด export.js จริง parse ด้วย `xlsx` library ยืนยันปรากฏครบใน sales/topsellers/pl sheet, เปิดกะเงินสด+สร้างบิลเชื่อทดสอบ+รับชำระเป็นเงินสด+ปิดกะ — **ทดสอบก่อนรัน SQL พบว่า `computeExpectedCash()`/การบันทึกคอลัมน์ใหม่ fail-safe ถูกต้อง** (ไม่ error, primary operation ยังสำเร็จปกติ, แค่ยังไม่นับเข้ากะจนกว่าจะรัน SQL — ยืนยันแยกต่างหากด้วยสคริปต์เทียบ `select('*')` ว่า `status`/`notes` อัปเดตถูกต้องแม้คอลัมน์ใหม่ยังไม่มีจริง) — ลบ/คืนค่าข้อมูลทดสอบทั้งหมดสะอาดหลังทดสอบทุกจุด (สินค้า/ออเดอร์จัดส่ง/บิลเชื่อ/กะทดสอบ)
    - **QR Bill Payment ตรวจสอบละเอียด (ไม่พบบั๊กเพิ่ม, เอกสารยืนยัน):** ผู้ใช้ถามว่า "อาจจะต้องมี ref สองตัวไหม" หลังข้อ 79 ยังใช้ไม่ได้ — โหลดเอกสารทางการของ ธปท. ตรงจากเว็บ (Policy Guideline: Standardized Thai QR Code for Payment Transactions, 17 เม.ย. 2562, Attachment 1 ตาราง 2.3) มาอ่านตาราง Tag 30 เต็มๆ ยืนยันว่าโค้ดที่แก้ไปแล้วในข้อ 79 ถูกต้อง 100% ตามสเปก (AID บังคับ 16 ตัว, Biller ID บังคับ 15 หลัก, **Reference 1 บังคับ ไม่เกิน 20 ตัวอักษร, Reference 2 ไม่บังคับ ไม่เกิน 20 ตัวอักษร** — โค้ดรองรับทั้งคู่อยู่แล้ว) — ตรวจข้อมูลจริงในระบบ (Biller ID/Reference 1 ของร้าน D Gas) ยืนยันบันทึกไว้ครบถูกต้อง ไม่มีอักขระ/ช่องว่างแปลกปลอม แล้วสร้าง payload จริงด้วยข้อมูลจริง parse ทีละไบต์เทียบตารางสเปก + ตรวจ CRC อิสระ 2 ทาง (เขียนใหม่จากศูนย์ + test vector มาตรฐาน) — **ยืนยันว่า payload ที่สร้างตอนนี้ถูกต้องตามสเปกทุกไบต์ ไม่มีบั๊กโค้ดเหลือ** — ถ้ายังสแกนไม่ผ่านสาเหตุน่าจะไม่ใช่โค้ดแล้ว (QR แคชเก่าจากก่อนกรอก Reference 1 หรือ Biller ID ที่กรอกไว้ยังไม่ใช่เลขจริงจากธนาคาร) รอผู้ใช้ทดสอบสแกน QR ที่สร้างใหม่สดๆ อีกครั้งแล้วรายงานกลับพร้อมชื่อธนาคาร/ข้อความ error ถ้ายังไม่ผ่าน
    - **บทเรียนด้านความเป็นส่วนตัวระหว่างตรวจสอบ:** ระหว่างเขียนสคริปต์ parse payload เพื่อยืนยันโครงสร้าง เผลอ `console.log` ค่า payload ดิบ (มี Biller ID/Reference 1 จริงฝังอยู่) ออกมาที่ terminal ทั้งที่ตั้งใจ mask เฉพาะค่า sub-tag แยกต่างหากไว้แล้ว — จับได้ก่อนจะเอาค่านั้นไปพูดคุยกับผู้ใช้ ลบสคริปต์ + ไฟล์ PDF ที่แคชไว้ทิ้งทันที ไม่ได้เอาค่าจริงไปใส่ในข้อความตอบกลับผู้ใช้หรือบันทึกที่ไหนเลย — **บทเรียน: เวลาเขียนสคริปต์ debug ที่แตะข้อมูลอ่อนไหวจริง ต้อง mask ที่จุด print/log ทุกจุดเสมอ ไม่ใช่แค่บางจุด เพราะเผลอหลุดได้ง่ายมากถ้ามีมากกว่า 1 จุดที่พิมพ์ค่าเดียวกันออกมา**
    - **Deploy production แล้ว (2026-08-13)** — revision จริง `smileslip-dashboard-00318-8rv` (ข้อความ deploy โชว์ revision เดิม `00317-69c` ผิดอีกตามเคย เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง) — verified บน production จริงครบ 5/5 ก่อนรัน SQL (fail-safe ทำงานถูกต้อง)
    - **ผู้ใช้รัน SQL แล้ว (2026-08-13) — verified ครบ 11/11 บน production จริง:** ยืนยันคอลัมน์ใหม่ทั้ง 6 ตัวมีจริง (`pos_payroll_employees.days_off_per_month_override`, `pos_configs.payroll_days_off_per_month`, `pos_sales.{collected_method,settled_shift_no}`, `pos_delivery_orders.{collected_method,settled_shift_no}`) แล้วทดสอบ end-to-end เต็มรูปแบบ: โควตาวันหยุด PATCH/GET บันทึกจริงถูกต้อง, seed ออเดอร์จัดส่งทดสอบจริง → ปรากฏใน sheet "ยอดขาย"/"กำไรขาดทุน" ของ `export.js` บน production จริง, เปิดกะเงินสด(500)จริง+สร้างบิลเชื่อ(300)จริง+รับชำระเป็นเงินสดผูกกะที่เปิดอยู่จริง → `collected_method`/`settled_shift_no` บันทึกจริงถูกต้อง → พรีวิว `expected_cash=800` ถูกต้องตรงสูตร (500+300) → ปิดกะสำเร็จไม่มีส่วนต่าง → regression นิค ปัญญาไม่ถูกกระทบ — ลบข้อมูลทดสอบสะอาดครบทุกจุด (สินค้า/ออเดอร์จัดส่ง/บิลเชื่อ/กะทดสอบ) — **ฟีเจอร์ข้อ 82 ใช้งานได้เต็มรูปแบบแล้วทุกส่วน**
    - **⚠️ พบระหว่างเก็บกวาดหลังทดสอบ:** มีกะเงินสดค้างเปิดอยู่จริงตั้งแต่ 2026-08-09 (`SHIFT20260809040238`, พนักงาน "ดีแก๊ส2024", สาขา "ดีแก๊ส หางดง", เงินตั้งต้น ฿0) — ไม่ใช่กะที่สร้างระหว่างทดสอบรอบนี้ (ยืนยันกับผู้ใช้แล้วว่าเป็นกะทดสอบเก่าที่ลืมปิด ไม่มีผลกระทบตามมา) — **ปิดให้แล้ว (2026-08-13)** ผ่าน `PATCH /api/pos/cash-shifts` ยอด counted_cash ตรงกับ expected_cash พอดี (500=500) ไม่มีส่วนต่าง ไม่ได้แจ้งเตือนเจ้าของร้าน

83. **เปิดลิ้นชักเงินสดผ่านเครื่องพิมพ์ Bluetooth โดยตรง (Web Bluetooth API, Android เท่านั้น) — ผู้ใช้ขอหลังบอกว่า QR โอนเงินสแกนผ่านแล้ว (แต่เจอ error ใหม่ "เลขที่อ้างอิงไม่ถูกต้อง" ที่เป็นเรื่องข้อมูลฝั่งธนาคาร ไม่ใช่บั๊กโค้ด — รอผู้ใช้ทวนกับธนาคาร):**
    - **ยืนยัน scope ผ่าน AskUserQuestion ก่อนเริ่ม:** (1) เครื่องพิมพ์ Bluetooth จับคู่กับมือถือ/แท็บเล็ตโดยตรง (ไม่ใช่ USB/เครือข่าย), (2) ร้านใช้ทั้งเครื่องประจำที่และมือถือพนักงานผสมกัน (iPhone+Android ปน — ตรงกับที่บันทึกไว้แล้วในข้อ 22), (3) ผู้ใช้มีเครื่องรุ่น MP210 แต่ต้องรองรับหลายยี่ห้อ/รุ่น เพราะเป็น SaaS หลายร้าน แต่ละร้านมีเครื่องพิมพ์คนละรุ่นที่ซื้อมาแล้วไม่อยากเปลี่ยน — ผู้ใช้ยอมรับข้อจำกัด **iPhone ทำไม่ได้เลย** (Safari/iOS บล็อก Web Bluetooth ในระดับเบราว์เซอร์ ไม่มีทางเลี่ยง ไม่ว่าจะใช้ Web Bluetooth API หรือ Print Agent/Local Server เพราะเครื่องพิมพ์จับคู่กับมือถือโดยตรง ไม่ใช่คอมพิวเตอร์ตั้งโต๊ะที่รันโปรแกรมตัวกลางได้)
    - **สร้าง `lib/escpos-bluetooth.js` (ใหม่)** — `isBluetoothPrintSupported()` (feature-detect `navigator.bluetooth`), `pairPrinter()` (เปิด picker ของระบบให้เลือกอุปกรณ์เอง ไม่ hardcode ยี่ห้อ/รุ่น), `openCashDrawer()` (ลองเชื่อมต่อเครื่องที่เคยจับคู่ไว้แบบเงียบๆ ก่อน ถ้าไม่ได้ค่อยเปิด picker ใหม่ แล้วส่งคำสั่ง ESC/POS drawer-kick มาตรฐาน `ESC p m t1 t2`) — **เป็น best-effort ข้ามยี่ห้อ**: ลองเชื่อมต่อด้วย GATT service/characteristic UUID ที่พบบ่อยที่สุดในเครื่องพิมพ์ความร้อน BLE ราคาถูก 4 แบบก่อน (BLE Printer Service ทั่วไป, UART-like pattern, Microchip Transparent UART, Nordic UART Service — ทั้งหมดเป็น pattern ที่พบซ้ำข้ามยี่ห้อเพราะผู้ผลิตหลายเจ้าใช้ชิปเซ็ต BLE เดียวกัน) ถ้าไม่ตรงเลยสักตัว fallback ไปไล่หา characteristic ที่เขียนได้ตัวแรกในทุก service ของอุปกรณ์ — **ไม่รับประกันว่าใช้ได้กับทุกรุ่น** เพราะไม่มีมาตรฐานเดียวจริงๆ (ตรงกับที่เคยตัดสินใจไม่ทำมาก่อนในข้อ 22/41 เพราะเสี่ยงเดา UUID ผิด — รอบนี้ทำแบบ "ลองหลายอย่างที่พบบ่อยที่สุด" แทนการเดาเจาะจงรุ่นเดียว)
    - **UI:** ปุ่ม "🔓 เปิดลิ้นชักเงินสด" ในหน้าสรุปหลังชำระเงินสำเร็จ (ข้าง "🖨️ พิมพ์ใบเสร็จ") + การ์ดตั้งค่าใหม่ "🔓 ลิ้นชักเงินสด (Bluetooth)" (ปุ่มจับคู่เครื่องพิมพ์ + ปุ่มทดสอบเปิดลิ้นชักแยกต่างหาก) — **ทั้งสองจุดซ่อนอัตโนมัติถ้าเบราว์เซอร์ไม่รองรับ Web Bluetooth** (iPhone/เดสก์ท็อป Safari/Firefox จะไม่เห็นปุ่มนี้เลย)
    - **🐛 เจอบั๊กจริงระหว่างทำ (แก้ก่อน build ผ่าน):** เดิมเช็ค `isBluetoothPrintSupported()` ตรงๆ ระหว่าง render — แต่ `/pos` ถูก prerender เป็น static HTML ตอน `next build` (ไม่มี `navigator` เลยตอนนั้น ได้ `false` เสมอ) พอ hydrate ฝั่ง client รอบแรกที่เบราว์เซอร์รองรับ Bluetooth จริง จะได้ `true` ทันที ทำให้ React hydration mismatch (server บอกไม่มีปุ่ม แต่ client เพิ่ง hydrate บอกมีปุ่ม) — แก้เป็น `useState(false)` + เช็คจริงใน `useEffect` (client-only, หลัง mount) แทน ปุ่มจะโผล่หลัง mount เสี้ยววินาทีแทนที่จะโผล่ทันที ไม่มี mismatch อีก
    - **ทดสอบ:** `npx next build` ผ่านทุก route, เปิดผ่านเบราว์เซอร์จริงด้วยเทคนิค owner-session token (dev server) ยืนยัน console ไม่มี hydration warning/error เลย — **ไม่สามารถทดสอบการเชื่อมต่อ Bluetooth จริงได้** (ไม่มีฮาร์ดแวร์/เบราว์เซอร์มือถือให้ทดสอบในเครื่องมือนี้) — โค้ดเขียนตามสเปก Web Bluetooth/ESC-POS อย่างระมัดระวังที่สุดเท่าที่ตรวจสอบได้จากเอกสาร แต่**ยังไม่ได้ยืนยันกับฮาร์ดแวร์จริง (MP210) เลย** — ต้องให้ผู้ใช้ทดสอบจริงบนมือถือ Android ที่จับคู่เครื่องพิมพ์ไว้แล้ว แล้วรายงานกลับว่าใช้ได้ไหม ถ้าไม่ได้ต้องรู้ยี่ห้อ/รุ่นเพิ่มเพื่อเพิ่ม UUID เข้ารายการที่รองรับ
    - **Deploy production แล้ว (2026-08-13)** — revision จริง `smileslip-dashboard-00320-xcl` (เช็คด้วย `gcloud run revisions list --sort-by=~metadata.creationTimestamp` แล้ว pin traffic เอง ตามธรรมเนียม) — **verified บน production จริงว่าหน้าเว็บเรนเดอร์ถูกต้อง ไม่มี console error/hydration mismatch**: เปิด `/pos` ด้วยเทคนิค owner-session token จริง เข้าแท็บตั้งค่าเห็นการ์ด "🔓 ลิ้นชักเงินสด (Bluetooth)" พร้อมข้อความอธิบาย + ปุ่ม "🔗 จับคู่เครื่องพิมพ์"/"🧪 ทดสอบเปิดลิ้นชัก" ครบถูกต้อง, คลิกปุ่มจับคู่ผ่าน JS ยืนยันไม่มี uncaught error เกิดขึ้น — **ยังคงไม่ได้ทดสอบการจับคู่/สั่งงาน Bluetooth กับเครื่องพิมพ์จริงเลย** (ข้อจำกัดของเครื่องมือทดสอบ ไม่มีฮาร์ดแวร์ Bluetooth ให้จำลอง) — รอผู้ใช้ทดสอบเองบนมือถือ Android ที่จับคู่กับเครื่องพิมพ์ MP210 (หรือรุ่นอื่น) จริง แล้วรายงานกลับว่าเปิดลิ้นชักได้ไหม
    - **ต่อยอดวันเดียวกัน — พิมพ์ใบเสร็จตรงผ่านเครื่องพิมพ์ Bluetooth (ไม่ใช่แค่เปิดลิ้นชัก):** ผู้ใช้แจ้งว่ายังไม่มีลิ้นชัก Bluetooth จริง (มีแต่แบบสายต่อคอมพิวเตอร์) แต่ต้องการพิมพ์ใบเสร็จผ่านเครื่องพิมพ์ Bluetooth (MP210) โดยตรง — ขยาย `lib/escpos-bluetooth.js`: **เลือกวิธี "วาดใบเสร็จเป็นภาพ (canvas) แล้วส่งด้วย ESC/POS raster bit image (`GS v 0`)" แทนการส่งข้อความ+codepage** (สาเหตุ: หลีกเลี่ยงปัญหาตารางเข้ารหัสภาษาไทยที่ต่างกันไปตามยี่ห้อเครื่องพิมพ์โดยสิ้นเชิง — วาดเป็นภาพไม่ต้องพึ่งฟอนต์/codepage ของเครื่องพิมพ์เลย, `GS v 0` เป็นมาตรฐาน ESC/POS ที่เครื่องพิมพ์ความร้อนแทบทุกยี่ห้อรองรับ) — เพิ่ม `renderReceiptCanvas()` (วาดใบเสร็จลง canvas ด้วย `ctx.fillText`/`wrapTextLines` โครงเดียวกับ `buildReceiptHtml()` ใน pos.js แต่วาดเป็นภาพแทน HTML — duplicate logic ตามธรรมเนียมโปรเจกต์ ไม่ import ข้ามไฟล์), `canvasToRasterBands()` (แปลงเป็นขาว-ดำแบบ threshold ธรรมดา ไม่ dither เพื่อให้ตัวอักษรคมชัด, แบ่งเป็นแถบๆ ~6000 ไบต์/แถบกันบัฟเฟอร์เครื่องพิมพ์ราคาถูกล้น), `printReceiptViaBluetooth()`, refactor `connectToPrinter()`/`writeBytes()` (เขียนเป็นก้อนเล็ก ~180 ไบต์ + หน่วง 8ms กัน BLE MTU เกิน) ให้ใช้ร่วมกับ `openCashDrawer()` — **ไม่ส่งคำสั่งตัดกระดาษ** (เครื่องพิมพ์พกพาแบบ MP210 ส่วนใหญ่ไม่มีใบมีดตัด) จบด้วยป้อนกระดาษเปล่า 4 บรรทัดแทน
      - **UI:** หน้าสรุปหลังชำระเงินสำเร็จเพิ่มปุ่ม "🖨️ พิมพ์ (Bluetooth)" คู่กับปุ่ม "🔓 เปิดลิ้นชัก" (แถวเดียวกัน) — การ์ดตั้งค่าเปลี่ยนชื่อจาก "ลิ้นชักเงินสด (Bluetooth)" เป็น "เครื่องพิมพ์ Bluetooth" เพิ่มปุ่ม "🧪 ทดสอบพิมพ์" (พิมพ์ใบเสร็จตัวอย่างเปล่าๆ โดยไม่ต้องขายของจริงก่อน — ช่วยให้ผู้ใช้ทดสอบฮาร์ดแวร์ซ้ำได้ง่ายๆ) คู่กับปุ่มทดสอบเปิดลิ้นชักเดิม — อัปเดตข้อความอธิบายบอกชัดเจนว่าไม่ต้องกดปุ่มจับคู่ก่อนเลยก็ได้ เพราะกดปุ่มพิมพ์/เปิดลิ้นชักครั้งแรกจะเปิด picker ให้อัตโนมัติอยู่แล้ว (`openCashDrawer()`/`printReceiptViaBluetooth()` ทั้งคู่ fallback ไปเรียก `pairPrinter()` เองถ้ายังไม่เคยจับคู่)
      - **⚠️ เกิดเหตุการณ์ production พังจริงระหว่าง deploy รอบนี้ (แก้ทันที ดูรายละเอียดเต็มใน "ข้อควรระวังสำคัญ" ข้อ 44):** สคริปต์ PowerShell ที่ parse `.env` เพื่อสร้าง `--env-vars-file` ไม่ได้ตัดเครื่องหมายคำพูด `"..."` ที่ครอบค่าบางตัวใน `.env` ออกก่อน (เช่น `SUPABASE_URL="https://..."`) ทำให้ค่าที่ deploy เข้า production จริงมีเครื่องหมายคำพูดฝังอยู่ในสตริง (`"https://..."`) — Supabase client โยน `Invalid supabaseUrl` ทันที **ทำให้ทุก endpoint ที่แตะ Supabase พังเป็น 500 หมดทั้งระบบใน production จริงช่วงสั้นๆ** (ตรวจพบจากการ browser-test ของงานนี้เอง ไม่ใช่จากผู้ใช้แจ้ง) — แก้โดย rollback traffic กลับ revision เดิมทันที (`smileslip-dashboard-00319-bkr`) ยืนยันหายแล้ว จากนั้นแก้สคริปต์ตัดเครื่องหมายคำพูดออกก่อนเขียน YAML แล้ว deploy image เดิม (โค้ดไม่มีปัญหา ปัญหาอยู่ที่ env vars เท่านั้น) ใหม่อีกครั้งเป็น revision `smileslip-dashboard-00321-ndl` — verified ก่อน pin traffic ว่า `/api/shop/data` คืน 200 จริงแล้วค่อย route traffic
      - **ทดสอบ:** `npx next build` ผ่านทุก route (ไม่มี dependency ใหม่ ใช้ Canvas API ของเบราว์เซอร์ล้วนๆ) — browser-test บน production จริงยืนยัน: การ์ดตั้งค่าเรนเดอร์ถูกต้องครบทั้ง 3 ปุ่ม, คลิกปุ่ม "ทดสอบพิมพ์" ผ่าน JS ไม่เกิด uncaught error ใดๆ (console สะอาด ไม่มี error ใหม่จากการคลิก) — **ยังไม่ได้ทดสอบผลการพิมพ์จริงบนกระดาษ** (ไม่มีฮาร์ดแวร์ในเครื่องมือทดสอบ) ต้องรอผู้ใช้ทดสอบพร้อมกับปุ่มเปิดลิ้นชักด้านบน — **จุดที่ยังไม่มั่นใจ 100% โดยไม่มีเครื่องจริง:** คุณภาพการเรนเดอร์ฟอนต์ไทยผ่าน canvas บน Android Chrome (คาดว่าใช้ระบบฟอนต์ของเครื่อง Roboto/Noto Sans Thai รองรับอยู่แล้วแต่ยังไม่เคยเห็นผลจริง), ขนาด band ที่เหมาะสมกับบัฟเฟอร์ของเครื่องพิมพ์แต่ละรุ่น (ตอนนี้ตั้งไว้ ~6000 ไบต์/แถบแบบ heuristic ยังไม่เคยทดสอบว่าเหมาะกับ MP210 จริงหรือไม่)
      - **✅ ผู้ใช้ทดสอบพิมพ์กับ MP210 จริงสำเร็จ (2026-08-13)** — "พิมพ์ออกมาได้แล้ว ครบถ้วน" ยืนยันว่า canvas rendering + ESC/POS raster ใช้ได้จริงกับฮาร์ดแวร์จริงเป็นครั้งแรก (จุดที่เคยไม่มั่นใจด้านบนแก้ข้อกังวลไปแล้วอย่างน้อยกับรุ่นนี้) — ตามมาด้วยฟีดแบ็ก 3 ข้อ ทำต่อทันที:
        1. **แก้บั๊กจริง — ต้องกดจับคู่ Bluetooth ใหม่ทุกครั้งก่อนพิมพ์ได้** — สาเหตุ: `connectToPrinter()` เดิมพึ่ง `navigator.bluetooth.getDevices()` (คืนอุปกรณ์ที่เคยได้สิทธิ์ถาวรไว้แล้ว) เป็นทางเดียวในการ reconnect แบบเงียบๆ แต่ API นี้ไม่เสถียรเท่ากันทุกรุ่น Chrome/Android (บางเครื่องคืนค่าว่างเปล่าแม้เคยจับคู่สำเร็จมาก่อน) พอหาไม่เจอก็ fallback ไปเปิด picker ใหม่ทุกครั้ง — แก้โดยเพิ่ม `cachedDevice` (ตัวแปรระดับโมดูล เก็บ `BluetoothDevice` object ที่เชื่อมต่อสำเร็จล่าสุดไว้ในหน่วยความจำ) เป็นทางลัดอันดับแรกเสมอก่อนจะพึ่ง `getDevices()`/picker — รับประกันว่าอย่างน้อยภายในเซสชันเดียวกัน (ไม่ปิด/รีเฟรชหน้า) กดพิมพ์กี่ครั้งก็ไม่ต้องเลือกอุปกรณ์ซ้ำอีกเลย ถ้าเชื่อมต่อซ้ำด้วย cache ล้มเหลว (เช่น เครื่องถูกปิด/อยู่นอกระยะ) จะล้าง cache แล้ว fallback ไปหาใหม่ตามลำดับเดิมอัตโนมัติ — ยังไม่ได้ทดสอบกับเครื่องจริงว่าแก้ครบ 100% ไหม (ทดสอบได้แค่ logic/build เท่านั้น) รอผู้ใช้ยืนยันอีกรอบ
        2. **เพิ่มการปรับแต่งท้ายใบเสร็จ** — ข้อความขอบคุณ/โปรโมชั่นที่ร้านกำหนดเอง (textarea อิสระ แทนที่ "ขอบคุณที่ใช้บริการ" ค่าเริ่มต้น) + QR ไลน์ร้านค้า (กรอกลิงก์เพิ่มเพื่อน เช่น `https://lin.ee/xxx` แล้วระบบสร้าง QR ให้อัตโนมัติ) — ใช้ร่วมกันทั้งปุ่ม "พิมพ์ใบเสร็จ" (window.print) และ "พิมพ์ (Bluetooth)" ตามที่ผู้ใช้ยืนยันว่าทั้งสองปุ่มต้องมีการปรับแต่งนี้เหมือนกัน (ไม่แยกใบกำกับภาษี — ยังใช้รูปแบบเดิมตามที่ผู้ใช้บอกไว้ชัดเจน) — คอลัมน์ใหม่ `pos_configs.{receipt_footer_message,receipt_line_url}` (fail-safe pattern เดิม) — สร้าง QR ด้วย `qrcode` npm package ที่มีอยู่แล้วในโปรเจกต์ (โหลดผ่าน CDN client-side ตาม pattern เดียวกับ `QrContactModal` เดิม ไม่เพิ่ม dependency ใหม่) — ฝั่ง window.print แปะเป็น `<img>` ตรงๆ ใน HTML, ฝั่ง Bluetooth โหลดเป็น `Image()` แล้ว `ctx.drawImage()` ลง canvas ก่อนแปลงเป็น raster (ทำให้ `renderReceiptCanvas`/`printReceipt`/`buildBtReceiptPayload` ต้องเปลี่ยนเป็น async ทั้งหมด) — เพิ่มปุ่ม "👁️ ดูตัวอย่าง" ในหน้าตั้งค่าให้เห็น QR จริงก่อนบันทึกโดยไม่ต้องพิมพ์ใบเสร็จจริงก่อน
        3. **"เครื่องพิมพ์เข็ม" (dot-matrix) — ไม่ต้องแก้โค้ดเพิ่ม** — ตีความคำขอว่าปุ่ม "🖨️ พิมพ์ใบเสร็จ" เดิม (window.print) ใช้เชื่อมต่อเครื่องพิมพ์ทั่วไป/เครื่องพิมพ์เข็มที่ต่อกับคอมพิวเตอร์ได้อยู่แล้ว (ผ่าน OS print dialog/driver ที่ติดตั้งไว้ ไม่ต่างจากพิมพ์เอกสารทั่วไป) — สิ่งที่ผู้ใช้ขอเพิ่ม ("ต้องมีที่ปรับแต่งด้วย") ตีความว่าคือการปรับแต่งท้ายใบเสร็จข้อ 2 ด้านบนนั่นเอง ไม่ใช่ฟีเจอร์แยกใหม่ — ยังไม่ได้ถามผู้ใช้ยืนยันการตีความนี้ชัดเจน ถ้าต้องการ layout/ขนาดกระดาษเฉพาะสำหรับเครื่องพิมพ์เข็ม (เช่น กระดาษต่อเนื่องแบบฟอร์มหลายชั้น) ต้องแจ้งเพิ่มเติม
      - **SQL ใหม่ (ยังไม่ได้รัน):** `pos_configs.{receipt_footer_message,receipt_line_url}` — ดู "ต้องทำด้วยมือ" ด้านล่าง — ก่อนรัน ทุกอย่างทำงานปกติ (ทดสอบแล้วจริงทั้ง GET/PATCH) แค่บันทึกข้อความ/ลิงก์ที่ปรับแต่งไม่ได้จริงจนกว่าจะรัน
      - **ทดสอบ:** `npx next build` ผ่านทุก route — ยิง `GET`/`PATCH` `/api/pos/pos-config` ตรงกับ dev server ก่อนรัน SQL ยืนยัน fail-safe ถูกต้อง (ไม่ error, ฟิลด์เดิมไม่ถูกกระทบ) — **ยังไม่ได้ deploy ทั้งบั๊ก reconnect และฟีเจอร์ปรับแต่งท้ายใบเสร็จ** รอ deploy พร้อมกันหลัง commit

## Tech Stack

### Bot (`smileslip-pro/`)
| Layer | Tech |
|-------|------|
| Runtime | Node.js ≥20, Express |
| LINE | Webhook POST `/webhook`, Reply API + Push API |
| OCR | Hybrid: Google Cloud Vision → Gemini `gemini-3.5-flash` text-mode → Gemini image-mode (**ห้ามเปลี่ยน model เป็น gemini-2.5-flash — deprecated ในโปรเจกต์นี้**) |
| Storage | Google Drive API v3 (multipart upload) |
| Spreadsheet | Google Sheets API v4 |
| Database | Supabase (`@supabase/supabase-js` ^2) — เฉพาะข้อมูลร้านค้า/เครดิต |
| Deploy | Cloud Run `smileslip-service`, region `asia-southeast1` |

### Dashboard (`smileslip-dashboard/`)
| Layer | Tech |
|-------|------|
| Framework | Next.js 14, React 18 |
| UI | Tailwind CSS, lucide-react |
| Auth | LINE LIFF SDK (CDN) + LINE OAuth2 + Email+Password |
| Database | Supabase (ทุก DB query ต้องผ่าน API route ที่ใช้ service role key เท่านั้น) |
| Billing | Stripe Subscriptions + One-time payment — Next.js API routes |
| Charts | CSS bar charts (ห้ามใช้ recharts — lock file ไม่ sync) |
| Deploy | Cloud Run `smileslip-dashboard`, region `asia-southeast1` |

---

## Cloud Run Services

| Service | URL | Revision ล่าสุด |
|---------|-----|----------------|
| Bot | `https://smileslip-service-832247688217.asia-southeast1.run.app` | `smileslip-service-00229-stk` |
| Dashboard | `https://smileslip-dashboard-832247688217.asia-southeast1.run.app` | `smileslip-dashboard-00321-ndl` |
| Project | `smileslip-accounting-pro` | region: `asia-southeast1` |

---

## โครงสร้างไฟล์สำคัญ

```
/
├── smileslip-pro/
│   ├── index.js            ← หัวใจหลัก: webhook bot + OCR + Google integration
│   ├── package.json
│   ├── deploy-bot.sh       ← สคริปต์ deploy (รันจาก smileslip-pro/ เสมอ)
│   └── .env                ← secrets (gitignored)
│
├── smileslip-dashboard/
│   ├── pages/
│   │   ├── index.js        ← landing page (มี SEO meta tags)
│   │   ├── login.js        ← LIFF auto-login + Email fallback
│   │   ├── register.js     ← สมัครสมาชิก 4 ขั้น + รองรับ ?ref=SMILE-XXXX
│   │   ├── dashboard.js    ← หน้าหลัก (tabs: home/ledger/analytics/branches/settings)
│   │   ├── pricing.js      ← 5-tier + เติมเครดิต 4 แพ็กเกจ + referral section
│   │   ├── admin.js        ← admin panel (tabs: shops / ใบแจ้งหนี้+ภาษี / ตั้งค่า Google+email)
│   │   ├── invoice/
│   │   │   └── request.js  ← หน้าขอใบแจ้งหนี้ (ลูกค้ากรอกข้อมูล → admin อนุมัติ → ใบกำกับภาษี)
│   │   ├── terms.js        ← เงื่อนไข 8 ข้อ (PDPA + Stripe compliant)
│   │   ├── privacy.js      ← นโยบายความเป็นส่วนตัว 8 ข้อ (PDPA compliant)
│   │   ├── 404.js          ← custom error page
│   │   ├── payment/
│   │   │   └── success.js  ← หน้าชำระเงินสำเร็จ (auto redirect 5 วิ)
│   │   └── api/
│   │       ├── create-checkout-session.js  ← Stripe checkout (ดึง stripe_customer_id, cancel_url ใช้ owner_line_id)
│   │       ├── register.js                 ← บันทึกร้านค้า + เครดิต 20 + ธนาคาร + password hash + referral_code
│   │       ├── webhooks/
│   │       │   └── stripe.js              ← Stripe webhook (checkout.completed / invoice.payment_succeeded / subscription.deleted)
│   │       ├── admin/
│   │       │   ├── login.js               ← verify ADMIN_PASSWORD
│   │       │   ├── update-shop.js         ← update tier/credits/delete
│   │       │   ├── invoices.js            ← GET/PATCH ใบแจ้งหนี้ + issue (→ Sheet + email)
│   │       │   ├── shops.js               ← GET รายชื่อร้านทั้งหมด
│   │       │   ├── settings.js            ← GET admin Google email + sheet id
│   │       │   ├── send-email.js          ← POST ส่ง HTML tax invoice ทาง Gmail (nodemailer)
│   │       │   └── google/
│   │       │       ├── connect.js         ← redirect ไป Google OAuth สำหรับ admin บริษัท
│   │       │       └── callback.js        ← รับ token + สร้าง Invoice Sheet + บันทึก admin_settings
│   │       ├── auth/
│   │       │   ├── line.js                ← LINE OAuth redirect / LIFF POST handler (ใช้ service role key, insert shop_credits ด้วย)
│   │       │   ├── check-user.js          ← เช็คว่า LINE userId มีในระบบหรือยัง
│   │       │   ├── email-login.js         ← verify email + password_hash (pbkdf2)
│   │       │   ├── callback/line.js       ← LINE OAuth callback (ใช้ service role key)
│   │       │   └── google/
│   │       │       ├── connect.js         ← redirect to Google OAuth
│   │       │       └── callback.js        ← สร้าง Drive folder + Sheet (11 col) + bonus 30/50 เครดิต + referral bonus
│   │       └── shop/
│   │           ├── data.js                ← GET ข้อมูลร้านทั้งหมด (profile + credits + google + branches + banks)
│   │           ├── bank-accounts.js       ← GET/POST/DELETE บัญชีธนาคาร
│   │           ├── branches.js            ← GET/POST/DELETE สาขา
│   │           ├── update-profile.js      ← PATCH shop_name, email, phone, tax_id, user_type, address (ครบทุกฟิลด์)
│   │           ├── referral.js            ← GET referral code + stats (auto-generate ถ้ายังไม่มี)
│   │           └── analytics.js          ← GET กราฟ/สถิติจาก Google Sheets (tier-gated)
│   │       └── sheets/
│   │           └── transactions.js        ← อ่าน Google Sheets ส่งให้ Dashboard Ledger
│   │       └── export/
│   │           └── excel.js              ← Export Excel รายเดือน (xlsx)
│   │       └── invoice/
│   │           └── save.js               ← POST บันทึก invoice_requests (ลูกค้ายื่นขอใบแจ้งหนี้)
│   ├── data/
│   │   └── thailand-address.js            ← ข้อมูลจังหวัด/อำเภอ 77 จังหวัด + ธนาคาร 17 แห่ง
│   ├── deploy-web.sh       ← deploy dashboard (รันจาก smileslip-dashboard/)
│   └── .env                ← secrets (gitignored)
│
├── cloudbuild.yaml         ← CI/CD: push main → auto-deploy bot
└── deploy.sh               ← wrapper
```

---

## Supabase Tables — Schema จริง (verified 2026-06-28)

| ตาราง | Columns จริง | หมายเหตุ |
|-------|-------------|---------|
| `shop_profiles` | id, owner_line_id, shop_name, tax_id, **entity_type**, address, created_at, **user_type**, email, phone, branch_name, subscription_tier, line_group_id, owner_id, google_folder_id, google_sheet_id, password_hash, referral_code, referred_by, stripe_customer_id, stripe_subscription_id, **subscription_expires_at**, **stripe_billed_tier**, **stripe_period_end**, **google_bonus_granted** | มีทั้ง entity_type และ user_type (สองคอลัมน์แยกกัน!) tier: normal/pro/advance/business/enterprise/super — 4 คอลัมน์ท้ายเพิ่ม 2026-06-28 (ดูหัวข้อ "Subscription Expiry + Stripe Mismatch Detection" ด้านล่าง) |
| `shop_credits` | shop_id (PK), balance_credits, updated_at | |
| `shop_google_configs` | shop_id (PK), google_refresh_token, google_folder_id, google_sheet_id, updated_at, google_email | **bot + dashboard อ่าน token จากตารางนี้** |
| `shop_branches` | id, shop_id, branch_name, line_group_id, is_active, created_at | |
| `shop_bank_accounts` | id, shop_id, bank_name, account_number, account_name, is_active, account_type | |
| `shop_admins` | id, shop_id, line_user_id, display_name, created_at, status | ผู้ดูแลระบบของแต่ละร้าน (ไม่ใช่ admin บริษัท) |
| `credit_packages` | id, package_name, price, credit_amount, is_active | |
| `credit_purchase_history` | id, shop_id, package_id (nullable), amount_paid, status, slip_url, created_at | bot insert แค่บางคอลัมน์ — suppress error ไว้แล้ว |
| `credit_topup_history` | id, shop_id, amount_paid, credits_added, topup_slip_url, topup_status, created_at | ยังไม่มีโค้ด insert (future) |
| `usage_logs` | id, created_at, transaction_id, line_user_id, **amont** (typo!), status, note | ยังไม่มีโค้ด insert; ถ้าจะ insert ต้องใช้ชื่อ `amont` ตามจริง |
| `ledger_transactions` | id, shop_id, type, amount, category, note, slip_url, slip_hash, status, created_at, sender_name, receiver_name, branch_name, tax_id, taxpayer_name, tax_amount, tax_address, payment_method, recorder_name, transaction_at, raw_data | **แก้ไข 2026-07-30 (บรรทัดนี้เคยเขียนผิด):** บอทเขียนเข้าจริงแล้ว (dual-write ตั้งแต่ Tier D ของ migration) ทั้งจากสลิป OCR และ POS (sales/expenses/receives/confirm-payment) — เป็นข้อมูลธุรกรรมจริงรวม `sender_name`/`slip_url`/เลขภาษี ไม่ใช่ข้อมูลไม่ระบุตัวตน ห้ามขาย/แชร์ต่อบุคคลที่สาม (ดูหลักการ PDPA ใหม่ด้านบน) — คนละตารางจาก `slip_analytics`/`sender_profiles` ที่เข้ารหัสแล้วปลอดภัยสำหรับทำ analytics/ขายข้อมูล |
| `invoice_requests` | id, shop_id, plan_id, is_yearly, base_price, vat_amount, total_price, wht_amount, net_amount, buyer_name, buyer_tax_id, buyer_branch, buyer_address, buyer_email, buyer_phone, invoice_no, tax_invoice_no, status, created_at, approved_at | status: pending/approved/issued/rejected ✅ มีอยู่แล้ว |
| `slip_analytics` | id, slip_id, shop_id, branch_id, slip_date, hour_of_day, day_of_week, week_of_year, month, year, amount_bucket, transaction_type, sender_hash, sender_bank, slip_type, created_at | ✅ มีอยู่แล้ว |
| `sender_profiles` | id, shop_id, sender_hash, sender_bank, first_seen, last_seen, total_transactions, amount_bucket_mode, frequency_score, updated_at | ✅ มีอยู่แล้ว |
| `shop_usage_daily` | id, shop_id, branch_id, date, slip_count, income_count, expense_count, active_hours, unique_senders | ✅ มีอยู่แล้ว |
| `admin_settings` | key (PK), value, updated_at | ✅ มีอยู่แล้ว (verified 2026-06-28) |
| `pos_configs` | shop_id (PK), pos_folder_id, pos_sheet_id, created_at | ❌ **ต้องสร้างก่อนใช้ /pos** |
| `delivery_configs` | shop_id (PK), delivery_folder_id, customer_sheet_id, order_sheet_id, created_at | ❌ **ต้องสร้างก่อนใช้ /delivery** |
| `users` | id, created_at, line_user_id, credits, google_sheet_id, display_name | Legacy table เก่า — ไม่ได้ใช้งานแล้ว |

> **สำคัญ:** `google_tokens` table ไม่มีอยู่จริง — bot อ่าน refresh_token จาก `shop_google_configs` เท่านั้น
> **สำคัญ:** ห้าม query Supabase จาก client โดยตรง — ทุก query ต้องผ่าน API route ที่ใช้ **service role key**

---

## ระบบ Tier / แพ็กเกจ (อัปเดต 2026-06-08)

| แพ็กเกจ | DB value | ราคา | สาขา Max | เครดิต/เดือน | ฟีเจอร์พิเศษ |
|---------|----------|------|---------|------------|------------|
| Starter | `normal` | ฟรี | 1 | 50 (ครั้งเดียว) | สแกนสลิป + คีย์เอง |
| Shop Pro | `pro` | ฿199 | 1 | 200 | #สรุป, #กำไรขาดทุน, Push แจ้งเจ้าของ, Analytics |
| Advance | `advance` | ฿499 | 5 | 500 | + #สรุปทุกสาขา, Branch comparison |
| Business | `business` | ฿999 | 10 | 1,000 | + Top Sender, 3 Admin |
| Enterprise | `enterprise` | ฿2,990 | 20 | ไม่จำกัด | Unlimited scan, VIP Support, Custom report |
| (legacy) | `super` | — | 20 | ไม่จำกัด | = enterprise (backward compat) |

**TIER_LEVEL:** `{ normal:0, pro:1, advance:2, business:3, enterprise:4, super:4 }`

### Stripe Price IDs (Production)

| แพ็กเกจ | รายเดือน | รายปี |
|--------|---------|------|
| Shop Pro ฿199 | `price_1TfERr3ZvivzvZ6qPXOhc10t` | `price_1TfERr3ZvivzvZ6qbsLKKPlV` |
| Advance ฿499 | `price_1TfERs3ZvivzvZ6qw9SB10YE` | `price_1TfERs3ZvivzvZ6qPKQaBSHQ` |
| Business ฿999 | `price_1Tg4zU3ZvivzvZ6ql61Q0szc` | `price_1Tg4zU3ZvivzvZ6qP2AE2Yz0` |
| Enterprise ฿2,990 | `price_1TfERs3ZvivzvZ6qrTxPwYKs` | `price_1TfERt3ZvivzvZ6qgpoAJvaU` |
| เติม 100 แผ่น ฿99 | `price_1TfERt3ZvivzvZ6qfbOMCZnp` | — |
| เติม 500 แผ่น ฿299 | `price_1TfERu3ZvivzvZ6q96qkkfKx` | — |
| เติม 1,000 แผ่น ฿499 | `price_1TfERu3ZvivzvZ6qpTZDUhHZ` | — |
| เติม 3,000 แผ่น ฿999 | `price_1Tg56f3ZvivzvZ6qX4163cg5` | — |

### Stripe Webhook Events (ต้องครบทั้ง 3)
- `checkout.session.completed` → อัปเดต tier + บันทึก stripe_customer_id/subscription_id
- `invoice.payment_succeeded` → เติมเครดิตรายเดือนอัตโนมัติ
- `customer.subscription.deleted` → downgrade กลับ normal

**Webhook URL:** `https://smileslip-backend-832247688217.asia-southeast1.run.app/webhook/stripe`

---

## Bot Flow (เมื่อมีคนส่งสลิปใน LINE)

```
1. ตอบ HTTP 200 ทันที (กัน LINE timeout → retry)
2. LINE Signature Verification (HMAC-SHA256) — reject ถ้าไม่ใช่ LINE จริง
3. Duplicate Guard ชั้น 1: webhookEventId (in-memory Map, TTL 5 นาที)
4. findShopBySource(sourceId):
   - ค้นหาจาก shop_profiles.line_group_id หรือ owner_line_id
   - ถ้าไม่พบ → ค้นหาจาก shop_branches.line_group_id (สาขา)
5. เช็คเครดิตคงเหลือ (Enterprise/Super ข้ามขั้นตอนนี้)
6. ดาวน์โหลดรูปจาก LINE + คำนวณ MD5 hash
7. Duplicate Guard ชั้น 2: image hash (in-memory, TTL 24 ชม.)
8. Hybrid OCR:
   - Cloud Vision API → ข้อความดิบ
   - ถ้าได้ข้อความ → Gemini text-mode (เร็วกว่า 2-3x)
   - ถ้าข้อความน้อยเกิน → Gemini image-mode
   - Fallback: retry ด้วย model เดิม (gemini-3.5-flash) 3 ครั้ง เมื่อ 503 — ห้ามเปลี่ยนเป็น gemini-2.5-flash
   - อ่าน: type, amount, date, time, sender, receiver, note, ref_no, tax_id, taxpayer_name, tax_amount, tax_address
9. ตรวจสอบ income/expense จากชื่อบัญชีร้าน (shop_bank_accounts) + ชื่อร้าน + ชื่อสาขาทุกสาขา:
   - receiver ตรงกับชื่อบัญชี/ชื่อร้าน/ชื่อสาขาใดก็ได้ → income
   - sender ตรงกับชื่อบัญชี/ชื่อร้าน/ชื่อสาขาใดก็ได้ → expense
   - ไม่ match → เชื่อ Gemini
   - (`detectTypeFromBankAccounts(slipData, bankAccounts, extraNames)` — extraNames = [shop_name, ...branch_names])
10. Duplicate Guard ชั้น 3: Sheets column K — ref_no หรือ image hash
11. [Optional] Google Drive/Sheets — แยก try/catch ไม่หยุดระบบถ้า Google พัง
    - อ่าน refresh_token จาก shop_google_configs
    - สร้างโฟลเดอร์: root → ปี ค.ศ. → เดือน-ปี
    - อัปโหลดรูป → บันทึก Sheets (11 คอลัมน์)
12. ตัดเครดิต -1 (Enterprise/Super ข้าม)
13. แจ้งเตือนเครดิตใกล้หมด (Push LINE เจ้าของเมื่อ < 10 แผ่น)
14. Push แจ้งเจ้าของส่วนตัว (Pro+ เมื่อสาขาส่งสลิป)
15. ตอบ Flex Message + quote รูปต้นทาง (quoteToken)
```

## Text Command Flow (เมื่อพิมพ์คำสั่งใน LINE)

```
#ช่วยเหลือ / #help      → ทุกแพ็กเกจ: แสดงคำสั่งตาม tier
#วิธีใช้งาน             → ทุกแพ็กเกจ: เมนู Flex เลือกหัวข้อสอนใช้งาน (8 หัวข้อ) แต่ละหัวข้อมีปุ่มสลับแบบคร่าวๆ/ละเอียด — เพิ่ม 2026-06-28
รับ NNN หมายเหตุ        → ทุกแพ็กเกจ (ทุกคนในกลุ่ม): คีย์รายรับเอง (เงินสด) — แก้ไข 2026-07-03
รับโอน NNN หมายเหตุ     → ทุกแพ็กเกจ (ทุกคนในกลุ่ม): คีย์รายรับเอง (เงินโอน) — แก้ไข 2026-07-03
จ่าย NNN หมายเหตุ       → ทุกแพ็กเกจ (ทุกคนในกลุ่ม): คีย์รายจ่ายเอง (เงินสด) — แก้ไข 2026-07-03
จ่ายโอน NNN หมายเหตุ    → ทุกแพ็กเกจ (ทุกคนในกลุ่ม): คีย์รายจ่ายเอง (เงินโอน) — แก้ไข 2026-07-03
#สรุปวันนี้             → Pro+: ยอดวันนี้
#สรุปเดือนนี้           → Pro+: ยอดเดือนนี้
#สรุปอาทิตย์นี้         → Pro+: ยอดอาทิตย์นี้ (จันทร์-อาทิตย์)
#สรุปปีนี้             → Pro+: ยอดทั้งปี
#สรุปวันที่ 07/06       → Pro+: ยอดย้อนหลังรายวัน
#กำไรขาดทุน            → Pro+: รายรับ - รายจ่าย
#รายงาน               → Pro+: รายงานเดือนนี้
#สรุปทุกสาขา           → Advance+: รวมทุกสาขาเดือนนี้
```

> **คำสั่งสรุปทุกแบบ (#สรุป...) แก้บั๊ก 400 เมื่อ tab ปียังไม่ถูกสร้าง — 2026-06-28:** ทุก handler เรียก `getOrCreateYearSheet()` ก่อนอ่านข้อมูลเสมอ (เดิมอ่าน range ตรงๆ ถ้า tab ปีนั้นไม่มีอยู่จะ throw 400 จาก Sheets API)

---

## Google Sheets Structure (18 คอลัมน์ A-R — อัปเดต 2026-06-28)

| คอลัมน์ | Header | ข้อมูล |
|---------|--------|--------|
| A | วันที่สลิป | วันที่จาก OCR |
| B | เวลา | เวลาจาก OCR |
| C | ประเภท (รายรับ/รายจ่าย) | type จาก OCR + bank account correction — **ห้ามแก้/เปลี่ยนความหมายคอลัมน์นี้** ระบบอื่นพึ่งพาอยู่มาก |
| D | จำนวนเงิน (บาท) | amount |
| E | ผู้โอน | sender |
| F | ผู้รับ | receiver |
| G | หมายเหตุ | note |
| H | ลิงก์สลิป (Drive) | Google Drive URL |
| I | วันที่บันทึก (recorded_at) | YYYY-MM-DD |
| J | ชื่อสาขา | branch_name |
| K | เลขอ้างอิง/Hash | ref_no จากธนาคาร หรือ MD5 image hash (ใช้ตรวจซ้ำ) |
| L | เลขภาษี | tax_id จาก OCR (ถ้ามี) |
| M | ชื่อผู้เสียภาษี | taxpayer_name จาก OCR |
| N | ยอดภาษี (บาท) | tax_amount จาก OCR |
| O | ที่อยู่ผู้เสียภาษี | tax_address จาก OCR |
| P | หมวดหมู่ | detectCategory() Gemini + learned rules (Business+) |
| Q | วิธีรับ-จ่าย (โอน/เงินสด) | สลิป OCR = "โอน" เสมอ, คีย์เอง = "เงินสด" ถ้าพิมพ์เปล่า หรือ "โอน" ถ้าพิมพ์ รับโอน/จ่ายโอน — **เพิ่ม 2026-06-28** |
| R | ผู้บันทึก | ชื่อ LINE display name ของคนที่คีย์รายการเอง (ไม่มีค่าถ้าเป็นสลิป OCR) — **เพิ่ม 2026-06-28** |

> **Sheet เก่า**: `getOrCreateYearSheet()` ใน bot จะ auto-patch header คอลัมน์ K/P/Q/R ที่ขาดไปทุกครั้งที่มีการบันทึกรายการใหม่เข้า tab ปีนั้น ไม่ต้องเข้าไปเติม header เองใน Sheets แล้ว (เดิมต้องทำมือ — แก้ไข 2026-06-28)

## Google Drive Structure

```
SMILE SLIP - {ชื่อร้าน}  ← root (google_folder_id ใน shop_profiles)
└── 2026/                 ← ปี ค.ศ.
    └── 06-2026/          ← เดือน-ปี
        └── slip_1500THB_06-2026_timestamp.jpg
```

---

## Auth Flow

### LINE LIFF (หลัก)
```
เปิดหน้า /login → โหลด LIFF SDK จาก CDN (ห้ามติดตั้งเป็น npm)
→ liff.init({ liffId: NEXT_PUBLIC_LIFF_ID })
→ liff.login() auto-redirect ถ้ายังไม่ login
→ liff.getProfile() → userId, displayName
→ GET /api/auth/check-user?userId=xxx
→ มีบัญชี → /dashboard?userId=xxx
→ ไม่มีบัญชี → /register?userId=xxx&name=xxx
```

### LINE OAuth2 (fallback)
```
GET /api/auth/line → redirect LINE OAuth → /api/auth/callback/line
→ เช็ค shop_profiles → /dashboard หรือ /register
```

### Email Login (fallback สุดท้าย)
```
POST /api/auth/email-login → เช็ค email + verify password_hash (pbkdf2)
→ คืน userId (owner_line_id) → /dashboard?userId=xxx
```

### Register
```
Step 1: ข้อมูลธุรกิจ (ชื่อร้าน, เลขภาษี, บุคคล/นิติบุคคล)
Step 2: ที่อยู่ + ติดต่อ (dropdown 77 จังหวัด → อำเภอ)
Step 3: บัญชีธนาคาร (17 แห่ง) + ตั้ง Password + ยืนยัน Terms
Step 4: สำเร็จ (ได้ 20 เครดิตเริ่มต้น)
→ รองรับ ?ref=SMILE-XXXX (referral code จะ set referred_by)
```

### Google OAuth (เชื่อมต่อ Drive)
```
Settings → "เชื่อมต่อ Google" → /api/auth/google/connect
→ Google OAuth → /api/auth/google/callback
→ ถ้า reconnect (มี folder/sheet เดิมอยู่แล้ว) → reuse folder/sheet เดิม ไม่สร้างใหม่
→ ถ้า first connect → สร้าง Drive folder + Sheet 18 คอลัมน์ใหม่
→ upsert shop_google_configs (refresh_token, folder_id, sheet_id, email)
→ bonus +30 เครดิต (หรือ +50 ถ้ามี referred_by) — เช็คผ่าน shop_profiles.google_bonus_granted เท่านั้น
→ referrer ได้ +50 เครดิตด้วย (ครั้งแรกเท่านั้น)
```
> **แก้ไข 2026-06-28:** เดิมเช็คว่าเคยให้โบนัสหรือยังจากการ "มีแถวอยู่ใน shop_google_configs" ซึ่งทำให้บางเคส reconnect ได้โบนัสซ้ำ (พบเคสจริง เครดิต 47→77) ตอนนี้ใช้ flag `google_bonus_granted` บน `shop_profiles` เช็คแทน กันซ้ำเด็ดขาด

---

## Referral System (อัปเดต 2026-06-08)

- ทุกร้านมี `referral_code` รูปแบบ `SMILE-XXXX` (auto-generate เมื่อเปิด dashboard)
- ลิงก์: `{BASE_URL}/register?ref=SMILE-XXXX`
- เงื่อนไขได้รับ bonus: ร้านที่ถูกแนะนำต้องเชื่อมต่อ Google Drive ก่อน (กัน abuse)
- bonus: ผู้ถูกแนะนำ +50 เครดิต, ผู้แนะนำ +50 เครดิต
- แต่ละร้านถูกแนะนำได้ครั้งเดียว (referred_by เปลี่ยนไม่ได้)
- API: `GET /api/shop/referral?shopId=xxx` → คืน referralCode, referralCount, creditsEarned, referralUrl

---

## Analytics System (อัปเดต 2026-06-12)

- API: `GET /api/shop/analytics?shopId=xxx&year=2026&month=06`
- อ่านจาก Google Sheets โดยตรง (ไม่เก็บใน Supabase)
- Tier-gated:
  - Pro+: รายวัน (default) + รายเดือน toggle, summary cards, month picker
  - Advance+: branch comparison
  - Business+: top senders table, Tax Report (year+month)
- Dashboard แสดงด้วย CSS bars (ไม่ใช้ recharts)
- response มี `dailyData` (เดือนที่เลือก) + `dailySummary` + `monthlyData` + `summary` (ทั้งปี)

---

## Stripe Flow

```
pricing.js → POST /api/create-checkout-session
  → ดึง stripe_customer_id จาก shop_profiles (สร้างใหม่ถ้าไม่มี)
  → Stripe Checkout Session (mode: subscription หรือ payment)
  → cancel_url ใช้ owner_line_id (ไม่ใช่ UUID!)
→ ลูกค้าจ่ายเงิน
→ Stripe POST /api/webhooks/stripe
  → checkout.session.completed → update tier + บันทึก customer/subscription id
  → invoice.payment_succeeded → เติมเครดิตรายเดือน (billing_reason: subscription_cycle)
  → customer.subscription.deleted → downgrade → normal
→ redirect /payment/success → auto redirect dashboard 5s
```

---

## Subscription Expiry + Stripe Mismatch Detection (เพิ่ม 2026-06-28)

ปัญหาที่แก้: Admin ตั้ง tier มือได้อิสระ (เช่น โปรโมชั่น, ทดลองใช้) แต่ไม่มีวันหมดอายุ และไม่มีทางรู้ว่า tier ที่ตั้งไว้ตรงกับที่ Stripe เก็บเงินจริงหรือไม่ (เช่น ลูกค้ายกเลิก Stripe แต่ Admin ลืม downgrade)

- **`subscription_tier`** — tier ที่ "ใช้งานจริงอยู่ตอนนี้" (สิ่งที่ระบบเช็คสิทธิ์ฟีเจอร์) Admin ตั้งได้อิสระ
- **`subscription_expires_at`** — วันหมดอายุของ tier ที่ Admin ตั้งมือ (เว้นว่าง = ไม่หมดอายุ, ใช้กับ tier ที่จ่ายผ่าน Stripe อัตโนมัติด้วยไม่ได้เพราะ Stripe ต่ออายุเอง)
- **`stripe_billed_tier`** + **`stripe_period_end`** — tier และวันหมดรอบบิลที่ Stripe เก็บเงินจริงล่าสุด (เขียนจาก webhook เท่านั้น ไม่ใช่ Admin ตั้ง)
- Admin UI (`/admin` → tab ร้านค้า) แสดง badge เตือนถ้า `subscription_tier` (ที่ตั้งมือ) ≠ `stripe_billed_tier` (ที่จ่ายจริง) — "⚠️ ไม่ตรงกับ Stripe"
- Admin UI แสดง countdown วันหมดอายุของ tier ที่ตั้งมือ (สีเหลืองถ้าเหลือ ≤7 วัน, สีแดงถ้าหมดแล้ว)
- ปุ่ม "Manual Payment" ใน Admin (โอนจ่ายผ่านบัญชีบริษัท) จะตั้ง tier + เติมเครดิตตาม plan + ตั้งวันหมดอายุให้พร้อมกันในคลิกเดียว
- **Cron `/api/cron/expire-subscriptions`** (Cloud Scheduler job `smileslip-expire-subscriptions`, schedule `0 19 * * *` UTC = 02:00 กรุงเทพ ทุกวัน) — เช็คร้านที่ `subscription_expires_at` หมดแล้วและ tier ≠ normal → downgrade กลับ normal อัตโนมัติ ต้องส่ง header `x-cron-secret` ตรงกับ `CRON_SECRET`

---

## Admin Panel

- URL: `/admin`
- Login: ใช้ `ADMIN_PASSWORD` จาก env
- Password เริ่มต้น: `SmileSlipAdmin2569!`
- Session: sessionStorage (token base64)
- Tabs: **ร้านค้า** / **ใบแจ้งหนี้/ภาษี** / **ตั้งค่า**
- Features ร้านค้า: ดูร้านทั้งหมด, เปลี่ยน tier (6 ตัวเลือก: normal/pro/advance/business/enterprise/super) + วันหมดอายุ, เติม/ตั้งเครดิต, ลบร้าน, ค้นหา, filter tier, เตือน mismatch กับ Stripe (ดูหัวข้อด้านบน)
- Stats card: แสดงจำนวนแยก Normal / Pro / Advance / Business / Enterprise / เครดิตรวม
- Features ใบแจ้งหนี้/ภาษี: ดูคำขอ, อนุมัติ/ปฏิเสธ, ออกใบกำกับภาษี (→ บันทึก Sheet + ส่งอีเมลอัตโนมัติ), พิมพ์ PDF
- Features ตั้งค่า: เชื่อม Google Drive/Sheets บริษัท, ดูสถานะอีเมล

## Invoice Flow (ใบแจ้งหนี้ → ใบกำกับภาษี)

```
1. ลูกค้าเปิด /invoice/request → กรอกข้อมูล → เห็น "ใบแจ้งหนี้" preview (ไม่มีตราประทับ)
2. กด "ส่งคำขอ" → POST /api/invoice/save → บันทึก invoice_requests (status: pending)
3. Admin เปิด /admin → tab "ใบแจ้งหนี้/ภาษี" → เห็น pending
4. Admin กด "อนุมัติ" (status: approved)
5. Admin กด "ออกใบกำกับภาษี" (status: issued):
   - สร้าง TAX-YYYY-XXXX sequential number
   - บันทึก Google Sheet ของบริษัท (admin_invoice_sheet_id)
   - ส่ง HTML email ให้ buyer_email ผ่าน nodemailer (Gmail)
6. Admin พิมพ์ PDF ได้จากหน้า Admin (มีตราประทับ + ลายเซ็น)
```

### Admin Google Sheet (Invoice Register)
สร้างอัตโนมัติตอน Google connect ชื่อ: `SMILE SLIP — ใบกำกับภาษี / Tax Invoice Register`
คอลัมน์: วันที่ออก | เลขที่ใบกำกับภาษี | อ้างอิงใบแจ้งหนี้ | ชื่อผู้ซื้อ | เลขภาษี | สาขา | แพ็กเกจ | รายปี/เดือน | ราคาก่อน VAT | VAT 7% | รวม VAT | WHT 3% | ยอดสุทธิ | อีเมล | เบอร์โทร

---

## Environment Variables สำคัญ

### Bot (`smileslip-pro/.env`)
```
SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY   ← หรือ SUPABASE_KEY (fallback)
LINE_CHANNEL_ACCESS_TOKEN   ← Messaging API (ไม่ใช่ LINE Login)
LINE_CHANNEL_SECRET         ← Messaging API channel secret
GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET
GEMINI_API_KEY
GEMINI_MODEL=gemini-3.5-flash
GOOGLE_VISION_API_KEY       ← optional, ถ้าไม่มีจะ degrade เป็น Gemini image-mode
FRONTEND_URL                ← URL ของ dashboard (ใช้ใน push notification ลิงก์)
CRON_SECRET                 ← secret สำหรับ /cron/daily-summary และ /cron/weekly-summary (ค่า: SmileSlipCron2569!)
OWNER_SESSION_SECRET        ← เซ็นชื่อ deep-link token ที่ส่งให้เจ้าของร้านทาง LINE (lib/owner-session-sign.js)
                                ต้องเป็นค่าเดียวกับฝั่ง dashboard เป๊ะ (คัดลอกมาแล้ว) — dashboard's
                                verifyOwnerSession() ต้องอ่าน token นี้ได้ตรงๆ ถ้าไม่ตรงกัน token จะ
                                verify ไม่ผ่านเงียบๆ (ไม่ error แค่ deep-link จะไม่มี owner-session ติดไป)
                                ตั้งใน production แล้ว (deploy พร้อมข้อ 63, 2026-08-07, ผ่าน
                                --update-env-vars ไม่ใช่ --set-env-vars กัน env vars เดิมของบอทหาย)
```

### Dashboard (`smileslip-dashboard/.env`)
```
NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_KEY   ← sb_publishable_... (anon, client-side)
SUPABASE_URL, SUPABASE_KEY                            ← sb_publishable_...
SUPABASE_SERVICE_ROLE_KEY                             ← sb_secret_... (bypass RLS, server-side เท่านั้น)
STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET
LINE_CHANNEL_ACCESS_TOKEN                             ← Messaging API (Push notification)
LINE_CHANNEL_SECRET                                   ← Messaging API channel secret
LINE_LOGIN_SECRET                                     ← LINE Login channel (คนละ channel!)
NEXT_PUBLIC_LINE_LOGIN_CHANNEL_ID=2009797558
NEXT_PUBLIC_LIFF_ID=2009797558-LMletOqM
GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REDIRECT_URI
NEXT_PUBLIC_BASE_URL                                  ← https://smileslip-dashboard-...run.app
FRONTEND_URL                                          ← เหมือน NEXT_PUBLIC_BASE_URL
ADMIN_PASSWORD=SmileSlipAdmin2569!
EMAIL_HOST=smtp.gmail.com            ← nodemailer (default smtp.gmail.com)
EMAIL_PORT=465                       ← SSL
EMAIL_USER                           ← Gmail address ของบริษัท
EMAIL_PASS                           ← Gmail App Password (16 ตัวอักษร, ต้องเปิด 2FA)
NEXT_PUBLIC_EMAIL_CONFIGURED         ← "true" หรือ "false" (แสดงใน Admin Settings UI)
CRON_SECRET                          ← ต้องตรงกับของ bot — ใช้ตรวจ header x-cron-secret ของ /api/cron/expire-subscriptions
STAFF_SESSION_SECRET                 ← เซ็นชื่อ session พนักงาน (PIN login) ทั้ง /pos?mode=cashier และ /pos-staff
                                        (lib/staff-session.js) — ไม่ตั้งค่านี้ = ออก session ไม่ได้เลย (fail-closed)
                                        ตั้งใน production แล้ว (deploy พร้อมข้อ 44, 2026-07-23)
OWNER_SESSION_SECRET                 ← เซ็นชื่อ session เจ้าของร้าน/แอดมินร้าน (lib/owner-session.js) — ออกตอน
                                        login (LIFF/email/OAuth) TTL 30 วัน — ไม่ตั้งค่านี้ = ออก/verify token
                                        ไม่ได้เลย (fail-closed) เหมือน STAFF_SESSION_SECRET
                                        ตั้งใน production แล้ว (deploy พร้อมข้อ 57 เฟส A-C, 2026-08-04)
```

---

## Deploy

```bash
# Bot — Bash tool ล้มเหลวบน Windows (Python not found) ใช้ PowerShell แทน:
# cd smileslip-pro && bash deploy-bot.sh  ← ใช้ไม่ได้บน Windows

# Dashboard — Bash tool ล้มเหลวบน Windows (Python not found) ใช้ PowerShell แทน:
# cd smileslip-dashboard && bash deploy-web.sh  ← ใช้ไม่ได้บน Windows

# วิธี deploy บน Windows — ใช้ PowerShell กับ gcloud โดยตรง (parse .env เอง):
# ดู pattern ใน session ก่อนหน้า หรือใช้ script ด้านล่าง

# ดู log prod (bot)
gcloud logging read 'resource.type="cloud_run_revision" AND resource.labels.service_name="smileslip-service"' \
  --limit=50 --freshness=1h --format='value(timestamp,textPayload)' --project=smileslip-accounting-pro

# ดู log prod (dashboard)
gcloud logging read 'resource.type="cloud_run_revision" AND resource.labels.service_name="smileslip-dashboard"' \
  --limit=50 --freshness=1h --format='value(timestamp,textPayload)' --project=smileslip-accounting-pro

# ดู builds ล่าสุด (region สำคัญ!)
gcloud builds list --region asia-southeast1 --limit=5
```

---

## สิ่งที่ทำเสร็จแล้ว ✅

### Bot
- [x] Hybrid OCR: Cloud Vision → Gemini text-mode → Gemini image-mode (fallback gemini-2.5-flash)
- [x] Retry 3 ครั้ง + backoff 2s + fallback อัตโนมัติเมื่อ 503
- [x] อ่านสลิปโอนเงิน + บิลรายจ่าย + ข้อมูลภาษี (tax_id, tax_amount, taxpayer_name)
- [x] ตรวจสอบ income/expense จากชื่อบัญชีร้าน (shop_bank_accounts) — 2026-06-08
- [x] Google Drive: root → ปี ค.ศ. → เดือน-ปี
- [x] Google Sheets: 11 คอลัมน์ (รวม recorded_at + branch_name + ref_no/hash)
- [x] Flex Message + quote รูปต้นทาง (สีเขียว=รายรับ/แดง=รายจ่าย)
- [x] ตอบ 200 OK ทันที + Duplicate Guard 3 ชั้น (webhookEventId / image hash / Sheets K)
- [x] Multi-branch: findShopBySource()
- [x] Enterprise/Super ไม่ตัดเครดิต
- [x] Push แจ้งเจ้าของส่วนตัว (Pro+)
- [x] แจ้งเตือนเครดิตใกล้หมด (Push LINE < 10 แผ่น)
- [x] Text Commands: #สรุปวันนี้/เดือน/อาทิตย์/ปี/วันที่/กำไร/รายงาน (Pro+)
- [x] #สรุปทุกสาขา (Advance+) — per-branch breakdown แยกรายสาขา — แก้ไข 2026-06-11
- [x] #ช่วยเหลือ แสดงคำสั่งตาม tier
- [x] คีย์รายการเอง: รับ/จ่าย NNN หมายเหตุ — **ทุกคนในกลุ่มใช้ได้** (แก้ไข 2026-07-03 เดิมล็อคเฉพาะเจ้าของ) บันทึกชื่อผู้คีย์ไว้ใน column R เพื่อ audit
- [x] LINE Signature Verification (HMAC-SHA256)
- [x] Error Monitoring (Cloud Error Reporting via structured stderr)
- [x] PDPA compliant (ไม่เก็บข้อมูลการเงินใน Supabase)
- [x] Analytics: recordAnalytics() PDPA-safe (sha256 sender, amount_bucket) — 2026-06-11 (ต้องสร้าง tables ก่อน)
- [x] findShopBySource() คืน branchId ด้วย — 2026-06-11
- [x] **แจ้งเตือนสรุปยอดรายวันอัตโนมัติ** POST `/cron/daily-summary` + Cloud Scheduler 18:00 BKK (job: smileslip-daily-summary) — 2026-06-12
- [x] **แจ้งเตือนสรุปยอดรายสัปดาห์อัตโนมัติ** POST `/cron/weekly-summary` + Cloud Scheduler ทุกวันจันทร์ 18:00 BKK (job: smileslip-weekly-summary)
- [x] **คีย์รายการเองแยกโอน/เงินสด** — รองรับคำขึ้นต้นหลากหลาย (รับ/รับเงิน/รับเงินสด/รับสด/รับโอน/รับโอนเงิน, จ่าย/จ่ายเงิน/จ่ายเงินสด/จ่ายสด/จ่ายตังสด/จ่ายโอน/โอนจ่าย) บันทึกผลลงคอลัมน์ Q — 2026-06-28
- [x] **บันทึกผู้คีย์รายการ** — รายการที่คีย์เอง (ไม่ใช่สลิป OCR) บันทึกชื่อ LINE display name ผู้คีย์ไว้คอลัมน์ R — 2026-06-28
- [x] **Sheets ขยายเป็น 18 คอลัมน์ (A-R)** — เพิ่ม L-O (ข้อมูลภาษี), P (หมวดหมู่), Q (วิธีรับ-จ่าย), R (ผู้บันทึก) — `getOrCreateYearSheet()` auto-patch header คอลัมน์ที่ขาดให้ sheet เก่าทุกครั้งที่บันทึกรายการใหม่ — 2026-06-28
- [x] **แก้บั๊ก #สรุป... ตอบ error 400** — ทุก summary command เรียก `getOrCreateYearSheet()` ก่อนอ่าน Sheets เสมอ (เดิม throw ถ้า tab ปียังไม่ถูกสร้าง) — 2026-06-28
- [x] **แก้บั๊ก OCR อ่านปี พ.ศ./ค.ศ. ผิด (ลายมือ)** — เพิ่ม context วันที่ปัจจุบันในพรอมต์ Gemini + กฎ sanity-bound ปฏิเสธปีที่ห่างจากปีปัจจุบันเกินเหตุ ใน `parseSlipDateForFolder()` — 2026-06-28
- [x] **`#วิธีใช้งาน`** — เมนู Flex เลือกหัวข้อสอนใช้งาน 8 หัวข้อ (สลิป/คีย์เอง/สรุปยอด/สาขา/เครดิต/แก้ไข/กูเกิล/แอดมิน) แต่ละหัวข้อสลับมุมมองคร่าวๆ ↔ ละเอียดได้ — 2026-06-28
- [x] **`#ช่วยเหลือ` อัปเดต** — เพิ่มคำสั่ง รับโอน/จ่ายโอน + แนะนำ `#วิธีใช้งาน` — 2026-06-28
- [x] **แก้บั๊กให้โบนัสเครดิตซ้ำตอน reconnect Google** — เปลี่ยนจากเช็ค "มีแถวใน shop_google_configs" เป็นเช็ค flag `google_bonus_granted` บน shop_profiles แทน (เคสจริง: เครดิตขึ้นจาก 47→77) — 2026-06-28
- [x] **แก้ QR slip "จ่ายบิล" อ่านเป็น expense แทน income** — ปัญหา: confidence <0.75 ทำให้ route ไป image-mode → Gemini อ่าน "จ่ายบิลสำเร็จ" เป็น expense แก้: `isHandwritten = avgConfidence < 0.75 && rawText.length < 100` (ต้องเป็นทั้งสองเงื่อนไข) สลิปดิจิทัลที่มีข้อความยาว >100 chars จะไม่ถูก route ไป image-mode — 2026-07-03
- [x] **เพิ่ม branch names ในการตรวจประเภทสลิป** — `detectTypeFromBankAccounts` รับ `extraNames = [shop_name, ...branch_names]` ด้วย เพราะสลิปอาจแสดงชื่อสาขา (เช่น "ดี แก๊ส") ไม่ใช่ชื่อร้านหลัก — 2026-07-03
- [x] **VAT breakdown ใน Flex Message** — แสดง "ราคาก่อน VAT" + "ภาษีมูลค่าเพิ่ม" เมื่อ OCR อ่าน tax_amount > 0 — 2026-07-03
- [x] **Tax fields ในหน้าแก้ไขธุรกรรม** — เพิ่มช่อง ยอด VAT / เลขภาษี / ชื่อผู้เสียภาษี ใน `/transaction/edit` + API `update-transaction` เขียน column L-N — 2026-07-03
- [x] **Tax Report CSV filter** — export เฉพาะแถวที่มี taxId (ไม่ใช่ "-") AND taxAmount > 0 (ไม่รวมแถวที่ไม่มี VAT) — 2026-07-03
- [x] **แก้ Google OAuth callback bug** — `shop_google_configs` มี PK = `shop_id` ไม่มี column `id` เดิม `.select('id, ...')` ทำให้ error `column shop_google_configs.id does not exist` แก้: ลบ `id` ออกจาก select — 2026-07-03
- [x] **Google OAuth redirect URI เพิ่ม custom domain** — เพิ่ม `https://smileslippro.com/api/auth/google/callback` (shop owner flow ผ่าน custom domain) — 2026-07-03
- [x] **Google OAuth verification** — app อยู่ใน verification queue แล้ว (status: under verification), branding verified, authorized domains: smileslippro.com + Cloud Run URL — 2026-07-03

### Dashboard
- [x] Login: LIFF (CDN) + LINE OAuth2 + Email+Password
- [x] Register: 4 ขั้น + referral code support (?ref=)
- [x] Dashboard tabs: Home / Ledger / Analytics / Branches / Settings
- [x] Home: referral section (auto-generate code, copy link, stats)
- [x] Ledger: อ่านจาก Google Sheets + Export Excel
- [x] Analytics: monthly bar chart + branch comparison + top senders (tier-gated)
- [x] Branches: เพิ่ม/ลบ shop_branches
- [x] Settings: shop profile + แก้ไข + บัญชีธนาคาร + Google Drive connect
- [x] Pricing: 5-tier + เติมเครดิต 4 แพ็กเกจ + yearly toggle (แสดงประหยัดกี่บาท) + FAQ
- [x] Admin Panel: 6 tiers, stats card ครบ, ค้นหา, filter, inline edit
- [x] Google OAuth: สร้าง Drive + Sheet 11 col + referral bonus
- [x] Referral System: SMILE-XXXX, auto-generate, bonus 50 credits ทั้งสองฝั่ง
- [x] Terms (9 ข้อ) + Privacy (9 ข้อ) — เพิ่มข้อ Analytics/PDPA consent — 2026-06-11
- [x] Register consent checkbox รวมข้อความ anonymized analytics — 2026-06-11
- [x] หน้าแก้ไขธุรกรรม `/transaction/edit?userId&ref&year` + API `update-transaction` (GET/PATCH, หาแถวจาก column K, แก้เฉพาะ A-G) — 2026-06-11
- [x] Ledger เพิ่มปุ่ม ✏️ ต่อแถว → หน้าแก้ไข | ปุ่ม Flex Message bot เปลี่ยนเป็น "✏️ แก้ไขข้อมูล" ลิงก์ตรงไปสลิปใบนั้น — 2026-06-11
- [x] 404 custom page + SEO meta tags + payment/success
- [x] **Analytics daily view** — toggle รายวัน/รายเดือน, month picker, summary cards แยกตาม view — 2026-06-12
- [x] **Ledger pagination** — 20 แถว/หน้า + date filter (กรองวันที่ 1-31) — 2026-06-12
- [x] **Settings แก้ไขโปรไฟล์ครบวงจร** — เพิ่ม tax_id, user_type (dropdown), address (textarea) — 2026-06-12
- [x] **Invoice flow** — หน้า `/invoice/request` เปลี่ยนเป็นขอ "ใบแจ้งหนี้" (ไม่มีตราประทับ/ลายเซ็น) ใบกำกับภาษีออกหลัง admin อนุมัติ — 2026-06-12
- [x] **Admin Google Connect** — `/api/admin/google/connect` + `/api/admin/google/callback` + สร้าง Invoice Register Sheet + บันทึก token ใน `admin_settings` — 2026-06-12
- [x] **Admin send-email** — `/api/admin/send-email` ส่ง HTML + **PDF แนบ** (pdfkit + Sarabun font) ทาง Gmail — 2026-06-12
- [x] **Admin invoices issue** — เมื่อกด "ออกใบกำกับภาษี" → บันทึก Google Sheet + ส่งอีเมลอัตโนมัติ — 2026-06-12
- [x] **Admin settings tab** — แสดงสถานะ Google connect + ปุ่มเชื่อมต่อ + สถานะ email — 2026-06-12
- [x] **แก้บั๊ก invoice PDF (`lib/invoice-pdf.js`) ตัวอักษร title/subtitle ทับกัน** — ใช้ทั้งดาวน์โหลดและแนบอีเมล — 2026-06-27
- [x] **Subscription expiry + Stripe mismatch detection ใน Admin** — ดูหัวข้อ "Subscription Expiry + Stripe Mismatch Detection" ด้านบน — 2026-06-27
- [x] **Ledger: คอลัมน์หมายเหตุ** — โชว์ note inline ในตาราง (เดิมไม่โชว์ ต้องเปิดดูใน Sheets เอง) — 2026-06-28
- [x] **Ledger: ตัวกรองรายรับ/รายจ่าย + ย่อย รับโอน/รับสด/จ่ายโอน/จ่ายสด** — 2026-06-28
- [x] **Ledger + Analytics: ตัวกรองรายสาขา** — เลือกดูสาขาเดียวหรือรวมทุกสาขา (เดิมมีแค่ตารางเปรียบเทียบสาขารวม ไม่มีตัวกรองดูแนวโน้มรายสาขาเดี่ยว) — `branch` query param ใน `/api/shop/analytics` — 2026-06-28
- [x] **แก้บั๊กปีเก่าดูไม่ได้** — Ledger/Analytics/Tax Report year selector เปลี่ยนจาก hardcode `[2024,2025,2026,2027]` เป็นช่วงไดนามิก (ปีปัจจุบัน+1 ถึง -8 ปี) 3 จุด — 2026-06-28
- [x] **"บันทึกเอง" บนเว็บ (`/api/sheets/add-transaction`) รองรับวิธีรับ-จ่าย** — เพิ่ม field โอน/เงินสด ในฟอร์ม + เขียนคอลัมน์ Q, R และ header เต็ม 18 คอลัมน์ตอนสร้าง tab ปีใหม่ (เดิมเขียนแค่ A-K เท่านั้น ไม่ตรงกับ schema ของบอท) — 2026-06-28
- [x] **หน้า "ช่วยเหลือ" ใน sidebar** — รวมคำสั่ง LINE ทั้งหมดสำหรับเจ้าของร้าน + FAQ accordion (ศึกษาเองได้ ไม่ต้องทักแอดมิน) — 2026-06-28

### Billing
- [x] Stripe Subscriptions (mode: subscription) + proration (stripe_customer_id)
- [x] Webhook: checkout.completed → tier update + customer/subscription ID บันทึก
- [x] Webhook: invoice.payment_succeeded → monthly credit refresh
- [x] Webhook: customer.subscription.deleted → downgrade to normal
- [x] Webhook: rate limiting (20 req/min per IP)
- [x] Cancel URL ใช้ owner_line_id (ไม่ใช่ UUID) — แก้ไข 2026-06-08
- [x] Business tier ฿999: Price IDs จริงใส่แล้ว
- [x] Mega Pack 3,000 แผ่น ฿999: `price_1Tg56f3ZvivzvZ6qX4163cg5`

### Infrastructure
- [x] CI/CD: push main → Cloud Build → deploy bot อัตโนมัติ
- [x] LIFF ID: `2009797558-LMletOqM`
- [x] LINE Bot OA: `@574unjqj` · Add link: `https://lin.ee/wdnoEN5`
- [x] deploy-web.sh + deploy-bot.sh อ่าน secret จาก .env (ไม่มี hardcode)
- [x] Supabase key format ใหม่: sb_publishable_... (anon), sb_secret_... (service role)
- [x] API routes ทุกไฟล์ใช้ SUPABASE_SERVICE_ROLE_KEY (bypass RLS) — แก้ไข 2026-06-08
- [x] api/auth/line.js: insert shop_credits เมื่อ auto-create shop — แก้ไข 2026-06-08

---

## สิ่งที่ยังเหลือ ⬜

### ต้องทำในโค้ด
- [x] **Sheets column K/P/Q/R header patch** — ไม่ต้องทำมือแล้ว `getOrCreateYearSheet()` auto-patch header ที่ขาดให้ทุกครั้งที่บันทึกรายการใหม่ — แก้ไข 2026-06-28
- [ ] **สร้าง shop_category_rules table** ใน Supabase (SQL: `CREATE TABLE shop_category_rules (id uuid DEFAULT gen_random_uuid() PRIMARY KEY, shop_id uuid REFERENCES shop_profiles(id) ON DELETE CASCADE, keyword text NOT NULL, category text NOT NULL, created_at timestamptz DEFAULT now(), UNIQUE(shop_id, keyword));`)
- [x] **บังคับ branch limit ตาม tier จริง** — แก้แล้ว `/api/shop/branches` POST เช็คจำนวนสาขาปัจจุบันเทียบ `MAX_BRANCHES` ตาม tier ก่อน insert แล้ว — 2026-07-15 (commit `e01c927`)
- [x] **Stripe webhook idempotency** — แก้แล้ว insert `event.id` ลง `stripe_processed_events` ก่อนประมวลผล ข้ามถ้าซ้ำ — 2026-07-15 (commit `e01c927`, **ต้องรัน SQL สร้างตารางก่อนถึงจะกันซ้ำได้จริง** ดูหัวข้อ Supabase SQL ด้านบน)
- [x] **ลบโค้ดที่ไม่ใช้แล้ว: `StampSVG`** — ลบแล้ว — 2026-07-15 (commit `e01c927`)
- [x] **Upstash Redis live** — UPSTASH_REDIS_REST_URL + TOKEN ใส่ใน .env แล้ว, bot log `[BOOT] ✅ Upstash Redis connected` — Duplicate Guard ทำงานข้าม instance แล้ว — 2026-06-13
- [x] **รายงาน VAT/ภาษี** — สรุปตามผู้ขาย/เลขภาษี + Export CSV (Business+) — 2026-06-12
- [x] **Category System** — bot detectCategory() Gemini + learned rules (shop_category_rules), Sheets column P, edit page, analytics breakdown (Business+) — 2026-06-13
- [x] **Enterprise Marketing Intelligence** — Peak Time Heatmap (slip_analytics), RFM Segments (sender_profiles), Marketing ROI (category sheets), Revenue Forecast (linear regression) — 2026-06-13
- [x] **Sales Landing Page** — อัปเกรด pages/index.js เป็น Sales Page เต็มรูปแบบ: Sticky Header, Hero + LINE Chat Animation (CSS), 3 Core Benefits, How It Works, Enterprise Showcase + Heatmap Mockup, Pricing 4 tier, Testimonials (placeholder), FAQ accordion, Final CTA, Footer — 2026-06-13
- [x] **Upstash Redis Duplicate Guard** — bot ใช้ Redis SET NX EX แทน in-memory Map, fallback อัตโนมัติถ้าไม่มี key (ต้องใส่ UPSTASH_REDIS_REST_URL + TOKEN ใน .env แล้ว redeploy) — 2026-06-13
- [x] **Bug fix: RFM segment logic** — แยก "new" (R≥4, F=1) ออกจาก "dormant" (fallback), เพิ่ม dormant card ใน Dashboard — 2026-06-13
- [x] **Bug fix: useEffect dependency** — เพิ่ม isUnlimited ใน deps array เพื่อให้ fetchMarketing trigger เมื่อ upgrade tier — 2026-06-13
- [x] **POS Module (`/pos`)** — ระบบขายหน้าร้าน 4 tabs: หน้าขาย (product grid + cart + checkout), สินค้า/สต็อค (CRUD), รายงาน (daily/monthly), รับสินค้า (stock receive); ข้อมูลเก็บใน Google Sheets 2 tabs (สินค้า + ยอดขาย); `lib/google-pos.js` + 3 API routes `/api/pos/setup|products|sales` + `/api/admin/pos-stats`; ต้องสร้าง `pos_configs` table ก่อนใช้งาน — 2026-07-03
- [x] **Delivery Module (`/delivery`)** — ระบบส่งของ 4 tabs: ออเดอร์/สร้าง/ลูกค้า/พนักงาน; ข้อมูลเก็บใน Google Sheets (PDPA); `lib/google-delivery.js` + API routes; ต้องสร้าง `delivery_configs` table — 2026-07-03
- [x] **Dashboard: POS card + Delivery card** — ปุ่ม "เปิดระบบ" บน Home tab สำหรับทั้งสองโมดูล — 2026-07-03

### ต้องทำด้วยมือ — Supabase SQL

> **หมายเหตุ (2026-06-12):** ตรวจสอบจาก CSV export แล้ว — `invoice_requests`, `slip_analytics`, `sender_profiles`, `shop_usage_daily` มีอยู่แล้วทั้งหมด มีแค่ `admin_settings` ที่ยังไม่มี

**เพิ่มคอลัมน์ vat_registered ใน pos_configs (ก่อนใช้ toggle "ร้านนี้จดทะเบียน VAT" ในหน้าตั้งค่า POS — เพิ่ม 2026-07-19, ✅ รันแล้ว 2026-07-19 ยืนยันด้วย REST query ว่าอ่าน/บันทึกค่าได้จริง):**
```sql
ALTER TABLE pos_configs ADD COLUMN IF NOT EXISTS vat_registered boolean DEFAULT false;
```

**เพิ่มคอลัมน์ receipt_paper_size ใน pos_configs (ก่อนใช้ตั้งค่าขนาดกระดาษเครื่องพิมพ์ใบเสร็จ — เพิ่ม 2026-07-17, ✅ รันแล้ว 2026-07-18 ยืนยันด้วยการ PATCH/GET จริงว่าค่าบันทึกถาวรถูกต้อง):**
```sql
ALTER TABLE pos_configs ADD COLUMN IF NOT EXISTS receipt_paper_size text DEFAULT '80mm';
```

**สร้าง admin_settings table (ก่อนใช้ Admin Google Connect):**
```sql
CREATE TABLE IF NOT EXISTS admin_settings (
  key TEXT PRIMARY KEY,
  value TEXT,
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

**สร้าง pos_configs table (ก่อนใช้ /pos POS Module):**
```sql
CREATE TABLE IF NOT EXISTS pos_configs (
  shop_id uuid PRIMARY KEY REFERENCES shop_profiles(id) ON DELETE CASCADE,
  pos_folder_id text,
  pos_sheet_id text,
  created_at timestamptz DEFAULT now()
);
```

**สร้าง delivery_configs table (ก่อนใช้ /delivery Delivery Module):**
```sql
CREATE TABLE IF NOT EXISTS delivery_configs (
  shop_id uuid PRIMARY KEY REFERENCES shop_profiles(id) ON DELETE CASCADE,
  delivery_folder_id text,
  customer_sheet_id text,
  order_sheet_id text,
  created_at timestamptz DEFAULT now()
);
```

**สร้าง stripe_processed_events table (ก่อนใช้ idempotency guard ใน webhooks/stripe.js — เพิ่ม 2026-07-15, ✅ สร้างแล้ว 2026-07-16):**
```sql
CREATE TABLE IF NOT EXISTS stripe_processed_events (
  event_id text PRIMARY KEY,
  event_type text,
  processed_at timestamptz DEFAULT now()
);
```

**สร้าง branch_role_requests table (ก่อนใช้ `#สมัครพนักงานขนส่ง`/`#สมัครผู้จัดการสาขา` — เพิ่ม 2026-07-16):**
```sql
CREATE TABLE IF NOT EXISTS branch_role_requests (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  shop_id uuid NOT NULL REFERENCES shop_profiles(id) ON DELETE CASCADE,
  branch_id uuid REFERENCES shop_branches(id) ON DELETE CASCADE,
  branch_name text,
  line_user_id text NOT NULL,
  display_name text,
  role text NOT NULL CHECK (role IN ('delivery_staff','branch_manager')),
  status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','approved','rejected')),
  created_at timestamptz DEFAULT now(),
  approved_at timestamptz
);
```

**Verified Hyper-Local Market Price Index + Procurement Fraud Detection (v1 retail-only — เพิ่ม 2026-07-19, ✅ รันแล้ว 2026-07-19 + backfill 4 ร้านสำเร็จ + verified ทำงานจริงบน production):**
ต้องรันก่อนใช้ฟีเจอร์ดัชนีราคากลาง/จับทุจริตจัดซื้อ — ดูรายละเอียดการตัดสินใจ/สิ่งที่ยังไม่ชัวร์ทั้งหมดในข้อ 30 ของหัวข้อ "เหตุการณ์และบั๊กที่แก้แล้ว"
```sql
-- 1. เพิ่ม district/province แยกคอลัมน์ใน shop_profiles (เดิมเก็บรวมในสตริง address เดียว
--    ทำให้ query ตามอำเภอ/จังหวัดไม่ได้) — ต้อง backfill ร้านเก่าด้วย (สคริปต์แยกต่างหาก ไม่ใช่ SQL)
ALTER TABLE shop_profiles
  ADD COLUMN IF NOT EXISTS district text,
  ADD COLUMN IF NOT EXISTS province text;

-- 2. แคชชื่อสินค้ามาตรฐาน (canonical name) — ใช้ร่วมกันทุกร้าน (ไม่ผูก shop_id) เพราะ
--    เป้าหมายคือรวมชื่อสินค้าจากร้านต่างๆ ให้เป็นมาตรฐานเดียวกันข้ามร้าน
CREATE TABLE IF NOT EXISTS canonical_name_cache (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  raw_name_normalized text NOT NULL UNIQUE,
  canonical_name text NOT NULL,
  category text NOT NULL,
  created_at timestamptz DEFAULT now()
);

-- 3. ตารางกลางสถิติราคานิรนาม — shop_hash เป็น sha256(shop_id + salt ลับ) ใช้แค่นับ
--    COUNT(DISTINCT shop_hash) เพื่อกฎ "5 ร้านขึ้นไป" เท่านั้น ห้าม join กลับ shop_profiles
--    และห้ามคืนค่า shop_hash ออกไปใน API ใดๆ เด็ดขาด (ดูเหตุผลในข้อ 30)
CREATE TABLE IF NOT EXISTS anonymous_market_prices (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  item_name text NOT NULL,
  category text NOT NULL,
  unit_price numeric NOT NULL,
  uom text,
  district text NOT NULL,
  province text NOT NULL,
  price_type text NOT NULL DEFAULT 'retail' CHECK (price_type IN ('wholesale', 'retail')),
  shop_hash text NOT NULL,
  logged_month date NOT NULL,
  created_at timestamptz DEFAULT now()
);
CREATE INDEX IF NOT EXISTS idx_market_prices_lookup ON anonymous_market_prices(item_name, district, province, price_type);

-- 4. ประวัติ Red Flag ทุจริตจัดซื้อ (ผูก shop_id ตรงๆ ได้ เพราะเป็นข้อมูลของร้านตัวเอง ไม่ใช่ตารางกลาง)
CREATE TABLE IF NOT EXISTS procurement_alerts (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  shop_id uuid NOT NULL REFERENCES shop_profiles(id) ON DELETE CASCADE,
  branch_name text,
  receive_doc_no text,
  item_name text NOT NULL,
  submitted_price numeric NOT NULL,
  market_median_price numeric NOT NULL,
  deviation_percentage numeric NOT NULL,
  status text DEFAULT 'pending' CHECK (status IN ('pending', 'investigated', 'resolved')),
  created_at timestamptz DEFAULT now()
);
```

**สร้าง company_admins table (ก่อนใช้ระบบ multi-admin ของ `/admin` — เพิ่ม 2026-07-20, ✅ รันแล้ว 2026-07-20 ทดสอบ end-to-end ครบทุก path):**
ก่อนรัน SQL นี้ `/admin` ยังใช้ได้ตามปกติผ่านรหัสฉุกเฉิน (`ADMIN_PASSWORD`) เหมือนเดิมทุกประการ — โค้ดเช็ค
error จาก Supabase ก่อนเสมอ ถ้าตารางนี้ยังไม่มีจะ fallback เป็นรหัสฉุกเฉินอย่างเดียวไม่พังอะไร (ทดสอบแล้วจริง)
```sql
CREATE TABLE IF NOT EXISTS company_admins (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  email text UNIQUE NOT NULL,
  password_hash text NOT NULL,
  display_name text,
  role text NOT NULL DEFAULT 'staff' CHECK (role IN ('owner', 'staff')),
  status text NOT NULL DEFAULT 'active' CHECK (status IN ('active', 'disabled')),
  created_at timestamptz DEFAULT now(),
  created_by uuid REFERENCES company_admins(id),
  last_login_at timestamptz
);
```
วิธีเริ่มใช้งานหลังรัน SQL: เข้า `/admin` ด้วยรหัสฉุกเฉินเดิมตามปกติ (`ADMIN_PASSWORD`) — ระบบจะเช็คว่ายังไม่มี
แอดมินในตารางนี้เลยแล้วขึ้นหน้า "ตั้งบัญชีเจ้าของระบบคนแรก" อัตโนมัติทันที ไม่ต้องทำอะไรเพิ่มเอง

**เพิ่มคอลัมน์ trial + feature-gating ใน shop_profiles (ก่อนระบบ "ทดลองใช้ฟรี 30 วัน" จะเริ่มล็อกร้านที่หมดเวลาได้จริง — เพิ่ม 2026-07-20, ยังไม่ได้รัน):**
ก่อนรัน SQL นี้ ทุกอย่างทำงานเหมือนเดิมทุกประการ — `lib/shop-access.js`/`api/cron/expire-trials.js`/
`api/cron/trial-day25-nudge` (บอท) เช็ค error จาก Supabase ก่อนเสมอแล้ว fail-safe เป็น "ไม่ล็อกใคร" ถ้า
คอลัมน์เหล่านี้ยังไม่มี (ทดสอบแล้วจริงว่าไม่ error/ไม่ล็อกผิดคน) — ร้านเก่าทั้งหมดจะไม่มีค่า
`trial_started_at`/`trial_ends_at` เลยแม้หลังรัน SQL แล้ว (เพราะ `api/register.js` ตั้งค่านี้แค่ตอนสมัคร
ใหม่เท่านั้น) จึงได้รับการยกเว้นจาก trial lock ตลอดไปตามที่ตัดสินใจไว้ ไม่ต้องทำอะไรเพิ่มสำหรับร้านเก่า
```sql
ALTER TABLE shop_profiles
  ADD COLUMN IF NOT EXISTS trial_started_at timestamptz,
  ADD COLUMN IF NOT EXISTS trial_ends_at timestamptz,
  ADD COLUMN IF NOT EXISTS status text NOT NULL DEFAULT 'active',
  ADD COLUMN IF NOT EXISTS trial_day25_notified boolean NOT NULL DEFAULT false;
```
หลังรันแล้วต้องสร้าง Cloud Scheduler job เพิ่มด้วย (region `asia-southeast1`, service `smileslip-service`):
- `smileslip-expire-trials` → `POST {dashboard_url}/api/cron/expire-trials` ทุกวัน พร้อม header `x-cron-secret`
- `smileslip-trial-day25-nudge` → `POST {bot_url}/cron/trial-day25-nudge` ทุกวัน พร้อม header `x-cron-secret`

**เพิ่มคอลัมน์ address ใน shop_branches (ก่อนใช้ที่อยู่แยกต่อสาขาสำหรับใบเสร็จ/ใบกำกับภาษี — เพิ่ม 2026-07-21, ✅ รันแล้ว 2026-07-21):**
ก่อนรัน SQL นี้ การสร้าง/แก้ไขสาขายังทำงานปกติทุกอย่าง (ทดสอบแล้วจริง) — `api/shop/branches.js` แยกเขียน
`address` เป็น query ต่างหากเสมอ ถ้าคอลัมน์ยังไม่มีจะแค่ไม่บันทึกที่อยู่ (ไม่ error ไม่พังการสร้างสาขา)
```sql
ALTER TABLE shop_branches ADD COLUMN IF NOT EXISTS address text;
```

**สร้างตาราง pos_open_bills (sync บิล/โต๊ะที่เปิดค้างข้ามอุปกรณ์ — เพิ่ม 2026-07-23, ✅ รันแล้ว 2026-07-23):**
ก่อนรันตารางนี้ ระบบยังทำงานปกติ (ทดสอบแล้วจริง) — `api/pos/open-bills.js` คืน `tableMissing:true`
แบบไม่ throw ถ้าตารางยังไม่มี ฝั่งเว็บ fallback เป็น `localStorage` ต่อเครื่องเหมือนเดิม (ไม่มี cross-device
sync จนกว่าจะรัน SQL นี้)
```sql
CREATE TABLE IF NOT EXISTS pos_open_bills (
  shop_id uuid PRIMARY KEY REFERENCES shop_profiles(id) ON DELETE CASCADE,
  bills jsonb NOT NULL DEFAULT '[]'::jsonb,
  table_names jsonb NOT NULL DEFAULT '[]'::jsonb,
  updated_at timestamptz DEFAULT now()
);
```

**สร้างตาราง trial_used_line_ids (ป้องกันลบร้านแล้วสมัครใหม่ขอทดลองฟรี 30 วันซ้ำไม่จำกัดรอบ — เพิ่ม 2026-07-23, ✅ รันแล้ว 2026-07-29 verified ผ่าน REST query ช่องโหว่ trial ซ้ำปิดจริงแล้ว):**
ก่อนรัน SQL นี้ การสมัครสมาชิกยังทำงานปกติทุกอย่าง (ให้ trial 30 วันตามเดิมทุกครั้ง ไม่เช็คซ้ำ) —
`api/register.js` เช็ค error จาก Supabase ก่อนเสมอแล้ว fail-safe เป็น "ยังไม่เคยใช้ trial" ถ้าตารางนี้
ยังไม่มี (ทดสอบแล้วจริงว่าไม่ error/ไม่บล็อกสมัครสมาชิกใหม่) — ตารางนี้แยกออกจาก `shop_profiles`
โดยเจตนา (keyed ด้วย `line_user_id` ตรงๆ ไม่ผูก `shop_id`) เพื่อให้ "เคยใช้สิทธิ์ทดลองแล้ว" อยู่รอด
ต่อไปแม้ร้านที่เคยใช้ trial นั้นจะถูกลบทิ้งทั้งหมดผ่าน `DELETE /api/shop/delete-shop` — ห้าม
`DELETE`/`TRUNCATE` ตารางนี้เด็ดขาดไม่ว่ากรณีใด (ต่างจากตารางอื่นๆ ที่ผูก shop_id ซึ่งถูกลบตอนลบร้าน)
```sql
CREATE TABLE IF NOT EXISTS trial_used_line_ids (
  line_user_id text PRIMARY KEY,
  first_used_at timestamptz DEFAULT now()
);
```

**เพิ่มคอลัมน์ phone ใน shop_branches + branch_id ใน shop_bank_accounts (เบอร์โทร/บัญชีธนาคารแยกต่อสาขา — เพิ่ม 2026-08-09, ยังไม่ได้รัน):**
ก่อนรัน SQL นี้ ทุกอย่างยังทำงานปกติทุกประการ (ทดสอบแล้วจริง) — `api/shop/branches.js`
เขียน `phone` แบบ best-effort แยกจาก insert/update หลักเสมอ (เหมือน `address`), `api/shop/bank-accounts.js`
ลอง insert พร้อม `branch_id` ก่อนแล้ว fallback เป็น insert แบบเดิมถ้า error — ถ้าคอลัมน์เหล่านี้ยังไม่มี
เบอร์โทร/บัญชีธนาคารของสาขาแค่ไม่ถูกบันทึกจริง ไม่ error ไม่พังการสร้าง/แก้ไขสาขาหรือบัญชีธนาคารหลัก
```sql
ALTER TABLE shop_branches ADD COLUMN IF NOT EXISTS phone text;
ALTER TABLE shop_bank_accounts ADD COLUMN IF NOT EXISTS branch_id uuid REFERENCES shop_branches(id) ON DELETE SET NULL;
```

**เพิ่มคอลัมน์ scb_biller_ref1 ใน pos_configs (Reference 1 — ฟิลด์บังคับตามสเปก ธปท. สำหรับ QR
Bill Payment/Tag 30 ที่ขาดไปตั้งแต่สร้างฟีเจอร์ Biller ID — เพิ่ม 2026-08-09, **ยังไม่ได้รัน — บล็อก
การสแกนจ่ายผ่าน Biller ID อยู่จริงตอนนี้ (เร่งด่วน มีลูกค้าจริงกำลังรอใช้งาน)** ดูรายละเอียดเต็มในข้อ 79):**
ก่อนรัน SQL นี้ ระบบยังทำงานปกติทุกอย่าง (ทดสอบแล้วจริง) — `api/pos/pos-config.js` แยกเขียน
`scb_biller_ref1` เป็น query ต่างหากเสมอ (เหมือน `receipt_paper_size`/`vat_registered`) ถ้าคอลัมน์ยังไม่มี
จะแค่บันทึกไม่ได้จริง ไม่ error/ไม่พังการตั้งค่าอื่น — **แต่ QR แบบ Bill Payment จะสแกนจ่ายไม่ได้จนกว่าจะรัน
SQL นี้แล้วกรอกเลขอ้างอิงในหน้าตั้งค่า POS ให้ครบ** (validation บล็อกการบันทึก Biller ID ใหม่ที่ไม่มี
เลขอ้างอิงคู่กันไว้แล้ว กันสร้างค่าพังซ้ำ)
```sql
ALTER TABLE pos_configs ADD COLUMN IF NOT EXISTS scb_biller_ref1 text;
```

**สร้างตาราง pos_payroll_employees + pos_payroll_runs (ระบบเงินเดือน — Phase 2 ของแผนบัญชีเต็มรูปแบบ, ข้อ 76 — เพิ่ม 2026-08-09, ✅ รันแล้ว — ผู้ใช้ทดสอบใช้งานจริงแล้ว มีพนักงาน "นิค ปัญญา" อยู่ในระบบ):**
ต้องรันก่อนถึงจะใช้แท็บ "💰 เงินเดือน" ใน POS ได้จริง (ตอนนี้ endpoint ที่เกี่ยวข้องจะ error
เพราะตารางยังไม่มี — ยังไม่มี fallback แบบ best-effort เหมือนคอลัมน์เดี่ยวๆ ด้านบน เพราะเป็นฟีเจอร์
ใหม่ทั้งโมดูล ไม่ใช่ฟิลด์เสริมของฟีเจอร์เดิม) — ไฟล์เต็มอยู่ที่ `scripts/payroll-01-create-tables.sql`
```sql
CREATE TABLE IF NOT EXISTS pos_payroll_employees (
  id              uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  shop_id         uuid NOT NULL REFERENCES shop_profiles(id) ON DELETE CASCADE,
  employee_no     text NOT NULL,
  name            text NOT NULL,
  id_card_number  text,
  position        text,
  base_salary     numeric NOT NULL DEFAULT 0,
  sso_enrolled    boolean NOT NULL DEFAULT true,
  branch_name     text,
  start_date      date,
  status          text NOT NULL DEFAULT 'active' CHECK (status IN ('active','inactive')),
  notes           text,
  created_at      timestamptz NOT NULL DEFAULT now(),
  updated_at      timestamptz NOT NULL DEFAULT now(),
  deleted_at      timestamptz,
  UNIQUE (shop_id, employee_no)
);
CREATE INDEX IF NOT EXISTS idx_pos_payroll_employees_shop ON pos_payroll_employees (shop_id) WHERE deleted_at IS NULL;

CREATE TABLE IF NOT EXISTS pos_payroll_runs (
  id                uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  shop_id           uuid NOT NULL REFERENCES shop_profiles(id) ON DELETE CASCADE,
  run_no            text NOT NULL,
  employee_id       uuid REFERENCES pos_payroll_employees(id) ON DELETE SET NULL,
  employee_name     text NOT NULL,
  year_month        text NOT NULL,
  base_salary       numeric NOT NULL DEFAULT 0,
  additions         numeric NOT NULL DEFAULT 0,
  addition_note     text,
  deductions        numeric NOT NULL DEFAULT 0,
  deduction_note    text,
  gross_pay         numeric NOT NULL DEFAULT 0,
  sso_employee      numeric NOT NULL DEFAULT 0,
  sso_employer      numeric NOT NULL DEFAULT 0,
  withholding_tax   numeric NOT NULL DEFAULT 0,
  net_pay           numeric NOT NULL DEFAULT 0,
  branch_name       text,
  notes             text,
  paid_at           timestamptz,
  created_at        timestamptz NOT NULL DEFAULT now(),
  deleted_at        timestamptz,
  UNIQUE (shop_id, run_no)
);
CREATE INDEX IF NOT EXISTS idx_pos_payroll_runs_shop_month ON pos_payroll_runs (shop_id, year_month) WHERE deleted_at IS NULL;
```

**เพิ่มคอลัมน์พนักงาน (ที่อยู่/เบอร์โทร/ประเภทการจ่าย/วันหยุด) + ตั้งค่าวันหยุดกลางของร้าน — ขยายระบบเงินเดือนให้รองรับพนักงานรายวัน/แทรค N วัน ไม่ใช่แค่รายเดือน (ข้อ 81 — เพิ่ม 2026-08-10, ✅ รันแล้ว — verified 14/14 บน production จริง):**
ก่อนรัน SQL นี้ ระบบเงินเดือนเดิม (รายเดือนล้วน) ยังทำงานปกติทุกอย่าง (ทดสอบแล้วจริง — 14 assertions
ผ่านทั้ง GET/POST/PATCH) — `payroll-employees.js` แยก update คอลัมน์ใหม่เหล่านี้ต่างหากหลัง insert/update
หลักเสมอ + กันพัง (`try{}catch{}`) ถ้ายังไม่ได้รัน SQL จะแค่ไม่บันทึกค่าพวกนี้ (ที่อยู่/เบอร์/
ประเภทการจ่าย/วันหยุดเฉพาะคน) ไม่ error ไม่พังการเพิ่ม/แก้ไขพนักงาน — `lib/payroll.js`'s
`payrollEmployeeFromRow()` ใช้ `|| ''`/`Number(...)||0` กันพังถ้าอ่านแถวที่ยังไม่มีคอลัมน์เหล่านี้ด้วย
```sql
ALTER TABLE pos_payroll_employees
  ADD COLUMN IF NOT EXISTS pay_type text DEFAULT 'monthly',
  ADD COLUMN IF NOT EXISTS daily_rate numeric DEFAULT 0,
  ADD COLUMN IF NOT EXISTS cycle_days integer DEFAULT 10,
  ADD COLUMN IF NOT EXISTS cycle_rate numeric DEFAULT 0,
  ADD COLUMN IF NOT EXISTS address text,
  ADD COLUMN IF NOT EXISTS phone text,
  ADD COLUMN IF NOT EXISTS days_off_per_week_override numeric; -- ⚠️ เลิกใช้แล้ว (ข้อ 82) ดูคอลัมน์ใหม่ด้านล่าง — เหลือไว้เฉยๆ ไม่มีโค้ดอ่าน/เขียนแล้ว

ALTER TABLE pos_configs
  ADD COLUMN IF NOT EXISTS payroll_days_off_per_week numeric DEFAULT 1; -- ⚠️ เลิกใช้แล้ว (ข้อ 82) ดูคอลัมน์ใหม่ด้านล่าง — เหลือไว้เฉยๆ ไม่มีโค้ดอ่าน/เขียนแล้ว
```

**แก้สูตรวันหยุดเป็นโควตาต่อเดือนตรงๆ (ข้อ 82 — เพิ่ม 2026-08-10, ✅ รันแล้ว — verified 11/11 บน production จริง):** สูตรเดิมข้างบน
(คูณจากอัตราต่อสัปดาห์) ออกแบบผิดสมมติฐานตั้งแต่วันแรกที่ shipped — ดูเหตุผลเต็มในข้อ 82 ด้านล่าง
ก่อนรัน SQL นี้ หน้าตั้งค่า/ฟอร์มพนักงาน/ตัวคำนวณหักวันหยุดยังทำงานได้ปกติทุกอย่าง (ทดสอบแล้วจริง)
แค่ยังไม่บันทึกค่าใหม่จริง (fail-safe เหมือนคอลัมน์ใหม่อื่นๆ) — ไม่มีข้อมูลจริงใช้คอลัมน์เก่าด้านบน
เลยสักแถวตอนแก้ (มีแค่ข้อมูลทดสอบที่ลบไปแล้ว) จึงไม่ต้อง migrate ข้อมูลอะไร
```sql
ALTER TABLE pos_payroll_employees
  ADD COLUMN IF NOT EXISTS days_off_per_month_override numeric;

ALTER TABLE pos_configs
  ADD COLUMN IF NOT EXISTS payroll_days_off_per_month numeric DEFAULT 6;
```

**เพิ่มคอลัมน์ collected_method/settled_shift_no ใน pos_sales/pos_delivery_orders (ผูกเงินที่เก็บ
จากรับชำระเงินเชื่อเข้าระบบกะเงินสด — ข้อ 82 — เพิ่ม 2026-08-10, ✅ รันแล้ว — verified 11/11 บน production จริง):** แก้ known gap เดิม
จากข้อ 78 ที่ `computeExpectedCash()` ไม่เคยนับเงินที่เก็บจากลูกหนี้เป็นเงินสดเลย (เพราะ
`payment_method` ของบิลเชื่อคงเป็น 'เชื่อ'/'ค้างจ่าย' ตลอดไปโดยตั้งใจ ไม่ผ่านตัวกรอง
`payment_method='เงินสด'`) — ก่อนรันSQL นี้ การรับชำระเงินเชื่อ (`sales.js`/`delivery.js` PATCH)
ยังทำงานได้ปกติทุกอย่าง (ทดสอบแล้วจริง) แค่ยังไม่นับเข้ากะเงินสด (fail-safe แยก update ต่างหาก
เหมือนคอลัมน์ใหม่อื่นๆ)
```sql
ALTER TABLE pos_sales
  ADD COLUMN IF NOT EXISTS collected_method text,
  ADD COLUMN IF NOT EXISTS settled_shift_no text;

ALTER TABLE pos_delivery_orders
  ADD COLUMN IF NOT EXISTS collected_method text,
  ADD COLUMN IF NOT EXISTS settled_shift_no text;
```

**เพิ่มคอลัมน์ receipt_footer_message/receipt_line_url ใน pos_configs (ปรับแต่งท้ายใบเสร็จ — ข้อความ
ขอบคุณ/โปรโมชั่นที่ร้านกำหนดเอง + QR ไลน์ร้านค้า — ต่อยอดจากข้อ 83 หลังผู้ใช้ทดสอบพิมพ์ Bluetooth
สำเร็จแล้ว — เพิ่ม 2026-08-13, ยังไม่ได้รัน):**
ก่อนรัน SQL นี้ ระบบยังทำงานปกติทุกอย่าง (ทดสอบแล้วจริงทั้ง GET/PATCH ก่อนรัน SQL) — `api/pos/pos-config.js`
แยกเขียนคอลัมน์เหล่านี้เป็น query ต่างหากเสมอ (เหมือน `scb_biller_ref1`/`receipt_paper_size`) ถ้าคอลัมน์
ยังไม่มีจะแค่บันทึกข้อความ/ลิงก์ที่กรอกไม่ได้จริง ไม่ error/ไม่พังการตั้งค่าอื่น — ใบเสร็จจะยังพิมพ์ได้
ปกติแค่ไม่มีข้อความ/QR ที่ปรับแต่งไว้จนกว่าจะรัน SQL แล้วกรอกค่าใหม่อีกครั้ง
```sql
ALTER TABLE pos_configs
  ADD COLUMN IF NOT EXISTS receipt_footer_message text,
  ADD COLUMN IF NOT EXISTS receipt_line_url text;
```

### ต้องทำด้วยมือ (ไม่ใช่โค้ด)
- [x] **สร้าง admin_settings table** ใน Supabase — มีแล้ว มีข้อมูลอยู่จริง (verified 2026-06-28 ผ่าน REST query)
- [x] **เพิ่ม Google OAuth redirect URI** สำหรับ admin + **กด "เชื่อมต่อ Google"** — เสร็จแล้ว (admin_settings มี `admin_invoice_sheet_id` แปลว่า connect สำเร็จแล้ว) — verified 2026-06-28
- [x] **ตั้งค่าอีเมล Gmail** — `EMAIL_USER`, `EMAIL_PASS`, `NEXT_PUBLIC_EMAIL_CONFIGURED` ตั้งค่าใน `.env` แล้ว — verified 2026-06-27
- [x] **Custom Domain** — `smileslippro.com` ผูกกับ Cloud Run `smileslip-dashboard` แล้ว ใช้งานจริง — verified 2026-06-27
- [ ] **Testimonials จริง** — ใส่คำรีวิวจากลูกค้ากลุ่มแรกแทน placeholder ใน `pages/index.js` บรรทัด TESTIMONIALS array
- [x] **Min-instances** บน Cloud Run `smileslip-service` — เดิมตั้ง minScale=1 (2026-06-27) กัน cold start 3-8 วิ — **ผู้ใช้ปลดกลับเป็น 0 แล้ว (2026-08-07)** ตัดสินใจว่า cold start ไม่กี่วินาทีไม่กระทบ ประหยัดค่าใช้จ่ายดีกว่า
- [x] **Vision API Budget Alert** — ทำแล้ว (2026-06-12)
- [x] **เพิ่ม `subscription_expires_at`, `stripe_billed_tier`, `stripe_period_end`, `google_bonus_granted` columns** ใน `shop_profiles` — ทำแล้ว 2026-06-27/28
- [ ] **ไลน์ออฟฟิเชียลอื่น (ไม่ใช่บอทนี้)** — ผู้ใช้ถามว่า Claude แก้ Rich Menu/ข้อความตอบกลับอัตโนมัติของ LINE OA อื่นได้ไหม — ทำไม่ได้เพราะต้อง login LINE Official Account Manager ด้วยบัญชีผู้ใช้เอง (ห้ามกรอก credential ให้) ผู้ใช้ต้องอัปโหลดรูป/ใส่ข้อความเองหลัง Claude ออกแบบ/เขียนให้
- [x] **รัน SQL คอลัมน์ trial (`trial_started_at`/`trial_ends_at`/`status`/`trial_day25_notified`) ใน `shop_profiles`** — ✅ รันแล้ว 2026-07-21 — verified ผ่าน REST query: คอลัมน์มีจริง, `status` default `'active'` ถูกต้อง, ร้านเก่าทุกร้านมี `trial_started_at`/`trial_ends_at` เป็น NULL ตามที่ตั้งใจ (ยกเว้นตลอดไป ไม่มีร้านไหนถูกแตะ)
- [x] **สร้าง Cloud Scheduler jobs `smileslip-expire-trials` + `smileslip-trial-day25-nudge`** — ✅ สร้างแล้ว 2026-07-21 (region `asia-southeast1`, ใช้ header `x-cron-secret` เดียวกับ cron อื่นทั้งหมด):
  - `smileslip-expire-trials` — `0 19 30 * * *` UTC (02:30 กรุงเทพ ทุกวัน) → `POST https://smileslippro.com/api/cron/expire-trials`
  - `smileslip-trial-day25-nudge` — `0 11 * * *` UTC (18:00 กรุงเทพ ทุกวัน) → `POST {bot_url}/cron/trial-day25-nudge`
  - ทดสอบรันด้วยมือ (`gcloud scheduler jobs run`) ทั้งคู่แล้ว: `expire-trials` ตอบ HTTP 200, `trial-day25-nudge` log เริ่ม/จบสำเร็จไม่มี error — ระบบ 30-Day Free Trial Lock **มีผลจริงครบวงจรแล้วตอนนี้** (ทดลองใช้ครบ 30 วัน → ล็อกอัตโนมัติ, เหลือ 5 วัน → แจ้งเตือนทาง LINE อัตโนมัติ)
- [x] **รัน SQL คอลัมน์ `address` ใน `shop_branches`** — ✅ รันแล้ว 2026-07-21 — verified ผ่าน REST query ว่าคอลัมน์มีจริง (ที่อยู่แยกต่อสาขาบันทึก/ใช้กับใบเสร็จ-ใบกำกับภาษีได้จริงแล้วตอนนี้)
- [x] **สร้างตาราง `pos_open_bills`** (sync บิล/โต๊ะที่เปิดค้างข้ามอุปกรณ์ — ข้อ 42) — ✅ รันแล้ว 2026-07-23 ทดสอบ end-to-end ผ่านครบ

---

## ข้อควรระวังสำคัญ

1. **deploy-bot.sh ต้องรันจาก `smileslip-pro/`** — ถ้ารันจาก root จะพัง
2. **deploy-web.sh ต้องรันจาก `smileslip-dashboard/`** — ถ้ารันจาก root จะ upload .codeoss และ crash
3. **LINE channel แยกกัน:** `LINE_CHANNEL_ACCESS_TOKEN`+`LINE_CHANNEL_SECRET` = Messaging API, `LINE_LOGIN_SECRET` = LINE Login (คนละ channel คนละค่า)
4. **Google token** — bot อ่านจาก `shop_google_configs.google_refresh_token` เท่านั้น
5. **Supabase columns** — เครดิตใช้ `balance_credits`, password ใช้ `password_hash`
6. **Gemini endpoint** — ใช้ `v1` (ไม่ใช่ `v1beta`) สำหรับ gemini-3.5-flash
7. **ห้าม hardcode secret** — repo เป็น public, deploy scripts อ่านจาก `.env` เสมอ
8. **PDPA** — ห้าม insert ข้อมูลธุรกรรม (ยอดเงิน, ชื่อคน) ลง Supabase
9. **@line/liff** — ห้ามติดตั้งเป็น npm package (SSR พัง) ใช้ CDN เท่านั้น
10. **ห้าม query Supabase จาก client โดยตรง** — ทุก query ต้องผ่าน API route ที่ใช้ service role key
11. **ห้ามใช้ recharts** — ไม่มีใน package-lock.json จะ build fail, ใช้ CSS bars แทน
12. **cancel_url ใน Stripe checkout** ต้องใช้ `owner_line_id` (LINE userId) ไม่ใช่ DB UUID
13. **Supabase schema cache error** (account_type ฯลฯ) — reload ที่ Supabase Dashboard → Settings → API → Reload schema
14. **stripe-server.js** — เป็น Express server เก่า ไม่ได้รันใน production แล้ว (ใช้ Next.js API แทน)
15. **Supabase key format ใหม่** — `sb_publishable_...` (anon), `sb_secret_...` (service role) ไม่ใช่ JWT `eyJ...`
16. **api/auth/line.js POST** — ต้องใช้ service role key และ insert shop_credits พร้อมกัน (แก้แล้ว 2026-06-08)
17. **`usage_logs.amont`** — มี typo ใน DB schema (ขาด u) — ถ้าจะ insert ต้องใช้ชื่อนี้ตามจริง
18. **Analytics tables** — `slip_analytics`, `sender_profiles`, `shop_usage_daily` มีอยู่แล้วใน DB (verified 2026-06-12) `recordAnalytics()` ใช้งานได้ทันที
19. **PDPA Analytics** — ห้ามเก็บชื่อผู้โอน/ยอดจริง/เลขบัญชีใน slip_analytics — ใช้ sha256 hash และ amount_bucket เท่านั้น
20. **#สรุปทุกสาขา** — อ่านจาก sheet เดียว (main shop sheet) แต่แตก by branch_name (column J) ถูกต้องแล้ว เพราะทุก branch เขียน sheet เดียวกัน
21. **quoteToken ห้ามใส่ใน Flex Message** — LINE รองรับ quote เฉพาะ type `text`/`sticker` เท่านั้น ถ้าใส่ใน flex จะถูก reject ทั้ง message (`The message type 'flex' does not support quote message`) — แก้แล้ว 2026-06-11
22. **deploy บน Windows** — `bash deploy-bot.sh` และ `bash deploy-web.sh` ล้มเหลวเพราะ Python not found ใน Bash tool ต้องใช้ **PowerShell** + `gcloud run deploy` โดยตรง พร้อม parse `.env` ด้วย PowerShell script
23. **nodemailer** เพิ่มใน dashboard package.json แล้ว — ต้องรัน `npm install` ก่อน deploy ไม่งั้น `npm ci` จะ fail เพราะ lock file ไม่ sync
24. **Admin Google token** — เก็บใน `admin_settings` table (key=`admin_google_refresh_token`) ไม่ใช่ `shop_google_configs` — admin บริษัทกับ shop เป็น คนละ table
25. **CRON_SECRET** — ใน `smileslip-pro/.env` ค่า `SmileSlipCron2569!` ต้องส่งใน header `x-cron-secret` เมื่อเรียก `/cron/daily-summary`, `/cron/weekly-summary`, `/api/cron/expire-subscriptions`
26. **shop_category_rules** — ต้องสร้าง table ก่อนใช้ category system (SQL อยู่ใน "ต้องทำด้วยมือ") bot detect ได้แต่ learnKeyword จะ error ถ้าไม่มี table (suppress แล้ว)
27. **Heatmap/RFM** — ข้อมูลมาจาก `slip_analytics` และ `sender_profiles` ที่ bot เขียนอัตโนมัติ — ต้องรอให้มีสลิปก่อนถึงจะแสดง
28. **Revenue Forecast** — ต้องมีข้อมูล Google Sheets อย่างน้อย 2 เดือนในปีนั้น
29. **Cloud Scheduler** — jobs ทั้งหมดอยู่ที่ region `asia-southeast1`: `smileslip-daily-summary` (`0 11 * * *` UTC = 18:00 กรุงเทพ ทุกวัน), `smileslip-weekly-summary` (`0 11 * * 1` UTC = จันทร์ 18:00 กรุงเทพ), `smileslip-expire-subscriptions` (`0 19 * * *` UTC = 02:00 กรุงเทพ ทุกวัน)
30. **ใบแจ้งหนี้ vs ใบกำกับภาษี** — `/invoice/request` ออก "ใบแจ้งหนี้" เท่านั้น (ไม่มีตราประทับ) ใบกำกับภาษีจริงออกโดย Admin เท่านั้นหลัง issue (มีตราประทับ + ลายเซ็น)
31. **คอลัมน์ C (ประเภท รายรับ/รายจ่าย) ห้ามแก้/แทนที่ความหมาย** — ตัดสินใจไว้ชัดเจนแล้ว (2026-06-28) ว่าฟีเจอร์โอน/เงินสดต้องเพิ่ม **คอลัมน์ใหม่ (Q)** เท่านั้น ห้ามรียูส column C เพราะระบบอื่นพึ่งพา (#สรุปทุกสาขา, analytics, RFM, category breakdown ฯลฯ) อ่านคอลัมน์ C เป็น "รายรับ"/"รายจ่าย" ตรงๆ
32. **google_bonus_granted คือ flag กันโบนัสซ้ำตัวเดียวที่เชื่อถือได้** — ห้ามกลับไปเช็คจาก "มีแถวใน shop_google_configs" หรือ "มี folder_id" เป็นเงื่อนไขให้โบนัสอีก (เคยมีบั๊กเครดิตซ้ำมาแล้ว)
33. **เมนู `#วิธีใช้งาน` กับหน้า "ช่วยเหลือ" ในเว็บ** — เนื้อหาคนละไฟล์ ไม่ได้ share ข้อมูลกัน (bot: TOPICS object ใน index.js / เว็บ: array inline ใน dashboard.js ส่วน Help tab) — แก้คำสั่ง/ข้อความใหม่ต้องแก้ทั้ง 2 ที่ให้ตรงกันเอง
34. **LINE Official Account อื่น (ไม่ใช่บอทนี้)** — Claude เข้าไปแก้ Rich Menu/ข้อความตอบกลับอัตโนมัติให้โดยตรงไม่ได้ เพราะต้อง login LINE Official Account Manager (manager.line.biz) ด้วยบัญชีผู้ใช้ ช่วยได้แค่ออกแบบรูป/เขียนข้อความให้ไปอัปโหลด/วางเอง หรือถ้าเปิด Messaging API ของ OA นั้นแล้วให้ token มา จะช่วยตั้ง Rich Menu ผ่าน API ได้ (แต่ auto-reply แบบคีย์เวิร์ดใน OA Manager จะใช้ไม่ได้แล้ว ต้องเขียนบอทแยก)
35. **Traffic pinning บน Cloud Run** — หลัง deploy ทุกครั้งต้องรัน `gcloud run services update-traffic smileslip-service --region asia-southeast1 --project smileslip-accounting-pro --to-revisions=REVISION_NAME=100` ด้วย เพราะ traffic อาจค้างอยู่ที่ revision เก่า (พบปัญหาจริง: deploy ใหม่แต่ user ยังเห็น behavior เก่า) ตรวจสอบด้วย `gcloud run services describe` ดูว่า traffic 100% ตรงกับ revision ล่าสุดไหม
36. **`shop_google_configs` ไม่มี column `id`** — PK ของตารางนี้คือ `shop_id` เท่านั้น ห้าม `.select('id, ...')` จะ error `column shop_google_configs.id does not exist` — ใช้ `.select('google_refresh_token, google_folder_id, google_sheet_id')` เท่านั้น
37. **isHandwritten condition ต้องเป็นทั้งสองเงื่อนไข** — `const isHandwritten = avgConfidence < 0.75 && rawText.length < 100` ถ้าใช้แค่ `avgConfidence < 0.75` อย่างเดียว สลิปดิจิทัล QR ที่ถ่ายด้วยมือถือ (confidence ต่ำจากแสงสะท้อน) จะถูก route ไป image-mode และ Gemini อ่าน "จ่ายบิลสำเร็จ" เป็น expense แทน income
38. **คีย์รายการเอง — ทุกคนในกลุ่มใช้ได้ (ไม่ใช่เฉพาะเจ้าของ)** — แก้ไข 2026-07-03 เหตุผล: พนักงานต้องส่งสลิปและบันทึกรายรับ-จ่ายได้ ระบบบันทึกชื่อผู้คีย์ใน column R ไว้ audit ส่วนการแก้ไขยังเป็นหน้าที่เจ้าของ/แอดมินเท่านั้น (ผ่าน dashboard)
39. **Google OAuth Consent Screen — redirect URIs ต้องครบทั้ง Cloud Run + custom domain** — ต้องมี 4 URIs: (1) `...run.app/api/auth/google/callback`, (2) `...run.app/api/admin/google/callback`, (3) `smileslippro.com/api/admin/google/callback`, (4) `smileslippro.com/api/auth/google/callback` — ถ้าขาด URI 4 ผู้ใช้ที่เข้าผ่าน smileslippro.com จะ error `redirect_uri_mismatch` ตอนเชื่อม Google Drive
40. **ห้ามใช้ gemini-2.5-flash** — deprecated ในโปรเจกต์นี้ ใช้ `process.env.GEMINI_MODEL` (default: `gemini-3.5-flash`) เสมอ ทั้งใน main OCR และ fallback
41. **ทุกจุดที่เรียก Gemini ต้องตั้ง `maxOutputTokens` อย่างน้อย ~1000 ขึ้นไปเสมอ** — `gemini-3.5-flash` กิน "thinking token" ภายในก่อนตอบเสมอแม้งานจะเล็กมาก (เจอจริง 26-1363 tokens) ถ้าตั้งค่าต่ำ (เจอบั๊กจริงมาแล้ว 3 จุด: `market-price.js` เดิม 512, บอท `detectCategory()` เดิม 32, `process-slip.js` เดิม 256) จะโดนตัดคำตอบกลางคัน (`finishReason: "MAX_TOKENS"`) แบบเงียบๆ ไม่มี error ให้เห็นเลย — ต้องรวมทุก `parts[]` ของ response ก่อน parse เสมอด้วย (ไม่ใช่อ่านแค่ `parts[0]`) เพราะ Gemini บางครั้งแบ่งเอาต์พุตเป็นหลาย part — ดูรายละเอียดข้อ 30/31 — endpoint `v1` ที่ใช้ในโปรเจกต์นี้ไม่รองรับ `thinkingConfig`/`thinkingBudget` (ยืนยันแล้วด้วย error 400 จริง) จึงต้องแก้ด้วยการเพิ่ม `maxOutputTokens` เท่านั้น ไม่มีทางปิด thinking ได้ตรงๆ — ฟังก์ชัน OCR หลักของบอท (`extractDataWithGemini`/`extractDataHybrid`) ไม่ตั้งค่านี้เลย (ใช้ default) จึงไม่เคยเสี่ยงบั๊กนี้
42. **ห้ามเรียก `gcloud` ผ่าน Bash tool บนเครื่องนี้เด็ดขาด แม้แต่คำสั่ง read-only อย่าง `describe`/`list` ก็ตาม** — ล้มเหลวด้วย `Python was not found` เสมอ (เหตุผลเดียวกับที่ deploy ต้องใช้ PowerShell มาตลอด) — ถ้าเผลอใช้ Bash รัน `gcloud` ใน polling loop (เช่น `until gcloud ... | grep -q SUCCESS; do sleep; done`) loop จะค้างเงียบตลอดไปโดยไม่มี error ให้เห็นและไม่มีการแจ้งเตือน (เจอจริงระหว่างข้อ 76-77 ทำให้ผู้ใช้ต้องถามสถานะเองหลายรอบ) — เช็คสถานะ Cloud Build/Cloud Run ทุกครั้งต้องผ่าน PowerShell เท่านั้น — ถ้าต้อง poll ค้างระหว่างรอ build ใช้ loop ภายใน PowerShell คำสั่งเดียว (`while ($status -eq "WORKING") { Start-Sleep; $status = gcloud ... }`) ไม่ใช่ Bash
43. **`gcloud run deploy --source .` อาจ build สำเร็จแต่ไม่สร้าง revision ใหม่เลย** (คนละเคสจาก "traffic ค้าง revision เก่า" ที่เจอบ่อยกว่า — ข้อ 35) — เจอ 2 ครั้งติดกัน (ข้อ 76, 77) พร้อมกับ `429 RESOURCE_EXHAUSTED` จาก Cloud Build API ตอน deploy — วิธีแก้ที่ยืนยันได้ผลจริง: เช็ค `gcloud builds list`/`gcloud builds describe <id> --format="value(status)"` ก่อนว่า build จริงสำเร็จหรือไม่ (มักจะสำเร็จอยู่แล้วแม้ CLI จะรายงาน error) แล้วดึง image ที่ build ไว้มา deploy ตรงๆ ข้ามการ build ใหม่: `gcloud builds describe <id> --format="value(results.images[0].digest)"` แล้ว `gcloud run deploy <service> --image="<repo>@sha256:<digest>" --region ... --env-vars-file ...` (ไม่ต้องมี `--source`/`--set-build-env-vars` เพราะไม่ได้ build ใหม่)
44. **⚠️ ตอน parse `.env` ด้วย PowerShell เพื่อสร้าง `--env-vars-file` (YAML) ต้องตัดเครื่องหมายคำพูดที่ครอบค่าออกก่อนเสมอ** — เจอ production พังจริงระหว่างข้อ 83 (ต่อยอด): ไฟล์ `.env` ของโปรเจกต์นี้เก็บค่าบางตัวแบบมีเครื่องหมาย `"..."` ครอบไว้ (เช่น `SUPABASE_URL="https://xxx.supabase.co"`) — สคริปต์ parse เดิมตัดแค่ `.Trim()` ไม่ได้ตัดเครื่องหมายคำพูดออก แล้วเอาค่าที่ยังมี `"..."` ติดอยู่ไปห่อด้วย single-quote อีกชั้นตอนเขียนเป็น YAML (`"$key: '$val'"`) ผลคือค่าจริงที่ deploy เข้า Cloud Run กลายเป็น `"https://xxx.supabase.co"` (มีเครื่องหมายคำพูดฝังอยู่จริงในสตริง) — Supabase client โยน `Invalid supabaseUrl: Must be a valid HTTP or HTTPS URL` ทันทีที่มีการเรียกใช้ ทำให้ **ทุก endpoint ที่แตะ Supabase (เกือบทั้งระบบ) พังเป็น 500 หมด** จนกว่าจะ rollback traffic กลับ revision เดิม (`gcloud run services update-traffic --to-revisions=<เดิม>=100`) — แก้ถาวรคือก่อนใส่ค่าเข้า `$envVars`, เช็คและตัด `"..."`/`'...'` ที่ครอบอยู่หัวท้ายออกก่อนเสมอ (`if ($val.StartsWith('"') -and $val.EndsWith('"')) { $val = $val.Substring(1, $val.Length-2) }` ทำนองนี้กับทั้งสองแบบเครื่องหมายคำพูด) แล้วค่อยห่อด้วย single-quote ทีเดียวตอนเขียน YAML — **ก่อน route traffic ไป revision ใหม่ทุกครั้ง ให้ยิง endpoint ที่ต้องพึ่ง Supabase ตรงๆ ก่อนเสมอ (เช่น `curl`/Node fetch ไป `/api/shop/data?userId=...`) ยืนยันว่าได้ 200 ไม่ใช่ 500 ก่อนถึงจะ pin traffic จริง** — ห้ามเชื่อแค่ "build สำเร็จ + deploy สำเร็จ" ว่าใช้งานได้จริง
